# Дизайн-аудит: матеріали v1.0 проти живого продукту

Прогін: 2026-08-30, 11:40–13:10. Прод — `localhost:5173` на комітах `f7d90dc` / `cb49a1e`,
чистий акаунт `design-audit@example.com` із контрольною коморою (вершки 33% 400 мл, фует
180 г, багет — відкритий). Матеріали — 8 файлів `.dc.html` v1.0.

**Метод.** Дизайн-файли віддав браузеру через `apps/web/public/da/` (Vite їх сервить як
статику) і читав поруч із продуктом. Далі — не на око: computed styles із DevTools проти
значень кіта, реальний Tab-прохід із читанням `:focus-visible`, заміри тривалості
секундоміром у JS, і посилання на конкретні рядки коду.

**Про скріни.** Розширення зберігає їх у `/tmp` на твоєму Mac, а не в моєму середовищі, —
покласти їх у `design-audit/` я не можу тим самим шляхом, яким пишу цей файл. Тому
доказова база тут — computed-значення, рядки коду й заміри в мілісекундах; вони точніші за
скрін. Якщо скріни все одно потрібні для розмови з дизайном — скажи, зберу окремо.

---

## Покриття

Другий захід (після уточнення Пилипа) закрив те, що лишалось: десктопний хендоф,
Cook Mode, «Моушн», «Мінімалізм — розгортка» і «Хендоф — екрани» повністю. Пропущено
свідомо тільки «Патерни та оверлеї» — сторіз-оверлеїв у проді немає, і бріф їх виводить
за межі аудиту (`DESIGN-BRIEF-2.md` §8).

## Підсумок

| | Перевірено | Збігається | Розходиться | Немає в проді |
|---|---|---|---|---|
| Токени (кольори, моушн, типографіка) | 31 | 31 | 0 | 0 |
| Компоненти й стани | 24 | 14 | 4 | 6 |
| Екрани (моб + десктоп + Cook Mode) | 14 | 3 | 7 | 4 |
| Моушн | 17 | 4 | 2 | 11 |
| Бренд | 5 | 5 | 0 | 0 |

**База збігається — це підтверджую твердо.** Палітра до символу в обох темах, моушн-токени,
обнулення під reduced-motion, знак ◌● і в компоненті, і в `icon.svg`, жодного порушення з
«чого не робимо». Чотири заявлені фікси: три підтверджені виміром, один частковий.

**Але це база.** Вище рівня токенів картина інша: з чотирнадцяти екранів хендофа збігається
структурно три, а з сімнадцяти анімацій «Моушну» в проді існує чотири. Пропозиція — екран,
заради якого продукт існує — зроблена принципово інакше, ніж вирішив дизайн.

## Major

### DA-01 · Фокус-кільце є лише на `Button`; решта показує синє кільце Chrome

Кіт: «focus = кільце 2px шавлія з offset 2px». У коді це є рівно в одному місці —
`apps/web/src/components/Button/Button.module.css:17`:

```css
.btn:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
```

Глобального `:focus-visible` немає (`grep` по `apps/web/src/styles/*.css` — нуль
збігів), а `<button>` рендериться напряму ще в одинадцяти файлах: `TabBar`, `TagInput`,
`Pantry`, `Shopping`, `CookLog`, `Recipe`, `Profile`, `Cook`, `Recipes`, `Feed`, `Share`.
Своє правило дописали тільки рядку комори (`Pantry.module.css:60`).

Прохід Tab-ом по стрічці з читанням реального `:focus-visible`:

| Елемент | Кільце |
|---|---|
| «Рецепт →», «Уточнити» *(компонент Button)* | `2px solid rgb(88, 117, 78)`, offset `2px` — **кіт** ✓ |
| ⌚ ІСТОРІЯ, + НОВИЙ | `1px auto rgb(0, 95, 204)` — **дефолт Chrome** |
| 📎, 🎙 | `1px auto rgb(0, 95, 204)` |
| ◉ Стрічка, ▤ Комора, ☰ Список *(і решта таб-бару)* | `1px auto rgb(0, 95, 204)` |
| Поле вводу «Що купив або що готуємо?» | `0px none` — **кільця немає взагалі** |

Три різні поведінки на одному екрані, з них кітова — одна. Синій `#005fcc` не належить
палітрі ні в темній, ні в світлій темі.

Найдорожче тут — поле вводу: це головний елемент продукту, і з клавіатури не видно, що
воно у фокусі.

**Фікс** — одне глобальне правило в `tokens.css` замість дописування по файлах:

```css
:where(button, a[href], input, textarea, select, [tabindex]:not([tabindex='-1'])):focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

`:where` має нульову вагу, тож наявне правило `.btn` і `-2px` у рядка комори лишаться
чинними.

### DA-02 · Продукт не показує, що думає

Кіт («Моушн», розділ «Система думає»): три крапки зі stagger, мітка «КУХНЯ · РОЗБИРАЮ» +
дієслово.

У проді немає нічого. `sending` у `Feed.tsx` використовується тільки щоб задизейблити
поле й кнопки (`Feed.tsx:606,616,630,635`); жодного індикатора не рендериться —
`grep` по `думаю|вариться|dots|typing|thinking` у `Feed.tsx`/`Feed.module.css` порожній.

Я міряв відповіді моделі в цьому ж прогоні: 8-9 секунд від відправки до появи картки.
Тобто дев'ять секунд екран стоїть, поле сіре, і нічого не пояснює, що відбувається.
Це єдина розбіжність із кіта, яка впливає на відчуття продукту сильніше за всі решта
разом.

### DA-03 · Порожній стан комори втратив дію

Заявлений фікс («empty states now use the kit's own texts») виконаний наполовину.

| | Кіт | Прод (`Pantry.tsx:114-115`) |
|---|---|---|
| Заголовок | Комора порожня | Комора порожня ✓ |
| Текст | Сфотографуй полицю або перелічи 5–10 позицій — решту доберемо. | Сфотографуй полицю або перелічи 5–10 позицій **у стрічці** — решту доберемо **по ходу**. |
| Дія | **[Показати кухню]** | **немає** |

Кіт озаглавлює цей блок «ПОРОЖНІ СТАНИ — КОРОТКО, З ДІЄЮ». Дія і є суть патерна: людина
з порожньою коморою не має куди тапнути. Текст при цьому подовжили, а не скоротили.

Список: кіт «Список чистий», прод «Список **порожній**» (`Shopping.tsx:88`); текст із
кіта на місці, але з дописаним «Або скажи в стрічці: «додай молоко в список»».

### DA-04 · Стани поля вводу з кіта не існують

Кіт, розділ 02, специфікує шість станів композитора. У проді — два (default, focus):

| Стан кіта | У проді |
|---|---|
| DEFAULT «Записати в журнал…» | є, але текст інший — «Що купив або що готуємо?» |
| FOCUS | є |
| УТОЧНЕННЯ «↳ ФУЕТ?» | **немає** |
| ERROR «✕ НЕ РОЗІБРАВ — СПРОБУЙ СЛОВАМИ» | **немає** |
| ШВИДКИЙ ВВІД «+ молоко 1 л» | **немає** |
| ОФЛАЙН ОК | **немає** |
| DISABLED «Недоступно» | є (через `disabled={sending}`), без тексту |

`grep` по `НЕ РОЗІБРАВ|УТОЧНЕННЯ|ШВИДКИЙ ВВІД|ОФЛАЙН` в `apps/web/src` — нуль збігів.

`DESIGN-BRIEF-2.md` §3 закриває офлайн-стани **мета-рядка**, не поля вводу, тож ERROR і
УТОЧНЕННЯ сюди не потрапляють — це повноцінна прогалина реалізації.

Окремо про плейсхолдер: розбіжність текстів фіксую як рішення для дизайна, не як дефект.
Кітове «Записати в журнал…» описує щоденник, продове «Що купив або що готуємо?» —
розмову; продове точніше описує те, що там насправді відбувається.

---

### DA-14 · Пропозиція зроблена принципово інакше, ніж вирішив дизайн

Найбільша структурна розбіжність аудиту. Екран описаний двічі — «Хендоф — екрани» №08 і
«Мінімалізм — розгортка» №2, — і обидва рази однаково.

| Дизайн | Прод |
|---|---|
| **Одна пропозиція за раз**, пагінація «← ПРОПОЗИЦІЯ 1/3 →» | усі три як рівні картки одна під одною, пагінації немає |
| **ЧОМУ ЗАРАЗ** — абзац із причиною | є (`cards.tsx:145-148`), рендериться тільки коли модель віддала `why` |
| **РЯТУЄ** з状态ом на кожному чипі: «● вершки · 3 ДНІ», «● багет · ВІДКРИТИЙ» | «● вершки 33%» — без терміну й без стану |
| **БРАКУЄ** з дією «+ у список» і поясненням «Фетучіні · або будь-яка довга паста» | статичні чипи без дії й без альтернатив |
| **ІНШІ ВАРІАНТИ** — компактний список із часом: «Тост з фуетом і яйцем · 10ХВ» | немає; замість цього другий і третій варіанти розгорнуті на повну |
| Дії: **«Готуємо це» / «Інше»** | «Рецепт →» / «Уточнити» |

Різниця не косметична. Дизайн будує вибір як «ось моя рекомендація, ось чому, ось що
поруч»; прод будує його як список рівних опцій. Це два різні продукти на одному екрані, і
цього немає серед відкритих питань `DESIGN-BRIEF-2.md`.

### DA-15 · Десктоп: немає правої контекст-панелі

Хендоф Д01 задає патерн у три колонки: сайдбар 232px · центр 640–720px · **права панель
320px**. У панелі три блоки:

- **ГОРИТЬ · 2** — «Вершки 33% ≈3 ДНІ», «Багет ЧЕРСТВІЄ»
- **ОЧІКУЮТЬ РІШЕННЯ · 2** — «Комора: 3 зміни ◌», «Профіль: Оля ◌»
- **ВЕЧЕРЯ СЬОГОДНІ** — страва, «25ХВ · РЯТУЄ ВЕРШКИ», кнопка «Готуємо →»

У проді її немає. Перевірив DOM на 1728px: `aside` / контекст-контейнера нуль, `body`
має тільки клас `with-sidebar`. Найближче за змістом — рядок «СКОРО ЗГОРИТЬ» усередині
стрічки (`Feed.tsx:568`), але це елемент стрічки, а не панель.

Центральна колонка при цьому правильна: **676px**, у межах хендофових 640–720, картки не
розтягнуті.

### DA-16 · Десктоп: сайдбар — 6 пунктів замість 4 + профіль унизу

Хендоф: сайдбар **232px**, чотири пункти навігації (Стрічка · Комора 23 · Рецепти ·
Список 4) і **окремий блок користувача внизу** — аватар «П» + «Пилип».

Прод: **200px** (`tokens.css:154`), **шість** рядків (`TabBar.tsx:30-35`: Стрічка, Комора,
Список, Рецепти, Журнал, Профіль), блоку користувача немає — Профіль просто шостий пункт
із гліфом ⚙.

Важливе уточнення до `DESIGN-BRIEF-2.md` §2: питання «4 чи 6» бріф ставить для
**мобільного** таб-бару. Десктопний хендоф відповідає на нього інакше й з іншої причини —
там чотири пункти саме тому, що Профіль поїхав униз окремим блоком, а Журнал зник із
навігації. Це не те саме питання, і воно лишається без відповіді.

### DA-17 · Cook Mode світлішає разом із темою

Хендоф Д05 і «Хендоф — екрани» №10 однакові: «Cook Mode — окремий повноекранний **темний**
режим», «темний **в обох темах**». Логіка зрозуміла — планшет на підставці біля плити,
контраст і нічна кухня.

`Cook.module.css:6` — `background: var(--bg-body)`. Тобто на світлій темі Cook Mode
світлий. Рішення дизайну про режим, який не залежить від теми, у код не доїхало.

Решта Cook Mode збігається краще за будь-який інший екран: «✕ Вийти», «НАЗВА · КРОК 3/5»,
таймер, «Пауза», «+1 хв», «ДАЛІ», «Крок готово ✓» і дослівне «Працює без мережі · таймер
живе локально».

### DA-18 · Моушн: із сімнадцяти анімацій у проді чотири

`grep @keyframes` по всьому `apps/web/src` дає рівно чотири: `spin`, `card-enter`,
`toast-in`, `pulse`. «Моушн» специфікує сімнадцять живих прикладів. Немає:

| Анімація з кіта | Специфікація |
|---|---|
| Undo — закреслення | лінія зліва направо 250ms, потім opacity .6 *(у проді статичний `line-through`)* |
| Флеш зміненого рядка комори | тінт шавлії 16%, 700ms fade, скрол до рядка |
| Лічильник у шапці | цифра прокручується вертикально 250ms |
| Перемикання табів | пігулка їде 250ms, контент crossfade + X±10 |
| Додавання рядка (список/комора) | висота 0→auto + fade 250ms, сусіди з'їжджають |
| Видалення / куплено | чекбокс 150ms → пауза 800ms → колапс 250ms |
| Надсилання повідомлення | Y+14, scale .97→1, 250ms enter |
| Система думає | 3 крапки, 1.2s, stagger 150ms |
| Стрімінг відповіді | чанками по фразі, fade 150ms, каретка 900ms |
| Cook Mode — зміна кроку | вертикальний слайд 400ms разом із прогрес-смугою |
| Скелетон-шимер | 1.4s linear ∞ |
| Кільце впевненості | дуга 0→62%, 400ms *(є в `DESIGN-BRIEF-2` §7)* |
| Свайп-дії рядка | spring 380/30, поріг 40% *(є в `DESIGN-BRIEF-2` §7)* |

Дві останні — вже відкриті питання, решта одинадцять просто не зроблені.

### DA-19 · Голосове введення: механіка є, дизайну немає

«Моушн» розділ 04 — це найдетальніше описана частина всього кіта: чотири стани з
таймінгами. Прод має робочий `SpeechRecognition` (`lib/speech.ts`), кнопку 🎙 і зміну
гліфа на `●` під час слухання (`Feed.tsx:621`). Усе інше з чотирьох станів відсутнє:

- **0 · Морф ↑⇄🎙.** Кіт: поле порожнє → 🎙, перший символ → морф у ↑, scale .6⇄1 + fade
  150ms. Прод: 📎, 🎙 і ↑ стоять поруч одночасно — три кнопки замість однієї, що
  перетворюється.
- **1 · Активація.** Кільце-пульс 1.4s ∞ поки слухає — немає.
- **2 · Жива хвиля.** Бари 3px, `scaleY` від **реальної амплітуди** («не фейк-луп»),
  моно-таймер «0:07», стоп тапом або 2с тиші — немає нічого.
- **3 · Транскрипт наживо.** Слова fade-in по мірі розпізнавання, нестабільні —
  **бурштином**, після фіналізації біліють — немає.
- Reduced motion: замість хвилі «● REC + таймер» — немає.

Кухонний кейс, під який це писалось («руки зайняті або брудні»), зараз не закритий:
людина не бачить, чи її чутно.

### DA-20 · Хептики і звуку немає

«Моушн» розділ 05: apply → light impact, undo → medium, error → notification-error, таймер
закінчився → повтор кожні 30с доки не підтверджено, старт/стоп запису → light/medium.

`grep vibrate|haptic` по `apps/web/src` — нуль. Найдорожче тут не хептика взагалі, а саме
повтор сигналу таймера: кіт прописав його з тієї ж причини, що й голос — руки зайняті,
екран не в полі зору.

## Minor

### DA-05 · У шапці немає лічильника списку

Хендоф: «Кухня / КОМОРА 23 · СПИСОК 4». Прод показує тільки «КОМОРА 4» — списку в шапці
немає, хоча badge на табі «Список» існує. Лічильник комори працює правильно (звірив із
`GET /v1/pantry` — 4 і 4).

### DA-06 · Чипи інгредієнтів: два типи з п'яти

Кіт: `✓ з комори` · `бракує` · `◔ відкрито` · `АНТИ лактоза` · `⚠ алерген`.

- Пропозиція (`cards.tsx:130,140`): «● назва» для РЯТУЄ, простий чип для БРАКУЄ — тобто
  два типи, і замість кітового `✓` стоїть `●`.
- Рецепт (`Recipe.tsx:113-114`): `●` (у коморі) / `○` (бракує) — теж два.
- `◔ відкрито`, `АНТИ`, `⚠ алерген` у списках інгредієнтів не рендеряться ніде, хоча дані
  для них у продукті вже є: `state === 'opened'` у партії, `antipatterns` і `allergies` у
  профілі й у їдців.

Останнє варте окремої уваги: у QA-7 я знайшов, що модель пропонує алерген домашнього
(арахісова паста при алергії Оксани). Чип `⚠ алерген` із кіта — це другий рубіж захисту,
який ловив би такі промахи на екрані, навіть коли модель промахнулась. Він специфікований
і не зроблений.

### DA-07 · БРАКУЄ без дії «+ у список»

Хендоф проставляє біля блоку БРАКУЄ дію «(+ у список)». У проді чипи БРАКУЄ статичні —
докупити можна тільки окремою реплікою в чат. (Плюс сама дія зараз усе одно впала б:
див. QA7-01, `INSERT` у `shopping_item` зламаний.)

### DA-08 · Loading-кнопка: спінер замість «Застосовую…»

Кіт: LOADING = текст «Застосовую…». У проді `Button` має проп `loading`, який ставить
спінер і лишає початкову підпис («Застосувати», «У список», «У рецепти»…) —
`Button.tsx:33-34`, використання в `cards.tsx:99,184,216,257,282`. Механіка є, копі кіта
не використано.

### DA-09 · Тост зʼявляється з Y+8, кіт малює Y+16

`Feed.module.css:265-267`: `@keyframes toast-in { from { transform: translateY(8px) } }`.
Кіт — 16. Дрібниця, але це той самий клас, що решта моушн-токенів, які збіглися точно.

### DA-10 · Undo-закреслення статичне

Кіт: лінія-закреслення проходить зліва направо, потім opacity .6. У проді
`Feed.module.css:67` — `text-decoration: line-through` без анімації; opacity приглушення є.

### DA-11 · Скелетон завантаження не існує

Кіт: «СКЕЛЕТОН ЗАВАНТАЖЕННЯ · ШИМЕР 1.4С LINEAR ∞», живий приклад у «Моушн».
`grep` по `skeleton|shimmer|1.4s|1400ms` в `apps/web/src` — нуль збігів. Екрани з
`loading` показують порожнечу до приходу даних.

Побічно це видно й у продукті: `/profile` перші ~2 секунди показує «ще жодної» на місці
восьми алергій, а стрічка — порожній стан замість CTA (це я фіксував ще в QA-6 як
QA6-08). Скелетон із кіта саме цей клас і закриває.

---

### DA-21 · Онбординг: один CTA замість трьох, і система називає себе сама

Хендоф №03: шапка «Кухня · КОМОРА 0», репліка від «ЗАРАЗ · КУХНЯ» з іменем людини
(«Пилипе, привіт. Я веду твою комору й готую разом із тобою…») і **три CTA**:
«📷 Сфотографувати полицю», «🧾 Кинути чек», «Перелічити текстом».

Прод: порожній стан із одним CTA «📷 Сфотографуй чек — я розкладу», без згадки полиці й
без варіанта «перелічити текстом». Імені людини в репліці немає — натомість модель
вигадує ім'я **собі** («Я Рома», «Я Шеф» на двох чистих акаунтах — QA-6). Кіт це питання
вирішив: система звертається на ім'я й підписана «КУХНЯ», а не персонажем.

### DA-22 · «Лист надіслано»: немає таймера й маскування

| Хендоф №02 | Прод (`MagicLinkSent.tsx`) |
|---|---|
| ✓ Перевір пошту | ◌ ЛІНК ЛЕТИТЬ · Перевір пошту |
| «Посилання діє 15 хвилин і працює один раз» | «Лінк діє 15 хвилин» ✓ |
| «Надіслали на **p\*\*\*@gmail.com**» | адреси немає взагалі |
| «НЕ ПРИЙШЛО? · **Надіслати ще раз · 0:42**» | «Лист не приходить? Перевір «Промоакції» або спам. Можна повторити через 15 хвилин.» |

Таймер зворотного відліку — не косметика: без нього людина не знає, коли кнопка оживе, і
текст «через 15 хвилин» читається як «іди звідси». Маскування адреси — єдиний спосіб
помітити одруківку в email до того, як чекати листа.

### DA-23 · Вхід: немає рядка «v1.0 · працює офлайн після входу»

Хендоф №01 закінчує екран цим рядком — це обіцянка, яка відрізняє продукт від чату.
`grep "працює офлайн"` по `apps/web/src` — нуль збігів. У проді підвал: «Пароля немає.
Лінк діє 15 хвилин, одноразовий.»

### DA-24 · Комора: зони як заголовки, а не сегменти; немає групи «СПОЧАТКУ ГОРИТЬ»

Хендоф №05 будує екран навколо свіжості:

- **сегменти-фільтри** зверху: Усе · Холодильник · Морозилка · Сухе
- перша група — **«СПОЧАТКУ ГОРИТЬ»** з «◔ Вершки 33% · ВІДКРИТО · ≈3 ДНІ» і «◔ Багет ·
  ВІДКРИТО · ЧЕРСТВІЄ»
- унизу — **«+ молоко 1 л — швидкий ввід, працює офлайн»**

Прод (`Pantry.tsx:124-129`) виводить зони як заголовки секцій у фіксованому порядку
(Свіже, Холодильник, Морозилка, Суха шафа, Спеції, Напої), фільтрів немає, групи «горить»
немає, швидкого вводу немає. Тобто екран організований за місцем зберігання, а дизайн
організував його за терміном — це різні продукти на тих самих даних.

Сам рядок партії при цьому збігається дослівно: гліфи `●`/`◔` і «ВІДКРИТО · ≈3 ДНІ»
(`Pantry.tsx:146`).

### DA-25 · Список: немає «Копіювати текстом», походження позиції й підказки про комору

Хендоф №06: «КОПІЮВАТИ ТЕКСТОМ» у шапці, під кожною позицією — звідки вона
(«ДЛЯ: ВЕРШКОВА ФЕТУЧІНІ», «ДЛЯ ОЛІ · АНТИ ЛАКТОЗА», «ДОДАЛА ОКСАНА · ДЛЯ ХУМУСУ»),
а куплена позиція підписана «КУПЛЕНО → ПЕРЕНЕСТИ В КОМОРУ?» з кнопкою «У комору».

У проді немає ні кнопки копіювання, ні рядка походження — `ShoppingItemRow.reason` у
типі є й у картках заповнюється, на екрані не рендериться. Пілюля «→ В КОМОРУ (1)» є ✓.

Копіювання текстом — це та функція, заради якої люди фотографують список перед магазином;
без неї список замкнений у застосунку.

### DA-26 · Рецепти: немає груп і статусу проти комори

Хендоф №07 ділить екран на три групи — **«МОЖНА ЗАРАЗ · З КОМОРИ»**, **«ЗБЕРЕЖЕНІ»**,
**«ГОТУВАЛИ НЕДАВНО»** — і підписує кожен рецепт статусом: «4/5 З КОМОРИ · БРАКУЄ:
ФЕТУЧІНІ», «ВСЕ Є», «НУТ У СПИСКУ · ГОТУВАЛИ 2 РАЗИ», «ОЦІНКА ✓ СПОДОБАЛОСЬ». У шапці —
**«+ ІМПОРТ»**.

Прод (`Recipes.tsx`) — один плоский список без груп, без статусу проти комори й без
кнопки імпорту (імпорт існує тільки як вкладення в чаті). `recipe-match.ts` у домені вже
вміє рахувати збіг із коморою — на екрані цього не видно.

### DA-27 · Розбір чека: немає ані стану «РОЗБИРАЮ…», ані «Правити», ані пропущених рядків

Хендоф №12 показує три речі, яких у проді немає:

- проміжна репліка «19:02 **КУХНЯ · РОЗБИРАЮ…** · Розпізнаю рядки чека — скорочення
  розгортаю» *(частина DA-02)*
- пояснення розгортання під кожним рядком: «Сир Камбоцола 70% · «СИР КАМБОЦ.70% 193Г» ·
  **РОЗГОРНУТО**»
- **пропущені рядки показані**, а не викинуті: «− Серветки · НЕ ЇЖА · ПРОПУЩЕНО», і дії
  «**Застосувати 2**» / «**Правити**»

У проді картка чека — звичайний `intake_diff` із «Застосувати»/«Ні», без лічильника в
кнопці, без «Правити» і без сліду того, що модель відкинула. Останнє прямо стикується з
QA6-02: коли модель мовчки губить позицію, дизайн кіта це показав би.

### DA-28 · Профіль: інша структура, ніж у хендофі

Хендоф №11 / Д06: **«СІМ'Я · ОДНА КОМОРА НА ВСІХ»** (аватари учасників, «+ Запросити»,
«ПОСИЛАННЯ ДІЄ 72 ГОД · ОДНОРАЗОВЕ»), далі **«ПАМ'ЯТЬ ПРО ЛЮДЕЙ · МОДЕЛЬ ВРАХОВУЄ В
КОЖНІЙ ПРОПОЗИЦІЇ»** — типізовані рядки «АНТИ · Оля: без лактози», «АЛЕРГІЯ · Оксана:
молюски · ЛОВИТЬ І «М'ЯСО МІДІЙ»», «ЛЮБИТЬ · Пилип: гостре, кінза», «НОТАТКА · Вечеря
зазвичай о 19:30», і **«ОБЛАДНАННЯ»** чипами.

Прод має п'ять окремих блоків (АЛЕРГІЇ, ПОБАЖАННЯ, ЧОГО НЕ ЇМ, ТЕХНІКА, ВИСНОВКИ) плюс
ДІМ. Дані ті самі, але дизайн згрупував їх **за людиною**, а прод — **за типом запису**.
Плюс: «ЛОВИТЬ І «М'ЯСО МІДІЙ»» у хендофі — це підпис, який пояснює, як працює позначка;
у проді цю думку винесено в хінт секції.

### DA-29 · Шапка на мобільному: логотип замість «Кухня»

І «Мінімалізм», і «Хендоф — екрани» дають мобільній шапці заголовок **«Кухня»** з
лічильниками під ним. Прод показує вордмарк «Kitchen OS». Дрібниця, але вона з'їдає
рядок, у якому дизайн тримає лічильники (див. DA-05).

## Note

### DA-12 · Перемикача теми немає, тема тільки системна

`apps/web/src/theme.ts` слухає `prefers-color-scheme` і ставить `data-theme` на `<html>`;
у самому коментарі написано «можна потім додати перемикач у профілі». На `/profile`
перемикача немає (блоки: АЛЕРГІЇ, ПОБАЖАННЯ, ЧОГО НЕ ЇМ, ТЕХНІКА, ВИСНОВКИ, ДІМ, Вийти).

Бріф аудиту припускав «перемикач у Профілі або системна» — фіксую фактичний стан. Паритет
тем при цьому дотриманий: перемкнув `data-theme` вручну, структура екрана ідентична,
жодного елемента, що зникає чи ламається.

### DA-13 · Таб-бар, степер, рядок здогаду, свайп, кільце впевненості

Усе це вже в `DESIGN-BRIEF-2.md` (§2, §7) як відкриті питання — за умовою аудиту не
рахую розбіжностями. Додам тільки те, чого бріф не згадує: на десктопі (≥1024px) таб-бар
стає сайдбаром на 200px і показує **шість** пунктів вертикально; питання «4 чи 6» у
брифі поставлене для мобільного таб-бару, а десктопний сайдбар — окремий макет, і в
хендофі десктопу його варто звірити ще раз після рішення.

---

## Перевірка заявлених фіксів

| Фікс | Стан | Доказ |
|---|---|---|
| Undo-тост 18с → 8с | ✓ **підтверджено виміром** | застосував intake-картку, тост прожив **7955 мс**; `Feed.tsx:228` `toast.onUndo ? 8_000 : …` |
| Авто-тост 5с → 4с | ✓ у коді | `Feed.tsx:228`, гілка без undo — `4_000` |
| Pending-пульс (1→.55→1, 1.2s, лише pending) | ✓ | `Feed.module.css:334` `.pending-pulse { animation: pulse var(--dur-breathe,1.2s) }`, застосовано тільки до pending-мітки (`Feed.tsx:512`) |
| Пульс вимикається при reduced motion | ✓ | `tokens.css:142-148` обнуляє `--dur-breathe` разом із рештою тривалостей |
| «АСИСТЕНТ» → «КУХНЯ» | ✓ | `Feed.tsx:515`, жодного «АСИСТЕНТ» у `apps/web/src` |
| Порожні стани за текстами кіта | ~ **частково** | тексти близькі, але не дослівні; **дія «Показати кухню» відсутня**; «Список чистий» → «Список порожній». Див. DA-03 |

---

## Що в кіті специфіковано, а в проді не існує

Вхід для планування, згруповано.

**Стани й зворотний зв'язок**
1. Індикатор «система думає» — 3 крапки зі stagger, мітка «КУХНЯ · РОЗБИРАЮ» *(DA-02)*
2. Скелетон завантаження, шимер 1.4с *(DA-11)*
3. Стан вводу ERROR «✕ НЕ РОЗІБРАВ — СПРОБУЙ СЛОВАМИ» *(DA-04)*
4. Стан вводу УТОЧНЕННЯ «↳ ФУЕТ?» *(DA-04)*
5. Швидкий ввід «+ молоко 1 л» — і в полі, і в коморі *(DA-04, DA-24)*
6. Стан ОФЛАЙН ОК *(DA-04)*
7. Стрімінг відповіді чанками по фразі *(DA-18)*

**Екрани й блоки**
8. Права контекст-панель 320px: ГОРИТЬ / ОЧІКУЮТЬ РІШЕННЯ / ВЕЧЕРЯ СЬОГОДНІ *(DA-15)*
9. Блок користувача внизу сайдбара *(DA-16)*
10. Пагінація пропозицій «← 1/3 →» та блок «ІНШІ ВАРІАНТИ» *(DA-14)*
11. «НА ЦЬОМУ КРОЦІ» в Cook Mode *(DA-17)*
12. Група «СПОЧАТКУ ГОРИТЬ» і сегменти зон у Коморі *(DA-24)*
13. «КОПІЮВАТИ ТЕКСТОМ» і рядок походження позиції в Списку *(DA-25)*
14. Групи «МОЖНА ЗАРАЗ / ЗБЕРЕЖЕНІ / ГОТУВАЛИ НЕДАВНО» і «+ ІМПОРТ» у Рецептах *(DA-26)*
15. «Правити» і показ пропущених рядків у розборі чека *(DA-27)*
16. Таймер «Надіслати ще раз · 0:42» і маскування email *(DA-22)*
17. Два CTA онбордингу з трьох *(DA-21)*
18. Лічильник «СПИСОК N» у шапці *(DA-05)*
19. Дія «Показати кухню» в порожній коморі *(DA-03)*

**Дрібне з компонентів**
20. Чипи `◔ відкрито`, `АНТИ`, `⚠ алерген` у списках інгредієнтів *(DA-06)*
21. Дія «+ у список» на блоці БРАКУЄ *(DA-07)*
22. Текст «Застосовую…» на loading-кнопці *(DA-08)*
23. Рядок «v1.0 · працює офлайн після входу» *(DA-23)*

**Моушн і фізика** *(DA-18, DA-19, DA-20)*
24. Одинадцять анімацій зі сімнадцяти
25. Голосове введення: морф ↑⇄🎙, кільце-пульс, жива хвиля від амплітуди, таймер,
    транскрипт наживо з бурштиновими нестабільними словами
26. Хептика й звук цілком, включно з повтором сигналу таймера кожні 30с

## Що збіглося точно

Щоб не тонуло серед знахідок.

**Палітра — 31 токен, обидві теми, збіг до символу.** Темна: `--accent #93b48b`,
`--amber #c9a86a`, `--plum #c99ab4`, `--danger #cc7a70`, `--bg-body #101317`,
`--bg-surface #181c21`, `--border #262c33`, `--fg #e7eaec`, `--fg-muted #9aa3ac`,
`--fg-dim #79828c`. Світла: `--accent #58754e`, `--accent-strong #3f5837`,
`--bg-body #d9dcdf`, `--bg-surface #eef0f1`, `--fg #1d2126`. Усі ці значення присутні в
`.dc.html` кіта — звірив частотним аналізом hex-ів по всіх восьми файлах.

**Моушн-токени:** `--dur-fast 150ms`, `--dur-base 250ms`, `--dur-slow 400ms`,
`--dur-breathe 1200ms`, `--ease-standard cubic-bezier(.2,0,0,1)`,
`--ease-enter cubic-bezier(0,0,.2,1)`, `--ease-exit cubic-bezier(.4,0,1,1)`.

**Кнопки:** pressed `scale(.98)` ✓, disabled `opacity .45` ✓, focus `2px solid accent /
offset 2px` ✓ (там, де компонент застосовано).

**Поява картки:** `card-enter var(--dur-base) var(--ease-enter)`, `translateY(10px)→0` —
рівно кітові «Y+10→0, 250ms».

**Рядок комори:** гліфи `●` / `◔` ✓, термін «ВІДКРИТО · ≈3 ДНІ» ✓ — дослівно як у кіті
(`Pantry.tsx:146`).

**Мета-рядок:** «11:56 ПРОПОЗИЦІЯ · ◌ ОЧІКУЄ» — формат кіта витриманий, ◌ пульсує.

**Знак.** `Logo.tsx` і `public/icon.svg` малюють те саме коло: `r=19`, `stroke-width=3`,
`stroke-dasharray="104 15"`, `rotate(-58)`, вузол `r=6` кольором `--accent` / `#93b48b`.
Розрив угорі-праворуч, як вимагає бренд-кіт. Жодного порушення з «чого не робимо»:
ковпака й виделки немає, кільце не заливається теплим, фото за лого немає, розрив не
обертається. `manifest.webmanifest` — `background_color` і `theme_color` `#181c21`,
іконка та сама SVG.

**Паритет тем** — структура екранів ідентична, жодного елемента, що ламається при
перемиканні.

---

## Порядок робіт

Розділив на дві черги, бо це різні розмови.

**Черга 1 — код доганяє рішення, які дизайн уже ухвалив.** Тут нічого вирішувати, треба
робити.

1. **DA-01** — одне глобальне `:focus-visible`. Найдешевший фікс із найбільшим ефектом:
   зараз половина продукту показує чуже синє кільце, а поле вводу — жодного.
2. **DA-02** — індикатор «думаю». Дев'ять секунд тиші на кожну репліку.
3. **DA-17** — Cook Mode темний в обох темах. Один рядок CSS.
4. **DA-03** — повернути дію «Показати кухню» в порожню комору.
5. **DA-11** — скелетон; заразом прибирає мигання порожніх станів при гідратації.
6. **DA-06** — чипи `⚠ алерген` і `АНТИ`. Це не косметика: у хендофі Д02 рядок комори
   підписаний «М'ясо мідій · ⚠ АЛЕРГІЯ ОКСАНИ», тобто дизайн уже вирішив показувати
   алерген домашнього прямо в коморі — рівно те, чого бракувало моделі в QA7-06. Другий
   рубіж поверх промахів моделі, і він намальований.
7. DA-05, DA-08, DA-09, DA-10, DA-22, DA-23, DA-29 — дрібне, за годину-дві разом.

**Черга 2 — потребує рішення, бо це не «недоробили», а «зробили інакше».**

8. **DA-14 · Пропозиція.** Дизайн: одна рекомендація з причиною + компактні альтернативи.
   Прод: три рівні картки. Треба обрати, і від цього залежить, як виглядає головний екран.
9. **DA-15 + DA-16 · Десктоп.** Права панель і блок користувача — це третина патерна Д01.
   Плюс питання «4 чи 6» для сайдбара, якого `DESIGN-BRIEF-2` §2 не покриває.
10. **DA-24, DA-25, DA-26, DA-27, DA-28** — Комора, Список, Рецепти, розбір чека, Профіль
    зроблені за іншою логікою групування, ніж хендоф. Кожен окремо невеликий, разом це
    інший продукт.
11. **DA-19, DA-20 · Голос і хептика.** Найдетальніше описана частина кіта, з якої зроблено
    тільки розпізнавання. Питання пріоритету, не виконання.

Кіт лишив відкритим за адресами `localhost:5173/da/ui.html`, `/da/brand.html`,
`/da/motion.html`, `/da/minimal.html`, `/da/screens-dark.html`, `/da/screens-light.html`,
`/da/desktop.html`, `/da/patterns.html` — зручно тримати поруч із продуктом. Приберу
`apps/web/public/da/`, коли скажеш.
