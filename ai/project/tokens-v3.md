# Ролі й токени v3 · Kitchen OS

Правило: компонент не має власного значення — тільки роль. Кожна роль несе всі параметри. Одне значення — одне написання.

## Типографіка (Onest, tabular-nums усюди)

| Роль | Кегль/інтерліньяж | Вага | Трекінг | Де |
|---|---|---|---|---|
| display | 36/1.05 | 700 | -0.03em | Cook Mode крок, заголовок документа — виміряно на кадрі «Рецепт · 1440» (36); «Публічний рецепт · 1440» і «Готово · 1440» дають 34 |
| h1 | 28/1.05 · 26 на 390 | 700 | -0.03em | заголовок екрана — виміряно на кадрах: 28 «Рецепти · 1440», «Профіль · 1440», «Календар · 1440», «Журнал · 1440»; 26 на «Комора · збірка» (1440) і на всіх 390; «Комора · мобайл» — 24 |
| h2 | 22/1.2 | 600 | -0.02em | назва картки, заголовок панелі |
| h3 | 17/1.3 | 600 | -0.01em | назва рядка-картки (рецепт, крок) |
| body | 16/1.55 | 400 | 0 | репліки, речення профілю |
| row | 15/1.4 | 500 | 0 | назва в рядку списку |
| small | 14/1.5 | 400 | 0 | другий рядок, пояснення |
| caption | 13/1.4 | 400 | 0 | мета в рядку, чіпи |
| label | 12/1.3 | 500 | 0.01em | підписи полів, назви груп |
| timer | 112/1 | 700 | -0.04em | Cook Mode — на кадрах «Cook · 390» 72, «Cook · 768» 56; не міняно рішенням 11.09, до етапу 8 |

Емфаза: 600 і колір `ink`; діє на меншості — **не більше 3 виділених рядків і 1 картки-акценту на екран**. Курсив — лише для `status: empty` у профілі.

## Колір (світла / темна)

| Токен | Світла | Темна | Дозволена форма |
|---|---|---|---|
| canvas | #c9ccd0 | #0c0d0f | тільки полотно документа |
| bg | #f4f3ef | #16181b | фон екрана, тінт усередині картки |
| card | #ffffff | #1f2226 | поверхня картки, панелі |
| line | #f0eee9 | #2a2e33 | роздільник усередині картки (≥1.5:1 до card) |
| line2 | #e6e4de | #33383e | рамка контролів |
| faint | #c9cbcd | #4a4f55 | дні іншого місяця |
| ink | #1a1c1e | #ecebe7 | текст, головна кнопка, активна ціль |
| muted | #6b6f74 | #a3a7ac | другий текст, іконки в спокої |
| dim | #9a9ea3 | #6f7379 | **не для тексту**: волосини, неактивні знаки, плейсхолдер у полі (його читає курсор, не око), дні іншого місяця разом із `faint`. Будь-яка мета, що читається, — `muted` |
| sage / sage-bg | #55724a / #e8efe3 | #93b48b / #26312a | факт «є»: з комори, applied, свіже. Заливка + текст. Кнопка — тільки в Cook Mode |
| amber / amber-bg | #866425 / #f6ecd6 | #d2ad6b / #332a1c | час тисне: горить, чекає рішення, сезон |
| plum / plum-bg | #885b77 / #f1e5ec | #c99ab4 / #2f2329 | обмеження: не їм, не можна, піст |
| danger | #b5473c | #d47f74 | прострочено, видалити. Текст і знак, ніколи заливка кнопки |

Контраст: текст ≥ 4.5:1 · заголовки ≥ 3:1 · знаки ≥ 3:1. Порогу для волосин `line`/`line2` **немає** (знято 12.09): межу в рядках тримає ритм і відступ, а не контраст лінії; «1.6» у попередній версії було арифметичною помилкою (виміряно 1.16–1.35). Ієрархія небезпечних дій однакова в обох темах: «Видалити акаунт» — текст danger без рамки; «Вийти» — контурна кнопка.

**Тон і слово (12.09).** Тон роду живе на знаку, крапці, шкалі й підкладці; **слово — `ink` або `muted`**: «бракує: яйця» — чорнилом із бурштиновою іконкою, «ще 3 дн» — `muted` з бурштиновою крапкою. Єдиний виняток — чіп із тоновою підкладкою (`amber-bg` + `amber`): там слово тоном. Щоб виняток був чесним, світлі хекси трьох родів затемнено 12.09 (`sage #5b7a4f → #55724a`, `amber #a67c2e → #866425`, `plum #8a5c78 → #885b77`): тепер кожен проходить 4.5 на своїй підкладці, на `card` і на `bg` (sage 4.6 / 5.4 / 4.9 · amber 4.6 / 5.4 / 4.9 · plum 4.5 / 5.5 / 5.0; до правки amber давав 3.2). Темні хекси не мінялись — там усі роди ≥ 5.9.

Уточнення 11.09 (рішення власника): **шавлія — рід факту, не стан контролу.** «Обрано / відкрито / активно» малюється чорнилом (заливка, inset-кільце 1.5) або тінню — як у Prototype; шавлія лишається за «є вдома», «усе є», «Готуємо» й іншими фактами «добре». Ієрархія небезпечних дій однакова в обох темах: «Видалити акаунт» — текст danger без рамки; «Вийти» — контурна кнопка.

## Простір

Крок 4. Рядок списку 48 (десктоп) / 56 (тач). Контроли 36 · 44. Відступи картки 16 · 20 · 24. Проміжок між картками 12 · 16. Колонка розмови 720.

## Поверхні

Три площини: `bg` (екран) → `card` (картка/панель, тінь sh-a+sh-b) → `bg` як тінт усередині картки (блок таймера, активний рядок). Рамка `line2` — тільки контроли й контурні кнопки. Тіні: sh-a 0 1px 2px · sh-b 0 8px 24px · панель 0 16px 48px.

## Радіуси (усі — continuous / squircle)

8 контрол · 12 рядок-картка, кнопка · 16 картка · 20 панель, шторка · 24 Cook Mode картки · pill.

## Знаки

Lucide, сітка 24, штрих 1.75, round cap/join. Розміри — закритий союз **12 · 16 · 18 · 20 · 24**: 12 у чіпах і походженні, 16 у тексті й хедерах, 18 у рейці та композиторі, 20 в інтерфейсі, 24 у Cook Mode (кадри з 13/15/17/19/28 підтягуються до союзу, різниця ±1 px не читається; 28 у Cook → 24). Жива зона 44. Колір `muted` у спокої → `ink` на ховері/активний. Текстові гліфи заборонені. Три сім'ї: система (з моушном) · продукти · готування (статичні).

## Рух

| Роль | Тривалість | Крива |
|---|---|---|
| state | 120 мс | cubic-bezier(.4,0,.2,1) | колір, натиск (ховер) |
| geometry | 240 мс | те саме | звично: розгортання, зсув, перестановка рядка |
| space | 400 мс | те саме | простір: панель, шторка, зміна екрана |
| icon | 600–1100 мс | ease-in-out | рух частин іконки, 1–2 px |
| live | 1.8–2.4 с цикл | ease-in-out | вогонь, таймер, мікрофон, «думаю» — тільки поки триває |

Вихід — ×0.6 тривалості кривою `.4,0,1,1`; без пружин. Код: `--dur-fast:120 / --dur-base:240 / --dur-slow:400`. Канон — HANDOFF «Незмінні правила», тут — та сама трійка (виправлено 12.09; 160/300 було старим).

Reduced-motion: усе вимикається, крім зміни кольору.

## Слоти рядка комори (три осі)

```
[шкала свіжості 56×4] [назва row · паспортна caption dim] [безпека] [походження] [час caption] [кількість caption]
```
- час: `свіже до 14 вер` · `≈ ще 5 дн` · `не псується` · `розрахунок скінчився` (danger) — слово + шкала
- безпека: `не їм` (plum контур) · `не можна` (plum заливка + ban)
- походження: іконка 12 — receipt (чек) · pencil (рукою) · message-circle (з розмови) · circle-dashed (домислено — «межа є, але не точна»); `list-checks` знято разом із джерелом «зі списку»
- пороги свіжості: 3 / 5 / 1 — одне місце


## Канон компонентів (аудит 10.09)
- Хедер зони / картки-секції: чорнило (--ink на --bg тексті), 44 px, padding 0 16, іконка 15, назва 14/600, лічильник або підпис 13/400 opacity .6. Однаково: Комора, Профіль, Журнал, фрагменти лендінга.
- Рейка: 60 px; кнопки 38 × 38, радіус 10, іконки 18; активна — чорнило. Сайдбар розгорнутий — 256 (виміряно на «Чат · 1920», Responsive R0; R1 текстом — 256).
- Композитор: min-height 52, радіус 26, кнопки всередині 42 (коло), іконки 18.
- Кнопки: primary 40/10 (у списках і шапках), 44/10 у шторках, 48/12 як єдина головна дія екрана; пілли 34–36/999 для фільтрів і чіпів стану; чіп роду 28–32/999.
- Радіуси карток: 14 у застосунку; 16–20 на лендінгу й вході (більше повітря, більша картка); 12 у дрібних вкладених.
- Іконки дій: cooking-pot — «Готуємо»; flame — тільки «Горить»; chef-hat — тип рецепта; reply — уточнити.
- Словник знаків: один знак — одне значення. Єдиний виняток — `refrigerator`: у рейці означає весь склад («Комора»), у хедері зони — сам прилад. Оператори (`plus` · `minus` · `x` · `check` · `chevron-*` · `arrow-*`) — граматика, не значення: їхній зміст несе слово поруч, і вони не рахуються у правилі (степер `minus`/`plus` — «менше/більше», «Нічого не змінилось» — `equal`).
- Знаки зон — позичені, своєї сімʼї нема: leaf Свіже · refrigerator Холодильник · snowflake Морозилка · archive Суха шафа · amphora Спеції · wine Напої. Зелень (категорія) — `leafy-green`, не `leaf`.
- `house` = дім як обʼєкт скрізь (чіп «Дім зараз», картка «Дім» у профілі, кікер «Запрошення в дім») — одне значення. `sun` = сезон, `moon` = піст; перемикач теми — тільки `sun-moon`. `alert-triangle` = прострочено/впало (danger); «не можна», алерген — `ban` (plum). `bookmark` = зберегти на потім («Колись», «У рецепти»); «сподобалось» — `heart`. `trash-2` = викинути (списати · зіпсувалось · назавжди). Денний ліміт — `hourglass`, не `moon`.
- «2 порції» і «використає: …» — словами, без знака (§6, §7).
- Колонки списків (комора, список, журнал): одна до 1280 **за шириною контейнера**, дві від 1280, три від 1400 (1920). Ширина вікна не рахується — панель і сайдбар забирають простір.
- Календар 1440 — сітка «v3 · Календар · сітка» (Screens, за Redesign 6c): місяць · тиждень · список, права колонка; 390 — стрічка днів (D3b).
- Стелі: смуги сезонів у тижні — ≤ 3 за рангом (є в коморі → починається цього тижня → закінчується найближче) + рядок «ще N» `muted` з бурштиновою крапкою; чіпи сезонів у шапці — ті самі 3 + нейтральний чіп «ще N», тап відкриває список. «Дім зараз · Зараз» — 3 + «Ще N — у календарі» (§13).
- Порядок у зоні «за місцем» — за строком: прострочене вгорі, далі за зростанням «свіже до», без строку — внизу; після правки рядок переставляється рухом 240 мс, не лишається на місці.
- Число на «Коморі» в сайдбарі = прострочено (R1/R2); крапка в рейці — danger, без числа; при нулі — ні крапки, ні числа. «Горить N» живе чіпом у шапці чату, у навігацію не дублюється.
- Жодних гліфів-символів у тексті (◌ ● ○ ✓ ✕ ＋ → ☆) — тільки Lucide або слова.
- Вхід: Google першим (чорна кнопка, тільки кольорова іконка G), пошта другою.


## Скло і градієнт (v3.1)

Правило: **скло — матеріал усього продукту, градієнт — ознака тільки розмови.**

Токени в обидва блоки (`.v3` і `.v3[data-theme="dark"]`):

| Токен | Світла | Темна |
|---|---|---|
| `--glass-bg` | `color-mix(in srgb, var(--card) 72%, transparent)` | `… 78% …` |
| `--glass-edge` | `rgba(255,255,255,.7)` | `rgba(255,255,255,.10)` |
| `--glass-top` | `rgba(255,255,255,.9)` | `rgba(255,255,255,.16)` |
| `--glass-side` | `rgba(255,255,255,.5)` | `rgba(255,255,255,.07)` |
| `--glass-sh` | `0 12px 40px rgba(26,28,30,.16), 0 0 0 1px rgba(255,255,255,.6)` | `0 12px 40px rgba(0,0,0,.5), 0 0 0 1px rgba(255,255,255,.06)` |
| `--chat-grad` | бурштин 32 %, 240 px | нейтральний підйом: `color-mix(in srgb, var(--card) 45%, var(--bg))` 0 → `var(--bg)` 260 px (12.09; було шавлія 65 %) |

Два класи (у `<helmet><style>`, бо псевдоелемент інлайном не задати):

```css
.glass-rail { background:var(--glass-bg); backdrop-filter:blur(18px) saturate(1.25); -webkit-backdrop-filter:blur(18px) saturate(1.25); border-right:1px solid var(--glass-edge); box-shadow:inset -1px 0 0 var(--glass-side); }
.glass-panel { background:var(--glass-bg) !important; backdrop-filter:blur(18px) saturate(1.25); -webkit-backdrop-filter:blur(18px) saturate(1.25); position:relative; isolation:isolate; }
.glass-panel::after { content:''; position:absolute; inset:0; border-radius:inherit; pointer-events:none; border:1px solid var(--glass-edge); box-shadow:inset 0 1px 0 var(--glass-top), inset 1px 0 0 var(--glass-side); }
```

Зі скла: рейка 60 / сайдбар 256, артефакти правої панелі (один шаблон на всі), шторки й нижній бар на 390. Щільні (`--card`): картки контенту, бабли, тости, модалки, картка кроку Cook Mode. Максимум дві скляні поверхні в кадрі.

Текст на склі — тільки `ink` і `muted`; `dim` і тонові слова на склі заборонені. Виміряно на найгіршому тлі (скло над верхом градієнта чату): `muted` 4.9 світла / 6.5 темна, `ink` 16.5 / 13.3, `dim` 2.6 / 3.3 — тому й заборона. Тон — лише на знаку й крапці.

Чому темний градієнт нейтральний: шавлієвий підйом 65 % робив підкладку sage-чіпа («усе є») невідрізненною від тла (1.12:1 при тій самій барві), а plum-bg — 1.00; тон у нас — рід факту, тло під чіпами не має його позичати. Нейтральний підйом тримає sage-bg на 1.26 (як на рівному `bg`). Світла лишається бурштиновою 32 %: на світлому чіпи розрізняються насиченістю, не яскравістю.

`--chat-grad` вішається на **рамку екрана** (спільний предок рейки, контенту й артефакта) — на колонці він зсувається з появою панелі. Живі приклади: Components М1 / М2.

## Правка канону 11.09 (рішення власника)

Розміри — як на кадрах Screens, не як у тексті. Три числа переміряно з рендеру
кадрів (`scripts/side-by-side.mjs` рендерить `.dc.html` у Chromium): h1 32 → 28
на 1440 / 26 на 390; display 40 → 36; сайдбар 240 → 256. Підстава — попереднє
`Redesign.dc.html` → Screens зменшили h1 (32 → 26–28), а таблиця лишилась
старою (`CORRESPONDENCE-V3.md`, «Трішки зменшити»). Кожне число — з кадром,
звідки взяте, у самій таблиці. Решта ролей не мінялась.
