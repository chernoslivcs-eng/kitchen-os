# Звіт дизайн-чату · синхронізація пакета з продом · 12.09

База — файли з `ai 2/project` (tokens-v3, HANDOFF, COPY і Responsive підтягнуті з бази перед правками; решта .dc.html — та ж лінія, з моїми правками 12.09 згори).

## По файлах

### Kitchen OS - Prototype.dc.html
**Змінено:** композитор — одне гніздо праворуч, завжди чорнильне (mic / стрілка / квадрат), окремий мікрофон ліворуч знято (№29, №30, №42); стан «думаю» перенесено у стрічку — дієслово процесу · три живі крапки · час, після 45 с «Ще тримаю» / «Довгий чек, ще тримаю» (№28); пілюля сесії без меню, тап відкриває сайдбар, «+ Нова» поруч (№22); сайдбар 240 → 256, кнопка згортання переїхала в рядок профілю праворуч, рядок профілю веде в профіль, шеврон і `settings-2` знято (№13, №14, №18); чіпи стану: «Прострочено N» (danger), «Піст · до …» (plum), «Готуємо · таймер» (sage), «Чекають на тебе · N» (§11), «Дім зараз · ще N»; шапка стала container-aware — від 1040 повний набір, вужче компактний із «Дім ●●● N» і числом (§13); тости на 390 переїхали вгору під шапку (№11); «Прострочено» більше не `flame`; картка рецепта в стрічці — `egg-fried` замість `flame`; «2 порції» без знака.
**Додано:** ResizeObserver на контейнер розмови; стан «Дім · тихо» для нуля станів.
**Підписано:** кадри «Готово» — «поза продом».

### Kitchen OS - Screens.dc.html
**Змінено:** D3a (стрічка тижнів) замінено на сітку місяця з Redesign — ‹ › «Сьогодні», Місяць · Тиждень · Список, «+ Подія», чіпи тривалого, легенда без «чек», клітинки без «Сільпо · 14» (№25, №26, Р96); рейка кадру приведена до 60 / 38 / 18 і скла, хекси Redesign переведені в токени; профіль — підказка переїхала в праву колонку (Підказка → Дім → Акаунт), ліва лишилась реченнями + нотатками (№16); хедери карток «Дім», «Мережі», «Акаунт» — чорнильні 44 (№15); «≈ ще N дн» переведено на `muted` + бурштинова крапка (§3); «+ Імпорт» → «Записати свій»; Спеції → `amphora`; «2 порції» без знака; «Прострочено» — `circle-alert`.
**Додано:** кадр «Cook Mode · 1440» (маршрут · фокус · «Усе для страви») з нотою про клікабельність усіх кроків і форму 768 при відкритому сайдбарі (№10).
**Зняте:** кадр D3a.

### Kitchen OS - Components.dc.html
**Змінено:** «сайдбар 240» → 256 у ноті про скло; блок A4 переписано як **композитор з чотирьох станів** (порожній / з текстом / диктування / «думаю») з підписом про одне гніздо й відсутність «⌘K»; підпис у демо щільності виведено з коробки в потік і переведено на `muted` (він накладався на рядок і суперечив новому правилу).
**Додано:** блок «Канон 12.09» — чіп «Чекають на тебе · N» і «Дім · тихо» (§11, §13), порожній стан списку пунктирною карткою (№43), рядок нотатки профілю сіткою «дата · текст · прибрати» (№31), мобільна форма тоста зі змахом (№11), рядок комори з ховером r10 / ✕ на ховері / свайпом і тоновою крапкою (№4, §8, §3).

### Kitchen OS - Responsive.dc.html
**Змінено:** кадри D1–D3 замінено одним станом «Кидай — розберу» (оверлей inset 12, пунктир sage, drop → картка чека), заголовок пакета переписано (№24a); G1 (меню пілюлі) знято, лишилась нота (№22); 768 — артефакт перемальовано з нижньої шторки на плавучу скляну картку 380 праворуч (№34) і додано компактний «Дім ●●● 3» (№27, §13); «піст» — `church`; пілюлі без шеврона; 1920 — додано «Чекають на тебе · 2»; 390 — нота про тости вгорі зі змахом, робочий грабер і правило приховування бару (№11, №35, №21); у демо шторки прибрано композитор, що просвічував крізь скло.

### Kitchen OS - Icons.dc.html
**Додано:** `leafy-green` (Зелень), `amphora` (Спеції), `image` (Фото полиці), `circle-dashed` (домислено); блок із текстом винятку `refrigerator`.
**Зняте:** `settings-2` з рядка профілю, `users` з «Порції», `flame` з «Гриль» (→ `grill`).

### Kitchen OS - Errors.dc.html
**Додано:** кадр «CRASH · 390» (26/15, кнопка на всю ширину внизу, код інциденту під нею) і нота про тости вгорі зі змахом.
**Підписано:** смуга денного ліміту — «поза продом».

### Kitchen OS - Auth.dc.html
**Змінено:** знято темні токени й перемикач теми — лише світла тема (№2); картка запрошення без аватарів і лічильників, лишились назва дому й «для {пошта}».

### Kitchen OS - Onboarding.dc.html
**Додано:** кроки 12–18 «Знайомство» (сім карток інтейку з лідом, плейсхолдером і підказкою), межа кроків 10 → 17.

### Kitchen OS - Cook and Share.dc.html
**Підписано:** «Готово · 1440/390», «Поділитись · 390», C3/C4 — «поза продом»; доданий блок статусу з ремаркою про клікабельність усіх кроків і про те, що на проді після «Приготував» іде розмова.

### Kitchen OS - Landing Live.dc.html
**Змінено:** `flame` на картці страви → `egg-fried` (`flame` лишається тільки «Горить»).

### tokens-v3.md
**Змінено:** рух 120 / 240 / 400 (додано рядок `space`).
**Додано:** розділ «Рішення 12.09» — виняток `refrigerator`, колонки по контейнеру, правило `dim` і тону з винятком для чіпів із підкладкою, артефакт / шторка < 600, тости, композитор, чорнильний хедер, порожній стан, календар 6c, пілюля без меню, світлі лендінг і auth, перелік «поза продом».

### HANDOFF.md
**Додано:** рядок незмінних правил 12.09 у «Незмінні правила».

### COPY.md
**Додано:** таблиця «Дописано 12.09» — «Прострочено N» ↔ «Термін вийшов» (COPY-DEBT 8), «Крок готово» / «Приготував», плейсхолдери композитора, «Кидай — розберу», «Дім · тихо», «Чекають на тебе · N», «Ще тримаю», порожні слова «Дім зараз».

## На проді гірше за кадр (до виконавців, кадр не підтягував)
1. «Прострочено N» і «Термін вийшов» — два слова на один факт; кадр лишає обидва, бо так на екрані, але це борг копірайту, не рішення.
2. Чіп «Піст» у коді йде з `moon`, у словнику — `church`; у кадрах виправлено, у коді ні.
3. Кікери «Що вміє» на лендінгу в коді — `heart` / `ban` / число, у пакеті A9–A11 інші знаки; кадр лишив нейтральні, бо рішення A9–A11 ще відкрите.
4. `settings-2` у рядку профілю коду — знака в словнику більше нема.

## Греп-перевірка
- `--glass-edge` у файлах з токенами: Prototype 5, Screens 4, Components 4, Responsive 4 (по два блоки тем у кожному).
- `h1 | 28` ✓, `display | 36` ✓, «сайдбар 256» ✓, рух 120/240/400 ✓ — у tokens-v3.
- `D3a` у Screens — нема. «2 порції» зі знаком — нема в жодному файлі. `settings-2` у розмітці — нема.
- Баланс `<div>` у всіх десяти .dc.html — рівний.

## Не робив (за брифом)
Не перевирішував §1–§13 і §F; темних версій лендінгу й auth не малював; кадри «Готово», C3/C4, D1–D3 не видаляв без підпису (D1–D3 замінені за прямою вказівкою №24a, заміна описана в тексті пакета).

---

# Правки 12.09 (2)

За `DESIGN-SYNC-FIXES-0912.md`. Три з чотирьох «на проді гірше за кадр» зняті — прод правий: «Піст» = `moon` (стан обмеження), `church` лише «Свято / з традиції»; `settings-2` на проді нема; «Термін вийшов» — лише коментар коду. Кадри виправлені під прод.

## Знаки (§1) — грепом по всіх файлах

| що | було → стало | де |
|---|---|---|
| прострочено | `circle-alert` → `alert-triangle` (A10; `circle-alert` знято зі словника A15) | Prototype 5, Screens 1, Components 2 |
| «Піст» | `church` → `moon` | Prototype 3 (шапка 1440, чіп календаря, подія 20-го), Screens 1, Responsive 2 |
| «Піст · 12 з 48» у календарі | `ban` → `moon` | Screens 1 |
| «Сезон помідорів» | `leaf` → `sun` | Screens 1 |
| «Гості · 4 особи» | `party-popper` → `users` | Screens 3 |
| легенда календаря | «готували · чек · подія» → «готували · піст · подія», колір `dim` → `muted` | Screens |

`church` лишився тільки біля «свята · з традиції» (Components, Landing Live), `ban` — тільки біля «не можна / не їм».

## Screens.dc.html

- **«Чат · збірка»** приведений до Prototype 1440: пілюля сесії без шеврона й меню (№22); ряд чіпів — «Прострочено 10» (alert-triangle, danger) · «Піст · до 27 вер» (moon, plum) · **«Чекають на тебе · 2»** · «Дім зараз · ще 2»; композитор — **одне чорнильне гніздо** з мікрофоном (окремі mic + стрілка знято, №29/№30/№42); підпису «⌘K» нема. Смуга денного ліміту (hourglass) лишилась — це E2.
- **«Календар · 1440»**: знаки за таблицею §1; заголовок пакета D3 і бічна анотація переписані під сітку місяця (види Місяць · Тиждень · Список, ≤3 смуги + «ще N», права колонка сьогодні → цього тижня → триває → приховані сезони й свята) — старий текст про «тиждень легкою карткою» знято.
- **«Профіль · 1440» (D2a)**: шавлієва підказка переїхала **праворуч зверху** і стала sticky, за нею «Наш дім» → «Мережі» → «Акаунт»; з картки речень **знято чорнильний хедер «Про тебе»** — на проді над карткою абзац-вступ, картка без шапки (№15 стосується «Дім», «Мережі», «Акаунт», «Нотатки»).

## Responsive.dc.html

- **R0 · 1920**: пілюля без шеврона; композитор — одне чорнильне гніздо з mic; кнопка згортання сайдбара переїхала з рядка логотипа **у рядок профілю праворуч** (№13) — у всіх трьох сайдбарах файлу; підрядок «дім · 3 їдці» переведено з `dim` на `muted` (§3); «Піст» — moon.
- **R2 · 768**: плавуча картка артефакта 380 тепер починається **під шапкою** (top 72 = 56 + 16), а не поверх чіпів; шапка — пілюля 36 без «· сьогодні», компактні чіпи, «Дім ●●● 3».
- **R3 · 390**: у шапку доданий «Дім ●●● 3» поруч із «⚠ 10»; композитор — чорнильне гніздо з mic замість сірого mic у полі; у шухляді прибрані дати рядків, лишились стани.
- Пороги в анотаціях: **1040 → 964** (1024 − 60) і 704 (768 − 64).

## Prototype.dc.html

- Пороги шапки container-aware: **964** замість 1040 (≥964 повна · 704–963 R2 · <704 G3); «Нова» нижче 964 не показується.
- У рейці 60 з'явилась **кнопка розгортання внизу, над аватаром** (panel-left-open, 38) — раніше кнопки не було.
- Знаки за §1.

## Landing Live.dc.html

- Пілюля сесії в макеті лептопа — без шеврона; композитор — одне чорнильне гніздо (два гнізда знято, 2 місця).

## Самоперевірка §5 (було → стало, по 14 файлах)

| перевірка | було | стало |
|---|---|---|
| `circle-alert` | 8 | **0** |
| `party-popper` | 3 | **0** (4 лишились у `Redesign` — історія напрямів) |
| `⌘K` | 3 | **0** |
| `1040` у порогах | 8 | **0** |
| `chevron-down` у пілюлі | 5 | **0** у робочих файлах (1 у `Redesign`, 1 у старому `Landing` — історія) |
| композитор із двома гніздами | 4 | **0** у робочих файлах (1 у `Redesign`, 1 у старому `Landing`) |
| `church` біля «Піст» | 6 | **0** |
| `ban` біля «Піст / Сезон» | 2 | **0** у робочих файлах (2 у `Redesign`) |

`Kitchen OS - Redesign.dc.html` і `Kitchen OS - Landing.dc.html` не правив: за HANDOFF це історія рішень (напрями 1–3 і перший статичний лендінг), не дзеркало проду. Якщо їх теж треба тримати в словнику — скажіть, приведу.

## Відкрите

- Кікери «Що вміє» на лендінгу (A9–A11) — без правки, чекаю звірку парами після експорту.
