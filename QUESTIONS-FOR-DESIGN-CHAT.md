# Питання дизайн-чату · редизайн v3

Правило (уточнення власника 11.09): бандл **каже** — робиться як у бандлі; бандл
**мовчить** — вибір робить дизайн-чат, не реалізація. Тут — те, де бандл мовчить
або суперечить сам собі, і рішення потрібне не від власника, а від дизайну.

---

## 1. Чи потрібна зонам комори окрема вісь знаків?

**Що каже бандл.** `Screens.dc.html` ставить шести зонам: `leaf` · `refrigerator` ·
`snowflake` · `archive` · `flame` · `wine`. Окремої сімʼї для зон у `Icons.dc.html`
немає: там три сімʼї (system 24 · products 20 · cooking 20), і всі шість знаків
зон позичені з них.

**Що з цього виходить на екрані.** Два знаки несуть по два значення:

| Знак | Значення 1 | Значення 2 |
|---|---|---|
| `leaf` | зона **Свіже** | категорія **Зелень** (products) — і чіп «пісне» |
| `refrigerator` | навігаційна **Комора** (весь склад) | зона **Холодильник** (один прилад) |

І одна зона взагалі без знака: `flame` на **Спеціях** суперечить правилу HANDOFF
«`flame` — тільки „Горить"», яке старше за набір (рішення Р21 від 10.09: «Спеції
→ новий знак»). Знака поки немає, у коді стоїть заглушка `flask-conical`.

**Що було зроблено й відкочено.** Реалізація 10.09 завела «четверту сімʼю» —
`sprout / refrigerator / snowflake / archive / flask-conical / wine` — і звільнила
`leaf` і `flame`. Це було рішення там, де бандл не мовчав, і 11.09 його
відкочено. Аргумент лишається як матеріал: зони — це місця, не продукти й не
техніка, і власна вісь прибрала б обидві колізії разом. Але це аргумент для
дизайн-чату, не підстава робити.

**Питання.** Три варіанти, і вибір не реалізації:

- (а) зонам — власна сімʼя з шести знаків; тоді обидві колізії зникають;
- (б) лишити як у бандлі; тоді `leaf` і `refrigerator` двозначні свідомо, і це
  треба записати в канон як виняток із правила «один знак — одне значення»;
- (в) лишити як у бандлі, а навігаційну «Комору» перевести на інший знак —
  тоді зникає одна колізія з двох.

І окремо, у будь-якому варіанті: **який знак у «Спецій»** замість `flame`.

**І ще один знак без відповіді — походження «домислено».** Рішення Р6 замінило
«зі списку» (`list-checks`, під яким у даних нічого не було) на «домислене»
(`inference` — те, з чого робиться «?домисл.N%»). Бандл знака для нього не має.
У коді заглушка `sparkles` («Думаю») — знак моделі, і саме так підписана.

**Що чекає в коді.** `PENDING_DESIGN_CHAT` у `apps/web/src/components/Icon/icons.ts`
називає обидві колізії; тест тримає їх видимими, і коли знаки розійдуться —
впаде, щоб запис не застарів мовчки.

---

## 2. Респонсив на 768: одна колонка чи дві?

`Responsive.dc.html` дає Коморі на 768 **одну** колонку; код на цій ширині дає
**дві**, і з трьома слотами рядка вони не влазять (DEBT §30, доведено підлогою
назви 7em). Бандл каже «одна» — але це варто підтвердити як задум, а не як
випадковість кадру, бо міняє точку перемикання для всіх списків.

---

## 3. `dim` не проходить власний поріг канону — що з ним робити?

`tokens-v3.md` каже «текст ≥ 4.5:1» і в тому ж файлі дає `dim = #9a9ea3` (світла) /
`#6f7379` (темна). Виміряно: на `card` це **2.69 / 3.35**, на `bg` — **2.43 / 3.73**.
Нижче порога на кожній поверхні в кожній темі. `muted` проходить усюди.

Аудит показує це як «37 текстів нижче AA» на Коморі — але це один токен у 37
місцях, не 37 дефектів. Найгірші рядки на всіх шести екранах мають один і той
самий контраст.

Три варіанти (`DEVIATIONS-V3.md` Р26): затемнити хекс · `dim` лише для не-тексту,
мета на `muted` · прийняти 3:1 як виняток і записати в канон. Реалізація може
зробити будь-який; який — питання палітри.

---

**Дописано 11.09 після прогону обох тем (етап 5).** Питання ширше за `dim`.
Темна палітра: лише `dim` не проходить 4.5 (усі 94 провали на шести екранах —
він один). Світла: `dim` (2.32–2.69), **`amber` як текст — ніде** (3.23 на
своєму тлі, 3.79 на `card`), `sage` поза `card` (4.12–4.36), `muted` на `line`
(4.36), `plum` на своєму тлі (4.42). Бандл ставить `amber` текстом на чіпи
сезонів, «бракує: …», «майже», лічильники комори. Отже рішення про `dim` за
варіантом (б) не закриє світлу тему: потрібне рішення про тонові кольори як
текст — або темніші хекси у світлій, або правило «тон — лише на знаку і крапці,
слово — `ink`/`muted`».

Поруч, того самого роду: **Р34** — волосини `line`/`line2` дають 1.16 / 1.27
(світла) і 1.17 / 1.35 (темна) проти порогу «≥ 1.5»; документ писав «1.6», і
це арифметична помилка. Варіанти в Р34; рекомендація — зняти поріг, бо межу
в рядках тримає ритм, не волосина.

---

## 4. Рядок комори в Prototype відстав від Components

`HANDOFF.md` §0 називав Prototype «єдиним джерелом правди про поведінку»; 11.09
рядок виправлено на «поведінку і флоу; канон компонентів — Components; екрани —
Screens; де вони розходяться з прототипом, прототип застарів». Це порядок
читання, і його правлено в репо.

Сам прототип від цього не змінився. Виміряно (10.09, греп по бандлу) — рядок
комори в `Prototype.dc.html` не знає шести речей, які `Components` і `Screens` уже
мають:

| | Prototype | Components / Screens |
|---|---|---|
| «не псується» | 0 | 3 |
| «розрахунок скінчився» / «−9 дн» | 0 | є в обох, двома написаннями |
| «не казав» (`status: empty`) | 0 | 1 |
| `pencil` — походження «рукою» | 0 | 3 + 4 |
| «сьогодні» на простроченому | **13** — стара механіка `days<=0` | немає |
| три канали рядка (безпека · походження · час) | одна вісь | три слоти |

Тобто прототип показує рядок комори таким, яким він був **до** етапу 2a, і саме
на цьому 10.09 я помилково відкрив ~~Р9~~ «два джерела правди суперечать».

**Рішення дизайн-чату, не реалізації**, і воно не блокує жодного етапу:

- (а) перезібрати прототип у Claude Design за поточним Components і
  переекспортувати бандл;
- (б) лишити як стенд флоу з позначкою «рядок комори застарів, канон — Components»
  просто в артборді.

Реалізації байдуже, який: код іде за Components і за тим, що на екрані.

---

## 5. «Дім зараз»: коли «до нд», а коли «ще 3 дн»?

Бандл (Components · «Дім зараз») показує обидві форми — «до нд» і «≈ ще 5 дн» — і
мовчить, коли яка. Реалізація 11.09 розвела за **джерелом**: своя подія
(`source: chat | user`) у межах тижня — днем тижня, бо своє читається як зустріч;
сезон із каталогу — днями. Це вибір там, де бандл мовчить, тобто не реалізації.

Умова одна, в `apps/web/src/lib/period.ts`, `nowWhen`:

```ts
if (it.source !== 'catalog' && left <= 7) return `${pre}до ${WEEKDAY[...]}`;
```

Переграється одним рядком. **Не блокує.**

---

## 6. Календар: `users` на двох змістах, і дві картки серії

**Знак `users`.** Бандл ставить його і на «2 порції» (Redesign, Prototype,
Screens D1 — рецепт), і на подію дому «Мама · чт – нд» / кікер «подія дому ·
своє» (Screens D3, Redesign). Один знак — два змісти, правило словника
(«один знак — одне значення») порушене в самому бандлі. У словнику це
`cook.portions` і `live.household` на одному гліфі, записано в
`PENDING_DESIGN_CHAT` (`apps/web/src/components/Icon/icons.ts`) поруч із
`leaf` і `refrigerator`. Тест знає і чекає відповіді. Кандидат для порцій —
`utensils` вже зайнятий подачею; для події дому — `house` зайнятий «Дім зараз».

Поруч, без питання: `live.season` було `calendar-days` — знака в бандлі немає
жодного разу, сезон там усюди `sun` (Components A2, Screens D3 ×3, Cook and
Share). Виправлено на `sun`; традиція — `church`, завіз — `truck`, як у A2.

**Дві картки серії.** У Components їх дві: `A1` (галочки, «Записати в
календар») і пізніша «Календар · що впливає на кухню» (чіпи традицій,
перемикачі, сезони, «додати»). Реалізація розвела за входом: A1 — з чату
(картка має застосуватись), друга — з календаря (перемикач пише одразу).
Див. `DEVIATIONS-V3.md` Р30. Якщо задум був інший — одна з двох зайва, і
краще сказати яка. **Не блокує.**

**Screens D3a — 7 колонок на 1440.** Не зроблено: PLAN §7 того ж бандла каже
«сітка лишається», і це збігається з рішенням власника 03.09 (стрічка на всіх
ширинах). Р28. Якщо дизайн-чат хоче сітку — це питання власнику, не
реалізації.

---

## 7. Рецепти: `flame` на «використає», дві картки бібліотеки, шапка шторки

**`flame` на «використає: помідори, фует»** (Screens D5) — HANDOFF резервує
`flame` за «Горить». Реалізація читає рядок як «використає те, що горить» —
той самий зміст, тому знак лишено `flame`, а `recycle` (був у словнику, у
бандлі — жодного разу) знято. Якщо це другий зміст — скажіть, і словник
отримає інший знак для «використає».

**Дві картки бібліотеки:** D1a («Можу зараз» / «Бракує 2» / «5 з 8», один
рядок під назвою — не обидва) і пізніша D5 («можу зараз / майже / далеко»,
«бракує» і «використає» обидва через `sc-if`). Зроблено за D5. Якщо D1a
«не обидва» — свідоме правило, а не старіша версія, — один рядок прибирається.

**«Знайти рецепт»** — у Prototype напис без поведінки (Р31). Це вхід у чат,
поле пошуку по бібліотеці чи декор? Не зроблено до відповіді.

**Шапка шторки** — бандл малює кікер текстом + ✕ (D3c), код узяв рядок шапки
панелі (знак панелі + вкладка-знак), бо панель уже так живе з етапу 3 (Р32).
Якщо кікер текстом важливий — це зміна і панелі, не лише шторки.

---

## 8. ~~Масштаб: «трішки зменшити» долетіло до кадрів, але не до канону~~ — закрито рішенням власника 11.09

Власник просив дизайн-чат трішки зменшити елементи. Порівняння попереднього
`Redesign.dc.html` з пізнішими Screens і `tokens-v3.md` (`CORRESPONDENCE-V3.md`):
кадри зменшили рейку 72 → 60, h1 32 → 26–28, рядки 56–62 → 48; кнопки рейки
виросли 36 → 38. А `tokens-v3.md` лишив **h1 32, display 40** і сайдбар 240
(Responsive малює 256). Тобто канон, з якого зроблено `roles.css` і який тримає
тест 1.2, більший за власні кадри.

**Рішення власника 11.09: як на кадрах Screens.** `tokens-v3.md` виправлено з
дозволу (h1 28 / 26 на 390 · display 36 · сайдбар 256, кожне число з кадром),
`roles.css` і тест 1.2 — під виправлений канон. Нижче — як стояло питання.

Питання, і воно блокує етап оболонки: **h1 — 32 (tokens) чи 26–28 (Screens)?**
І з ним: сайдбар 240 чи 256; рядок списку 48 (tokens) чи те, що намальовано.
Код зараз менший за обидва (h1 20) — підганяти його є куди, але треба знати,
під що. Проти проду бандл більший усюди (h1 20 → 26/32, мета 10.5 → 13) — це
не заперечення, а факт, який варто бачити поруч із проханням «зменшити».

---

## 9. ✕ у рядку комори: слово є, кадру нема

Components («Причина списання без модалки»): «✕ у рядку і „Списати" в картці
працюють одним тапом». Кадри «Комора · збірка» і «Комора · мобайл» — рядок без
✕; `x` у бандлі всюди — закриття карток, файлів, шторок (Prototype 4,
Components 4, Screens 6, Responsive 7, у рядку комори 0). Код показує ✕
завжди (2c). Де він живе за задумом — на ховері на десктопі, свайпом на тачі,
лише в картці? Поки — як є; це видно в парах 6b як відома різниця.

---

## 10. Рух: три джерела — три числа

HANDOFF «Незмінні правила»: ховер **120** / звично **240** / простір 400.
`tokens-v3.md` «Рух»: state **160** / geometry **300** / icon 600–1100 / live
1.8–2.4 с. Код: `--dur-fast` **150** / `--dur-base` **250** / `--dur-slow` 400
(`tokens.css:101`). Крива виходу `.4,0,1,1` і «без пружин» — збігаються
всюди. Яка пара правильна — 120/240 чи 160/300? Код перейде на неї одним
рядком; гейт reduced-motion тримає токени, не числа.

## §11 · Кегль знаків: 12/16/18/20/24 (словник) проти 13/15/17/19 (кадри)

`Icons` і `Icon.tsx` тримають закритий союз 12 · 16 · 18 · 20 · 24. Кадри
Screens/Prototype ставлять 13 (час, бракує, check у чипі), 15 (пошук, import,
plus), 17 (круглі знаки шапки 390), 19 (знак страви 44). Код бере найближче
з союзу. Питання: союз розширити до кадрів чи кадри підтягнути до союзу?
Поки — союз, різниця ±1 px.

## §12 · «Чекають на тебе · N» — де жити, коли панель стала карткою

Prototype: права панель — одна картка-артефакт на всю висоту, під нею
нічого. У коді під карткою стоїть блок «Чекають на тебе · N» (картки, що
чекають рішення: чек, кошик). Варіанти: чип у шапці чату поруч із «Горить
N» (Prototype має «Горить N» і «Готуємо · таймер» саме там); або лічильник
на цілі «Стрічка» в рейці. До відповіді блок лишається під карткою.

## §13 · «У рецепти» і «Поділитись» — де вони живуть у потоці чату

Факт по бандлу: на сторінці рецепта вони є (Screens «Рецепт · 1440»); у
панелі артефакта й на картці в стрічці — немає (Prototype: панель — стан,
назва, склад, кроки, «Готуємо»; картка — cooking-pot і reply, згорнута —
«+»); у «Готово» після готування є шерінг (Cook and Share). Можливо, бандл
має на увазі, що зберегти й поділитись — дії сторінки й «Готово», а не
чату. До відповіді в стрічці їх не малюємо (рішення власника 11.09); з
чату зберегти рецепт можна лише через сторінку рецепта.

## §14 · «Дім зараз»: що рахує «ще N», стеля «Зараз», тихі рядки в «Дім ●●● 3»

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Не блокує. Три питання до одного обʼєкта:
- що рахує «· ще N» у «Дім зараз · ще N» — на 768 кадр показує «ще 2» при
  трьох станах і двох тихих рядках, і це не сходиться. Тимчасово в коді:
  N = рядки панелі без свого чіпа («горить», але не прострочено; «Зараз» не
  суворі);
- стеля й хвіст для «Зараз» у панелі: кадр — 3 рядки без хвоста; у вересні
  сезонів може бути пʼять. Тимчасово: 3 рядки + «Ще N — у календарі»;
- чи «Дім ●●● 3» рахує тихі рядки (мама, список) — за G3 «сірий квадрат, без
  числа в чіпі», тобто ні; так і зроблено;
- 768 (R2): компактного чіпа готування в кадрі нема — виведено за патерном
  R2 «знак + найкоротший факт»: timer «6:32» sage. Так?
- 768 (R2): чіпа «Дім зараз» нема — як відкривати панель? Тимчасово — тап по
  будь-якому компактному чіпу; коли станів нуль, панель на 768 недосяжна.

## §15 · Моушн знаків зон і cooking — бандл мовчить

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Icons.dc.html дає рух 24 системним знакам (масив `system`, поле `m`) і
чотирьом живим станам; продукти — «без моушну, статичні позначки». Зони
(четверта сімʼя коду: холодильник · морозилка · суха шафа · свіже · спеції ·
напої) і cooking (тип страви, техніка, кроки) бандл не згадує. До відповіді —
статичні (1.5b, `motion.ts`: запис лише для системної сімʼї).


## §16 · Лендінг (Landing Live) — де бандл мовчить або суперечить словнику

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Етап 9, гілка `feat/landing-v3`. Реалізація — за трьома кадрами Landing Live;
нижче лише те, що не вирішується читанням кадру. Реєстр відхилень — Р41–Р49.

**Три знаки з іншим значенням у словнику.** Правило етапу: знак лендінгу, який
`icons.ts` уже тримає за іншим змістом, не береться (один знак — одне значення).
Що зроблено тимчасово й що спитати:

| Знак у бандлі | Де на лендінгу | У словнику вже | Зроблено | Питання |
|---|---|---|---|---|
| `bookmark` | кікер «Памʼять і різноманіття» | `sys.later` «Колись» | `cook.love` (heart) — той самий знак, що в «що тобі сподобалось» поруч | «Памʼять» і «Колись» — один зміст (збережене) чи два? Якщо один — кікер повертається на bookmark |
| `alert-triangle` | чіп «креветки · алерген · Оля» (правило 03) | `live.overdue` «Прострочено» | `cook.ban` — tokens-v3 сам ставить «не можна» як plum + ban | алерген — це «не можна» (ban) чи окрема тривога (triangle)? Якщо друге — треба звільнити triangle від «Прострочено» або дати простроченому інший знак |
| `minus` | степер кількості в кошику лептопа (1920) | `live.nothing` «Нічого не змінилось» | кількість числом у пілюлі, як на кадрі 1024 (там степера нема) | степер у кошику — потрібен взагалі? Якщо так — знак для «менше» (circle-minus?) і «Нічого не змінилось» лишається за minus |

Поруч, без питання — зроблено за правилами HANDOFF, записано в Р41: `flame` на
рядку «Шакшука» → `chef-hat` (flame лише «Горить»); `arrow-right` на «Далі» →
`sys.next` (chevron-right, «Далі» у словнику), на кнопці пошти 390 → `sys.send`;
«★★★★★» у журналі → пʼять `cook.rating`; «+» в аватарах тарифу «Дім» → `sys.add`.
`chevron-left` на чіпі «Кошик · 9» заведено як `landing.toPanel` («У панель») —
якщо в Prototype цей шеврон означає інше, назвіть.

**«Що вміє»: шість чи вісім.** `renderVals()` тримає `feats` з восьми рядків
(є «Поживність» з `activity` і окреме «Різноманіття» з `shuffle`), але всі три
артборди рендерять `rows` — шість рядків, де «Памʼять» і «Різноманіття» злиті
в один. Зроблено шість, як на кадрах. Якщо вісім — задум, а шість — стиснення
під сцену, скажіть: сцена на 1920 витримає вісім, на 390 стек стане довшим.

**Темна тема.** Landing Live намальовано лише світлим (`.v3` з фіксованими
хексами). На токенах темна приходить сама, але три місця стають нечитабельними
або зникають, і їх вирішено провізорно (Р42): `dim` як текст на темній →
`muted` (dim у темній дає 3.7:1); скло-фрагменти — біла тінь .34 на темному
дає молочну пляму → .09/.04; ілюстрації (чорний контур на прозорому) на
темному невидимі → `invert(1) hue-rotate(180deg)`. Другий текст на чорнильних
поверхнях (пауза, тариф «Дім») — `color-mix(bg 60 %, ink)`: у світлій це той
самий dim бандла (#9d9d9c ≈ #9a9ea3), у темній читається. Потрібні власні
темні значення бандла або підтвердження цих.

**reduced-motion — що є «кінцевим станом».** Бандл під reduce глушить
анімації, і статичний кадр показує ВСЕ разом: чотири повідомлення, індикатор
«друкує» і курсор над кнопкою. Зроблено інакше (Р43): індикатор і курсор
сховані, чат стоїть на останній репліці з кошиком. Якщо задум — «усе разом»,
це два рядки CSS.

**Кнопка меню на 390.** Кадр малює `menu` праворуч від «Увійти», поведінки не
показує. Зроблено список трьох якорів шапки під кнопкою (Р48).

**Футер.** «Приватність» і «Умови» на всіх кадрах — текст без адрес; так і
зроблено (span, не a). Куди вони ведуть — питання не реалізації.

---

# Зведення потоків (12.09) — з файлів `QUESTIONS-<потік>.md`

## landing · додатки до §16

Файл потоку; номер потоку — **§16**, додатки під ним. §16 (лендінг) уже в
спільному `QUESTIONS-FOR-DESIGN-CHAT.md` через PR #52 — тут не дублюється.
Правило: бандл **каже** — робиться як у бандлі; бандл **мовчить** — вибір робить
дизайн-чат, не реалізація.

---

## §16 · додаток: Перевір пошту · Запрошення (Auth.dc.html) — де бандл мовчить

Етап 9а, гілка `feat/auth-v3` (нащадок `feat/landing-v3`). Реєстр — DEVIATIONS-V3-landing Р49.

**Дані картки дому.** Кадр «Запрошення» показує аватари трьох людей і «3 людини ·
61 позиція в коморі · для olya@gmail.com». `/v1/invites/info` віддає лише пошту,
назву дому й роль — кількість людей і позицій нема звідки взяти без зміни
контракту API (рішення власника, не дизайн-чату). Зроблено картку з назвою і
«для {пошта}», без аватарів і лічильників. Питання дизайн-чату: чи картка без
аватарів — прийнятна форма, або треба інший знак замість них (house у роді).

**`house` на кікері «Запрошення в дім».** Бандл ставить той самий знак, що на
«Дім зараз» у шапці чату. Записано в `PENDING_DESIGN_CHAT` поруч із leaf /
refrigerator / users: стан «Дім зараз» і дім як спільнота — один знак чи два?

**Ширини.** Бандл малює лише 1024 і 390. Зроблено: ≥768 — кадр 1024 fluid (без
шапки лендінгу 80 і без центрованої nav — їх у кадрі нема), <768 — кадр 390.
Чи потрібен окремий кадр 1920 (шапка 80, як у лендінгу) — питання дизайн-чату.

**Недійсне запрошення.** Бандл: «Errors E1 із кікером danger і „На головну“».
E1 у коді — `ErrorScreen` етапу Е1 (кікер одним стилем, без роду); етап 10
(Помилки) його переверстає. Копі розкладено у два тони за правилом ErrorScreen:
«Запрошення недійсне.» / «Цей лінк уже не працює.» + «Попроси надіслати новий.»
(було одним абзацем у Invite.tsx) — у COPY-DEBT не вносив, слова ті самі.

**Стан «Перевіряю запрошення…».** У кадрі його нема; зроблено тим самим каркасом
з другим рядком заголовка «Перевіряю запрошення…» і порожнім підзаголовком.

## §16 · додаток 2: помилки (Errors.dc.html) — де бандл мовчить

Етап 10, гілка `feat/errors-v3`. Реєстр — DEVIATIONS-V3-landing Р50.

**Смуга денного ліміту.** E2 малює четверту смугу — `moon` · «на сьогодні» ·
«На сьогодні я майже все.» · «Дві відповіді — і до півночі відпочиваю.» У коді
такого стану немає (є лише 429 за `Retry-After` і `kind`). Це логіка, не
вигляд — етап 10 її не додає. Кому належить: власнику (контракт «денний ліміт»
на сервері) чи це лишається текстом у COPY.md?

**E1 на 390.** Кадру нема, є речення: «кегль 26/15, кнопка на всю ширину внизу
екрана». Зроблено так; знімки `errors-crash-390-*`. Чи потрібен кадр.

**Розрив кільця.** У кадрі сектор-розрив стоїть угорі праворуч (polygon 50 %
50 % → 50 % −20 % → 120 % −20 % → 120 % 50 %) — відтворено тим самим
полігоном. Логотип у застосунку (`Logo`) має інший розрив (dasharray 104/15,
поворот −58°). Один знак чи два — питання дизайн-чату; ErrorScreen узяв
кадр.

**Смуги E2 — код розходиться з кадром** у радіусі, кеглях і кольорі тексту
(Р50, список). Етап 5 робив їх за словами PLAN; кадр Errors зʼявився пізніше.
Переверстати під кадр — окреме рішення, бо ті самі числа тримають тести
етапу 5 і рядок стану дії (ActionState) поруч.

## §16 · додаток 3: онбординг (Onboarding.dc.html) — де бандл мовчить

Етап 11, гілка `feat/onboarding-v3`. Реєстр — DEVIATIONS-V3-landing Р51.

**Ширини між кадрами.** Бандл малює десктоп (мірявся на 1440) і 390. Зроблено:
≥1024 — сітка десктопу (дві колонки вміщаються від ~900), <1024 — колонка 390.
Чи потрібен окремий кадр 768 (планшет), як у решти екранів Responsive?

**Ремарка Семена.** Завдання казало «курсивом / muted»; кадр і Prototype
малюють її шавлією 500 без курсиву окремим рядком бабла — зроблено за кадром.
Якщо задум — курсив, це один рядок CSS.

**Картка знайомства у стрічці** — знято №37 (11.09): знайомство стало повним
екраном одразу після Семена, кроки 12–18 на /welcome; картка в стрічці схована
прапорцем до окремого PR на прибирання коду. №40 (12.09): розкладка знайомства —
за Prototype v3.1 (своя шапка з «Заповню потім» і 7 рисками, сітка текст · 300),
питання про «Заповню потім» закрите — воно є (Р51).

**Капс у підписах станів картки** («ЗАПИСАНО», «ПРОПУЩЕНО», «НІЧОГО ТАКОГО») —
тримають тести О2; у канон «без капсу» переводити разом із проходом копі
(COPY-DEBT блок 1).

## time-people · §17–§18

Правило — як у спільному `QUESTIONS-FOR-DESIGN-CHAT.md`: бандл каже — робиться
як у бандлі; мовчить — вибір дизайн-чату, не реалізації. Діапазон потоку —
§17–§18. Зведення у спільний файл робить власник після мерджу.

---

## §17 · Знаки з кадрів календаря й профілю, яких немає у словнику

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Словник (`components/Icon/icons.ts`) — файл потоку icon-motion; потік
time-people-v3 його не чіпає. Кадри ставлять знаки, яких у словнику нема, —
на екрані замість них слово або найближчий знак зі словника:

| кадр | знак у бандлі | у коді | де |
|---|---|---|---|
| D3a/D3b «Що на вечерю?» | arrow-right 11/13 | chevron-right (`sys.next`) | календар |
| A3 «Не показувати» · «Додати в список» | eye-off 14 · list-plus 15 | без знаків | подія |
| A1 «обмежую» | lock 11 | без знака, слово plum 500 | серія |
| D4 підказка | lightbulb 13 | без знака | профіль |
| D4 «Мені не можна» · Components | alert-triangle 15 · circle-alert 15 | ban (`cook.ban`, «Не можна» дослівно) | профіль |
| D4 запрошення «чекає» | mail 13 у пунктирному колі | пунктирне коло | профіль |
| D4 «Лист не дійшов» | mail-x 14 | без знака | профіль |
| D4 рядок учасника | more-horizontal 15 | дії словами | профіль |
| D4 видалення | trash-2 13 | без знака | профіль |
| Components «Обрізано на межі» | scissors 12 | стану в коді нема (Р63) | профіль |

Питання: додати ці десять до системної сімʼї (з рухом чи без) — чи бандл
свідомо тримає союз малим і слова тут правильніші? Не блокує.
## §18 · Сезонні смуги в тижні: стеля чи групування при живому каталозі

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Кадр D3a малює над тижнем три смуги (піст · помідори · мама). Живий каталог
сезонів у вересні дає **сім** одночасних сезонів плюс своє — у dev над кожним
тижнем вісім смуг по 20 px (пари `tp1-calendar-1440`), і картка тижня росте
на 180 px ще до днів. Бандл про це мовчить — у його даних три події.

Варіанти, і вибір не реалізації: (а) стеля — ≤3 смуги за рангом
(обмеження → своє → сезон), решта одним рядком «ще 5 сезонів» тоном
сезону, що веде в підписки; (б) сезони каталогу — однією зведеною смугою
«сезони · 7» на тиждень, окремими — лише своє й обмеження; (в) лишити як
є, а щільність каталогу вирішувати в самому каталозі (DEBT §36, варіант б).
Поки — (в): рядок на подію, як намальовано.

## things · §19–§20

Правило: бандл каже — робиться як у бандлі; мовчить — вибір робить дизайн-чат.
Номери §19–20. Зведення в `QUESTIONS-FOR-DESIGN-CHAT.md` — власник після мерджу.

## §19 · Порядок рядків усередині зони при «за місцем»

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Кадр «Комора · збірка» показує Свіже так: стейк −9 · айсберг −4 · помідори
сьогодні · апельсини ≈ 9 · цибуля ≈ 21 — тобто за строком, і банер каже
«решта нижче за свіжістю». Код тримає Ф2а: усередині зони новіші за
`added_at` зверху, однакова дата — за назвою, щоб рядки не стрибали після
правки; за строком — окремий порядок «за свіжістю» (плаский список). Чи
порядок у кадрі — задум («за місцем» = зони, а в зоні за строком), чи
випадковість ручних даних? Не блокує: перемикається одним компаратором у
`filter.ts` (`byAddedThenName`).

## §20 · Три знаки картки позиції, яких нема в словнику: circle-alert · trash-2 · activity

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Components «ITEM CARD» і G6 ставлять на плашку простроченого `circle-alert`,
на «Зіпсувалось» / «Списати» — `trash-2`, на «На 100 г» — `activity`.
`Icons.dc.html` цих трьох у сімʼях не має, а словник `icons.ts` — у потоці
icon-motion, і things-v3 його не чіпає. Тимчасово: `live.overdue`
(triangle-alert) на плашці, кнопки й рядок без знака. Питання — чи ці три
входять у системну сімʼю (і тоді з яким рухом, §15), чи кадр малював їх поза
словником.

## cook-share · §21–§25

## §21 · Cook Mode: sun і moon як перемикач теми проти словника Icons

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Cook and Share («Cook · 768/390») і Prototype ставлять у шапці Cook Mode два
знаки sun / moon як сегментний перемикач теми. Словник Icons дає sun
«Сезон» (live), moon у коді — «Піст», і має окремий `sun-moon` «Тема». У
коді — `cook.themeLight` / `cook.themeDark` (окремий блок icons.ts), колізія
записана в PENDING_DESIGN_CHAT. Один знак sun-moon чи два sun/moon? І чи
«Cook Mode памʼятає свою тему окремо» (кадр 390) — це справді окрема
памʼять, чи тема застосунку (Р72)?

## §22 · Cook Mode: підказка й порада до кроку — звідки дані

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Prototype малює lightbulb-пораду до кроку («Фует — уже в тарілку…») і
«Далі: спагеті у воду — постав воду вже зараз» — підказку наступного кроку.
Контракт кроку в коді — `t · c · s` (`api.ts:977`): ні `tip`, ні `hint`.
Або модель дає два нові поля (правка `packages/prompts`, рішення власника),
або кадр показує те, чого не буде. До відповіді — порада не малюється (Р73),
«Далі:» — назва наступного кроку (Р74).

## §23 · Рядок розмови в сайдбарі: один рядок (Prototype) чи два (Responsive)

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Prototype nav малює розмови одним рядком 36 (назва 13, активна card+sh);
Responsive R1/R2/R3 — двома (назва 14 + «сьогодні · 18:42» / «18:42 · чек
чекає рішення» 12). Зроблено два (Р81). Другий рядок у коді — час; стан
«чекає рішення» / «приготовано» — з карток дому, які знає лише стрічка. Чи
потрібен стан у сайдбарі (тоді це новий стор, не вигляд), чи час достатньо?

## §24 · Число на «Коморі» в сайдбарі: «прострочено» чи «горить»

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

R1/R2 пишуть «Комора 10» = прострочено, рейка — крапку danger. У коді один
факт на обидва — «горить» (`isSoon`, ≤ 3 дн, прострочені всередині; Р82).
Якщо число має бути саме «прострочено», крапка в рейці теж має рахувати
його — інакше крапка є, а числа нема.

## §25 · Знак у рядку профілю

**Закрито 12.09** — відповідь у `ai/project/ANSWERS-FROM-DESIGN-CHAT.md` (§1–§13 і пакет A–F), у коді — `fix/design-answers-2` (DEVIATIONS Р110–Р113).

Prototype nav — settings-2 (15, dim); R3 — шеврон. У словнику Icons
settings-2 нема. Поставлено шеврон на всіх ширинах (Р83). Який знак і що
він означає — «налаштування» чи просто «далі»?
