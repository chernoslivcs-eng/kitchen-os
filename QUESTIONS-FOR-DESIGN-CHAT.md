# Питання дизайн-чату · редизайн v3

Правило (уточнення власника 11.09): бандл **каже** — робиться як у бандлі; бандл
**мовчить** — вибір робить дизайн-чат, не реалізація. Тут — те, де бандл мовчить
або суперечить сам собі, і рішення потрібне не від власника, а від дизайну.

---

## 1. Чи потрібна зонам комори окрема вісь знаків?

**Що каже бандл.** `Screens.dc.html` ставить шести зонам: `leaf` · `refrigerator` ·
`snowflake` · `archive` · `flame` · `wine`. Окремої сімʼї для зон у `Icons.dc.html`
немає: там три сімʼї (system 24 · products 20 · cooking 20), і всі шість знаків
зон позичені з них.

**Що з цього виходить на екрані.** Два знаки несуть по два значення:

| Знак | Значення 1 | Значення 2 |
|---|---|---|
| `leaf` | зона **Свіже** | категорія **Зелень** (products) — і чіп «пісне» |
| `refrigerator` | навігаційна **Комора** (весь склад) | зона **Холодильник** (один прилад) |

І одна зона взагалі без знака: `flame` на **Спеціях** суперечить правилу HANDOFF
«`flame` — тільки „Горить"», яке старше за набір (рішення Р21 від 10.09: «Спеції
→ новий знак»). Знака поки немає, у коді стоїть заглушка `flask-conical`.

**Що було зроблено й відкочено.** Реалізація 10.09 завела «четверту сімʼю» —
`sprout / refrigerator / snowflake / archive / flask-conical / wine` — і звільнила
`leaf` і `flame`. Це було рішення там, де бандл не мовчав, і 11.09 його
відкочено. Аргумент лишається як матеріал: зони — це місця, не продукти й не
техніка, і власна вісь прибрала б обидві колізії разом. Але це аргумент для
дизайн-чату, не підстава робити.

**Питання.** Три варіанти, і вибір не реалізації:

- (а) зонам — власна сімʼя з шести знаків; тоді обидві колізії зникають;
- (б) лишити як у бандлі; тоді `leaf` і `refrigerator` двозначні свідомо, і це
  треба записати в канон як виняток із правила «один знак — одне значення»;
- (в) лишити як у бандлі, а навігаційну «Комору» перевести на інший знак —
  тоді зникає одна колізія з двох.

І окремо, у будь-якому варіанті: **який знак у «Спецій»** замість `flame`.

**І ще один знак без відповіді — походження «домислено».** Рішення Р6 замінило
«зі списку» (`list-checks`, під яким у даних нічого не було) на «домислене»
(`inference` — те, з чого робиться «?домисл.N%»). Бандл знака для нього не має.
У коді заглушка `sparkles` («Думаю») — знак моделі, і саме так підписана.

**Що чекає в коді.** `PENDING_DESIGN_CHAT` у `apps/web/src/components/Icon/icons.ts`
називає обидві колізії; тест тримає їх видимими, і коли знаки розійдуться —
впаде, щоб запис не застарів мовчки.

---

## 2. Респонсив на 768: одна колонка чи дві?

`Responsive.dc.html` дає Коморі на 768 **одну** колонку; код на цій ширині дає
**дві**, і з трьома слотами рядка вони не влазять (DEBT §30, доведено підлогою
назви 7em). Бандл каже «одна» — але це варто підтвердити як задум, а не як
випадковість кадру, бо міняє точку перемикання для всіх списків.

---

## 3. `dim` не проходить власний поріг канону — що з ним робити?

`tokens-v3.md` каже «текст ≥ 4.5:1» і в тому ж файлі дає `dim = #9a9ea3` (світла) /
`#6f7379` (темна). Виміряно: на `card` це **2.69 / 3.35**, на `bg` — **2.43 / 3.73**.
Нижче порога на кожній поверхні в кожній темі. `muted` проходить усюди.

Аудит показує це як «37 текстів нижче AA» на Коморі — але це один токен у 37
місцях, не 37 дефектів. Найгірші рядки на всіх шести екранах мають один і той
самий контраст.

Три варіанти (`DEVIATIONS-V3.md` Р26): затемнити хекс · `dim` лише для не-тексту,
мета на `muted` · прийняти 3:1 як виняток і записати в канон. Реалізація може
зробити будь-який; який — питання палітри.

---

**Дописано 11.09 після прогону обох тем (етап 5).** Питання ширше за `dim`.
Темна палітра: лише `dim` не проходить 4.5 (усі 94 провали на шести екранах —
він один). Світла: `dim` (2.32–2.69), **`amber` як текст — ніде** (3.23 на
своєму тлі, 3.79 на `card`), `sage` поза `card` (4.12–4.36), `muted` на `line`
(4.36), `plum` на своєму тлі (4.42). Бандл ставить `amber` текстом на чіпи
сезонів, «бракує: …», «майже», лічильники комори. Отже рішення про `dim` за
варіантом (б) не закриє світлу тему: потрібне рішення про тонові кольори як
текст — або темніші хекси у світлій, або правило «тон — лише на знаку і крапці,
слово — `ink`/`muted`».

Поруч, того самого роду: **Р34** — волосини `line`/`line2` дають 1.16 / 1.27
(світла) і 1.17 / 1.35 (темна) проти порогу «≥ 1.5»; документ писав «1.6», і
це арифметична помилка. Варіанти в Р34; рекомендація — зняти поріг, бо межу
в рядках тримає ритм, не волосина.

---

## 4. Рядок комори в Prototype відстав від Components

`HANDOFF.md` §0 називав Prototype «єдиним джерелом правди про поведінку»; 11.09
рядок виправлено на «поведінку і флоу; канон компонентів — Components; екрани —
Screens; де вони розходяться з прототипом, прототип застарів». Це порядок
читання, і його правлено в репо.

Сам прототип від цього не змінився. Виміряно (10.09, греп по бандлу) — рядок
комори в `Prototype.dc.html` не знає шести речей, які `Components` і `Screens` уже
мають:

| | Prototype | Components / Screens |
|---|---|---|
| «не псується» | 0 | 3 |
| «розрахунок скінчився» / «−9 дн» | 0 | є в обох, двома написаннями |
| «не казав» (`status: empty`) | 0 | 1 |
| `pencil` — походження «рукою» | 0 | 3 + 4 |
| «сьогодні» на простроченому | **13** — стара механіка `days<=0` | немає |
| три канали рядка (безпека · походження · час) | одна вісь | три слоти |

Тобто прототип показує рядок комори таким, яким він був **до** етапу 2a, і саме
на цьому 10.09 я помилково відкрив ~~Р9~~ «два джерела правди суперечать».

**Рішення дизайн-чату, не реалізації**, і воно не блокує жодного етапу:

- (а) перезібрати прототип у Claude Design за поточним Components і
  переекспортувати бандл;
- (б) лишити як стенд флоу з позначкою «рядок комори застарів, канон — Components»
  просто в артборді.

Реалізації байдуже, який: код іде за Components і за тим, що на екрані.

---

## 5. «Дім зараз»: коли «до нд», а коли «ще 3 дн»?

Бандл (Components · «Дім зараз») показує обидві форми — «до нд» і «≈ ще 5 дн» — і
мовчить, коли яка. Реалізація 11.09 розвела за **джерелом**: своя подія
(`source: chat | user`) у межах тижня — днем тижня, бо своє читається як зустріч;
сезон із каталогу — днями. Це вибір там, де бандл мовчить, тобто не реалізації.

Умова одна, в `apps/web/src/lib/period.ts`, `nowWhen`:

```ts
if (it.source !== 'catalog' && left <= 7) return `${pre}до ${WEEKDAY[...]}`;
```

Переграється одним рядком. **Не блокує.**

---

## 6. Календар: `users` на двох змістах, і дві картки серії

**Знак `users`.** Бандл ставить його і на «2 порції» (Redesign, Prototype,
Screens D1 — рецепт), і на подію дому «Мама · чт – нд» / кікер «подія дому ·
своє» (Screens D3, Redesign). Один знак — два змісти, правило словника
(«один знак — одне значення») порушене в самому бандлі. У словнику це
`cook.portions` і `live.household` на одному гліфі, записано в
`PENDING_DESIGN_CHAT` (`apps/web/src/components/Icon/icons.ts`) поруч із
`leaf` і `refrigerator`. Тест знає і чекає відповіді. Кандидат для порцій —
`utensils` вже зайнятий подачею; для події дому — `house` зайнятий «Дім зараз».

Поруч, без питання: `live.season` було `calendar-days` — знака в бандлі немає
жодного разу, сезон там усюди `sun` (Components A2, Screens D3 ×3, Cook and
Share). Виправлено на `sun`; традиція — `church`, завіз — `truck`, як у A2.

**Дві картки серії.** У Components їх дві: `A1` (галочки, «Записати в
календар») і пізніша «Календар · що впливає на кухню» (чіпи традицій,
перемикачі, сезони, «додати»). Реалізація розвела за входом: A1 — з чату
(картка має застосуватись), друга — з календаря (перемикач пише одразу).
Див. `DEVIATIONS-V3.md` Р30. Якщо задум був інший — одна з двох зайва, і
краще сказати яка. **Не блокує.**

**Screens D3a — 7 колонок на 1440.** Не зроблено: PLAN §7 того ж бандла каже
«сітка лишається», і це збігається з рішенням власника 03.09 (стрічка на всіх
ширинах). Р28. Якщо дизайн-чат хоче сітку — це питання власнику, не
реалізації.

---

## 7. Рецепти: `flame` на «використає», дві картки бібліотеки, шапка шторки

**`flame` на «використає: помідори, фует»** (Screens D5) — HANDOFF резервує
`flame` за «Горить». Реалізація читає рядок як «використає те, що горить» —
той самий зміст, тому знак лишено `flame`, а `recycle` (був у словнику, у
бандлі — жодного разу) знято. Якщо це другий зміст — скажіть, і словник
отримає інший знак для «використає».

**Дві картки бібліотеки:** D1a («Можу зараз» / «Бракує 2» / «5 з 8», один
рядок під назвою — не обидва) і пізніша D5 («можу зараз / майже / далеко»,
«бракує» і «використає» обидва через `sc-if`). Зроблено за D5. Якщо D1a
«не обидва» — свідоме правило, а не старіша версія, — один рядок прибирається.

**«Знайти рецепт»** — у Prototype напис без поведінки (Р31). Це вхід у чат,
поле пошуку по бібліотеці чи декор? Не зроблено до відповіді.

**Шапка шторки** — бандл малює кікер текстом + ✕ (D3c), код узяв рядок шапки
панелі (знак панелі + вкладка-знак), бо панель уже так живе з етапу 3 (Р32).
Якщо кікер текстом важливий — це зміна і панелі, не лише шторки.

---

## 8. ~~Масштаб: «трішки зменшити» долетіло до кадрів, але не до канону~~ — закрито рішенням власника 11.09

Власник просив дизайн-чат трішки зменшити елементи. Порівняння попереднього
`Redesign.dc.html` з пізнішими Screens і `tokens-v3.md` (`CORRESPONDENCE-V3.md`):
кадри зменшили рейку 72 → 60, h1 32 → 26–28, рядки 56–62 → 48; кнопки рейки
виросли 36 → 38. А `tokens-v3.md` лишив **h1 32, display 40** і сайдбар 240
(Responsive малює 256). Тобто канон, з якого зроблено `roles.css` і який тримає
тест 1.2, більший за власні кадри.

**Рішення власника 11.09: як на кадрах Screens.** `tokens-v3.md` виправлено з
дозволу (h1 28 / 26 на 390 · display 36 · сайдбар 256, кожне число з кадром),
`roles.css` і тест 1.2 — під виправлений канон. Нижче — як стояло питання.

Питання, і воно блокує етап оболонки: **h1 — 32 (tokens) чи 26–28 (Screens)?**
І з ним: сайдбар 240 чи 256; рядок списку 48 (tokens) чи те, що намальовано.
Код зараз менший за обидва (h1 20) — підганяти його є куди, але треба знати,
під що. Проти проду бандл більший усюди (h1 20 → 26/32, мета 10.5 → 13) — це
не заперечення, а факт, який варто бачити поруч із проханням «зменшити».

---

## 9. ✕ у рядку комори: слово є, кадру нема

Components («Причина списання без модалки»): «✕ у рядку і „Списати" в картці
працюють одним тапом». Кадри «Комора · збірка» і «Комора · мобайл» — рядок без
✕; `x` у бандлі всюди — закриття карток, файлів, шторок (Prototype 4,
Components 4, Screens 6, Responsive 7, у рядку комори 0). Код показує ✕
завжди (2c). Де він живе за задумом — на ховері на десктопі, свайпом на тачі,
лише в картці? Поки — як є; це видно в парах 6b як відома різниця.

---

## 10. Рух: три джерела — три числа

HANDOFF «Незмінні правила»: ховер **120** / звично **240** / простір 400.
`tokens-v3.md` «Рух»: state **160** / geometry **300** / icon 600–1100 / live
1.8–2.4 с. Код: `--dur-fast` **150** / `--dur-base` **250** / `--dur-slow` 400
(`tokens.css:101`). Крива виходу `.4,0,1,1` і «без пружин» — збігаються
всюди. Яка пара правильна — 120/240 чи 160/300? Код перейде на неї одним
рядком; гейт reduced-motion тримає токени, не числа.

