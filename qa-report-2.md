# Kitchen OS — QA-прогін №2

**Прогін:** 2026-08-29, вручну через Chrome, dev (`:5173` + `:3000`, backend postgres, model_provider openrouter).
**Коміт:** `542cf4f` (`Add QA-2 brief`), поверх `5ba418c` («QA-report фікси: 8 знахідок Cowork»).

**Підсумок: 34 перевірки пройшли, 8 провалів — 3 blocker, 2 major, 3 minor.**
Області B (Cook Mode), C (ретро+фото), E (Journal) — **чисті повністю**.
Область A (Recipe) провалена: до неї не веде жоден UI-шлях.
**До Vercel-деплою не готово** — див. розділ «Вердикт».

**FIX-01 працює.** `POST /v1/recipes/generate` → 200, рецепт генерується за 13.5 с.
Це і розблокувало все нижче: області A-E цього разу пройдені вперше.

---

## Стан 11 правок із першого звіту

| # | Де | Стан |
|---|---|---|
| FIX-01 | `model.ts` дефолти | ✓ `claude-sonnet-4.5` / `claude-haiku-4.5`; `.env` не задано, працює на дефолтах |
| FIX-02 | `api.ts` `buildHeaders` | ✓ код і тест; **UI-кнопку блокує QA2-03** |
| FIX-03 | «Ні» | ✓ перевірено: кнопки зникли, комора не змінилась |
| FIX-04 | `toggleOpened` | ✓ у коді (шле label/value/unit/zone) |
| FIX-05 | `extractJson` | ✓ код + 6 тестів; сирого JSON за 4 запити не було |
| FIX-06 | «Рецепт →» на кожній | ✓ у коді; **у UI не перевірити — QA2-01** |
| FIX-07 | `plural()` | ✓ «18 ПОЗИЦІЙ», «21 ПОЗИЦІЯ», «Списано 2 позиції», «1 ГОТУВАННЯ». **✗ Recipe/Share — QA2-05** |
| FIX-08 | TabBar sticky | ✓ таб-бар видно завжди. **Ціна — QA2-03** |
| FIX-09 | плейсхолдер | ✓ «Що купив або що готуємо?» |
| FIX-10 | одиниці | ✓ «32.99 шт», «100 г», «1 Л». Дрібниця — QA2-08 |
| FIX-11 | `catch` у `remove()` | ✓ |

Тести: `catalog 18` + `web 20` + `api 69` + `domain 8` = **115 зелених**.
`packages/db` не рахую — у моєму пісочниці немає DNS до Neon (`EAI_AGAIN`), це не код.

Із `DEPLOY-CHECKLIST.md` теж зроблено: `includeFiles`, міграції в `buildCommand`, `maxDuration: 120`.

---

## BLOCKER

### QA2-01 · Картка `proposal` не з'являється взагалі

**Кроки:** Стрічка → «дай рецепт із шоколаду» → «так, дай три варіанти десерту» → «що приготувати на вечерю»

**Очікував:** картку `proposal` з кнопками «Рецепт →»
**Отримав:** три відповіді поспіль **без жодної картки**. Модель описує варіанти прозою:

```
відповідей: 3 · з карткою: 0 · остання картка: null
"Комора тут без нічого солодкого… Три варіанти залежать від того, чи готовий ти докупити:
 **1. Шоколадний мус** (якщо є дома яйця й вершки)…"
```

Це **регресія від FIX-01**. Промпт писався під `claude-3-haiku`: та модель слухняно віддавала
JSON-картку. `claude-sonnet-4.5` віддає перевагу розмові — уточнює, пропонує списком, картку не емітить.

**Важливо для діагностики:** `intake_diff` при цьому працює бездоганно —
«купив кефір 1 л» → картка з `ops:[{кефір 1 л, 1, l}]`. Тобто контракт карток не зламаний глобально,
не спрацьовує саме гілка `proposal`.

**Наслідок:** у Recipe не потрапити через інтерфейс. Області A-E я пройшов, підклавши рецепт
у router-state напряму (`history.pushState({usr:{recipe}})`) — тобто **вхід симульовано**.
Це треба перевірити ще раз після фікса.

**Куди дивитись:** `packages/prompts/versions/*/role.md` і `card-rules.md` — гілка `proposal`.
Найімовірніше потрібен жорсткіший контракт: «на запит рецепта/ідеї — ЗАВЖДИ картка `proposal`,
проза тільки поза JSON». Плюс серверний страж: якщо в тексті юзера маркер рецепта, а картки немає —
логувати як промах промпта, щоб це було видно в метриках, а не тільки очима QA.

**Severity:** blocker.

---

### QA2-02 · Інгредієнти з комори рендеряться як сирий UUID, плейсхолдери не підставляються

**Кроки:** відкрити будь-який згенерований рецепт (Recipe → Cook Mode → публічний `/r/<id>`)

**Отримав** на всіх трьох екранах:
```
ІНГРЕДІЄНТИ
• [64b7dbe0-3702-4516-a2ee-1c5f4487ca87]
• [96231cee-fe63-456b-840c-741ac704d73e]

КРОКИ
2  Надрізати. Зробити глибокий надріз уздовж {0}, не дорізаючи до кінця
3  Начинити. Вкласти всередину кожної булочки по {1} шоколаду
```

**Причина.** Модель показує на партію комори через `ing.p` — це задумана поведінка
(`model.ts`, `RecipeIng.p`: «id партії з комори — модель показує пальцем»). Але **жоден екран
не резолвить `p` у назву**: `grep -n "pantry\|batches" Recipe.tsx Cook.tsx` — порожньо.

- `Recipe.tsx:86` — `const name = ing.n ?? (ing.p ? \`[${ing.p}]\` : '—')`
- `Recipe.tsx:171` і `Cook.tsx:387` — `renderStepContent()` дублюється, обидві дивляться лише
  на `it.v/it.u/it.n`; коли модель дала `{p, u}` без `v`, повертається `{0}` як є.

**Третій наслідок, найгірший.** `Recipe.tsx:87` рахує алергени від цього ж рядка:
```ts
const flags = flagsFor(name);   // name === "[64b7dbe0-…]"
```
Тобто **мітка «⚠ молюски» на комірному інгредієнті не може спрацювати ніколи** — вона зіставляє
алергію з UUID. Перевірка з брифу («алергія молюски → червона мітка») не просто не пройшла —
вона й не могла пройти.

**І це видно чужим людям.** Ті самі `[uuid]` і `{0}` — на публічній сторінці `/r/<id>`,
яку відкриває той, кому надіслали посилання.

**Що робити:** підтягнути комору на Recipe/Cook (`api.pantry()`), зробити мапу `id → label`,
і винести `renderStepContent` у спільний модуль, щоб не правити двічі. Фолбек, коли партії вже
нема в коморі, — `ing.n`, потім «інгредієнт».

**Severity:** blocker — рецепт нечитабельний, алергічна мітка мертва, і це публічно.

---

### QA2-03 · Sticky-таб-бар перекриває «ПРИБРАТИ З КОМОРИ» — кнопка неклікабельна

**Це регресія від мого ж FIX-08**, і я її в першому звіті позначив як ризик. Ризик стався.

**Кроки:** Комора → тап на партію → шит → дивитись на низ шита

**Заміряно в консолі:**
```
кнопка_видалення: { top: 708, bottom: 747 }
tabbar:           { top: 694, bottom: 769, position: "sticky", z: "20" }
document.elementFromPoint(центр кнопки) → SPAN._glyph_…   (гліф таб-бару)
кнопка_клікабельна: false
```

Причина проста: `Sheet.tsx:59` має `zIndex: 20`, і `TabBar.module.css:6` тепер теж `z-index: 20`.
За рівного z-index виграє той, хто пізніше в DOM, — таб-бар.

Тобто **FIX-02 полагодив DELETE на рівні API, а FIX-08 забрав у користувача кнопку**.
Разом: видалити партію через інтерфейс досі неможливо.

**Патч — `apps/web/src/components/TabBar/TabBar.module.css:6`:**
```diff
 .wrap {
   position: sticky;
   bottom: 0;
-  z-index: 20;
+  /* Нижче за Sheet (Sheet.tsx, zIndex 20) — інакше таб-бар перекриває
+     нижні дії модалки, зокрема «Прибрати з комори». */
+  z-index: 10;
```
Перевірити заразом: `.tab` має `z-index: 5` (рядок 74) — лишається всередині свого контексту, не чіпати.

**Перевірка:** Комора → партія → шит → `document.elementFromPoint` у центрі «ПРИБРАТИ З КОМОРИ»
повертає саму кнопку; клік видаляє позицію, лічильник −1.

**Severity:** blocker.

---

## MAJOR

### QA2-04 · Маркдаун від моделі рендериться сирим

**Отримав** у стрічці: `**1. Шоколадний мус** (якщо є дома яйця й вершки)` — із зірочками.

Ще одна регресія від зміни моделі: `claude-3-haiku` писала плоским текстом,
`claude-sonnet-4.5` за замовчуванням форматує. Чат рендерить `reply` як plain text.

Два шляхи, обрати один:
- **дешевий:** у промпті заборонити маркдаун у полі `reply` (додати в `role.md` рядок
  «без **, ##, списків — інтерфейс не рендерить маркдаун»);
- **правильний:** мінімальний рендер `**bold**` / `\n\n` у бульбашці відповіді.

Перший швидший і не тягне залежностей, але тримається на слухняності моделі — тобто зламається
знову при наступній заміні. Другий — один невеликий компонент.

**Severity:** major — видно в кожній довгій відповіді.

---

### QA2-05 · `plural()` не доїхав до Recipe і Share

FIX-07 закрив Комору, Cook, Journal і Feed, але два екрани лишились:

| Екран | Видно | Має бути |
|---|---|---|
| Recipe (шапка) | `8ХВ · 1 ПОРЦІЇ · 280ККАЛ` | `1 ПОРЦІЯ` |
| Share (оверлей) | `8 ХВ` / `1 ПОРЦІЇ` | `1 ПОРЦІЯ` |
| SharedRecipe `/r/<id>` | `8ХВ · 1 ПОРЦІЇ · 280ККАЛ` | `1 ПОРЦІЯ` |

Той самий `plural(n, ['ПОРЦІЯ','ПОРЦІЇ','ПОРЦІЙ'])` із `lib/plural.ts`.
Знову ж: `/r/<id>` — публічна сторінка.

**Severity:** major для україномовного продукту (і воно на вітрині), технічно косметика.

---

## MINOR

**QA2-06 · `services/api/storage/` не в `.gitignore`.**
Після першого ж завантаженого фото в дев-режимі в репо з'являється
`services/api/storage/attachments/<uuid>.png` як untracked. У проді це Vercel Blob,
локально — бінарники, що просяться в коміт.
```diff
+ services/api/storage/
```

**QA2-07 · Шапка Журналу рахує скасовані готування.**
Після undo: чип «ЗА ТИЖДЕНЬ» зник (правильно — 0 активних), а в шапці лишилось «1 ГОТУВАННЯ».
Або рахувати `runs.filter(r => !r.undone_at)`, або підписати «1 ЗАПИС».

**QA2-08 · Одиниця в картці стрічки капсом.**
Картка `intake_diff`: `1 Л`. Комора і шит: `1 л`, `100 г`, `32.99 шт`.
Схоже на `text-transform: uppercase` на контейнері, що чіпляє й одиницю після FIX-10.

---

## Що пройшло

**Область B — Cook Mode: чисто, 9/9.**
Таймер стартує (2:00 → 1:56 за 5 с), кнопка стає «Пауза», пауза реально зупиняє
(1:56 і через 4 с те саме), «+1 хв» → 2:56, кроки просуваються 1→5, прогрес-бар заповнюється.
Фініш: «Списано 2 позиції» (**правильна форма для 2**), «← СКАСУВАТИ СПИСАННЯ» на місці.
Undo: `restored: 2`, комора 18 → 20, обидві партії повернулись.

**Область C — ретро + фото: чисто.**
4 зірки → зафарбувались, «ЗБЕРЕЖЕНО», `rating: 4` у БД. Textarea з'являється після оцінки,
blur зберігає (`verdict` у БД). Фото завантажилось і **реально відрендерилось**:
`<img naturalWidth 480, naturalHeight 360>` — не порожня рамка. Пілюля «ІНШЕ» на місці.

**Область D — Share: майже чисто.**
Оверлей правильний: «2 З 2 — З ТОГО, ЩО БУЛО» вгорі-ліворуч, лого вгорі-праворуч, назва внизу.
Фото підтягнулось із Cook. `/r/<uuid>` віддає 200 і рендериться read-only без таб-бару,
у власника видно «Готуй у себе».

**Область E — Journal: чисто, 7/7.**
«1 ГОТУВАННЯ», чип «ЗА ТИЖДЕНЬ · 1 ГОТУВАННЯ · ★4.0 · 2 З КОМОРИ», групування «СЬОГОДНІ»,
мініатюра 46×46 з видимим фото, ★★★★☆, verdict курсивом, «ЗНОВУ ⟳».
Після undo — «↩ … СКАСОВАНО».

---

## Не перевірено і чому

| Що | Чому |
|---|---|
| Область A через UI | QA2-01 — картки немає; рецепт підкладено в router-state вручну |
| FIX-06 («Рецепт →» на кожній) | те саме: немає картки, щоб клікнути. Код виправлено, перевірено читанням |
| Алергічна мітка | QA2-02 — не може спрацювати за побудовою; профіль із «молюски» не заводив |
| Десктоп-адаптація (7 перевірок) | `resize_window` повідомляє успіх, але `innerWidth` лишається 417, `outerWidth` = 0. Вікно не змінює розмір із цієї сесії |
| «Завантажити PNG», «Скопіювати підпис» | завантаження і буфер обміну недосяжні для автоматизації |
| Deep-link в incognito, `?next=` після signin | інкогніто не підняти; magic-link у stdout, якого я не бачу |
| Область F (household) | пропущено — той самий дозвіл, що першого разу |
| «ЗБЕРЕЖЕНО» на 2 с | перевірено через БД (`rating`/`verdict` збереглись), сам блимок не зловив |

**Одне спостереження без відтворення.** Один раз Комора показала «0 ПОЗИЦІЙ» і порожній список,
тоді як `/v1/pantry` віддавав 18. Сталося одразу після `resize_window`; помилок у консолі не було.
Повторити звичайною навігацією не вдалося (18 із 18 рендеряться). Швидше за все — артефакт
інструмента, а не продукту. Лишаю як помітку, не як баг.

---

## Вердикт: до Vercel ще ні

Два блокери прямо на вітрині. QA2-02 псує публічну сторінку `/r/<id>` — саме те, що людина надсилає
друзям; замість інгредієнтів там UUID. QA2-03 робить неможливим видалення партії, а це базова дія.
Обидва — правки на пів години.

QA2-01 глибший: він не в коді, а в тому, що промпт і модель розійшлись. Поки картка не повернеться,
у Recipe немає входу з інтерфейсу — тобто половина продукту недосяжна, хоч генерація й ожила.

Що ще лишилось для деплою поза цим прогоном: `APP_URL` у Vercel Env (зараз `localhost:5173` — magic-link
поведе в нікуди) і клієнтський ресайз фото під ліміт тіла 4.5 МБ. Обидва з `DEPLOY-CHECKLIST.md`,
код для них не потрібен.

---

## Про відчуття після фіксів

Стало помітно краще, і найбільше — від зміни моделі. Стара відповідала як автомат, що вгадав
намір; нова питає «звучить як готові батончики, а не сировина — ти хочеш рецепт чи просто з'їсти?».
Це вже схоже на кухню, де хтось є, а не на форму вводу.

Але та сама зміна відкусила contract: модель, яка вміє розмовляти, гірше хоче віддавати JSON.
Це не разова поламка, а нова постійна робота — промпт тепер треба тримати під модель, і кожна
наступна заміна слага знову це похитне. Варто завести один eval-кейс «запит рецепта → має бути
картка proposal» і ганяти його в CI: він коштує цент і ловить рівно те, що я ловив очима.

Cook Mode — найкраща частина продукту. Таймер, кроки, «Списано 2 позиції», undo одним тапом:
там нема жодної зайвої деталі, і воно працює точно. Якби я показував Kitchen OS комусь, я б
починав звідти, а не зі стрічки.

І одна річ, яку добре видно на живих даних: рецепт із комори, де лежать дрова й туалетний папір,
виглядає безглуздо не тому, що модель дурна, а тому що комора не розділена на «їстівне / інше».
Каталог із `catalog/expand` це закриває категорією `нехарчове` — його варто влити раніше, ніж
здається, бо він лікує не пошук, а першу фразу, яку продукт каже людині.
