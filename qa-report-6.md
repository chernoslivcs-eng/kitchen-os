# QA-6 — онбординг, ручна робота, списки, наміри

Прогін: 2026-08-29, 22:40–00:15. Комміт `7737ead`, prompt `2026-08-28`,
чат і recipe_gen — `anthropic/claude-sonnet-4.5`.
Чисті акаунти: `qa6-a@example.com`, `qa6-b@example.com` (`qa6-c` не знадобився).

**Провізіонер працює — але не тим шляхом, який описує бриф.** `/tmp/kos-magic-links.log`
лежить на твоєму Mac, а мій shell сидить в окремій Linux-VM з власним `/tmp` і власним
PID-namespace: ані `curl localhost:3000`, ані `tail /tmp/...` звідти не працюють (це та сама
причина, через яку п'ять прогонів поспіль не було чистого акаунта). Файл я дістав так:
через `device_bash` поклав у репо симлінк `apps/web/public/qa6-links.log → /tmp/kos-magic-links.log`,
і Vite віддав його як статику на `localhost:5173/qa6-links.log`. Симлінк прибрав.

**Щоб наступному прогону не робити цей трюк** — постав дефолт у репо, а не в `/tmp`:

```ts
export const MAGIC_LINK_LOG = process.env.MAGIC_LINK_LOG
  ?? new URL('../../../.qa-magic-links.log', import.meta.url).pathname;
```

плюс рядок у `.gitignore`. Тоді лог видно і з QA-сесії, і з будь-якого шелла в репо.

---

## Головне

Онбординг нарешті видно, і в ньому знайшлось те, чого не показав жоден із п'яти
попередніх прогонів: **продукт ніколи не питає людину про обмеження.** Блок
`onboarding-stage2.md` починається словами «питання вже поставлене» — і його не ставить
ніхто. Алергії потрапляють у профіль тільки якщо людина заговорить про них сама.

Регресія QA-5 при цьому здебільшого зелена: 10 із 12 пунктів тримаються повністю.

---

## Обов'язкова таблиця: регресія 12 фіксів QA-5

| # | Що перевірялось | Стан |
|---|---|---|
| QA5-01 | алерген не пропонується сам | ✓ «Є шоколад з мигдалем — але в профілі стоїть алергія на мигдаль» |
| QA5-01 зворотний | на прямий запит дати рецепт із позначкою | ✗ **досі відмовляє**: «Дай рецепт з горіховим соусом» → «У профілі алергія… Прибрати позначку?», рецепта немає |
| QA5-02 | статус картки `[НЕ ЗАСТОСОВАНО]` | ~ на пряме питання ✓ («Ні, ще не в коморі — ти не натиснув “Застосувати”»), у фоновому твердженні ✗ — див. QA6-03 |
| QA5-03 | модель пам'ятає власні пропозиції | ✓ «аліо-оліо, карбонару чи путанеску» — всі три названі точно |
| QA5-04 | `equip` пише `lacks` | ✓ `equipment: {"немає духовки": "lacks"}` — але лейбл кривий, див. QA6-07 |
| QA5-04 наслідок | альтернатива без духовки | ✓ «Духовки немає — можу показати рибу на пательні або в каструлі» |
| QA5-05 | межа видимості | ✓ «Не бачу так далеко — тільки останні 20 реплік» (перевірено на сесії з 22 повідомлень) |
| QA5-06 | маркдаун у прозовій відповіді | ✓ «Святкова вечеря на 6 осіб» повернула нормальний рецепт, прози й зірочок немає |
| QA5-07 | дата | ✓ «Субота, 29 серпня 2026 року» |
| QA5-09 | кількість не дублюється в кроці | ✓ «пенне або спагеті — злий, скинь у пательню», без «200 г» |
| QA5-09 | капіталізація | ~ працює тільки для першого символу кроку, див. QA6-11 |
| QA5-10 | лічильник `opened` | ✓ API повертає `opened: 1`, партія лишається в коморі, рядок у `Cook.tsx` є |
| QA5-11 | «✕ ВІДХИЛЕНО» | ✓ |

---

## Блокери

### QA6-01 — онбординг ніколи не питає про обмеження

`onboarding-stage2.md`, перший рядок:

> ЗНАЙОМСТВО, ЕТАП 2: ЛЮДИНА. Комора наповнена, **питання вже поставлене**: обмеження,
> дім, календар.

Блок написаний під момент інтерфейсу, якого не існує. Далі він каже «Записуй усе почуте»
— тобто інструктує, як реагувати на відповідь, але ніде не інструктує спитати. І ніщо в
UI не питає теж.

Повний прогін на чистому акаунті `qa6-a` (комора 0 → 3, профіль порожній, `stage=2`
активний):

| я | продукт |
|---|---|
| Купив спагеті 500 г | «Спагеті в сухому. Що готуємо?» + `intake_diff` |
| Ще купив помідори 500 г і пармезан 200 г | «Записав помідори й пармезан…» + `intake_diff` → apply, комора = 3 |
| Ок | «Робимо щось із цього зараз?» |
| Давай | «Яку страву з трьох обираєш — аліо-оліо, карбонару чи путанеску?» |
| **А що ще треба знати про мене?** | «Алергії, дієта, нелюбі продукти — щоб не пропонувати зайвого. Або нічого такого немає?» |

Питання з'явилось рівно тоді, коли я його сам попросив. Без цього рядка людина проходить
онбординг і йде готувати, а профіль лишається порожнім назавжди.

Наслідок системний: **уся алергічна логіка продукту — `[ПРОФІЛЬ]` у контексті, «АЛЕРГІЯ —
ТВЕРДА МЕЖА», позначки в UI — залежить від того, чи людина сама здогадається про це
сказати.** Ми три прогони лагодили те, що в реальному житті просто не наповнюється.

**Фікс.** Переписати блок з опису стану на дію:

```md
ЗНАЙОМСТВО, ЕТАП 2: ЛЮДИНА. Комора наповнена, про людину ти ще нічого не знаєш.

- Спитай ОДИН раз, одним реченням, і тільки після того, як щось запропонував:
  «Перед тим як готувати — є щось, чого ти не їси? Алергії, дієта, просто нелюбе?»
- Не став це питання першим у розмові й не повторюй його, якщо вже питав у цій сесії
- Відповіли «нічого» — нічого не записуй і більше не питай
```

І прибрати три застарілі правила, які я фіксував ще в QA5-08 і які досі на місці:
`kind "member"` (заборонений у `card-rules`, не зберігається), `kind "tradition"` /
`"date"` (**таких значень немає в `ProfileKind` взагалі**), і «АЛЕРГІЯ — виняток: спитай,
де проходить межа» (прямо суперечить новому правилу «записуй ШИРОКО одразу»).

### QA6-02 — «купив» на нехарчове: або в список покупок, або взагалі нікуди

`card-rules.md` однозначний: «якщо в репліці є явний факт про комору (“купив”, “поклав”,
“закінчилось”) — картка `intake_diff` **обов'язкова**», і «card = shopping ТІЛЬКИ тоді,
коли розмова саме про покупки: “додай у список”, “що купити”».

Чистий акаунт `qa6-b`, три спроби:

1. **«Купив туалетний папір, мийний засіб і серветки»** → `{"type":"shopping","items":[…3 позиції…]}`.
   Людина сказала, що вже купила — продукт поставив це в список того, що треба купити.
2. **«Купив хліб і туалетний папір»** → reply: «**Записую хліб і папір**», картка:
   `intake_diff` з **одним** оп — тільки хліб. Папір зник мовчки, при тому що в reply він
   названий.
3. **«Ні, я їх уже приніс додому, поклади в комору»** → reply: «Запишу хліб у комору,
   туалетний папір **у список побутового**», картка знову тільки з хлібом. «Списку
   побутового» в продукті не існує — модель вигадала місце, куди нібито поклала річ.

Дві окремі проблеми:

- **Маршрутизація.** Модель прочитала правило «у shopping додавай УСЕ, включно з
  нехарчовим» як «нехарчове йде в shopping». Правило про те, що класти в список, коли
  розмова про список, треба відв'язати від правила про те, куди йде «купив».
- **Reply суперечить картці.** Це важливіше. Проза перелічує дві позиції, картка містить
  одну — і людина, яка читає репліку й тапає «Застосувати», отримує не те, що прочитала.
  Картка є контрактом; проза не має права обіцяти більше.

**Фікс.** У `card-rules.md`, поруч із «ДВОЗНАЧНИЙ ІНТЕНТ»:

> «Купив», «приніс», «поклав» — це ЗАВЖДИ `intake_diff`, незалежно від того, їстівне це
> чи ні. Побутове теж живе в коморі (зона `dry`). У `shopping` річ потрапляє тільки з
> «треба купити», «додай у список», «збираюсь у магазин».
> У reply називай РІВНО ті позиції, що є в картці. Не згадуй того, чого в ops немає.

Плюс страховка в коді — розбіжність між reply і картою можна ловити грубо, але дешево:
логувати, коли `reply` містить кому-перелік із N іменників, а `ops.length < N`. Спершу
просто лог, як `recipe-ing-schema-violation`.

---

## Major

### QA6-03 — фікс QA5-02 тримається на прямому питанні й не тримається у фоні

Пряма перевірка з брифу проходить бездоганно: «Купив молоко 1 л» → не застосовую →
«Це вже в коморі?» → **«Ні, ще не в коморі — ти не натиснув «Застосувати». Застосуєш зараз?»**

Але коли те саме знання потрібне не як відповідь, а як фон, правило не спрацьовує.
На `qa6-b` комора мала три реальні позиції (туалетний папір, мийний засіб, серветки,
додані вручну), а незастосована картка з хлібом висіла шість реплік тому:

> «Що приготувати?» → «**У коморі тільки хліб.** Докупимо мінімум під одну страву?»

Хліба немає (картка `[НЕ ЗАСТОСОВАНО]`), а три реальні позиції проігноровані.

Механіка реалізована правильно — я перечитав `chat.ts:136-147`, мітка `[ЗАСТОСОВАНО]` /
`[НЕ ЗАСТОСОВАНО]` йде в історію, `summarizeCard` є, правило «ДЖЕРЕЛО ПРАВДИ» в
`card-rules.md:11` на місці. Модель просто не застосовує його там, де не питають прямо.

**Фікс** — не нове правило, а зміна порядку: підняти «ДЖЕРЕЛО ПРАВДИ» з середини
`card-rules` у `role.md`, і перефразувати з заборони на дію:

> Перш ніж сказати щось про стан комори, профілю чи списку — подивись у відповідний блок
> даних. Якщо позиції там немає, її немає, хоч би що ти казав раніше.

### QA6-04 — список покупок не в контексті моделі, і вона про це не знає

Підтвердження діри з таблиці пам'яті QA-4, але з несподіваним поворотом. `serializeShopping`
не існує, `[СПИСОК]` у промпт не йде.

У межах однієї сесії все виглядає справно, бо працює історія:

> «А молоко вже в списку?» → «Так, молоко щойно додала — воно в списку.» ✓
> «Додай молоко в список» → «Молоко вже є в списку з попереднього разу. Додати ще одну
> позицію?» ✓ (дубля немає)

У новій сесії — інакше. У списку лежать молоко і яйця:

> «Що в мене в списку покупок?» → **«Список покупок порожній. Хочеш щось додати?»**
> «Додай молоко в список» → «Додаю молоко.» + картка `shopping`

Тобто не «не бачу», а «порожній» — і дубль за один тап. Правило «ТВОЯ ВИДИМІСТЬ
ОБМЕЖЕНА» перелічує комору, профіль і готування; списку в переліку немає, тому модель
не має підстав вважати його поза зоною видимості.

**Фікс** — або серіалізувати список (він короткий, це дешево):

```ts
function serializeShopping(items: ShoppingItem[]): string {
  if (!items.length) return '';
  return '\n\n[СПИСОК ПОКУПОК]\n' + items
    .map((i) => [i.label, i.value && i.unit ? `${i.value}${i.unit}` : null, i.checked ? 'куплено' : null]
      .filter(Boolean).join(' · '))
    .join('\n');
}
```

— або, якщо свідомо не хочемо, додати список у правило видимості. Але серіалізація краща:
`findShoppingItemByLabel` у репозиторії вже є і не використовується — дублі зараз ловить
тільки історія, тобто лише в межах сесії.

### QA6-05 — у продукту немає імені, і модель щоразу вигадує своє

Перша репліка на двох чистих акаунтах, той самий промпт, той самий вхід «Привіт»:

> `qa6-a`: «Привіт! Я **Рома**, твій **кухар**. Допоможу розібрати продукти, підказати, що
> готувати, і врахую всі особливості. Почнемо з комори — скинь чек або фото покупок.»
>
> `qa6-b`: «Привіт! Я **Шеф** — твій **помічник на кухні**. Давай наповнимо комору: скинь
> фото чеків або перелічи, що є вдома.»

Слова «Рома» немає в жодному промпті — я перевірив усі шість файлів у
`versions/2026-08-28/`. Модель вигадує ім'я й роль наново для кожної людини. «Помічник»
до того ж прямо суперечить `role.md`: «Ти кухар… а не автор кулінарної колонки».

Плюс рід не тримається: та сама модель в іншій репліці каже «молоко щойно **додала**» —
жіночий рід при чоловічому імені.

Це не мовна помилка, це відсутність рішення. Або продукт має ім'я — і воно в `role.md`
разом із родом, — або асистент безіменний, і тоді в промпті має бути «не називай себе
ім'ям, ти просто кухар цієї кухні». Зараз перше враження в кожного нового юзера різне.

---

## Minor

- **QA6-06 · unpack не питає каталог про зону.** «→ В КОМОРУ» переклало молоко в зону
  `dry`, бо `ShoppingItem.zone` був `null`, а дефолт — комора. Каталог на 2341 позицію
  знає, що молоко — `fridge`. Прогнати лейбл через `resolveLabelToKey` при unpack
  (і при `intake_diff` без зони теж).
- **QA6-07 · лейбл техніки з запереченням усередині.** «В мене немає духовки» →
  `{"op":"add","kind":"equip","label":"немає духовки","has":false}` → у профілі
  `{"немає духовки": "lacks"}` → `serializeProfile` віддасть «НЕМАЄ ТЕХНІКИ: немає
  духовки». Подвійне заперечення. У `card-rules` треба приклад: `label` — сама назва
  техніки («духовка»), заперечення живе в `has: false`.
- **QA6-08 · екрани показують порожній стан ~2 с до гідратації.** Скриншот `/app` через
  2 с після входу не має кнопки «📷 Сфотографуй чек» — головного CTA онбордингу; вона
  з'являється пізніше. `/profile` у цей же момент показує «ще жодної» замість восьми
  алергій. Дані потім приходять і все правильно, але перші секунди продукт показує
  людині неправду про її ж профіль.
- **QA6-09 · техніку ніде не видно.** На `/profile` три блоки: АЛЕРГІЇ, ПОБАЖАННЯ, ЧОГО
  НЕ ЇМ. `equipment` не показаний — людина не може ні перевірити, ні прибрати запис про
  духовку.
- **QA6-10 · дірка в stage-логіці на 1-2 позиціях.** `stage=1` при 0, `stage=2` при ≥3,
  а між ними — жодного блоку. З однією позицією в коморі модель запропонувала карбонару,
  путанеску й аліо-оліо, для яких бракує 4-5 продуктів, а наступною реплікою сказала
  «Тепер до всіх трьох страв є продукти — можна робити будь-яку» (насправді жодна не
  укомплектована). Або розтягнути `stage=1` до `< 3`, або дати правило про неповний набір.
- **QA6-11 · капіталізація в кроках лагодиться тільки на позиції 0.** `recipe.ts` робить
  `out.charAt(0).toUpperCase()`, і це працює: «Часник — продави пресом». Але модель тепер
  пише кілька речень у кроці, і всередині лишається як було: «Соус. Часник — продави
  пресом… **олія оливкова**: розігрій на пательні… **помідори в соку консервовані** —
  подави виделкою». Капіталізувати після `. `, `! `, `? ` теж.
- **QA6-12 · лічильник комори не реагує на пошук.** При фільтрі «парм» показана одна
  позиція, у шапці далі «9 ПОЗИЦІЙ». Має бути «1 з 9».
- **Токен-бюджет.** Вхідні токени стабільні: 5830 на першій репліці чистого акаунта,
  6652 на сесії з повним 20-реплічним вікном. Зростання від історії ~800 токенів, не
  вибухає.

---

## Що працює (перевірено)

**A · Онбординг**

- A1 ✓ порожній стан і CTA «📷 Сфотографуй чек — я розкладу» на місці (з затримкою, QA6-08).
  Модель сама першою не пише — чекає на людину
- A1 ✓ `onboarding-stage1` тримає свої заборони: не питає про алергії, не пропонує готувати,
  веде до комори
- A2 ✗ див. QA6-01
- A3 ✓ «Нічого такого немає, я сам» → нічого не записано (профіль порожній), більше не питає
- A4 ✓ з коморою тільки з побутового: «У коморі тільки хліб. Докупимо мінімум під одну
  страву?» — правило спрацювало, хоч і на брехливій підставі (QA6-03)

**B · Ручна робота з коморою — найчистіша область прогону**

| Що | Стан |
|---|---|
| B1 додавання: назва / кількість / одиниця / зона | ✓ позиція в правильній зоні, лічильник +1 |
| B1 порожня назва | ✓ «✕ НАЗВА ПОТРІБНА», шит не закривається |
| B1 кількість буквами | ✓ «✕ КІЛЬКІСТЬ — ЧИСЛО АБО ПОРОЖНЄ» |
| B1 порожня кількість | ✓ приймається, `value: null, unit: null` |
| B2 шит із заповненими полями | ✓ |
| B2 зміна назви / кількості / одиниці / зони | ✓ усе чотири, партія переїхала в іншу секцію |
| B2 Escape | ✓ зміни не збереглись |
| B3 «◔ Відкрито» | ✓ стан змінився |
| B3 незбережений `value` + toggle | ✓ **не втрачається** — 150 → 200 збереглось разом зі станом |
| B4 «Прибрати з комори» | ✓ confirm, лічильник −1, `state: depleted` (партія не видалена фізично) |
| B5 чат бачить ручні зміни | ✓ перейменував «пармезан» → «пармезан витриманий», нова сесія: «Пармезан витриманий — так і є в коморі» |
| B6 пошук при ≥8 позиціях | ✓ поле «Знайти в коморі», регістр не важить, «Нічого не знайшлось за «зззз». Спробуй інше слово.», порожній запит показує все |

**C · Списки**

- C1 ✓ «Додай молоко, хліб і яйця» → одна картка з трьома items → apply → три в списку.
  «Прибери хліб» → `op: remove` → хліб зник
- C2 ✓ чекбокс перекреслює, лічильник «1 / 2» рахує невикреслені, badge на табі оновився
- C3 ✓ пілюля «→ В КОМОРУ (1)» з'явилась тільки при checked, confirm «Перекласти 1 позицію
  із «куплено» в комору?», позиція зникла зі списку й з'явилась у коморі як `sealed`,
  unchecked лишилась. Зона — див. QA6-06
- C4 ✗ див. QA6-04
- C5 ✓ у межах сесії дубля немає («Молоко вже є в списку з попереднього разу»), у новій —
  див. QA6-04

**D · Наміри — найкраща область прогону**

- D1 ✓ «Хочу колись зробити качку по-пекінськи» → `wish` із формулюванням людини
- D3 ✓ «Хочу більше риби, менше цукру, і навчитись пекти хліб» → **три окремі ops**, і
  «менше цукру» правильно пішло в `anti`, не в `wish`
- D4 ✓ «Більше не хочу вчитись пекти хліб» → `op: remove` з точним лейблом, зник із профілю
- D5 ✓ wish і anti тримаються разом
- **D2 ✓✓ головне.** Записав «Давно хочу спробувати різото» → додав у комору рис арборіо,
  бульйон, пармезан, цибулю → **нова сесія**, «Що приготувати?»:

  ```json
  {"title":"Різото з пармезаном",
   "why":"ти давно хотів спробувати різото, арборіо й бульйон є"}
  ```

  Намір не лежить рядком — він справді чекає моменту й називає причину. Це працює точно
  так, як задумано.

---

## F · Чого немає — і чи це відчувається

Відчувається в одному конкретному місці, і це не «обрані рецепти».

Коли модель дала рецепт і ти його **приготував**, цикл замикається: cook-run зберігає
`RecipeRow`, з'являється `/r/:id`, є що переслати. Коли модель дала рецепт і ти його
**не готуєш зараз** — не лишається нічого. Я двічі за прогін отримав «Різото з пармезаном»
із розписаним why і needs, закрив сесію, і рецепта більше немає ніде: у Журналі порожньо
(готування не було), у чаті він у старій сесії без назви, `/r/` не існує, бо `recipe_id`
народжується тільки в `POST /v1/cook-runs`.

Тобто кнопки бракує не на екрані рецепта, а **під карткою пропозиції** — «лишити на
потім». І цікаво, що продукт уже вміє це робити для намірів: `wish` «давно хочу спробувати
різото» дожив до моменту, коли з'явились інгредієнти. Не вистачає того самого для
конкретної згенерованої страви — щоб «залишити на потім» клало рецепт туди ж, звідки
потім спливе «ти хотів зробити оце, тепер усе є».

Авторський рецепт і імпорт із фото за цей прогін не потягнулись жодного разу — комора й
чат покривають вхід. Уточнення до рецепта («а можна без вина?») хотілось один раз, але це
природно лягає в чат, а не в окрему функцію.

---

## Порядок робіт

1. **QA6-01** — переписати `onboarding-stage2.md` з опису стану на дію. Без цього
   профіль у реального юзера не наповнюється ніколи, і вся робота QA-4/5 над алергіями
   висить у повітрі.
2. **QA6-02** — «купив» → завжди `intake_diff`; reply не має називати те, чого немає в ops.
3. **QA6-03** — підняти «ДЖЕРЕЛО ПРАВДИ» в `role.md` і перефразувати на дію.
4. **QA6-04** — `serializeShopping`, плюс `findShoppingItemByLabel` на дублі.
5. **QA6-05** — вирішити питання імені й роду асистента. Це рішення, не баг.
6. **QA5-01 зворотний** — правило «позначка, не заборона» продублювати в
   `recipe-generator.md`: зараз воно тільки в `role.md` і читається як заборона.
7. QA6-06..12 — дрібне.
8. Провізіонер — дефолт у репо замість `/tmp`.

---

## Що лишилось

**Ти розлогінений.** Magic-link ставить cookie на той самий origin, тож вхід під
`qa6-a`/`qa6-b` замістив твою сесію. Зайди наново через свій лінк.

**Тестові акаунти лишаються в БД** (ендпоінта видалення юзера немає):
`qa6-a@example.com` (комора 3, профіль порожній, 1 сесія на 22 повідомлення) і
`qa6-b@example.com` (комора 8, 8 алергій, 3 побажання, 1 anti, `equipment` з духовкою,
список покупок з 1 позицією, 2 cook-run-и, ~10 сесій). `qa6-c@example.com` запитаний,
але не використаний — юзер створиться тільки при переході за лінком, тож його, найімовірніше,
в БД немає.

**Симлінк `apps/web/public/qa6-links.log` прибрав.** Твоя комора й профіль не чіпались
цього прогону взагалі — усе робилось на тестових акаунтах.
