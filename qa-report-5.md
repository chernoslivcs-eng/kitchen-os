# QA-5 — перевірка структурних фіксів QA-4

Прогін: 2026-08-29, 21:20–22:30. Акаунт `chernosliv.cs@gmail.com`, локальний стек
(web :5173 + api :3000), комміт `1e5c0d2`, prompt `2026-08-28`.
Чат — `anthropic/claude-sonnet-4.5` (підтверджено в `meta.model`), recipe_gen — той самий.

**Чистий акаунт не вийшов уп'яте.** Перевірив усі три шляхи з брифу: `curl localhost:3000`
з мого shell дає `000` (sandbox має `--unshare-net`), `psql` у ньому немає, а перезапустити
API я не можу — sandbox має власний PID-namespace (видно 8 процесів, жодного вузла Mac-а),
плюс `--die-with-parent` уб'є процес наприкінці виклику. `ConsoleMailer` тримає лінк тільки
в пам'яті й у stdout, файлу чи dev-ендпоінта немає. **Онбординг `stage=1` не бачено за
п'ять прогонів.**

Це лікується трьома рядками, і поки їх немає — найбільша сліпа пляма продукту лишається:

```ts
// mailer.ts, ConsoleMailer.sendMagicLink
if (process.env.NODE_ENV !== 'production') {
  await appendFile('/tmp/kos-magic-links.log', `${new Date().toISOString()} ${mail.to} ${mail.link}\n`);
}
```

Часткова заміна, яку я зробив: `stage=2` вмикається при порожньому профілі й коморі ≥3
позицій, тож я двічі спорожняв профіль і ганяв сценарії саме в цьому стані. Це не онбординг
з нуля, але це той самий промптовий блок.

---

## Відповідь на два питання прогону

**1. Чи історія закрила петлю уточнень? — Так.**
Той самий сценарій, що в QA-4: замість «5 реплік, 0 карток, те саме питання тричі» —
4 репліки й `proposal` на четвертій. Вітання посеред розмови зникло. Модель тримає нитку
через шість реплік із поверненням («Повернімось до лосося» → знає, про який).

**2. Чи sonnet тримає українську? — Так, і різниця велика.**
3 мовні дефекти на ~36 реплік проти 8 на ~24 у haiku. Деталі в `copy-report-5.md`.

**Але.** Історія відкрила два нові класи збоїв, обидва про правду: модель тепер вірить
власним обіцянкам (вважає незастосовані картки застосованими) і стверджує факти про
минуле, якого не бачить. І головне — **блокер QA4-02 закрито тільки наполовину**:
профіль у контексті є, модель його цитує на пряме питання, і все одно сама пропонує
алерген.

---

## Таблиця: чи закрились структурні проблеми QA-4

| # | Проблема | Стан |
|---|---|---|
| QA4-01 | історія розмови | ✓ **закрито**, але відкрило QA5-02 і QA5-03 |
| QA4-02 | профіль у контексті | ~ **наполовину**: провід є, поведінка ні — див. QA5-01 |
| QA4-03 | схема `ing` + знищення комори | ✓ **закрито повністю** (3/3 рецепти чисті, партія виживає, кількості в UI) |
| QA4-04 | undo профілю (аліасинг) | ✓ аліасинг виправлено; повна заміна документа лишилась і стала небезпечнішою |
| QA4-05 | `member`/`note` губились | ✓ **закрито** |
| QA4-06 | 502 на складний запит | ✓ закрито, але новий канал пропускає маркдаун — QA5-06 |
| QA4-07 | конфлікт wish ↔ anti | ✓ **закрито** |
| QA4-08 | «0дн тому» → «вчора» | ✓ **закрито** |

---

## Блокери

### QA5-01 — модель бачить алергію і все одно пропонує алерген сама

Це QA4-02, який виглядає закритим і не закритий. Провід зроблено правильно, тому баг
тепер значно важче помітити: на будь-яке пряме питання все відповідає ідеально.

Що я зробив: «У мене алергія на горіхи» → картка з 9 ops (арахіс, арахісова паста,
волоські, мигдаль, кешʼю, фундук, пекан, фісташки, макадамія) → apply. `GET /v1/profile`
підтверджує всі дев'ять.

**Нова сесія, «Що солодкого до чаю з того, що є вдома?»:**

> «Є шоколад **з мигдалем** і кокосом, і шоколад з полуницею й чіа. Обидва молочні,
> ЛТ Kor. Ще булочки з корицею — підігрій трохи, будуть м'які.»

Мигдаль першим пунктом, без жодної згадки про алергію. Те саме, що в QA-4.

**Але в тій же сесії, на пряме питання:**

> «Які в мене алергії записані?» → «Записані всі горіхи: арахіс, арахісова паста,
> волоські, мигдаль, кешʼю, фундук, пекан, фісташки, макадамія.»
> «А шоколад з мигдалем мені можна?» → «Шоколад з мигдалем у тебе в алергіях. Якщо
> насправді можна — скажи, приберу мигдаль зі списку.»

Тобто `[ПРОФІЛЬ]` у контексті є, модель його читає дослівно й застосовує **реактивно**.
Проактивно — ні. І правил у промпті аж два: `card-rules.md:47` («АЛЕРГІЯ — ТВЕРДА МЕЖА…
ніколи не пропонуй цей продукт сам») і `role.md:18` («Сам їх не пропонуй»). Обидва
проігноровані.

**Чому.** `serializeProfile` клеїться **в самий кінець** системного блоку, після
`[КОМОРА]` і `[ОСТАННІ ГОТУВАННЯ]` (model.ts:186-189). Коли людина питає «що є вдома»,
модель читає `[КОМОРА]` і перелічує мітки партій. У той момент її ніщо не зупиняє:
правило про алергію лишилось за пів промпту вище, а сам список алергенів — в іншому
блоці, який не має відношення до того, що вона зараз читає.

**Фікс — покласти обмеження туди, де дані, а не в окреме правило.**

```ts
// model.ts — serializePantry отримує профіль і позначає алергени просто в рядку партії
function serializePantry(bs: PantryBatch[], profile?: Profile | null): string {
  const allergens = (profile?.allergies ?? []).map((a) => a.toLowerCase());
  return bs.filter((b) => b.state !== 'depleted').map((b) => {
    const parts = [b.id, b.label, b.zone];
    …
    const hit = allergens.filter((a) => b.label.toLowerCase().includes(a));
    if (hit.length) parts.push(`⚠АЛЕРГЕН: ${hit.join(', ')} — НЕ ПРОПОНУВАТИ`);
    return parts.join(' · ');
  }).join('\n');
}
```

Тоді «Шоколад 25 Мол ЛТ Kor Мигдал Кок» модель фізично не може прочитати без позначки.
Плюс переставити `[ПРОФІЛЬ]` **перед** `[КОМОРА]` — обмеження до інвентарю, а не після.
Кеш при цьому не ламається: `cache_prefix` у манифесті це `["role","card-rules"]`, а обидва
блоки й так за ним.

Плюс страховка в коді, бо промпт — не контракт:

```ts
// routes/chat.ts, після callChat
if (call.card?.type === 'proposal' && profile?.allergies.length) {
  const hay = JSON.stringify(call.card).toLowerCase();
  const hit = profile.allergies.filter((a) => hay.includes(a.toLowerCase()));
  if (hit.length) req.log.warn({ hit, user_id }, 'proposal-contains-allergen');
}
```

Важливо: збіг за підрядком дасть хибні спрацювання («мигдаль» усередині «мигдальне
молоко» — правильно; «перець» усередині «перець чорний» — теж), тож це лог, а не блок.
Але без нього ніхто не дізнається, як часто це стається у проді.

**Окремо — протилежна помилка.** `role.md:18` і `card-rules.md:47` кажуть: алерген —
позначка, не заборона; на прямий запит дай, але назви вголос. На `/v1/recipes/generate`
з `«Салат з горіховим соусом»` модель **відмовила**:

> «Горіхи — у тебе алергія на всі види. Салат із горіховим соусом не зроблю.»

Людина готує не тільки собі. Відмова тут — теж баг, просто в інший бік. Правило треба
продублювати в `recipe-generator.md`: зараз воно є тільки в `role.md`, а на цей виклик
композиться `role + recipe-generator`, тож модель бачить його один раз і трактує як заборону.

### QA5-02 — модель вважає незастосовані картки застосованими і бреше про стан

Побічний ефект QA4-01: історія тепер несе обіцянки моделі, але не несе того, чи їх
дотримались.

Нова сесія:

| я | продукт |
|---|---|
| Купив молоко 1 л і батон | «Запишу молоко й батон. Що готуємо?» + картка `intake_diff` |
| *(картку НЕ застосовую)* | |
| Це вже в коморі? | **«Так, записав молоко й батон — вони в коморі. Що готуємо з ними?»** |

Нічого не записано. `[КОМОРА]` у тому ж самому промпті молока не містить. Модель повірила
власному «Запишу» з історії й перекрила ним фактичний блок даних.

Те саме видно в UI і виглядає гірше. Я сказав «Зі мною живе Оксана, вона веганка. І ми
постуємо по середах», картка `profile` лишилась у статусі **ОЧІКУЄ** — я її не застосовував.
Наступна репліка «Що приготувати сьогодні?»:

> «**Сьогодні середа — піст.** Пропоную три варіанти на двох з тим, що в коморі:»

Два ствердження, обидва неправдиві: піст не записаний, а сьогодні субота (див. QA5-07).

Це б'є в саму основу продукту — «картка це пропозиція, стан змінює людина». Якщо модель
не розрізняє запропоноване й застосоване, кнопка «Застосувати» перестає щось означати.

**Фікс** — покласти стан картки в історію, і зробити `[КОМОРА]` явним джерелом правди:

```ts
// routes/chat.ts
const history = (await repo.listMessages(session.id))
  .slice(-20)
  .map((m) => {
    const parts = [m.text].filter(Boolean) as string[];
    if (m.card) parts.push(summarizeCard(m.card) + (m.applied > 0 ? ' [ЗАСТОСОВАНО]' : ' [НЕ ЗАСТОСОВАНО]'));
    return { role: m.role, content: parts.join('\n') };
  })
  .filter((m) => m.content);
```

і рядок у `card-rules.md`:

> ДЖЕРЕЛО ПРАВДИ — блоки `[КОМОРА]`, `[ПРОФІЛЬ]`, `[ОСТАННІ ГОТУВАННЯ]`, а не твої
> попередні репліки. Картка з поміткою `[НЕ ЗАСТОСОВАНО]` нічого не змінила. Ніколи не
> кажи «записав», «вже в коморі», «врахував», якщо цього немає в блоках даних.

### QA5-03 — картки не потрапляють в історію: модель вигадує власні пропозиції й переграє старі запити

`chat.ts` кладе в історію тільки `m.text`. Уся суть картки — назви страв, позиції комори,
записи профілю — лишається за бортом. А `reply` при `proposal` це зазвичай один рядок
(«Три варіанти з лососем:»).

**Наслідок 1 — модель не пам'ятає, що сама запропонувала.** Фактично в картці було:
«Лосось терияки на пательні», «Лосось у вершковому соусі з кропом», «Лосось по-азійськи
з імбиром». Двічі поспіль модель переказала це як:

> «Який саме рецепт цікавить — терияки, **з імбиром** чи **прованські трави**?»

Дві з трьох вгадані, третя вигадана — «прованські трави» в цій розмові не існувало.
Людина обирає варіант, якого немає на екрані.

**Наслідок 2 — важчий.** Коли модель повертає саму картку без прози, `reply` порожній,
повідомлення зберігається з `text: ''`, і `.filter(m => m.text)` викидає його з історії
цілком. Лишається сирота: репліка користувача без відповіді.

Знайшов таку сесію (`a54cd386`, 4 повідомлення: `user`, `assistant[EMPTY+card]`, `user`,
`assistant[EMPTY+card]`) і написав у неї нову репліку «Привіт, що там у коморі?». Модель
побачила дві старі юзерські репліки підряд без відповідей і **переграла їх обидві**:

> «**Привіт!** Записую моцарелу й кефір. У коморі зараз переважно побутове…»
> + `intake_diff` з моцарелою 250 г і кефіром 1 л

Тобто заново запропонувала внести продукти, куплені кілька годин тому й давно оброблені.
**Це ймовірна справжня причина дублів у коморі**, які я в QA-4 списав на ingest чека:
кожна картка без прози лишає сироту, і будь-яка наступна репліка в тій сесії ризикує
згенерувати повторний intake. У поточній коморі кожна позиція з чека присутня рівно двічі.

Заразом перевірив побоювання щодо чергування ролей: три `user`-репліки поспіль API
приймає без помилки, 400 не буде. Це не проблема — проблема саме в переграванні.

**Фікс** — той самий `summarizeCard`, що в QA5-02:

```ts
function summarizeCard(c: Card): string {
  if (c.type === 'proposal')    return '[картка: пропозиції] ' + c.items.map((i) => i.title).join(' · ');
  if (c.type === 'intake_diff') return '[картка: комора] ' + c.ops.map((o) => `${o.op} ${o.label}`).join(' · ');
  if (c.type === 'shopping')    return '[картка: покупки] ' + c.items.map((i) => i.label).join(' · ');
  if (c.type === 'profile')     return '[картка: профіль] ' + c.ops.map((o) => `${o.op} ${o.kind}: ${o.label}`).join(' · ');
  return '[картка]';
}
```

Після нього `.filter(m => m.text)` треба замінити на фільтр по зібраному `content` —
інакше card-only репліки далі випадатимуть.

---

## Major

### QA5-04 — «в мене немає духовки» зберігається як «духовка Є»

`apply.ts:311`:

```ts
if (op.kind === 'equip') {
  if (removing) delete next.equipment[label];
  else next.equipment[label] = 'has';        // ← безумовно
}
```

Модель віддає правильну картку: `{"op":"add","kind":"equip","label":"духовка","has":false}`.
`applyProfileOp` поле `has` не читає взагалі й пише `'has'`. `serializeProfile` (написаний
у тому ж коміті) розрізняє `has`/`lacks` — тобто автор фікса це передбачив, але запис
`'lacks'` не існує ніде в кодовій базі.

Перевірив end-to-end. «В мене немає духовки» → apply → `GET /v1/profile`:
`equipment: {"духовка": "has"}`. Нова сесія, «Дай рецепт запеченої риби»:

> «Лосось **у духовці** з травами — те, що готував сьогодні. Показую рецепт.»

**Фікс:**

```ts
if (op.kind === 'equip') {
  if (removing) { delete next.equipment[label]; return true; }
  next.equipment[label] = op.has === false ? 'lacks' : 'has';
  return true;
}
```

Тип `ProfileOp` теж треба звузити: зараз `has?: boolean` живе поруч із `recipe`, `rating`,
`diet`, `wishes`, `antipatterns` — половина з них уже не використовується.

### QA5-05 — модель стверджує, що чогось не було, коли просто не бачить так далеко

Взяв найдовшу сесію (24 повідомлення). Ліміт `slice(-20)` відрізає перші чотири, а в них
було «Купив моцарелу 250 г, пелаті 400 г, пасту спагетті».

> «Що я купив на самому початку цієї розмови?»
> → «**У тебе немає покупок на початку** — ти лише додав алергію, потім прибрав її.»

Ліміт працює правильно (вхідні токени 7229 з повним вікном проти 6587 без історії —
20 реплік коштують ~640 токенів, вибуху немає), модель не ламається й не вітається.
Але замість «не бачу так далеко» вона впевнено заперечує подію.

Разом із QA5-02 і QA5-03 це один патерн: **порожнечу в контексті модель читає як
відсутність факту.** Три різні прояви, один фікс — сказати їй, де межа її зору:

> ТВОЯ ВИДИМІСТЬ ОБМЕЖЕНА: ти бачиш останні 20 реплік цієї сесії, комору, профіль і
> 5 останніх готувань. Якщо людина питає про те, чого ти не бачиш — скажи «не бачу так
> далеко» або «не пам'ятаю». Ніколи не стверджуй, що чогось не було.

Плюс технічно: коли `listMessages` повернув більше за ліміт, додавати першим рядком
історії `[раніше в цій розмові було ще N реплік]`.

### QA5-06 — фікс 502 пропускає маркдаун у стрічку

`recipes.ts` тепер повертає `{ recipe: null, reply: call.raw.slice(0,600) }`, і `raw` — це
сирий текст моделі. Контракт (`card-rules.md:1`) забороняє маркдаун, але цей канал його
не проходить:

> «У тебе в \*\*алергії\*\* мигдаль — печиво з ним небезпечне. Можу запропонувати варіанти
> без нього?\n\n\*\*Вівсяне печиво з шоколадом\*\* — хрустке зверху, м'яке всередині…»

Користувач побачить зірочки як текст. Плюс тут три страви прозою — знову втрата контракту
карток: немає куди тапнути.

**Фікс** — зачистити маркдаун перед віддачею (`replace(/\*\*/g,'')`, згорнути порожні рядки)
і обрізати до одного-двох речень; або, чесніше, віддавати цей текст назад у `callChat`
окремим ходом, щоб він вийшов нормальною реплікою з карткою.

---

## Minor

- **QA5-07 · Модель не знає дати.** `today()` у `chat.ts:10` рахується для сесії дня і в
  промпт не потрапляє. У суботу модель сказала «Сьогодні середа — піст». Один рядок:
  `+ '\n\n[СЬОГОДНІ] ' + new Date().toLocaleDateString('uk-UA',{weekday:'long',day:'numeric',month:'long'})`.
  Без цього вся гілка «традиції, свята, щоп'ятниці риба» з `card-rules.md` не може працювати.
- **QA5-08 · `onboarding-stage2.md` не оновлено.** Фікси QA-4 пішли в `card-rules.md` і
  `role.md`, а цей блок композиться в той самий промпт для кожного нового юзера і містить
  три застарілі правила: `kind "member"` (заборонено новим правилом і не зберігається),
  `kind "tradition"` / `"date"` (**таких значень немає в `ProfileKind` взагалі** — ніколи
  не було), і «АЛЕРГІЯ — виняток: спитай, де проходить межа» — рівно той допит, який
  QA-4 просив прибрати. Емпірично `card-rules` перемагає (перевірив під `stage=2`:
  «Зі мною живе Оксана, вона веганка» дало правильний `anti`, не `member`), тож це поки
  латентно. Але це єдиний блок, який ніхто ніколи не тестував у бою.
- **QA5-09 · Кількість вставляється в текст кроку.** `recipe.ts:44` тепер підставляє
  `назва + кількість`. До фікса QA4-03 `v`/`u` не існувало, тож гілка не працювала, а
  тепер ожила: на `/r/` видно «олія оливкова 30 мл полити зверху» і «помідори 300 г —
  скибочки». Кількість дублюється зі списком інгредієнтів і розриває речення. Прибрати
  кількість із підстановки — у списку вона вже є.
- **QA5-10 · Лічильник `opened` нікуди не виводиться.** `cook-runs.ts` повертає `opened`,
  `Cook.tsx:180-184` показує тільки «Списано» і «Частково використано». Синтетичний прогін
  із партією без кількості дав `{depleted:0, partial:0, opened:0}` — екран «Готово» скаже
  «Списано 0 позицій», і людина не дізнається, що комору не чіпали. Додати рядок
  «Кількість не вказана — комору не змінював».
- **QA5-11 · «Ні» не міняє статус картки.** Кнопки ховаються (фікс QA-1 тримається), але
  заголовок лишається «ПРОФІЛЬ · ◌ ОЧІКУЄ» назавжди. Має бути «✕ ВІДХИЛЕНО».
- **QA5-12 · `manifest.json` розійшовся з кодом.** `calls.chat.profile: "fast"`, а
  `callChat` тепер жорстко бере `smartModel()`. Конфіг більше не описує систему —
  або читати профіль із маніфеста, або прибрати поле.
- **Undo профілю лишається повною заміною документа.** Бриф просив зазначити як відомий
  недофікс — зазначаю, але з поправкою: **фікс аліасингу зробив його небезпечнішим.**
  Раніше знімок був зіпсований і undo здебільшого нічого не робив. Тепер знімок правильний,
  і undo найпершої картки коректно й повністю відкочує профіль до стану «до неї». Перевірив:
  профіль із 9 алергіями, 1 wish, 2 anti й технікою після undo картки №1 став порожнім по
  всіх чотирьох блоках. Людина тисне «↩» на старому тості й втрачає весь профіль.

---

## Що працює (перевірено, регресії немає)

**A · Історія розмови**

- A1 ✓ петля зникла: 4 репліки замість 5, `proposal` на четвертій, жодного повтору питання,
  жодного вітання посеред розмови
- A2 ✓ нитка тримається через 6 реплік із поверненням («Повернімось до лосося» — знає, про
  який), але див. QA5-03 про власні пропозиції
- A3 ✓ ліміт 20 працює, токени не вибухають (6587 → 7229 з повним вікном)
- A4 ✓ задвоєння немає: `listMessages` викликається до `saveMessage`, підтверджено рівним
  приростом ~50 токенів на репліку

**B · Профіль**

- B2 ~ на пряме питання алерген називається першою фразою ✓, але модель відмовляє замість
  «дай і познач» (див. QA5-01)
- B3 ✓ anti й wish працюють: із `anti: не їм свинину` й `wish: люблю гостре` пропозиції
  без свинини, з «Гострі яйця з чилі та часником»
- B4 ✓ профіль доходить і в `callRecipe`
- B5 ✗ див. QA5-04

**C · Комора — QA4-03 закрито повністю**

- C1 ✓ партія з відомою кількістю: олія 500 → 470 мл, `opened`; помідори 300 г із 300 г →
  `depleted`. Синтетичний рецепт зі старою зламаною схемою (`p` без `v`/`u`) партію **не
  знищив** — 470 мл на місці. Undo відновив 500 мл `sealed`
- C2 ✓ 3 з 3 згенерованих рецептів мають чисту схему: `p` XOR `n`, `v` число, `u` зі
  словника, жодного `q`
- C3 ✓ кількості видно і в Recipe, і на `/r/`: «300 г», «30 мл»

**D · Складний запит ✓** — «Святкова вечеря на 6 осіб: лосось, гарнір і десерт» → 200,
рецепт є (модель обрала одну страву за новим правилом «Один рецепт за раз»), не 502.

**E · Цілісність профілю**

- E1 ✓ аліасинг виправлено (алергія справді зникає), ✗ повна заміна документа — вище
- E2 ✓ `member` не генерується, замість нього `anti: «на двох: Оксана не їсть мʼяса, риби,
  яєць і молочного»` — рівно як прописано в новому правилі
- E3 ✓ «Не люблю кінзу» → apply → «Дуже люблю кінзу, додавай скрізь» → «У профілі записано,
  що кінза не подобається. Лишаємо старе обмеження чи міняємо на протилежне?» Картки мовчки
  немає

**F · Час ✓** — «Лосось уже робили **сьогодні** двічі». QA4-08 закрито.

**G · Регресія QA-1/2/3 — усе тримається**

| Що | Стан |
|---|---|
| DELETE партії (Content-Type) | ✓ через UI, 22 → 21 |
| «Ні» ховає кнопки | ✓ (статус — QA5-11) |
| «Рецепт →» на кожній пропозиції | ✓ 3 кнопки на 3 items |
| TabBar не перекриває Sheet | ✓ `elementFromPoint` на «ПРИБРАТИ З КОМОРИ» повертає саму кнопку |
| `{N}` не дублює назву | ✓ guard у `recipe.ts:46` на місці, дублювання не відтворилось |
| `/r/<id>` рендерить назви | ✓ |
| Плюрали | ✓ «22 ПОЗИЦІЇ» |
| Toast «Готую рецепт…» persist | ✓ `Feed.tsx:307` `persist:true` |
| Undo toast 18 с | ✓ `Feed.tsx:201` |
| Копі «ЛІНК ЛЕТИТЬ», «першу страву», «Далі →», «Чого не їм» | ✓ усі на місці |

---

## Порядок робіт

1. **QA5-01** — алерген у серіалізації комори + перестановка `[ПРОФІЛЬ]` + warn-лог.
   Безпека, і це єдине, що лишилось від блокера QA4-02.
2. **QA5-02 + QA5-03** — один фікс: `summarizeCard` + мітка `[ЗАСТОСОВАНО]` в історії.
   Закриває брехню про стан, вигадані пропозиції й, найімовірніше, дублі в коморі.
3. **QA5-04** — `op.has === false ? 'lacks' : 'has'`. Один рядок, інвертовані дані.
4. **QA5-05** — правило про межу видимості. Один абзац у промпті.
5. **QA5-07** — дата в контекст. Один рядок, без нього не працює цілий блок правил.
6. QA5-06, QA5-08, QA5-09..12 — коли дійдуть руки.
7. **Provisioner** — три рядки в `ConsoleMailer`. П'ять прогонів поспіль онбординг
   лишається неперевіреним, і це вже не випадковість, а системна діра в QA.

---

## Що лишилось у твоєму акаунті

Прибрав: комора назад до 20 позицій (олія й помідори видалені), профіль порожній,
cook-run-и цього прогону відкочені.

Лишилось і прибрати не можу: **один cook-run «QA: олія без кількості»** — синтетичний
прогін для перевірки QA4-03, у нього `changes: null`, тому `/undo` віддає 400
`no_changes_to_undo`, а ендпоінта на видалення cook-run немає. Без рейтингу й verdict-у.
Плюс ~10 сесій у «⌚ Історія» за сьогодні й кілька незастосованих карток у статусі ОЧІКУЄ.
