# Розбір бандла редизайну v3 · 10.09

Гілка `feat/redesign-v3`, коміт `6d2e562`. Прогонів моделі не робив, `packages/prompts/` і `design/` не чіпав.

---

## 1. §0 матриця Б1–Б4 проти коду

Матриця в PLAN.md каже, що покрито **макетами**. Нижче — та сама сітка, але питання інше: чи є під рядком **код**.

### Б1 · Дані

| §0 | Рядок | Код | Де |
|---|---|---|---|
| ✅ | 6 зон | **є** — `dry/fridge/freezer/fresh/spices/drinks` | `packages/catalog/seed.ts:34`, `apps/web/src/pages/Pantry/filter.ts:52` |
| ✅ | 7 полів профілю | **є повністю** | `packages/domain/profile-fields.ts:6` |
| ✅ | шкала свіжості | **є** — три стани, іконка | `filter.ts:25`, `Pantry/FreshIcon.tsx` |
| ✅ | 3 групи рецептів | **не знайшов** — у коді є `origin: 'generated'\|'imported'\|'catalog'`, це походження рецепта, інша вісь | `packages/domain/types.ts:519` |
| ✅ | `value/unit` незалежні | **є** | `normalizeUnit`, `api.ts:268` |
| ◐ | довгі назви (40+) | трійка `product·brand·variant` є, але одним **хвостовим** рядком, не другим яруcом | `Pantry/BatchCard.tsx:109` |
| ◐ | дві мітки на рядку | **код це свідомо зняв.** Крок Ф2 залишив одну вісь іконок; «не їм» — підрядком без іконки | `Pantry/Pantry.test.tsx:69` |
| ✗ | `days < 0` | **немає, і зведено в «сьогодні» у двох місцях** | `filter.ts:75`, `Feed/Feed.tsx:1718` |
| ✗ | три походження строку | тип є, дані — ні: «Поки всі — `category`». `manual` і `foodkeeper:*` не пише ніхто | `packages/domain/shelf-life.ts:33` |
| ✗ | `catalog_key: null` | поле є, UI на нього не розгалужується ніде | `api.ts:268` |
| ✗ | межа поля | ліміти є в домені, **лічильника в UI Профілю немає** | `profile-fields.ts` vs `pages/Profile/` |
| ✗ | скелетон без чисел | **скелетон є** і підключений на 5 екранах — це діра макета, не коду | `components/Skeleton/Skeleton.tsx` |

### Б2 · Значення

| §0 | Рядок | Код | Де |
|---|---|---|---|
| ✅ | картки трьох режимів | **є** | `packages/domain/modes.ts`, `card-modes.test.ts` |
| ✅ | `strict/approx` у календарі | **є, включно з `source`** | `apps/web/src/lib/period.ts:115`, `PeriodArtifact.tsx:185` |
| ◐ | безпека «не їм / не можна» | два поля вето є (`no`, `ban`, `indexed: true`) | `profile-fields.ts:20`, `packages/domain/veto.ts` |
| ◐ | стан пачки (ціле/відкрито) | **є повністю, і в UI теж** — тумблер «Позначити відкритою», `expiryOnOpen` менше-з-двох | `BatchCard.tsx:176`, `apply.ts:612`, `apply.ts:680` |
| ✗ | три осі рядка в окремих каналах | немає — одна вісь (див. Ф2 вище) | — |
| ✗ | 8 міток контексту | **усі вісім є — але тільки як текст промту**, жодна не рендериться | `packages/domain/context.ts:252–278` |
| ✗ | «прострочено ≠ сьогодні» | немає (див. Б1 `days<0`) | — |
| ✗ | `status` профілю видимий | тип є (`empty/filled/none`), UI трьох форм — немає | `profile-fields.ts:37` |
| ✗ | охоплення вето | **немає.** `matchVeto(text, index, scope?)` — тут `scope` це кеш, не охоплення | `veto.ts:67` |
| ✗ | джерело калорій | немає поля: `nu.kcal` одне число, рендериться голим | `types.ts:257`, `Feed/cards.tsx:1013` |

### Б3 · Дії

| §0 | Рядок | Код | Де |
|---|---|---|---|
| ◐ | undo-вікно | **undo працює**: `undo_token`, `/v1/cards/:id/undo`, undo для cook-runs. Видимого таймера немає | `api.ts:475`, `api.ts:655`, `api.ts:725` |
| ◐ | збереження blur/Enter | картка комори вже комітить полями | `BatchCard.tsx` (`commit`) |
| ✗ | причина списання «зіпсувалось» | **уже є в коді** — `depleted_reason: 'spoiled'`, і причина знімається при поверненні партії. Діра в макеті, не в коді | `apply.ts:702`, `contract.ts:477` |
| ✗ | «ще годиться?» | немає | — |
| ✗ | стани дії (ліміт/мережа/конфлікт/частковий/порожній) | частковий успіх у коді є (`applied`, `missed`, `already_there`, `truncated`); окремого «рядка стану» немає | `api.ts:651` |
| ✗ | уточнення нерозпізнаного рядка чека | немає | — |

### Б4 · Масштаб

| §0 | Рядок | Код |
|---|---|---|
| ✅ | 16 токенів, 4 радіуси, Lucide, дві теми з одних імен | є; `TOKEN_AUDIT.md` уже про це |
| ◐ | ролі типографіки без повних параметрів | **закрито в бандлі**: `tokens-v3.md` дає 10 ролей з кеглем/вагою/трекінгом/інтерліньяжем |
| ◐ | правило емфази не число | **закрито**: «≤ 3 виділених рядків + 1 картка» |
| ✗ | чек-лист повноти §5 | скелетон і `ErrorState/copy.ts` є; повного набору станів на екран — немає |
| ✗ | пороги `design-audit.mjs` | **закрито в бандлі**: `audit-thresholds.md`. Сам `scripts/design-audit.mjs` у теці лежить, але ще не відстежений |
| ✗ | паритет ієрархії небезпечних дій у темній | правило сформульовано в `tokens-v3.md`, перевірки немає |

**Підсумок §0.** Матриця в трьох місцях недооцінює код і в двох переоцінює.
Недооцінює: причина списання, undo, стан пачки, скелетон — усе це в коді вже є, дизайн їх рахує дірами.
Переоцінює: «3 групи рецептів» ✅ і «6 зон» ✅ (див. нижче).

---

## 2. Де прототип суперечить виміряним контрактам

### 2.1 Шість зон — прототип рендерить пʼять

Код: шість, включно з `spices: 'Спеції'`. У бандлі слова «Спеці» немає **жодного разу** — ні в Prototype, ні в Components (0 збігів). Прототип показує Свіже / Холодильник / Морозилка / Суха шафа / Напої.

Це не косметика: `spices` — єдина зона, яку правило строків накриває через `days: null` («не псується»), тобто саме на ній видно четверте слово часу з `tokens-v3.md`. Зона, на якій механізм найкраще видно, у макеті відсутня.

### 2.2 Пороги 3 / 5 / 1 — не «одне місце», і їх чотири

`tokens-v3.md` обіцяє «пороги свіжості: 3 / 5 / 1 — одне місце». У коді:

| Поріг | Що робить | Де |
|---|---|---|
| `SOON_CUT_DAYS = 3` | зріз «скоро зіпсується» + бурштин у колонці | `filter.ts:21` |
| `FRESH_SOON_DAYS = 5` | іконка «добігає» | `filter.ts:22` |
| `FRESH_CHECK_DAYS = 1` | іконка «перевірити» | `filter.ts:23` |
| `days <= 3` | те саме, **вдруге, окремою константою-літералом** | `Feed/Feed.tsx:507` |
| `days <= 7` | мітка `!Nдн` у промті | `context.ts:253` |

Це **дві різні драбини**, а не одна: 3 керує зрізом, 5…1 керує іконкою. Партія на 4 дні має іконку «добігає», але в зріз «скоро зіпсується» не потрапляє. Прототип малює одну шкалу — на ній ці два числа неминуче розійдуться. Плюс сьома доба живе в промті й на екран не виходить взагалі.

І «одне місце» вже неправда: трійка лежить у `apps/web/src/pages/Pantry/filter.ts` — файлі **сторінки**, не в `packages/domain`, — а Feed тримає власну копію трійки.

### 2.3 Три походження строку — прототип змішав дві різні речі

У коді це два незалежні поняття:

- **походження СТРОКУ** — `ShelfSource = 'category' | 'manual' | 'foodkeeper:${string}'` (`shelf-life.ts:33`). Коментар у самому файлі: «Поки всі — `category`». Тобто двох із трьох походжень **у даних не існує**;
- **походження ПАРТІЇ** — `Provenance = 'receipt_line' | 'package_label' | 'user_statement' | 'visual_guess' | 'inference'` (`types.ts:20`), пʼять значень, які веб-API зводить до трьох: `origin.kind: 'receipt' | 'manual' | 'chat'` (`api.ts:302`).

PLAN §2 віддає слот «походження» квартету партії (чек · рука · розмова · зі списку), а PLAN §3 окремо каже «строк із трьома походженнями». Це різні осі під однією назвою.

Далі — інверсія в самому прототипі:

| Іконка | Prototype | Components | Код |
|---|---|---|---|
| `receipt` (чек) | 44 | є | `'receipt'` ✓ |
| `message-circle` (розмова) | 4 | є | `'chat'` ✓ |
| `pencil` (рука) | **0** | 3 | `'manual'` ✓ — і тест на нього є (`BatchCard.test.tsx:35`, «додано рукою») |
| `list-checks` (зі списку) | 5 | — | **немає нічого** |

Прототип показує походження, якого в даних нема, і не показує того, яке в даних є і покрите тестом. Ще `package_label`, `visual_guess` і `inference` губляться на межі API — а `inference` це рівно те, з чого робиться мітка `?домисл.N%`, яку PLAN §2 хоче завести в чек.

І практичне: «перемикач „поставити дату“» з PLAN §3 — це запис `ShelfSource: 'manual'`. Шляху запису немає. Це не малювання, це домен.

### 2.4 Межі профілю — не 140/200/260, а пʼять різних

`profile-fields.ts:19`:

| Поле | Ліміт |
|---|---|
| `name` | **30** |
| `no`, `love`, `meh` | 200 |
| `ban` | 140 |
| `when` | **250** |
| `kit` | 260 |
| нотатка асистента | 140 (`NOTE_TEXT_LIMIT`) |

Прототип має лічильники на 140 / 200 / 250 / 260, але **не на 30** — а `name` найкоротше поле, і саме на ньому лічильник потрібен найбільше. PLAN §6 називає лише «140/200/260», тобто план тут відстає навіть від власного прототипу.

Окремо: `KIT_DEFAULTS` (плита, духовка, мікрохвильовка, холодильник) — «єдина дужка, яку додаємо самі». Це текст, якого людина не писала, у полі зі `status`. Ні PLAN, ні прототип його не розрізняють.

### 2.5 Прототип і Components розходяться між собою

HANDOFF §0 каже: «Prototype — єдине джерело правди про поведінку». Але нова лексика часу існує **тільки в Components**:

| Слово | Prototype | Components |
|---|---|---|
| `сьогодні` | 13 | — |
| `не псується` | **0** | 3 |
| `розрахунок скінчився` | **0** | 1 |
| `не казав` (`status: empty`) | **0** | 1 |

Тобто прототип поводиться так, як поводиться поточний код (`days<=0 → «сьогодні»`) — рівно те, що PLAN §2 велить прибрати. Два «джерела правди» суперечать одне одному на головному компоненті, і те з них, яке HANDOFF називає головним, стоїть на старій механіці.

### 2.6 Дрібніше, але варто знати

- **«Дві мітки на рядку»** — не ◐, а відкат. Крок Ф2 свідомо звів рядок до однієї осі іконок, і на це є тест (`Pantry.test.tsx:69`). v3 повертає дві мітки — це рішення проти рішення, і його треба назвати рішенням.
- **Колізія «Свіже»** (PLAN ⚠5) у коді гірша, ніж у плані: `ZONE_LABEL.fresh = 'Свіже'` і `FRESH_TITLE.fresh = 'Свіже'` — одне слово як назва зони і як `aria-label` іконки стану, в одному рядку.
- **Джерело калорій** (PLAN §8) вимагає поля, якого немає: `nu.kcal` — одне число. Прототип уже пише `≈ N ккал` шість разів, Components — `≈ 271 ккал · з каталогу`. Обидва обіцяють різницю, якої дані не несуть.
- **Охоплення вето** — не пробуйте прочитати з `matchVeto(…, scope?)`: там `scope` це кеш відпечатків.

---

## 3. Про пʼять етапів

З порядком PLAN.md погоджуюсь у головному: ролі першими, рядок комори другим. Але етапи 1 і 2 не рівні за вагою, і одну річ треба зробити **до** етапу 1.

**Етап 0 — узгодити контракти (нема в PLAN).** Три речі не малюються, вони або є в домені, або нема: `days < 0` як окремий стан; `ShelfSource: 'manual'` як записуваний; єдине місце для порогів (винести трійку з `pages/Pantry/filter.ts` у `packages/domain`, прибрати копію з Feed, вирішити долю сьомої доби). Поки цього нема, четверте слово часу й перемикач дати — намальовані обіцянки. Плюс тут же відповісти на шість ⚠ з кінця PLAN. Це найдешевший етап і він розблоковує решту.

**Етап 1 — ролі й токени.** Погоджуюсь, одна ітерація. `tokens-v3.md` і `audit-thresholds.md` уже це майже закрили; лишається завести `scripts/design-audit.mjs` під ці пороги.

**Етап 2 — рядок комори — я б розрізав навпіл.** Це найбільший шматок плану і три незалежні речі: (а) три канали рядка + чотири слова часу; (б) картка позиції з паспортною назвою і межами; (в) списання з причиною. З них (в) майже готове в домені (`depleted_reason`) — це лише UI, і його можна робити паралельно з (а). А (а) тягне за собою рішення по «двох мітках» — тобто впирається в етап 0.

**Етапи 3–5 — по плану**, з однією поправкою: «стани повноти на всі екрани» (етап 5) не має бути останнім цілком. Скелетони в коді вже є на пʼяти екранах; правило «нуль чисел до приходу даних» дешевше вкласти в етап 1 разом із порогами аудиту, ніж обходити всі екрани вкінці.

**Разом — сім шматків, не пʼять:**

| # | Шматок | Залежить від |
|---|---|---|
| 0 | Контракти: `days<0`, `manual`, одне місце порогів, шість ⚠ | — |
| 1 | Ролі, токени, пороги аудиту + правило «без чисел у скелетоні» | — |
| 2a | Рядок комори: три канали, чотири слова часу, шоста зона | 0, 1 |
| 2b | Картка позиції: паспортна назва, межі полів, `status` | 1 |
| 2c | Списання з причиною (UI над готовим доменом) | 1 |
| 3 | Сліди, чек мережі, рядок стану дії | 1 |
| 4 | «Дім зараз», профіль, календар, рецепти | 2b |

2c і 3 можна вести паралельно з 2a. Довгий шлях — 0 → 1 → 2a → 4.

**Що я б зробив на першому екрані, а не наприкінці:** прогнав `design-audit.mjs` з `audit-thresholds.md`. HANDOFF це вже вимагає, і це єдина перевірка в бандлі, яка ловить регрес автоматично.
