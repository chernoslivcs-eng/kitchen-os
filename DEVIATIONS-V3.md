# Реєстр відхилень · редизайн v3

Дата: 2026-09-10. Гілка `feat/redesign-v3`. Етап 0 плану `docs/superpowers/plans/2026-09-10-redesign-v3.md`.

**Правило:** код — джерело правди. Розходження макета з кодом — відхилення дизайну, і воно правиться в макеті. Зміна в `packages/domain`, `packages/catalog`, `packages/prompts` — тільки окремим рішенням власника.

**Спосіб перевірки:** греп по всій теці `ai/project/` (14 файлів `.dc.html` + `.md`), не по памʼяті й не по двох файлах. Причина — нижче, ~~Р1~~.

---

## Знято як помилкові

### ~~Р1 · «Шоста зона відсутня в макетах»~~

**Знято 10.09 власником. Підстава перевірена.**

Слово «Спеці» є в бандлі **чотири рази у двох файлах**: `Kitchen OS - Responsive.dc.html` (1) і `Kitchen OS - Screens.dc.html` (3). У даних стоять усі шість зон:

```js
{ icon:'leaf', name:'Свіже', n:14 }, { icon:'refrigerator', … },
{ icon:'archive', name:'Суха шафа', n:29 }, { icon:'flame', name:'Спеції', n:11 },
{ icon:'wine', name:'Напої', n:16 },
```

Кадр свідомий і підписаний на екрані: **«за місцем · 4 зони з 6 у кадрі · Спеції й Напої нижче · 19 без категорії — у своїй зоні останніми»**. Порядок зон узятий із `product-map`: «Свіже · Холодильник · Морозилка · Суха шафа · Спеції · Напої».

**Чому я помилився:** грепнув `Спеці` по двох файлах із чотирнадцяти — `Prototype` і `Components` — і з нуля збігів зробив висновок про весь бандл. Помилка методу, не читання.

**Не відкривати вдруге.**

### ~~Р5 · «Інверсія іконок походження»~~

**Знято 10.09 за результатом повторної перевірки.**

Твердив, що прототип показує `list-checks` (немає в коді) і не показує `pencil` (є в коді + тест). Насправді `pencil` є **девʼять разів у трьох файлах** (`Components` 3, `Screens` 4, `Responsive` 1, плюс `tokens-v3.md`), і саме в слоті походження:

```js
{ name:'Помідори', …, origin:'pencil', time:'≈ сьогодні', … }
mk('Салат айсберг','','','pencil','−4 дн',D,D,'')
<i data-lucide="pencil" …></i>своє
```

Дані рядків у `Components` вживають усі чотири походження: `receipt` 7 · `pencil` 2 · `message-circle` 2 · `list-checks` 1. Бандл узгоджений із власною специфікацією.

**Чому я помилився:** та сама помилка методу — греп по `Prototype`. А 69 збігів `list-checks` я прийняв за слот походження, тоді як це іконка навігації (таб «Список»).

**Залишок, який справді є,** переїхав у Р6: із чотирьох походжень макета під `list-checks` у коді немає нічого.

### ~~Р7 · «Межі профілю — лічильника на 30 немає»~~

**Знято 10.09 за результатом повторної перевірки.**

`Screens.dc.html` показує лічильник рівно там, де треба, і з правильним числом:

```js
PR('Мені не можна', 'фініків', { danger:true, active:true, counter:'6/140' })
PR('Я не дуже люблю', …, { empty:true })
```

`140` — це точний ліміт `ban` у `profile-fields.ts:21`. Поле `Мене звати` є в пʼяти файлах бандла. Тобто макет демонструє **патерн** лічильника на одному полі, а не перелічує всі пʼять лімітів — так і має бути в специфікації компонента.

**Чому я помилився:** рахував голі числа `140|200|250|260` у файлі. Це шум: ті самі числа стоять у `width:200px` і подібному. Сигнал нульової якості.

**Залишок:** PLAN §6 у тексті каже «Межа 140/200/260» — три з пʼяти (нема 30 і 250). Це неточність тексту плану на один рядок, не відхилення макета. Правиться без рішення.

### ~~Р9 · «Два джерела правди суперечать одне одному»~~

**Знято 10.09 за результатом повторної перевірки.**

Твердив, що нова лексика часу є тільки в `Components`, а `Prototype` стоїть на старій механіці. Насправді це розподіл праці, оголошений у самому HANDOFF: `Components` — крок 3, «компоненти кроків 1–5… шкала „скільки днів лишилось“». Специфікація рядка живе там за задумом.

А прострочене в бандлі намальовано — просто іншим написанням: `Screens` рендерить **«−9 дн»** і **«−4 дн»** у `danger`, а `Components` пояснює: ««−9 дн» — прострочено (danger); «—» — не псується або без каталогу».

**Залишок:** бандл має **два написання одного стану** — «−9 дн» у `Screens` і «розрахунок скінчився» у `Components`/`tokens-v3`. Вибрати одне. Переїхало в Р3.

Окремо: фраза HANDOFF §0 «Prototype — єдине джерело правди про поведінку» і є те, що збило. Варта уточнення до «про поведінку і флоу; канон компонентів — Components».

### ~~Р11 · «Колізія „Свіже“»~~

**Закрито рішенням власника ⚠5 від 09.09**, не знято як помилкове. Див. «Рішення, вже прийняті» нижче. Залишок, який рішення не покриває, виділено в Р22.

---

## Рішення, вже прийняті власником (09.09) — не перевідкривати

| ⚠ | Рішення | Дата |
|---|---|---|
| ⚠2 | Картка позиції зберігається автоматично на blur/Enter. Кнопку «Зберегти» з макета 3b **прибрати** | 09.09 |
| ⚠3 | Причина списання **обовʼязкова** з картки, **необовʼязкова** з ✕ у рядку (тост із «Повернути») | 09.09 |
| ⚠4 | «Лежить тиждень+» у рядку **не показувати** — лише сортуванням «за датою» | 09.09 |
| ⚠5 | Зони лишаються як є («Свіже» — назва зони). Слово «свіже» для **стану** не вживається ніде. `product-map.md` тим самим не розходиться, дозволу не потрібно | 09.09 |

---

## Відкриті — потрібне рішення

### Р2 · Пороги свіжості: дві драбини, не одна

| | |
|---|---|
| **Код** | `SOON_CUT_DAYS = 3` (зріз «скоро зіпсується»), `FRESH_SOON_DAYS = 5` і `FRESH_CHECK_DAYS = 1` (іконка) — `apps/web/src/pages/Pantry/filter.ts:21-23`. Дубль `days <= 3` окремим літералом — `Feed/Feed.tsx:507`. Ще `days <= 7` для мітки `!Nдн` — `packages/domain/context.ts:253` |
| **Макет** | `tokens-v3.md`: «пороги свіжості: 3 / 5 / 1 — одне місце» |
| **Суть** | Драбини дві й вони не збігаються: партія на 4 дні має іконку «добігає», але в зріз не потрапляє. Плюс сьома доба живе в промті й на екран не виходить. «Одне місце» вже неправда — трійка лежить у файлі *сторінки*, а Feed тримає власну копію |

**Варіанти:** (а) звести до однієї драбини; (б) лишити дві, але назвати різними словами й винести обидві в `packages/domain`; (в) не чіпати.

**Рекомендація — (б).** Зріз і шкала відповідають на різні питання: «що показати першим» і «яким кольором». Зводити їх — втрата. Але обидві мусять жити в одному місці домену, і сьома доба туди ж. Дубль у `Feed.tsx:507` прибрати безумовно — це не рішення, це борг.

**Рішення: (б)** — дві драбини лишаються, але з різними іменами, і обидві переїжджають у `packages/domain`. Сьома доба з `context.ts:253` туди ж. Дубль `days <= 3` у `Feed/Feed.tsx:507` прибрати безумовно. (10.09)

### Р3 · `days < 0` зведено в «сьогодні»

| | |
|---|---|
| **Код** | `it.days <= 0 ? 'сьогодні'` — `filter.ts:75`; те саме `Feed/Feed.tsx:1718`. `freshness()` вертає `check` і для 0, і для відʼємного |
| **Макет** | Стан намальований **двічі й по-різному**: «−9 дн» у `danger` (`Screens`) і «розрахунок скінчився» (`Components`, `tokens-v3`, `PLAN`) |
| **Суть** | У даних Б1 — 10 прострочених зі 113 позицій. Це 9 % комори, яку зараз видно як «сьогодні» |

**Варіанти:** (а) додати в домен окремий стан і четверте слово; (б) лишити «сьогодні».

**Рекомендація — (а),** і вибрати одне з двох написань. Дешево: одна функція, два місця рендера. «Сьогодні» на девʼятиденному простроченні — це не косметика, це неправда на екрані. З двох написань я за «−9 дн»: воно числове, лягає в `tabular-nums` і не займає рядок.

**Рішення: (а)** — четвертий стан у домені. У рядку — «−9 дн» у `danger`. Картку не чіпати: вона вже каже «термін вийшов», тобто вже чесна. (10.09)

### Р4 · `ShelfSource: 'manual'` не пишеться ніде

| | |
|---|---|
| **Код** | `export type ShelfSource = 'category' \| 'manual' \| ` `foodkeeper:${string}` — `packages/domain/shelf-life.ts:33`. У самому файлі: «Поки всі — `category`» |
| **Макет** | Перемикач «поставити дату» в картці позиції (PLAN §3); «строк із трьома походженнями» |
| **Суть** | Двох походжень із трьох у даних не існує. Обіцянка на екрані є, шляху запису немає |

**Варіанти:** (а) завести запис `manual` разом із перемикачем (етап 2b); (б) прибрати перемикач із макета.

**Рекомендація — (а),** окремим кроком у 2b і з тестом. Без цього «три походження строку» — фікція, і перемикач на екрані буде кнопкою в нікуди.

**Рішення: (а)** — запис `manual` разом із перемикачем «поставити дату», в етапі 2b, з тестом. (10.09)

### Р6 · `Provenance` пʼять → API три; `list-checks` без підстави

| | |
|---|---|
| **Код** | `Provenance = 'receipt_line' \| 'package_label' \| 'user_statement' \| 'visual_guess' \| 'inference'` — `types.ts:20`. API віддає три: `origin.kind: 'receipt' \| 'manual' \| 'chat'` — `api.ts:302` |
| **Макет** | Чотири походження: `receipt · pencil · message-circle · list-checks` (`tokens-v3.md`), усі чотири вжиті в даних `Components` |
| **Суть** | Одне з чотирьох (`list-checks`, «зі списку») не має в коді нічого. З протилежного боку губляться три: `package_label`, `visual_guess`, `inference` — а `inference` це рівно те, з чого робиться мітка `?домисл.N%` (`context.ts:267`) |

**Варіанти:** (а) підняти в API `inference` четвертим і прибрати `list-checks` з макета; (б) лишити три й прибрати `list-checks`; (в) підняти всі пʼять.

**Рекомендація — (а).** `inference` потрібен, бо «домислено 60 %» — обіцянка лендінгу, і зараз вона на екран не виходить взагалі. `package_label` і `visual_guess` показувати нікуди — вони не змінюють дії людини. `list-checks` прибрати: покупка зі списку все одно приходить чеком або рукою, окремого походження в неї нема.

**Рішення: (а)** — `inference` піднімаємо в API четвертим походженням; `list-checks` з макета прибираємо. (10.09)

### Р8 · `KIT_DEFAULTS` — текст, якого людина не писала

| | |
|---|---|
| **Код** | `KIT_DEFAULTS = ['плита', 'духовка', 'мікрохвильовка', 'холодильник']` — «єдина дужка, яку додаємо самі» (`profile-fields.ts:52`) |
| **Макет** | Не розрізняє: поле `У мене на кухні є` подане як звичайний текст людини |
| **Суть** | `status: filled` на цьому полі бреше — людина цього не казала |

**Варіанти:** (а) тихий окремий рядок «база кухні» під полем; (б) не розрізняти.

**Рекомендація — (а).** Профіль — місце, де людина бачить, що про неї записали. Чужий текст під своїм імʼям там найдорожчий.

**Рішення: (а)** — «база кухні» тихим окремим рядком під полем. (10.09)

### Р10 · Дві мітки на рядку — відкат рішення Ф2

| | |
|---|---|
| **Код** | Крок Ф2 звів рядок до однієї осі іконок; на це є тест: «іконка лише за свіжістю — свіже / добігає / перевірити; „не їм“ без іконки, лише підрядком» (`Pantry/Pantry.test.tsx:69`) |
| **Макет** | Три канали: `[безпека] [походження]` окремими слотами праворуч від назви |
| **Суть** | Це рішення проти рішення, не діра. У даних Б1 — 17 рядків зі 113 мають дві мітки |

**Варіанти:** (а) повернути два слоти як у v3; (б) лишити одну вісь Ф2.

**Рекомендація — (а).** Ф2 знімав мітки з іншої причини: тоді вони були іконками на **одній** осі й змагалися за увагу. v3 дає їм окремі канали — це не повернення до того, що прибрали. Тест `Pantry.test.tsx:69` переписати свідомо, з коментарем чому.

**Рішення: (а)** — два слоти повертаємо. Аргумент прийнято: Ф2 знімав мітки з ОДНІЄЇ осі, v3 дає їм окремі канали — це не відкат. Тест `Pantry.test.tsx:69` переписати свідомо, з коментарем, який називає Ф2 і причину зміни. **Мовчки не видаляти.** (10.09)

### Р12 · Рецепт не має `est`, партія має

| | |
|---|---|
| **Код** | Партія: `est?: boolean \| null` (`api.ts:294`), і `filter.ts:58` уже робить `it.est ? '≈' : ''`. Рецепт: `nu?: { kcal, p, f, c }` (`types.ts:257`) — прапорця немає, рендериться голим (`Feed/cards.tsx:1013`) |
| **Макет** | Бандл послідовно розрізняє: `≈ 540 ккал` — оцінка, `392 ккал · Б 33 · Ж 28` і `532 ккал … збігається з рецептом` — точне. Близько 30 вживань `≈` на картках рецептів |
| **Суть** | Макет тут **правий і послідовний**; PLAN §8 («`est` → `≈`») теж. Даних під це немає лише на рецепті |

**Варіанти:** (а) додати `est` до `nu` рецепта; (б) прибрати `≈` з карток рецептів.

**Рекомендація — (а).** Механізм у проєкті вже є на партії — лишається продублювати. Без нього `≈` на тридцяти картках — декорація, і перше ж «чому тут приблизно, а тут ні» не має відповіді.

**Рішення: (а)** — `est` на `nu` рецепта. Це закриває **борг №21** («Рецепт показує два різні числа калорій на двох екранах», `DEBT.md:362`): там уже виміряно розбіжність до 30 % і названо обидва джерела — `recipe.nu` як оцінка моделі в чаті (`routes/chat.ts:700`) і `nutrition_calc` як розрахунок з каталогу на сторінці (`routes/recipes.ts:207`). Тобто розрізнення вже існує на сервері, на екран не виходить лише знак. (10.09)

### Р13 · Охоплення вето не існує

| | |
|---|---|
| **Код** | `matchVeto(text, index, scope?)` — `veto.ts:67`; `scope` тут **кеш відпечатків**, не охоплення. Трьох рівнів (категорія / продукт / вільне) немає |
| **Макет** | Охоплення в картці позиції (PLAN §2) |

**Варіанти:** (а) в обсяг v3; (б) відкласти за межі v3.

**Рекомендація — (б).** Це доменна робота — три рівні охоплення плюс міграція `veto_index`, — а не редизайн. У макеті вона живе одним рядком. Змішувати її з редизайном означає завалити обидва.

**Рішення: (б)** — **поза обсягом v3.** Заведено окремим пунктом у `DEBT.md` §24: три рівні охоплення + міграція `veto_index`, доменна робота. (10.09)

### Р14 · Дві специфікації порогів аудиту суперечать

| | |
|---|---|
| **Код** | `MAX_COMBOS = 6` — «С1: шість ролей», джерело `TASK-DESIGN-SYSTEM.md §С1–С3` (`scripts/design-audit.mjs`) |
| **Макет** | `audit-thresholds.md`: «стилів на екран ≤ 8», бо «10 ролей у tokens-v3» |
| **Суть** | Обидві специфікації живі й у репозиторії. Блокує завдання 1.3 |

**Варіанти:** (а) `audit-thresholds.md` перемагає; (б) скрипт перемагає; (в) 8 як стеля, 6 як мета.

**Рекомендація — (а) з додатком.** `tokens-v3.md` оголошує десять ролей — це канон бандла, а `TASK-DESIGN-SYSTEM.md §С1–С3` описував шість ролей попередньої системи, тобто історія того ж роду, що тека `design/`. Але додати окрему метрику «≤ 6 на одному екрані»: десять ролей існують у системі, на одному екрані живуть не всі — і саме там шість доречні.

**Рішення: (а)** — `audit-thresholds.md` перемагає, плюс окрема метрика «≤ 6 ролей на одному екрані». `TASK-DESIGN-SYSTEM.md §С1–С3` — історія, того ж роду, що тека `design/`. (10.09)

### ~~Р15 · «3 групи рецептів» ✅ без підстави~~

**Знято 10.09 власником. Підстава перевірена.**

Зріз існує — просто названий інакше. `apps/web/src/pages/Recipes/Recipes.tsx:24-27`:

```ts
{ id: 'all',    label: 'Усі' },
{ id: 'ready',  label: 'Можу приготувати зараз' },
{ id: 'near',   label: 'Майже' },
{ id: 'cooked', label: 'Готував' },
```

І в коментарі поруч (`Recipes.tsx:6`): «Фільтри — з прототипу: „Можу зараз“ (ready) / „Майже“ (near) / „Готував“». Тобто зріз узятий саме з прототипу, три робочі групи плюс «Усі».

**Чому я помилився:** шукав слова «група» і «зріз», а не самі фільтри. Ознака є в UI під іншими іменами — той самий рід помилки, що в ~~Р5~~ і ~~Р21~~.

**Рішення: ✅ у матриці §0 знести як помилкове, нової роботи не заводити. (10.09)**

### Р16 · Шістнадцять токенів не покривають ужитого

| | |
|---|---|
| **Код** | Поза шістнадцяткою живуть і вживаються: `--accent-strong`, `--accent-bg-strong`, `--accent-dim`, `--accent-fg-on`, `--amber-border`, `--plum-border`, `--border-strong` (`styles/tokens.css`) |
| **Макет** | 16 токенів + мапа старих імен у HANDOFF |
| **Суть** | Мапа не 1:1. Сім імен — це ховер заливки, текст на заливці, рамка семантики: те, чого шістнадцятка не передбачила |

**Варіанти:** (а) розширити канон сімкою похідних в іменах v3; (б) знести їх і брати `color-mix()`; (в) лишити старі імена поруч назавжди.

**Рекомендація — (а).** Сім імен не розмивають канон — вони добудовують те, чого в ньому нема. (б) гарніше, але `color-mix()` у двох темах дає непередбачувані контрасти, а пороги аудиту в нас жорсткі — текст `< 4.5:1` має бути нулем.

**Рішення: (а)** — сімку похідних імен додаємо в канон. `color-mix()` у двох темах при жорсткому порозі «текст < 4.5:1 = 0» — ризик, який не окупається. (10.09)

### Р19 · Гарнітур три, не одна

| | |
|---|---|
| **Код** | `--font-display: 'Onest'`, `--font-body: 'Golos Text'`, `--font-mono: 'IBM Plex Mono'` (`tokens.css:123-125`); усі три тягне `apps/web/index.html:18` |
| **Макет** | «Шрифт один — Onest»; `audit-thresholds.md`: «гарнітур 1» |

**Варіанти:** (а) все на Onest, `--font-mono` → Onest із `tabular-nums`; (б) лишити Golos для body.

**Рекомендація — (а),** але **не на етапі 1.** Цього прямо вимагає і HANDOFF, і мапа адмінки. Але Golos сидить у `body`, тобто під кожним компонентом: Onest ширший, рядки поїдуть, і це окремий крок із візуальною перевіркою, а не правка в шарі токенів.

**Рішення: (а)** — все на Onest, але **не на етапі 1**: окремим етапом із візуальною перевіркою. Golos сидить під `body`, поїдуть довжини рядків усюди. (10.09)

### Р20 · Капс і моно як мова станів

| | |
|---|---|
| **Код** | `components/MonoLabel/MonoLabel.module.css` — «той самий IBM Plex Mono, малий кегль, широкий трекінг, uppercase». `Button.module.css:79`: «Golos 600/14, ніколи не моно. Моно лишається мовою станів і метаданих» |
| **Макет** | «Капс не використовується»; мапа адмінки велить знімати капс і трекінг «як у продукті» |
| **Суть** | Формулювання мапи каже, що автор бандла вважав це вже зробленим. У продукті капс і моно на місці, і вони **семантика**, не оформлення |

**Варіанти:** (а) знести `MonoLabel` і капс, метадані розрізняти роллю `caption` + кольором; (б) лишити моно як мову станів.

**Рекомендація — (а), разом із Р19** — це одна робота. З попередженням: знявши гарнітуру, треба чимось відрізнити метадані, і v3 дає це кольором (`muted`/`dim`) та роллю, не шрифтом. Перевіряти на «Дім зараз», де метаданих найбільше.

**Рішення: (а)** — капс і моно знімаємо **тим самим етапом, що Р19**: це одна робота. Метадані розрізняти роллю `caption` + кольором; перевіряти на «Дім зараз». (10.09)

### Р21 · Зони не мають свого сімейства знаків — і тому позичають

**Переписано 10.09 після повторної перевірки. Попереднє формулювання («29 вживань, ламається у пʼяти сенсах») було наслідком тієї самої помилки методу: набір знаків лежить у `renderVals()`, а плитки звертаються до нього через `data-lucide="{{ it.icon }}"` — шаблонну дірку. Греп по літералах побачив 18 знаків замість 64.**

| | |
|---|---|
| **Набір** | Названо **64** знаки: `system` 24 · `products` 20 · `cooking` 20. HANDOFF обіцяє «Продукти/готування — 40 знаків названо» — це products + cooking **рівно**, обіцянка точна |
| **Всередині сімей чисто** | Жоден знак не несе двох значень у межах трьох сімей |
| **Причина** | **Зони — четверта вісь, і сімейства під неї немає.** Тому всі шість знаків зон позичені з інших сімей |

| Зона | Знак | Звідки позичено |
|---|---|---|
| Свіже | `leaf` | `products`: **Зелень** |
| Холодильник | `refrigerator` | `system`: **Комора** |
| Морозилка | `snowflake` | `cooking`: Морозилка — чисто |
| Суха шафа | `archive` | **немає в сеті взагалі** |
| Спеції | `flame` | `cooking`: **«Гриль, вогонь»** |
| Напої | `wine` | `products`: Напої — чисто |

Чотири з шести зон або конфліктують з іншим значенням, або малюються знаком, якого в наборі нема.

**І два місця, де сам набір суперечить правилам HANDOFF:**

| Правило HANDOFF | Набір Icons |
|---|---|
| «`flame` — тільки „Горить“» | `{ icon:'flame', label:'Гриль, вогонь' }` у сімʼї `cooking` — офіційне друге значення |
| «Дія „Готуємо“ — `cooking-pot`; `chef-hat` — тільки як тип рецепта» | `{ icon:'cooking-pot', label:'Плита' }` і `{ icon:'chef-hat', label:'Готуємо' }` — рівно навпаки |

Тобто «`flame` на зоні Спеції» — не недбалість, а наслідок: зона позичила знак у сімʼї, де він законно означає вогонь. Лікується не заміною однієї іконки, а четвертою сімʼєю.

Ще: `leaf` має три значення — Зелень (`products`), «пісне» (чіп у картці рецепта), зона Свіже. І `sparkles` («Думаю») у 64 не входить.

**Варіанти:** (а) додати четверту сімʼю «Зони» — шість власних знаків, і звільнити `flame`/`leaf`/`refrigerator`; (б) лишити позичання, але тоді зняти правило «`flame` — тільки „Горить“», бо воно вже не діє; (в) не чіпати.

**Рекомендація — (а).** Шість знаків — найдешевша з можливих правок, і вона прибирає причину, а не симптом.

**Поправка 11.09 — межа зони.** (а) було зроблено 10.09 і **відкочено** 11.09. Бандл не мовчав: `Screens.dc.html` ставить зонам `leaf / refrigerator / snowflake / archive / flame / wine`, а навігаційній «Коморі» — `refrigerator` (12 разів у `Responsive.dc.html`). Заміна на власну сімʼю і `boxes` в навігації — вибір там, де вибір уже зроблено, і тому не реалізації, хоч би яким логічним був аргумент. Код повернуто до бандла; обидві колізії (`leaf`, `refrigerator`) названі в `PENDING_DESIGN_CHAT` і тримаються тестом видимими; питання про окрему вісь для зон — у `QUESTIONS-FOR-DESIGN-CHAT.md` §1. Єдине, що лишилось від (а): заглушка на «Спеціях», бо `flame` там забороняє правило, а знак ще не обраний. Зони — це місця, а не продукти й не техніка: вони заслуговують своєї осі, як і решта трьох. Заодно вирішуються `archive` (нізвідки) і подвійний `leaf`.

Окремо і незалежно: привести набір і правила до однієї думки щодо `chef-hat` / `cooking-pot`. Тут я за правило HANDOFF («Готуємо» = `cooking-pot`), бо на ньому вже стоять «Незмінні правила» і глобальні обмеження плану.

**Рішення: (а)** — правило лишається, макет правимо. Прострочене → знак тривоги в `danger`; сезон → календарний в `amber`; «використає» → свій знак у `sage`; калорії в `Icons` → без знака; «Спеції» → новий знак. Залежить від Р23. (10.09)

### Р22 · Рішення ⚠5 поки не справджується у двох місцях

| | |
|---|---|
| **Рішення ⚠5** | Слово «свіже» для **стану** не вживається ніде |
| **Код** | `FRESH_TITLE = { fresh: 'Свіже', soon: 'Добігає', check: 'Перевірити' }` — і це `title` + `aria-label` іконки **стану** (`Pantry/FreshIcon.tsx:7,10`) |
| **Макет** | `tokens-v3.md`, перше з чотирьох слів часу: **«свіже до 14 вер»** |
| **Суть** | Обидва вживають слово стану — рівно те, що ⚠5 забороняє. Поки їх не змінити, рішення не діє, а колізія з зоною «Свіже» лишається |

**Варіанти:** (а) трійку станів перейменувати на «Добре · Добігає · Перевірити», перше слово часу — «до 14 вер» без прикметника; (б) інша пара слів.

**Рекомендація — (а).** Дві дрібні правки, і ⚠5 стає правдою. «Добре» тримає рід (шавлія = добре) і не змагається з назвою зони.

**Рішення: (а)** — «Добре · Добігає · Перевірити», перше слово часу «до 14 вер» без прикметника. Дві дрібні правки, і ⚠5 стає правдою. (10.09)

### Р23 · Іконкового шару в проєкті немає взагалі

| | |
|---|---|
| **Код** | Lucide **не встановлено**: у `apps/web/package.json` пʼять залежностей, жодної іконкової. Іконки — 15 рукописних inline `<svg>` у девʼяти файлах, компонента `Icon` немає |
| **Макет** | Уся система на Lucide: сітка 24, штрих 1.75, round cap/join, розміри 16/20/24, три сімʼї, моушн на частинах знака, `data-lucide="…"` у кожному прототипі |
| **Суть** | Це не міграція стилю, а **новий шар**: залежність, компонент `Icon`, мапа 64 імен, моушн на частинах, і виведення з обігу 15 рукописних SVG. Ні в §0 матриці, ні в PLAN.md його немає |
| **Уточнено 10.09** | Версія названа в самому бандлі: **`lucide@0.460.0`** (`Icons.dc.html:13`). І моушн-пас не обгортається навколо Lucide як є: для `book` і `fridge` бандл **переписує `innerHTML` SVG руками**, щоб розділити шляхи під анімацію (`Icons.dc.html:218-229`). Тобто `Icon` для частини знаків мусить рендерити власні шляхи, не проксувати бібліотеку |

**Варіанти:** (а) окремий етап між 1 і 2a; (б) фолдити в 2a; (в) лишити рукописні SVG і взяти з Lucide тільки нові знаки.

**Рекомендація — (а), окремий етап.** Він розблоковує Р21 (нема куди додати знак «Спецій»), метрику «гліфів 0» і моушн-таблицю M7. У 2a його фолдити не можна: рядок комори — найщільніший екран, і робити на ньому одночасно три канали й новий іконковий шар означає не знати, що зламалось.

---
**Рішення: (а)** — **окремий етап 1.5, між 1 і 2a.** Lucide як залежність, компонент `Icon`, мапа імен, виведення 15 рукописних SVG, моушн на частинах знака. У 2a не фолдити: рядок комори — найщільніший екран, і робити на ньому три канали й новий іконковий шар одночасно означає не знати, що впало. (10.09)


## Що §0 матриці недооцінює

У коді вже є, дизайн рахує дірами. У роботу брати як готове:

| §0 | Насправді | Де |
|---|---|---|
| ✗ причина списання | `depleted_reason: 'spoiled'`, і причина знімається при поверненні партії | `apply.ts:702`, `contract.ts:477` |
| ◐ undo-вікно | `undo_token`, `/v1/cards/:id/undo`, undo для cook-runs | `api.ts:475`, `:655`, `:725` |
| ◐ стан пачки | тумблер «Позначити відкритою» + `expiryOnOpen` менше-з-двох | `BatchCard.tsx:176`, `apply.ts:612` |
| ✗ скелетон | компонент є, підключений на пʼяти екранах | `components/Skeleton/` |
| ✗ частковий успіх | `applied` / `missed` / `already_there` / `truncated` | `api.ts:651` |
| ✗ 8 міток контексту | усі вісім є — але як текст промту, жодна не рендериться | `context.ts:252-278` |

---

### Р24 · Поріг трекінгу недосяжний за власним каноном

Знайдено 10.09 під час завдання 1.2, тестом на ролі типографіки.

| | |
|---|---|
| **Одна специфікація** | `audit-thresholds.md`: «написань трекінгу **≤ 4**», і перелічує рівно чотири: `-0.03 / -0.02 / -0.01 / 0.01em` |
| **Друга специфікація** | `tokens-v3.md`, таблиця «Типографіка», визначає **шість**: до тих чотирьох додаються `0` (body, row, small, caption) і **`-0.04em`** (timer) |
| **Суть** | Поріг аудиту не досягається жодним каноном, який бандл сам оголошує. Навіть якщо не рахувати `0` за написання — лишається пʼять через `-0.04em` на таймері |

Це той самий рід розходження, що ~~Р14~~: дві живі специфікації порогів, які не звірили між собою.

**Варіанти:** (а) поріг «≤ 6», бо стільки визначає таблиця ролей; (б) поріг «≤ 5», не рахуючи `0` за написання; (в) прибрати `-0.04em` у таймера, звести до `-0.03em`, і тримати «≤ 4» плюс `0`.

**Рекомендація — (а).** ~~Спершу радив (в) — звести таймер до `-0.03em`, бо «на 112 px різниця це чверть пікселя». Число було невірне: `0.01em` на 112 px це **1.12 px** на кожен проміжок, тобто ~3.4 px по ширині «6:42». Це видно, і тісніший трекінг на великому кеглі — не шум, а звичайна типографіка: чим більший знак, тим тісніше його ставлять.~~

Тому правильний хід — поріг, а не канон. Метрика «написань трекінгу» стоїть проти **розсипання** значень по компонентах: щоб ніхто не дописав `letter-spacing` на власний розсуд. Шість значень, оголошених в одному файлі ролей, — це не та хвороба, від якої вона лікує. Поріг має рахувати те, що канон визначає (шість), і падати на всьому, що з'явилось поза ним.

Тобто в `design-audit.mjs` міряти не «скільки написань усього», а «скільки написань **поза** списком ролей» — і цей поріг має бути **нуль**. Тоді метрика робить свою роботу й не змушує калічити таблицю ролей.

**Тест уже тримає факт, не бажане:** `apps/web/src/styles/tokens.test.ts`, тест «Р24» перелічує всі шість написань. Після рішення — правити його разом із `roles.css`.

**Рішення потрібне до завдання 1.3** — там ставляться пороги.

### Р25 · Поріг «найчастіший кегль 14–16» суперечить ролі `caption`

Знайдено 11.09 на здачі 2a, порівнянням аудиту з базою 1.5.

| | |
|---|---|
| **Одна специфікація** | `audit-thresholds.md`: «найчастіший кегль („часто") 14–16 — контент, не мікро-мітка» |
| **Друга** | `tokens-v3.md`: роль `caption` = **13** — «мета в рядку, чіпи» |
| **Факт** | У рядку комори мети більше, ніж назв, **за задумом**: назва одна, а поруч слово часу, зона, безпека, кількість. Отже 13 і буде найчастішим кеглем — і це не мікро-мітка, а роль, яку канон для мети й призначив |

Виміряно: після 2a «часто» на Коморі 13 (порожньо і з девʼятьма позиціями). До
2a було 14 лише тому, що мета сиділа на **10.5 px** — тобто порушувала власне
правило канону «`dim` тільки ≥ 13 px». Правка кегля була правильна, метрика
через неї стала червоною.

Третій випадок того самого роду, після ~~Р14~~ і Р24: дві живі специфікації
порогів, які не звірили між собою.

**Варіанти:** (а) поріг міряє найчастіший кегль **серед контенту**, не серед
усього тексту — тобто `bodySize`, який скрипт уже рахує («найчастіший кегль
читомого тексту»); (б) смугу опустити до 13–16; (в) лишити червоним до переїзду
компонентів на ролі.

**Рекомендація — (а).** Метрика ставилась проти екрана, де НАВІГАЦІЯ і мітки
дрібніші за контент, — і саме це вона мала ловити. `bodySize` це й міряє, і на
ньому Комора дає 13–14 замість 10–11 у базі, тобто рух правильний і видний.
Міряти «часто» по всьому тексту в щільному списку означає карати за щільність.

**Рішення (11.09, етап 5, моя зона — поріг аудиту): (а).** Скрипт міряє
`bodySize` — найчастіший кегль читомого тексту (не моно, ≥ 3 літери), і пише
поруч «усього з мітками». Після цього червоними лишились Рецепти 13 (картка
D5: мета 13 переважує назви 17 — це роль caption, не мікро-мітка; межа 14 тут
карає за щільність, і це видно), Календар 10 і Профіль 11 — а ці два вже не
про поріг: 10–11 px тексту нижче ролі caption 13, тобто порушення канону
«dim тільки ≥ 13 px». Знято з порогу, лишилось у коді — етап 5, чек.

### Р26 · Токен `dim` не проходить власний поріг контрасту канону

Знайдено 11.09 на здачі переїзду токенів, аудитом в обох темах.

| | |
|---|---|
| **Канон, правило** | `tokens-v3.md`: «Контраст: текст ≥ 4.5:1» |
| **Канон, значення** | `dim` = `#9a9ea3` (світла) / `#6f7379` (темна) — «мета, плейсхолдер (тільки ≥13px)» |
| **Факт** | `dim` на `bg`: **2.43** світла / **3.73** темна. На `card`: **2.69** / **3.35**. Нижче 4.5 на кожній поверхні в кожній темі. `muted` проходить (4.56 / 5.06 / 7.35 / 6.60) |

Аудит показує це як «37 текстів нижче AA» на Коморі й «53» на Календарі, і найгірші
рядки на всіх екранах — один і той самий контраст: «9 позицій», «0 / 0», «тиждень
37», «0/30». Це не 37 різних дефектів, а один токен у 37 місцях.

Приймання переїзду («текст < 4.5:1 = 0 в обох темах») **не досягається жодною
реалізацією**, поки `dim` вживається для тексту: хекс і поріг суперечать один
одному всередині одного файла. Застереження «тільки ≥13px» не рятує — WCAG
послаблює поріг до 3:1 лише від 18px (або 14px жирним), а 13 — не великий текст.

Четверте напруження специфікації того самого роду (Р14, Р24, Р25), і перше на
рівні **палітри**, а не порогу аудиту.

**Це не моє рішення.** Бандл каже обидві речі, і вони суперечать: правка хекса —
вибір палітри, тобто дизайн-чат. Варіанти для нього:

- (а) затемнити `dim` до ≥ 4.5 на `card` (≈ `#767a7f` світла / `#8a8e93` темна);
  роль лишається, слово «тихо» стає трохи менш тихим;
- (б) `dim` — лише для не-тексту (плейсхолдер у порожньому полі, вимкнений
  контрол — WCAG їх не рахує), а мета в рядку йде на `muted`;
- (в) свідомо прийняти 3:1 для `dim` як «вторинний текст» і записати це в канон
  як виняток — тоді аудит міряє `dim` окремим порогом.

**Рекомендація як матеріал, не як рішення — (б).** `muted` уже проходить, і саме
його канон призначає для «другого тексту»; `dim` за описом — «мета, плейсхолдер»,
тобто те, що WCAG і так не рахує. Тоді правило й хекс перестають суперечити без
зміни жодного числа. Ціна — переглянути, де `dim` стоїть на змістовному тексті
(лічильники, дати, «без категорії»), і перевести їх на `muted`.

Записано в `QUESTIONS-FOR-DESIGN-CHAT.md` §3.

**Прогін 11.09 (етап 5), обидві теми, засів — арифметику підтверджено й
розширено.** Аудит тепер пише «з них dim N, решта — за кольором». Темна: **усі**
провали AA — `dim` (10/10, 22/22, 9/9, 5/5, 29/29, 19/19); решта палітри в
темній проходить 4.5 на кожній поверхні. Світла: `dim` — половина, а друга
половина — теж токени палітри, і теж бандла:

| світла, текст на | card | bg | line | своє тло |
|---|---|---|---|---|
| `dim` | 2.69 | 2.43 | 2.32 | — |
| `amber` | **3.79** | **3.41** | **3.27** | **3.23** |
| `sage` | 4.84 | **4.36** | **4.17** | **4.12** |
| `muted` | 5.06 | 4.56 | **4.36** | — |
| `plum` | 5.41 | 4.87 | 4.66 | **4.42** |

`amber` як текст не проходить 4.5 **ніде** у світлій — а бандл ставить його на
чіпи сезонів, «бракує: …», «майже», лічильники комори; `sage` проходить лише на
`card`. 2.32 у Кухні («11.09 · 11:01») і Профілі («0/30») — той самий `dim`, на
третій поверхні `line`. Отже питання дизайн-чату ширше за один токен: у
світлій палітрі чотири з шести текстових кольорів не проходять власне правило
на якійсь із поверхонь; у темній — лише `dim`. Дописано в §3.

### Р33 · Пороги аудиту переглянуто одним проходом на числах обох тем

Етап 5, 11.09 — DEBT §32 просив саме цього: не по одному, а разом, і після
прогону, не до. Матеріал — прогони світлої і темної 11.09 на засіві
(`COMPLETENESS-V3.md`). Моя зона — пороги; палітра — ні (Р26, Р34).

| поріг (`audit-thresholds.md`) | числа 11.09 | вердикт |
|---|---|---|
| стилів ≤ 8 | 10–14 на всіх, однаково в темах | **лишається.** Це міра переїзду компонентів на ролі, і вона чесно червона: інлайнові кеглі (Кухня 3, Комора 1) — з того ж списку |
| найчастіший кегль 14–16 | 10–15 | **переписано (Р25 а):** по читомому тексту, не по всьому |
| display ÷ body = 2.0 ± 0.1 | 1.33–2.36 | **знято.** Писалось проти h1 = 32; h1 ролі — 20 (26 на ≥ 1024), display лише в Cook Mode. Стеля h1 лишається: макс. кегль ≤ 26 |
| ваг ≤ 3, 700 лише h1/display | 4 на всіх — 700 на «Кухня» 20 і «Kitchen OS» 17 | **уточнено.** h1 екрана і бренд у рейці — це і є «700 лише h1»; рахуються ваги решти. Після цього 700 лишилось в одному місці — назва рецепта 22/700 у картці стрічки, і це вже справжній червоний |
| роздільники < 1.5 = 0 | 11–51; `line` на `card` = **1.16** світла / **1.17** темна, `line2` — 1.27 / 1.35 | **не поріг — палітра.** Документ писав «line на card = 1.6», арифметика дає 1.16: волосини бандла свідомо тихіші за 1.5. Р34, дизайн-чат |
| текст < 4.5 = 0 | темна — лише `dim`; світла — `dim` + `amber` + `sage` + `muted` на `line` | **не поріг — палітра.** Р26 розширено, §3 |
| навігація не дрібніша за контент | Комора: 14 < 15 | **знято.** Сам документ у колонці «чому» ставить рейку 14/500 під контент 15–16 — поріг суперечив своєму поясненню |
| виділених рядків ≤ 3 + 1 | Рецепти 4 — усі чотири назви карток | **уточнено:** «виділений» = вага ≥ 600 **і** колір тону; назва картки D5 стоїть 17/600 чорнилом і емфазою не є |
| гліфів 0 · трекінгів поза ролями 0 · чисел до даних 0 · дисплейних ≤ 2 · гарнітур 1 · макс. кегль | зелені | лишаються |

Після проходу в обох темах червоними лишаються **чотири речі, і всі про код,
не про поріг**: стилів > 8 і кеглі інлайном (переїзд на ролі не завершено),
700 на назві рецепта в стрічці, текст 10–11 px у Календарі та Профілі. Плюс
дві про палітру — Р26 і Р34 — до дизайн-чату. `audit-thresholds.md` — файл
бандла, його не правлю; переписані пороги живуть у скрипті з посиланням сюди.

### Р34 · Волосини бандла не проходять власний поріг «роздільник ≥ 1.5:1»

| | |
|---|---|
| **Канон, правило** | `audit-thresholds.md`: «роздільники з контрастом < 1.5:1 — 0», з поясненням «line на card = 1.6:1 у світлій» |
| **Канон, значення** | `line` = `#f0eee9` на `card` `#ffffff` — **1.16**; `line2` `#e6e4de` — 1.27; темна: 1.17 / 1.35 |
| **Факт** | 11–51 «невидимих» роздільників на кожному екрані в обох темах — це всі волосини бандла, намальовані як задумано |

Того самого роду, що Р26: правило й хекс суперечать один одному в одному
бандлі, і «1.6» — арифметична помилка документа. **Не моє рішення.** Варіанти
для дизайн-чату: (а) прийняти волосини як є і зняти поріг (лінія — не текст,
WCAG для неконтентних меж просить 3:1 лише там, де межа несе зміст);
(б) підняти `line`/`line2` до ≥ 1.5; (в) поріг лише для роздільників, які
єдині відділяють рядки без відступу. Рекомендація як матеріал — (а): у
рядках комори й календаря межу тримає ритм 70 px, не волосина.


### Р27 · План хоче таймер undo — контракт строку не має

Знайдено 11.09 на етапі 3, перед тим як малювати. Рішення власника: **таймер НЕ
малювати**, правиться план, не сервер.

| | |
|---|---|
| **PLAN §4** | «`intake_diff` з чату → чорнильний слід „застосовано" + „Скасувати" (**вікно undo, таймер видимий**)» |
| **Код** | `packages/domain/apply.ts:867-869` — `undoCard` перевіряє `applied_at`, збіг `undo_token` і `undone_at`. **Жодного часу.** Скасувати можна, доки картку не скасовано |
| **Суть** | Таймер на екрані обіцяв би обмеження, якого сервер не накладає. Це той самий рід обіцянки без даних, що Р4 і 429 без `kind` — тільки навпаки: не «є на екрані, нема в даних», а «немає в даних, але план хоче намалювати» |

**Рішення (11.09):** «Скасувати» лишається доступним, доки сервер дозволяє —
тобто доки картку не скасовано. Без смужки, без секунд. Відхилення **плану від
коду**, і правиться план.

Окреме питання — чи undo без строку є правильним контрактом. Це домен, не v3:
`DEBT.md` §35.

### Р28 · Screens D3a хоче на 1440 тижні картками по 7 колонок — сітка лишається стрічкою

Знайдено 11.09 на етапі 4 (календар), перед тим як робити. **Не робиться.**

| | |
|---|---|
| **Screens D3a** | «Календар · 1440 · тижні картками, 7 колонок · тривале лежить смугами над днями, з підписом лише на початку» |
| **PLAN §7 (той самий бандл)** | «Сітка 6c лишається; додати: підписка… `approx` — пунктирна смуга; `strict` — суцільна» |
| **Код** | `Calendar.tsx`, шапка: рішення власника 03.09 (канвас «Календар — інтерфейс») — «дати зверху вниз на всіх ширинах, без сітки 7×N»; тривалі — рисками в жолобі |
| **Суть** | Бандл суперечить сам собі: екран малює сітку, план каже сітку не чіпати. План — останній документ бандла і його власний підсумок; і збігається з рішенням власника в коді |

**Рішення (11.09, моя зона — план бандла над екраном бандла):** стрічка днів на
всіх ширинах лишається. З D3a взято те, що не залежить від сітки: чіпи легенди
зі знаком роду (пігулка тоном роду, 12/500, звичайний регістр), підписи краю
тривалої тим самим знаком (D3b), порожній день без «Рідкісний спокій». Якщо
дизайн-чат наполягає на 7 колонках — це переграє рішення 03.09, тобто питання
власнику, не мені.

### Р29 · PLAN §7 ставить `approx` і `strict` як протилежності — у коді це дві осі

| | |
|---|---|
| **PLAN §7** | «`approx` дати — пунктирна смуга; `strict` — суцільна» |
| **Код** | `EventOccurrence.approx` (дати рахуються — місячний календар: Рамадан, обидва Іди) і `force: 'restrict' \| 'hint'` — незалежні. Рамадан у довіднику `approx` **і** без `restricts`; Страсна пʼятниця — `strict` **і** з точною датою |
| **Components (легенда картки підписок)** | «Суворо — заливка крапки; мʼяко — контур. „≈" перед строком — дати рахуються» — тут осі дві |

**Рішення (11.09, реалізація):** два канали, як у Components. `approx` → риска
пунктиром (`.rail-approx`, штрих 6 / пробіл 4) і «≈» перед датою; `restrict` →
крапка краю залита, `hint` → контур (`.dot-soft`). Тон — родом, як був.
Перевірено живим: Рамадан 2027 — пунктирна слива, Великий піст — суцільна.

### Р30 · Картка підписок пише «≈ дати рахуються» без дат — показуємо дати з «≈»

Бандл має **дві** картки серії: `A1` (галочки, дати в рядках, «4 з 5 · Ні ·
Записати в календар») і пізнішу «Календар · що впливає на кухню» (пʼять традицій
чіпами, перемикачі в рядках, сезони, «додати» свою подію). У другій рядок
Великого посту підписано «≈ дати рахуються · суворо» — без дат, хоча в A1 той
самий рядок має «18 лют – 4 квіт».

**Рішення (11.09, реалізація):** A1 лишається формою картки з чату
(`PeriodSeries` — там батч і apply, контракт), друга стає входом із календаря
(`PeriodSubscriptions`). У ній дати показуються, з «≈» коли рахуються
(«≈ 8 лют – 9 бер · докупити») — так каже легенда тієї ж картки («„≈" перед
строком — дати рахуються»), і так інформативніше за слово без дати. Плюс
рядок, що цього року минув, показується наступним вікном (`nextWindows`):
Рамадан зсувається на 11 днів щороку, торішні дати ввели б в оману.

Про що бандл мовчить і що вирішено реалізацією: жест «увімкнути набір» /
«вимкнути набір» — лінк у заголовку секції для переглядуваної традиції (чіп —
лише вибір, що дивимось; його знак ✓ — стан набору). Рядок традиції, яку ще
не вмикали, — не «заглушено», а просто дати (заглушено — лише знятий рядок у
живому наборі). Внизу календаря — ті самі два рядки, що в Screens D3, але
«свята» з одним лінком «змінити» / «підключити»: набір тепер перемикається
всередині картки.

**Перевірено живим лише на засіві:** у проді одна власна подія і жодної
підписки на традицію; «увімкнути набір» → PUT усіма рядками → чіп ✓ → риски й
чіпи легенди — усе це пройдено на `api-demo` (in-memory) із каталогом сезонів
і довідником традицій. Дві помилки, яких тести не бачили, знайшлись саме там:
рядки непідключеної традиції читались «заглушено», Рамадан показував торішні
дати.

### Р31 · Рецепти: «Знайти рецепт» у бандлі — напис без поведінки; ~~не робиться~~ зроблено 11.09

**11.09, переведення на кадри Screens «Рецепти · 1440 / 390» (зауваження
власника з живого):** «Знайти рецепт» зроблено як пошук по назві в уже
принесеному списку — даних за полем рівно стільки, скільки на екрані, тож
обіцянки без даних нема. Поле 260×40 у шапці з 768; на 390 — круглий знак,
що відкриває рядок пошуку над чипами (кадр 390 показує лише знак). Решта за
кадром: картки card+тінь r14 у сітці 2× з gap 12 (390 — колона gap 10),
шапка на картках (сегмент 36, «Записати свій» 40), чипи без рамки, ✕ 30 r8
у куті картки, стан — пігулка 26 у рядку назви на 1440 і слово в рядку часу
на 390. Кадр 390 не має ні «Журналу», ні ✕ — обидва лишено (екран журналу
існує, прибрати рецепт треба вміти; QA9-08). Лінія під шапкою і «☰» на
390 — оболонка 6a, не цей екран. Пари: `6b-recipes-1440(-dark)`,
`6b-recipes-390(-dark)`.

Первісний запис:

Screens D5 (1440 і 390) малює в шапці бібліотеки поле «Знайти рецепт» зі
знаком пошуку. У Prototype це `<span>` без `onClick` — жодного флоу за ним; у
коді пошуку по рецептах немає. Поле без пошуку — обіцянка без даних (той самий
рід, що Р4 і Р27). **Не робиться**, доки не зʼявиться або контракт пошуку, або
рішення, що це вхід у чат. Решта D5 зроблена: сегмент «Збережені · N / Журнал»,
«Записати свій» (`import`), фільтри з лічильниками, картка зі словом стану в
роді, «бракує» / «використає» рядками зі знаками, на 390 — круглі знаки в шапці.

Що вирішено реалізацією, бо бандл мовчить: на 390 «Журнал» — знаком
`rotate-ccw` («готував, знову»), бо сегмент на 390 у бандлі відсутній, а
журнал має лишатись досяжним. Калорії на картці — «≈ 540 ккал» без слова
джерела, як у D5: «≈» тут і є позначкою оцінки (PLAN §8 `est → ≈`), повний
підпис «оцінка моделі» лишається в чаті й на сторінці (Р12).

**PLAN §8 «лічильник у шапці рахує те, що під ним»** — D5 (пізніший за
план) кладе число в кожну пігулку фільтра, а «Збережені · N» = усі. Активна
пігулка і є «те, що під нею». Зроблено за D5.

**PLAN §8 «ліміт генерації — стан відмови окремий»** — уже є з етапу 3:
рядок стану над композитором з `kind: recipe_gen` («Десять рецептів за
хвилину — моя стеля. Ті, що вже є, на місці»). Генерація викликається лише з
чату, тож на екрані бібліотеки цього стану бути не може. Живим не
перевірено — 11 генерацій за хвилину означають прогони моделі.

### Р32 · Шторка і права панель — одна колода (зауваження власника 11.09)

Власник побачив на календарі: підписки в шторці (< 1200) і в правій панелі
(≥ 1200) виглядають різними компонентами. Бандл каже — це **одна картка з тим
самим рядком шапки**: Screens D3a `aside` (card, радіус 16, кікер + ✕) і D3c
шторка (card, радіус 20 зверху, ручка 36×4, кікер + ✕). У коді панель уже мала
свій рядок шапки (`.rail-tabs`: знак панелі ліворуч, вкладка-знак артефакта
праворуч) і шторка артефакта чату (`.rail-open`) — ту саму геометрію; а
загальна `Sheet` (календар, комора, профіль) — ні того, ні того: 720 завширшки,
без ручки, без шапки.

**Зроблено:** `Sheet` бере геометрію `.rail-open` (560, радіус 16, відступи
16, лінія зверху, та сама тінь), ручку з D3c і той самий рядок шапки з
`ArtifactPanel.module.css` (`rail-tabs`, `rail-tab rail-tab-on`); проп `kind`
каже, який знак у вкладці (`ARTIFACT_ICON`). Календар — `event`, комора —
`batch`, видалення акаунта — без знака. Свій ✕ у шторці комори знято —
«закрити» тепер у шапці.

Чого бандл не має і чого не зроблено: у бандлі рядок шапки — кікер текстом
(«подія дому · своє») + ✕; у коді — знак панелі + вкладка-знак. Код старший
(етап 3, панель), тому шторка йде за кодом панелі, не за кікером бандла.

### Р35 · Список за екраном «Список» із Prototype (зауваження власника 11.09)

Screens/Components/Responsive списку не малюють; єдине джерело — вкладка
«Список» у Prototype (той самий кадр «Прототип · чат → рецепт → Cook Mode →
списання», nav 5). Зроблено за ним: «N купити · M вже є» 14 muted біля
заголовка (AppHeader `fill`), дії — 40 r10 на card+тінь праворуч; картка
до 720, r14, 4 20; рядок 56 з волосиною `--line`, весь рядок — тогл (UX-9);
чекбокс 22 r6 1.5 line2, куплене — шавлія + check, рядок opacity .5 і
закреслення; назва 15/500, під нею 12 dim «кількість · причина» або «вже є
вдома · кількість»; «+ Додати…» — рядок 48 dim 14, він же поле (UX9-12), з
кнопкою «Додати», що зʼявляється з текстом. Пари: `6b-list-1440(-dark)`
(`--dc-click` у side-by-side — клік по вкладці бандла).

Чого бандл не має — і що лишено з коду: ✕ 30 r8 у рядку (прибрати зі
списку — функція є); «Додати додому · N» (перенести куплене в комору) і
непідключене Сільпо («Підключити / Увійти в Сільпо») — тим самим родом
кнопки 40, що й «Зібрати кошик». Ціни в рядку («89 ₴») — у коді даних
нема, колонка не показується (бандл із «—» для позиції без ціни). Мобайл
(<768) бандл не дає: «N купити · M вже є» ховається, підписи кнопок
скорочуються («Кошик», «Додому · N», «Сільпо»).

Кегль знаків: кадри ставлять 13/15/17/19, а словник `IconSize` тримає
закритий союз 12/16/18/20/24 (`Icon.tsx`) — це запобіжник, не помилка;
взято найближче з союзу (13→12, 15→16, 17→16, 19→20). Питання про союз —
QUESTIONS §11.

### Р36 · 6b-3: рецепт у панелі за артефактом «Рецепт» із Prototype

Зроблено за Prototype (aside `showRecipe`), числа — з DELTAS і завдання
власника: aside r16 на `--sh2`, полотно 12 з усіх боків (Prototype margin
12 12 12 0), ширина 400 на 1440 (Prototype `asideW:400`; Responsive R0 давав
340 — кадр 1440 старший) → 420 на 1920; кікер — знак 16 · «Рецепт» 15/600 ·
закриття 30 r8 `panel-right-close` (новий знак `sys.panelClose`); вкладок
немає. Вміст: пілюля 24, h2 22/700 (роль h2 — 600; 700 тут — рішення
власника 11.09), рядок 13 muted «час · ≈ ккал · оцінка моделі · порції» без
степера (порційник знято: кількості на `sv` рецепта), «Склад · N» / «Кроки ·
N» 12/500, рядки 40 з крапкою 7 і волосиною зверху, кола кроків 24 контурні
1.5 line2 з 11/600 muted, «Готуємо» 48 r12 чорнилом з cooking-pot на всю
ширину низу; «У список · N» текстом 13/500 над нею, лише коли є що
докупити. «У рецепти» / «Поділитись» — на картці в стрічці (`RecipeStreamCard`,
знаки `bookmark` / `share-2`), не в панелі. Пари: `6b3-recipe-panel-1440(-dark)`
(side-by-side: `--dc-click` + `--dc-wait` — прототип відповідає з затримкою).

Крапка роду в складі: шавлія — є вдома, **бурштин — бракує** (раніше line2;
у Prototype бурштин стоїть на «фует · 2 дні», тобто «увага», а не «нема» —
у коді уваги на інгредієнт немає, є лише «бракує», і воно теж «увага»).

Нотатка «не пересушити»: у Prototype в панелі рецепта її **немає** — вона
живе (а) підказкою до кроку в Cook Mode (`tip: 'Фует — уже в тарілку…'`) і
(б) полем «Нотатка на наступний раз — «фует не пересушувати»» на екрані
після готування. У коді `r.rk` (примітка моделі) лишено рядком 13 muted під
метою, поки Cook Mode не перебудовано — тоді піде в кроки.

**✱ Не з бандла — пропозиція реалізації, прийнята власником 11.09.** Комора
при панелі + розгорнутому сайдбарі (зауваження власника 11.09):
кількість наїжджала на ✕, бо дві колонки зон рахувались від вікна (≥768),
а не від ширини екрана комори (~700 на 1440 з сайдбаром 256 і панеллю
~480). Кадри цього стану не малюють (рейка 60, без панелі) — рішення
реалізацією: `container-type` на екрані, дві колонки лише від 800 ширини
самого екрана; `.row-main` не малює поверх ✕ (overflow hidden — тісно
стискається назва).

### Р37 · 6b-4: картка пропозицій за Prototype propDesk; «8 з 8 удома» у пропозиції нема звідки взяти

Зроблено за Prototype: одна картка r16 на тіні sh-a+sh-b, 0 18, пропозиції
рядками через волосину; розгорнута — знак 44 (line2), назва 18/600 −.015em,
рядок 13 muted, чіпи 26 (бурштин зі знаком «горить» — те, що рятуємо;
решта на bg), дії колом 36 стовпчиком: cooking-pot **шавлієвою заливкою**
(так на кадрі; це дія «є все — можна», не стан контролу) і reply контуром;
згорнута — знак muted, назва 15/600, «бракує: …» 12 бурштином, «+» 36.
Одна розгорнута за раз (кадр: перша розгорнута, друга з «+»). Картка
рецепта в стрічці (`RecipeStreamCard`) — тим самим родом: 44/36, розгорнута
cooking-pot + reply, згорнута «+»; мінус знято, згортає тап по назві;
bookmark/share-2 знято (QUESTIONS §13). Пари: `6b4-proposals-1440(-dark)`.

Чого на кадрі є, а в даних немає: «20 хв · 8 з 8 удома · ≈ 540 ккал» у
рядку пропозиції. Картка `proposal` від моделі несе title · desc · why ·
character · rescues · needs — ні часу, ні ккал, ні «N з M» (ці числа
приходять лише зі згенерованим рецептом). Рядок показує `character`;
«бракує: …» — з `needs`. Розділено на два (власник, 11.09):

- **«N з M удома» — не виводиться без моделі.** Пропозиція знає лише
  `needs` (чого бракує) і `rescues` (що рятує) — обидва часткові; повного
  складу M у ній нема, і звірка з коморою дає лише «бракує k», а не «N з M».
  Це теж контракт (`card-schemas.md`).
- **Час і ккал** — тільки від моделі: два поля в схемі `proposal`, правка
  `packages/prompts`, рішення власника. Кадр ставить їх на кожну пропозицію —
  бандл вважає їх частиною рішення «що готувати», не деталлю.

До рішення — `character` і «бракує», як зараз.

Пара знята з `--stub-messages`: стаб `api-demo` пропозицій не віддає, а
писати повідомлення в базу заради знімка не можна — `PG_URL` локального
стенда дивиться на Neon із 16 користувачами (це не засів; DEBT §37 про
запобіжник стосується хоста застосунку, не бази). Тому side-by-side
дописує повідомлення в **мережеву відповідь** `/v1/session/today` лише в
цьому знімку; база не чіпається.

### Р38 · 6b-5: шапка чату за Screens/Responsive, «Дім зараз» за Components; згортання — за шириною контейнера

Перша версія (`2a7e6e8`) робилась за Prototype — помилка в команді, Prototype
для шапки застарів (HANDOFF §0: екрани — Screens). Переписано: канон 1440 —
Screens «Чат · збірка»; 768 і 390 — Responsive R2/G1/G3; панель «Дім зараз» —
Components «home now». Композитор «+»/⌘K і репліка бота без службового рядка
з `2a7e6e8` лишаються.

**Числа — кадр · код** (1440, якщо не сказано інакше):

| елемент | кадр | код |
|---|---|---|
| шапка | 56 · 0 24 · gap 8 · без фону; поверх стрічки, градієнт bg→прозорий ≈ 40 (G3) | 56 · 0 24 40 · gap 8 · absolute, `linear-gradient(bg 0 56px, transparent)`, pointer-events лише на дітях; стрічка під нею з padding-top 64 |
| пілюля сесії | 32 · 0 12 0 8 · card + --sh · 13/500 · chevron-down 16 dim · «· сьогодні» dim 400; розкрита — inset 1.5 ink, chevron-up | те саме; `data-session-pill`, aria-expanded |
| меню розмов (G1) | 340 · card r14 --sh2 · 6; «Нова розмова ⌘N» 40 sage; групи 11 dim; рядки 44 r10, назва 14 + стан 12; активна на bg + check; «Усі розмови» 40 muted | те саме; 5 останніх; стан — «чекає рішення» бурштином (картки дому з session_id) або час dim; «Усі розмови» → сайдбар (≥1024) / шухляда; закриває клік поза, Esc, вибір |
| «+ Нова» | 32 · 0 10 · muted · plus 15 · 13 | 32 · 0 10 · muted · plus 16 · 13 (кегль союзу) |
| чіпи стану | 32 · 0 10 · r999 · 12/500 · знак 15 · gap 8; danger flame «Прострочено 10», plum moon «Піст · до 27 вер», sage timer «Готуємо · таймер» | те саме, знак 16; по одному на рід, лише коли стан є: `overdue` = days < 0 з каталожним ключем; strict-період — перший зі strict; готування — kos-cook-live |
| «Дім зараз · ще N» | 32 · card + --sh · house 15 · «· ще 2» dim 400 | те саме; N = рядки панелі без свого чіпа (тимчасово, §14): «горить», але не прострочено + «Зараз» не суворі |
| 768 (R2, контейнер 704–963) | 56 · 0 16; пілюля 36/14 без «· сьогодні» · розпірка · компактні чіпи 36/13: flame «10» danger, moon «піст» plum; «Нова» і «Дім зараз» нема | те саме; готування — за патерном R2 (знак + найкоротший факт): timer «6:32» sage ✱ виведено; панель «Дім зараз» відкриває тап по будь-якому компактному чіпу ✱ тимчасово (§14); панель 524 на 1440 дає контейнер 832 — теж ця форма |
| 390 (G3) | 14 16 8 + 40 фейду; panel-left-open 40 card; пілюля 36/14 (max 120, chevron після); «Дім ●●● 3»: 36, 0 4 0 12, крапки 8 gap 3, число 28 ink | те саме; крапки — роди активних станів danger/plum/sage, число — їх кількість; чіп → шторка |
| композитор (Screens/Prototype) | «+» 42 muted · поле · «⌘K» dim 12 · mic 42 muted · send 42 ink arrow-up; плейсхолдер «Що зʼявилось удома або що готуємо?», на 390 «Що зʼявилось удома?» | те саме; mic простий muted завжди (кольори стану лише поки слухає; «Стоп» під час думання — на його місці), send завжди: порожній драфт — світле коло на bg, muted стрілка, disabled; автофокус лише для вказівника (≥1024 або hover: hover) — на дотику фокус тільки від людини; порожнє поле — рівно 42, плейсхолдер не ростить композитор; CookCountdown без власного « · » |
| панель (Components) | card r18 --sh · 20 · gap 18; «Горить · N» 12/500 amber; рядки 44 hairline: назва 14/500 · крапка 6 + строк 13 (danger «−9 дн» / «≈ сьогодні», amber «2 дн»); хвіст 12 dim; «Зараз» 12/500 muted; рядки 48: крапка 8 (кільце 1.5 = орієнтовно), назва 14/500, підрядок 12 dim зі знаком 11, строк кольором роду | те саме; накладка 360 під чіпом (top 60, right 24); обидві секції є завжди: «Горить» порожня — «Нічого не горить. N позицій у порядку.», комора порожня — «Комора порожня — розкажи, що є вдома.» замість секції, «Зараз» порожня — «Зараз нічого не триває. Свята можна підключити в календарі.» (три слова з Components, по секціях); дії лише де є що робити: «Готуємо» (ink 34 → композитор «Приготуй щось із того, що горить»), «До плити» (sage 34 → Cook Mode); решта — шеврон; хвости «Ще N прострочених — у коморі, за свіжістю» (→ /pantry за свіжістю) і «Ще N — у календарі» (усього − показано; у dev /v1/now дає 6, показано 3 — «Ще 3» вірно); низ — «Що на вечерю?» у композитор |
| шторка 390 (G3) | ручка 36×4; «Дім зараз» 17/600 + дата 13 dim + ✕ 32; рядки 14 0 з волосиною: квадрат 40 r10 роду зі знаком 18 (flame/danger-bg · moon/plum-bg · timer/sage-bg; тихі — сірий квадрат, users / list-checks), назва 15/600, деталь 13 muted, дія («Готуємо» ink 34 / «До плити» sage 34) лише де є що робити, решта — шеврон | те саме на вузькому контейнері (<704); рядки: «Прострочено N» (або «Горить N» бурштином, або «Нічого не горить…» сірим; комора порожня — своє слово), суворий період, готування, тихі — до 3 подій «Зараз» (sun / users / book-marked) і «Список · N» (перші дві назви) |

**Згортання за шириною контейнера, не вʼюпорту — рішення реалізації (✱ не з
бандла, бандл міряє вʼюпорт).** Панель артефакта на 1440 лишає стрічці менше,
ніж вʼюпорт: `.screen` стрічки — `container-name: chat`, і три розкладки
перемикаються за його шириною. Пороги — ті самі, що у вʼюпортів, **мінус
рейка**: 1024 − 60 = 964, 768 − 64 = 704 (інакше вʼюпорт 768 із рейкою 64
давав контейнер 704 і правило 390). Той самий поріг 704 обирає накладку чи
шторку для «Дім зараз». Панель 720 на 1440 не досяжна: стеля панелі
`vw − 916` = 524 (V1: стрічка ≥ 640), тож пара `6b5-head-panel720-1440` знята
з панеллю 524 — контейнер 832, правило 768 без «Нова». 720 можлива від 1636.

**Сайдбар:** «ЗАРАЗ» і картка «Готування триває» зняті — один факт двічі не
показуємо; сайдбар = Kitchen OS · цілі · «Розмови · + Нова» · сесії по днях
(Сьогодні / Вчора / Пн · 7 вер; рядок = назва 14 + час 12 dim, G1) · профіль.
Решта сайдбара — 6b-6. Банер «Готуємо · крок» у стрічці на мобайлі теж
знято — його факт несе чіп і «Дім ●●● N».

**Словник:** `flame` лишається родом «Горить» (RESERVED); «Прострочено N» —
слово лічильника на чіпі, не другий сенс знака. «Прострочено» (чіп) проти
«Термін вийшов» (рядок комори) — COPY-DEBT блок 8. Нові знаки: `live.fast`
(moon), `sys.opened` (chevron-up).

**Дані:** стан дому — `store/homeNow.ts` (комора: партії з днями; GET /v1/now;
кеш 60 с, версія комори скидає); «чекає рішення» у меню розмов — з карток
дому, що чекають (session_id); «приготовано» / «кошик» другим рядком — даних
без окремих запитів немає (§14).

Правка команди (6b-5c): перша команда на 768 спиралась на блок R0 · 1920,
прочитаний як 768; Responsive не застарілий, у Screens 768-го чату нема —
768 за R2.

Пари: `6b5-head-1440(-dark)` (Screens «Чат · збірка», без готування),
`6b5-head-cooking-1440(-dark)` (з готуванням: kos-cook-live через
`--init-storage`), `6b5-head-768(-dark)` (R2, компактні чіпи), `6b5-head-390(-dark)`
(G3, чіп; бар і композитор у спокої), `6b5-home-390(-dark)` (G3, шторка
рядками), `6b5-home-1440(-dark)` (Components «home now», «Горить» порожнє),
`6b5-sessions-1440(-dark)` (G1, меню), `6b5-head-panel720-1440(-dark)` (панель
524 → форма R2), `6b5-composer-1440(-dark)` (Screens), `6b5-composer-390(-dark)` (G3).
У домі dev нічого не прострочено і немає суворого періоду — чіпи «Прострочено»
і «Піст» на парах не показані (стан відсутній = чіпа немає).

### Р39 · Шапка комори за Screens — окремим комітом із wip/6b-2

За рішенням власника 11.09 з припаркованого 6b-2 узято лише шапку (Screens
«Комора · збірка» / «мобайл»): h1 · лічильник 13 dim · розпірка · пошук
260×36 на card · «Фільтр» 38 r10 на card (ховає рейки порядок/тільки/стан;
брудний фільтр — крапка) · «Додати» 36 r10 чорнилом; на 390 — знаки 40
колом (пошук розкриває поле над рейками), «Додати» коло 42. Кадр 390 не має
кнопки «Фільтр» у шапці (там «Усе · sliders» у ряду чипів) — лишено в шапці,
бо чипів зон ще нема. Розкладка зон двома колонками, банер «розрахунки
скінчились», ритм календаря — лишаються на `wip/6b-2-pantry-calendar` за
чергою. З «Дім зараз» (хвіст «у коморі, за свіжістю») комора відкривається
одразу «за свіжістю» з розкритими рейками. Пари: `6b-pantry-head-1440(-dark)`,
`6b-pantry-head-390(-dark)`.

### Р40 · 1.5b: моушн знаків за Icons.dc.html — 24 системні рухи, 4 живі стани, натиск, reduced-motion

Гілка `feat/icon-motion` від `main` (d1a8839), окремий PR після #49. Чіпає
`components/Icon/` (Icon.tsx, Icon.module.css, icons.ts + новий `motion.ts`)
і чотири місця вжитку живих станів (чіпи шапки чату, `.mic-live`, «Думаю»).

**Правило:** рухається не знак, а його частина, 1–2 px, 600–900 мс, одна
крива `ease-in-out`, без відскоку. Бандл пише кожному руху своє число
(600…1200 мс) і дві криві — у коді три сходинки **токенами** в межах
канону (`--dur-icon-short` 700 · `--dur-icon` 800 · `--dur-icon-long` 900) і
одна крива (`--ease-icon`); живі стани — цикл із підписів бандла
(`--dur-live-*`). Рухів не вигадано: є ключ у масиві `system` — робиться він,
нема — статично (`null` у `motion.ts`). Продукти статичні; зони й cooking —
QUESTIONS §15. Натиск — scale .96 на всіх знаках-кнопках, `--dur-fast`
(150 проти 160 у бандлі — токен, не власне число). Reduced motion — токени
0 і страхувальне правило в `Icon.module.css`.

**Ховер** спрацьовує і на самому знаку, і на кнопці/посиланні, де він лежить
(у бандлі — тайл). Порядок частин — DOM lucide 0.460, як у бандлі; book і
fridge — власні шляхи (`CUSTOM_PATHS`, Icons.dc.html:218-229).

| # | знак | рух у бандлі | рух у коді | тривалість бандл → код | крива бандл → код |
|---|---|---|---|---|---|
| 1 | message-circle · Чат | bubble — бабл ледь дихає | path scale 1.06 / −.5px, origin bottom left | 1100 → 900 | ease-in-out |
| 2 | refrigerator · Комора | door — нижні дверцята прочиняються | власні шляхи 3–4: scaleX .6 skewY −6°, origin 5px 16px (view-box) | 1000 → 900 | ease-in-out |
| 3 | book-open · Рецепти | book — сторінка через корінець | власний 4-й шлях (листок): scaleX 1→0→−1, opacity | 1100 → 900 | ease-in-out |
| 4 | list-checks · Список | checks — дописує галочки | path 1–2 drawShort (dasharray 12), 1-й із затримкою 180 | 700 → 700 | ease-in-out |
| 5 | calendar · Календар | flip — гортає сторінку | rect + path 4 scaleY .55 (origin top); path 1–2 pins | 840 → 800 | cubic(.4,0,.2,1) → ease-in-out |
| 6 | house · Дім зараз | home — відкриває двері | path 1 door, origin left | 900 → 900 | cubic → ease-in-out |
| 7 | shopping-cart · Кошик | roll — колеса крутяться | circle spin 200°; path nudge 1.2px | 900 → 900 | cubic → ease-in-out |
| 8 | receipt · Чек | unroll — розгортається | path 1 scaleY .85→1 (origin top); path 2+ fadein | 840 → 800 | cubic → ease-in-out |
| 9 | plus · Додати | turn — поворот 90° | svg rotate 90° (transition) | 700 → 700 | cubic(.45,0,.2,1) → ease-in-out |
| 10 | mic · Голос | listen — капсула набирає, дуга домальовується | path 1 scaleY 1.08; path 2 fadeSoft | 1200/900 → 900 | ease-in-out |
| 11 | arrow-up · Надіслати | lift — зсув угору | path/line −2px | 720 → 700 | cubic → ease-in-out |
| 12 | paperclip · Вкласти | draw — домальовується | path draw (dasharray 64) | 1100 → 900 | ease-in-out |
| 13 | search · Пошук | orbit — лупа робить коло | circle orbit ±1.5/−2px | 1050 → 900 | cubic → ease-in-out |
| 14 | sliders-horizontal · Фільтр | sliders — бігунки в різні боки | line 7, 9 +3px; line 8 −3px | 1100 → 900 | ease-in-out |
| 15 | arrow-up-down · Порядок | swap — стрілки міняються місцями | path 1–2 down; path 3–4 up (±2px) | 780 → 800 | cubic → ease-in-out |
| 16 | check · Готово | draw — галочка ставиться | path draw | 1100 → 900 | ease-in-out |
| 17 | x · Закрити | close — хрестик стискається | path scale .82 | 800 → 800 | ease-in-out |
| 18 | undo-2 · Скасувати | back — стрілка їде назад | path 1 −2px | 780 → 800 | cubic → ease-in-out |
| 19 | chevron-right · Далі | draw — шеврон домальовується | path draw | 1100 → 900 | ease-in-out |
| 20 | external-link · У Сільпо | lift — стрілка вилітає з рамки | path/line −2px | 720 → 700 | cubic → ease-in-out |
| 21 | panel-left-close · Згорнути | fold — роздільник зʼїжджає | path 2 −2px | 750 → 800 | cubic → ease-in-out |
| 22 | sun-moon · Тема | dial — прокручується | path rotate 180° | 900 → 900 | cubic → ease-in-out |
| 23 | volume-2 · Звук | waves — хвилі по черзі | path 2 fadein; path 3 fadein +240 | 600 → 700 | ease |
| 24 | user · Профіль | nod — голова киває | circle +1.5px | 780 → 800 | cubic → ease-in-out |
| L1 | flame · «Горить» | дихає, поки є позиції ≤ 3 дні | path scale 1.04/1.09, origin bottom; на чіпі «Прострочено N» | 1.6 с (бандл CSS 2.4, підпис 1.6) → 1.6 с | ease-in-out |
| L2 | timer · таймер | тікає, поки таймер біжить | line 2 (стрілка 12,14→15,11) rotate 24°, вісь 12px 14px; селектор бандла `:nth-child(3)` у lucide 0.460 нічого не ловить | 1.2 с (CSS 2.4, підпис 1.2) → 1.2 с | ease-in-out |
| L3 | mic · «Слухаю» | пульс під час диктовки | path 1 scaleY 1.18; `.mic-live` тепер малює знак (sage на sage-bg), кільце micpulse на контейнері знято | 1.2 с (CSS 1.8, підпис 1.2) → 1.2 с | ease-in-out |
| L4 | sparkles · «Думаю» | погойдування, поки чекаємо | path scale .75 / opacity .55, затримки 300/600; на рядку стану дії | 1.4 с (CSS 2.2, підпис 1.4) → 1.4 с | ease-in-out |

Приймання: `icons.test.ts` — кожен system-знак має запис у `motion.ts`
(24 з бандла — свій ключ, решта `null`), кожен ключ — правило і `@keyframes`,
жоден products-знак не рухається, тривалості лише токенами в межах
600–900 / 1.2–1.6 с, reduced-motion вимикає все. Запис екрана 10–15 с в обох
темах — `icon-motion-light.webm`, `icon-motion-dark.webm` (ховер по рейці
зверху вниз, натиск, flame у шапці при прострочених — мережевий стаб
`/v1/pantry`, timer під час готування, композитор); знімає
`scripts/icon-motion-record.mjs`.

### Р41 · Етап 9: лендінг v3 за Landing Live — три кадри, знаки-заміни, таблиця «кадр · код»

Гілка `feat/landing-v3` (від 534bd9f). `pages/Landing/` переписано цілком за
«Kitchen OS - Landing Live.dc.html»: кадри «Лендінг · live» (≥1280 — кадр 1920,
fluid через `zoom = min(1, w/1920)` як у бандлі, sticky-hero, лептоп із живою
сесією, «Що вміє» sticky-сценою зі скло-фрагментами й нахилом за курсором,
телефон у фіналі), «Лендінг · 1024» (768–1279: без sticky, рядки текст /
фрагмент з чергуванням, фрагменти мобільної щільності, без нахилу й скла),
«Лендінг · 390» (стрічки зі снапом, телефон у hero з тим самим циклом). Копі —
`copy.ts`, дослівно з розмітки й `renderVals()`. Вхід — `SignInForm`: Google
першим (підтверджене відхилення), логіка `useMagicLink` і `/v1/auth/providers`
без змін. `Landing.dc.html` — історія, не джерело.

**Знаки.** Кожен знак кадру звірено зі словником `icons.ts`. Є з тим самим
значенням — узято (refrigerator · receipt · timer · list-checks · calendar ·
heart · ban · sun · chef-hat · cooking-pot · reply · shopping-cart ·
external-link · house · flame · check · rotate-ccw · church · menu ·
panel-right-close · chevron-down/right · plus · mic · arrow-up · star). Нема —
дописано в кінець мапи блоком `landing.*` (package-open «Відкрите», history
«Після вчорашнього», list «Готував нещодавно», shuffle «Різноманіття»,
chevron-left «У панель»); у `motion.ts` вони `null` — статичні, бандл руху для
них не має. Є з **іншим** значенням — не взято, винесено дизайн-чату
(QUESTIONS §16), тимчасово: bookmark (= «Колись») → `cook.love` на кікері
«Памʼять і різноманіття»; alert-triangle (= «Прострочено») → `cook.ban` на чіпі
алергену — tokens-v3 сам ставить «не можна» як plum + ban; minus (= «Нічого не
змінилось») → кількість у кошику числом, як на кадрі 1024. За правилами HANDOFF
без питання: flame на рядку «Шакшука» → chef-hat (flame — тільки «Горить»);
arrow-right «Далі» → `sys.next`, на кнопці пошти 390 → `sys.send`; «★★★★★» →
пʼять `cook.rating` (гліфів 0); «+» в аватарах тарифу → `sys.add`. Розміри
знаків — з союзу 12/16/18/20/24 (QUESTIONS §11): 15 → 16, 14 → 16, 13 → 12,
19 → 20, 17 → 18.

**«Що вміє» — шість рядків, не вісім.** Завдання називало 8 рядків `feats`;
бандл їх оголошує, але жоден із трьох артбордів не рендерить — усі три беруть
`rows` (6, «Памʼять» і «Різноманіття» злиті). Зроблено як на кадрах; питання
задуму — QUESTIONS §16.

**Таблиця «кадр · код»** — числа з рендеру (`getBoundingClientRect`,
`getComputedStyle`), кадр бандла і застосунок у тій самій ширині, світла тема,
reduce. Одне число — збіг; «— · N» — у кадрі елемента нема (1024/390 без
телефона у фіналі, 390 без текстової кнопки «Надіслати» і без nav).

| елемент | 1920 | 1024 | 390 |
|---|---|---|---|
| шапка h | 80 | 68 | 60 |
| nav кегль | 15/500 | 14/500 | — · 15/500 |
| Увійти h | 40 | 38 | 36 |
| h1 кегль | 96/700 | 64/700 | 40/700 |
| lead кегль | 22/400 | 18/400 | 16/400 |
| lead max-w | 820 | 640 | 350 |
| Google w×h | 520×56 | 480×54 | 350×52 |
| пілюля h | 56 | 54 | 52 |
| Надіслати h | 44 | 42 | — · 42 |
| примітка кегль | 13/400 | 13/400 | 12/400 |
| лептоп w×h | 1600×852 | 928×584 | — |
| екран h | 820 | 560 | — |
| рейка w | 60 | 56 | — |
| кошик w | 380 | 280 | — |
| рядок кошика h | 56 | 50 | — |
| композитор h | 52 | 48 | — |
| телефон екран h | 457 · **456** | — | 520 |
| болі h2 | 56/700 | 44/700 | 32/700 |
| болі іл. h | 280 | 200 | 200 |
| болі p | 17/400 | 15/400 | 15/400 |
| що вміє kick | 13/500 | 12/500 | 12/500 |
| що вміє h3 | 40/700 | 32/700 | 28/700 |
| що вміє p | 17/400 | 16/400 | 15/400 |
| пауза кегль | 56/700 | 36/700 | 26/700 |
| пауза padding | 160px | 96px 64px | 72px 24px |
| чат h2 | 48/700 | 36/700 | 30/700 |
| know картка padding | 12px 0px 0px | 6px 0px 0px | 4px 0px 0px |
| know кегль | 16/400 | 16/400 | 16/400 |
| облік h2 | 48/700 | 36/700 | 30/700 |
| облік рядок padding | 20px 0px | 18px 0px | 16px 0px |
| облік кегль | 16/400 | 15/400 | 13/400 |
| чіп домислено h | 28 | 26 | 26 |
| дім h2 | 48/700 | 36/700 | 30/700 |
| дім картка padding | 18px 20px | 18px 20px | 16px 18px |
| дім картка r | 16px | 16px | 14px |
| правила h2 | 56/700 | 44/700 | 32/700 |
| правило padding | 32px | 24px | 22px |
| правило r | 24px | 20px | 18px |
| правило назва | 24/600 | 20/600 | 20/600 |
| ціна h2 | 56/700 | 44/700 | 32/700 |
| тариф padding | 32px | 24px | 24px |
| тариф r | 24px | 20px | 20px |
| ціна кегль | 16/400 | 16/400 | 16/400 |
| тариф кнопка h | 48 | 46 | 46 |
| фінал h2 | 72/700 | 52/700 | 36/700 |
| футер кегль | 13/400 | 13/400 | 12/400 |

Інших відмінностей у числах немає. Телефон у фіналі 1920: екран 457 · 456 —
округлення (410 + шапка). Пари: `landing-1920.png`, `landing-1024.png`,
`landing-390.png` (світла, reduce, на всю висоту; кадр рендериться у вʼюпорті
1920×1080, щоб vh-висоти hero й сцен збіглися з застосунком); власні знімки
`landing-1440-light.png`, `landing-1920-dark.png`, `landing-390-dark.png`;
запис `landing-1920-scroll.webm` (29 с: hero → ціна, сценарій у лептопі, блік,
sticky-сцени) — усе в `docs/superpowers/plans/side-by-side/`.

### Р42 · Темна тема лендінгу — бандл малює лише світлу. **Закрито 12.09:** рішення власника (пакет правок 1, №2) — лендінг і auth-екрани завжди світлі, на всіх ширинах; провізорну темну знято (#60)

`.v3` у Landing Live — фіксовані світлі хекси. На токенах темна приходить сама,
але чотири місця в ній нечитабельні або зникають; вирішено провізорно, до
відповіді дизайн-чату (QUESTIONS §16): `dim` як текст → `muted` лише в темній
(`--dim-t`; dim у темній дає 3.7:1 на bg); скло-фрагменти — біла заливка
.34/.14 → .09/.04, відблиск .95 → .18; ілюстрації (чорний контур на прозорому)
→ `invert(1) hue-rotate(180deg)`, без multiply; блік на пристроях → opacity .4
(біле світло .42 на темному читалось смугою). Другий текст на чорнильних
поверхнях (пауза, тариф «Дім») — `--ink-2: color-mix(in srgb, var(--bg) 60%,
var(--ink))`: у світлій це dim бандла з точністю до відтінку (#9d9d9c проти
#9a9ea3), у темній — 4.9:1. Аудит темної: текстів нижче AA — **0** (світла: 7,
з них dim 5 — Р26). Знімки `landing-1920-dark.png`, `landing-390-dark.png`.

### Р43 · Рух: запуск за IntersectionObserver, статичний кінцевий стан під reduce

Бандл запускає kos-анімації з завантаження (`.kos-live` на всій сторінці);
тут — `[data-live]` → `data-play` за IntersectionObserver (лептоп, сцена «Що
вміє», телефон), до того кейфрейми стоять на 0 % (`animation-play-state:
paused`, `fill both`). Тривалості числом (9 с, 1 с) — за винятком гейта
no-hard-motion: кожний селектор `.k*` заглушено в блоці reduce того ж файла
через `animation: none`; `--dur-*` не задіяно, бо цикл сценарію — не тривалість
стану. Під reduce базові стилі = кінцевий стан: усі чотири репліки, чіп
«Готуємо», кошик; індикатор «друкує» і курсор сховані. Бандл під reduce показує
їх (нуль анімації = усе видно разом) — різниця свідома, названа в QUESTIONS §16.
Reveal (`data-reveal` → `data-in`, 700 мс, затримки з атрибута) і прогрес hero
(opacity 1 − p, scale 1 − .06p) — як у бандлі; scroll-linked рух під reduce
лишається, як і в бандлі.

### Р44 · Fluid 1280–1919 через zoom, як у бандлі

Кадр 1920 масштабується `zoom = min(1, innerWidth / 1920)`, а `--vh = innerHeight
/ zoom` — рівно так рахує `renderVals()` (`zoom`, `vh80`, `heroLift`, `vh80f`).
Це означає: на 1440 усе, включно з кеглями, — 0.75 від кадра 1920 (h1 72, не
96). Знімок `landing-1440-light.png`. Альтернатива (fluid через clamp) дала б
інші пропорції, ніж кадр, — не робилось.

### Р45 · `pages/SignIn/` лишається до етапу /sent

Екран `SignIn` мертвий («Вхід = лендінг», маршруту не було) — компонент і
`GoogleMark` прибрано; у файлі лишився `Mark`, бо його беруть `MagicLinkSent` і
`Invite` разом із `RingField` і `SignIn.module.css`. Тека зникне з етапом
Auth.dc.html («Перевір пошту» / «Запрошення в дім»). У `no-glyphs.test.ts`
`Landing` знято з винятків (гліфів у лендінгу нуль), `SignIn` лишається до того
ж етапу (RingField малює «✓» у підказці). `Hero.tsx`, `DotField.tsx` видалено.

### Р46 · Ілюстрації: webp 2× від кадра 1920, старі ill-* знято

`ai/project/assets/landing/{pain-1-t,pain-2-t,pain-3-t,home-family}.png` (1–2.5
МБ) → `apps/web/public/landing/*.webp`: болі — висота 600 (на кадрі 280, 2×),
дім — ширина 1120 (на кадрі 560, 2×); 86 · 47 · 63 · 155 КБ, разом 348 КБ
(стеля 1 МБ, кожна ≤ 250). `<img>` з width/height, `loading="lazy"`,
`decoding="async"`, `mix-blend-mode: multiply` як у бандлі. Вісім `ill-*.png`
(1.3 МБ) видалено — посилань у `apps/` нема (греп). Скрипт конвертації —
одноразовий (Pillow), у репо не лишався.

### Р47 · Інструменти здачі: side-by-side і design-audit для лендінгу

`scripts/side-by-side.mjs`: `--reduce` (reduced-motion на обох сторінках),
`--full` (застосунок на всю висоту), `--dc-viewport WxH` (кадри з vh рахують
висоти від вʼюпорта бандла), `--scale N` (кадр вищий за ~8000 px на 2×
обрізається стелею текстури Chromium — лендінг знімається на 1×), `--stub-json`
(відповідь GET у мережі знімка: `/v1/auth/providers={"google":true}` — інакше
кнопки Google на знімку нема, `/v1/me=401`; база не чіпається, API не
потрібен). `scripts/design-audit.mjs`: `--screens "Назва=/шлях"` — аудит «/»
без входу. База лендінгу (старий v5, той самий стенд) → новий, 1440, світла:
стилів 17 → 14 · гарнітур 3 → 1 · ваг 4 → 3 · трекінг поза ролями 6 → 1 ·
роздільників < 1.5 — 24 → 9 · текстів < AA — 44 → 7 (темна: 0) · гліфів 1 → 0;
макс. кегль 46 → 96 і «часто» 14 → 13 — це бандл (display 96 у hero, мета 13),
не регрес. Пороги застосунку (h1 ≤ 28, стилів ≤ 8) до лендінгу не
застосовуються — це окрема естетика, порівнюється лише з базою. JS лендінгу
тягне лише `Icon`, `api` і `store/auth` (через `useMagicLink`); сторів стрічки й
комори — ні; бандл один чанк 783 КБ (Landing не lazy — як і був).

### Р48 · Кнопка меню на 390 — поведінка не в кадрі

Кадр 390 малює `menu` праворуч від «Увійти» без розкриття. Зроблено список трьох
якорів шапки (картка card, r12, sh2, рядки 44) під кнопкою; закривається після
переходу. QUESTIONS §16.

### Р118 · Скло і градієнт v3.1 у коді — `feat/glass-v3.1` (без мерджу, власник дивиться на стенді)

Канон — `ai/project/glass-gradient-spec.md` §1–§3, §6 і `tokens-v3.md` «Скло
і градієнт». Тільки вигляд: поведінка, дані, контракти не чіпались. Гілка
від `origin/main` 273cdaf, у `.worktrees/glass-v3.1`.

**Токени** (`tokens.css`, в обидва блоки v3 — `--glass-edge:` рівно двічі):
`--glass-bg` (card 72 % світла / 78 % темна), `--glass-edge/-top/-side`,
`--glass-sh`, ручки `--glass-blur: 18px` і `--glass-sat: 1.25` (другий кандидат
для стенда — 46 % / 56 %, blur 30, sat 1.8), `--chat-grad` і `--chat-grad-top`
(верх градієнта — його бере фейд шапки чату замість плоского `--bg`).

**Градієнт** — на рамці: `body[data-screen='chat' | 'public-recipe']`
(`glass.css`, `background-image`, щоб колір полотна лишався для запобіжників
теми). Ознаку ставить Shell за маршрутом `/app`; публічний рецепт — сам.
Колонки `Feed .screen` і `SharedRecipe .screen` — прозорі (§6.3). Решта
екранів — рівний `--bg`: тест `glass.test.ts` стежить, що `chat-grad` не
зʼявляється в модулях комори, рецептів, календаря, профілю, Cook.

**Скло** — `styles/glass.css`, класи глобальні (носять три модулі):
`.glass` (isolation: isolate, зовнішній шар) + `.glass-inner` (тло, blur ×
saturate токенами, `contain: paint`, контур `::after` з `--glass-edge` та
inset-підсвіткою `--glass-top` / `--glass-side`); `.glass-rail` (одношарова,
межа праворуч; лише від 768 — той самий елемент < 768 є шухлядою, вона не в
переліку скла); `.glass-bar` (межа зверху). Подвоєний селектор (0,2,0)
перекриває власне тло/межу оболонок у модулях, геометрія (radius, overflow,
position) лишається за оболонкою; `::after` бере radius через inherit.

| поверхня | оболонка | шари |
|---|---|---|
| панель ≥ 1200 | `ArtifactPanel` aside = `.glass` (ручка ресайзу, полотно 12), `.rail-artifact` = `.glass-inner`, тінь `--glass-sh` (була `--sh2`) | два |
| плавуча картка 380 (600–1199) і шторка < 600 | той самий aside `.rail-open` = `.glass.glass-inner` (шар обирається за `RAIL_IN_FLOW` у TSX — скла всередині скла не буває) | один |
| `Sheet` (календар, комора, профіль) | `.panel` = `.glass.glass-inner` | один |
| рейка 60 / сайдбар 256 (`TabBar .wrap`) | `.glass-rail` від 768 | один |
| нижній бар 390 (`TabBar .bar`) | `.glass-bar` | один |

Щільними лишились: картки контенту, бабли, тости, модалки («Дім зараз» —
`--card`), картка кроку Cook Mode. У кадрі не більше двох скляних поверхонь:
рейка + панель (1440), бар + шторка (390).

**Продуктивність.** `contain: paint` на всіх скляних контейнерах; `will-change`
у дітей скла нема (тест). `@media (prefers-reduced-transparency: reduce)` —
щільний `--card`, `backdrop-filter: none`. Ідл-трейс Chromium
(`scripts/glass-paint-trace.mjs`, devtools.timeline, 3 с спокою): чат 1440 з
чеком у панелі, чат 390 зі шторкою й баром, комора 1440 з живим flame у
банері — **Paint 0 · Layout 0 · UpdateLayerTree 0** на гілці й на main;
рухаються лише композиторні кадри (Graphics.Pipeline ≈ 60 за 3 с). Тобто
живі знаки не ганяють перемальовування крізь скло.

**Пари «було · стало»** (`scripts/before-after.mjs`, main 273cdaf на :5193
проти гілки на :5192, стенд `stand-seed` на :3013; обидві теми):
`docs/superpowers/plans/side-by-side/glass/glass-chat-recipe-1440`,
`glass-chat-receipt-1440`, `glass-chat-float-768`, `glass-chat-bar-390`,
`glass-chat-sheet-390`, `glass-pantry-1440` (рівний фон, скляна рейка),
`glass-public-1440`, `glass-public-390`. Стаб рецепта в панелі —
`stub-recipe-glass.json`.

Чого бандл не має і що лишено з коду: фейд шапки чату бере `--chat-grad-top`
(у бандлі шапка без фону); тінь шторки знизу (`0 -12px 40px`) не міняно на
`--glass-sh` — тінь угору для шторки й далі своя.

## Відкриті ⚠ — до власника, етап 1 ними не блокується

| ⚠ | Питання | Пропозиція PLAN | Де впирається |
|---|---|---|---|
| ~~⚠1~~ | ~~Правило емфази числом~~ | **Рішення: 3 виділених рядки + 1 картка-акцент на екран; «Горить» показує до 3 позицій, решта лічильником.** Власник передав рішення мені (10.09). Підстава: обидва числа — та сама межа, за якою підсвітка себе знищує; при дванадцятьох бурштинових позиціях не горить жодна. Заведено в `design-audit.mjs` | закрито, завдання **1.3** |
| ~~⚠6~~ | ~~Нижній бар на мобайлі~~ | **Закрито 11.09, етап 6a:** бар із пʼяти цілей повернуто (Screens D5 · 390), ховається за `body.composer-focused` і `body.sheet-open` — обидві умови, класами з композитора, `Sheet` і шторки артефакта. Тест на CSS-правило й на пʼять цілей | закрито |

Завдання **1.1** і **1.2** не заблоковані ані цими двома, ані чимось іншим.

## Стан реєстру

| | |
|---|---|
| Знято як помилкові | 5 — ~~Р1~~, ~~Р5~~, ~~Р7~~, ~~Р9~~, ~~Р15~~ |
| Закрито рішенням власника | 5 — ⚠2, ⚠3, ⚠4, ⚠5 (+ Р11 через ⚠5) |
| Правимо самі, рішення не потрібне | 3 — Р17, Р18 і залишок ~~Р7~~ (текст PLAN §6) |
| **Вирішено 10.09** | **15** — Р2, Р3, Р4, Р6, Р8, Р10, Р12, Р13, Р14, Р16, Р19, Р20, Р21, Р22, Р23 |
| Лишились відкритими | **Р26 (палітра — дизайн-чат)**; ⚠6 закрито 11.09 (етап 6a); Р25 вирішено 11.09 (Р33). Р24, ⚠1 закриті 10.09; **Р27 закрито 11.09** — план правиться, не сервер; **Р28–Р30 (11.09, календар)**, **Р31–Р33 (11.09, рецепти, шторка, пороги)** — вирішено реалізацією, Р28 переграється лише рішенням власника; **Р34 (волосини) — дизайн-чат**, разом із Р26 |
| **Етап 9 (лендінг), 11.09** | **Р41–Р48** — зроблено за Landing Live; знаки-заміни (Р41) і темна тема (Р42) — питання дизайн-чату, QUESTIONS §16 |
| Пішло в `DEBT.md` | 1 — §24 (охоплення вето, з Р13) |
| Закрило наявний борг | 1 — §21 через Р12 (два числа калорій) |

**Помилок методу за етап 0: пʼять** (~~Р1~~, ~~Р5~~, ~~Р7~~, ~~Р9~~, ~~Р15~~), і всі одного роду — висновок про бандл або код із неповного грепу. Три причини, які варто знати наперед: у `.dc.html` вміст живе в `renderVals()`, а розмітка звертається до нього шаблонною діркою `{{ }}`; голі числа в HTML — сигнал нульової якості; ознака в UI може існувати під іншим іменем, ніж у брифі.

---

# Зведення потоків (11–12.09) — з файлів `DEVIATIONS-V3-<потік>.md`, зведено 12.09

Потокові файли зняті; історія — у git. Нові відхилення — сюди. Нумерація:
things Р50–Р53, time-people Р60–Р63, cook-share Р70–Р99, landing Р41–Р49 і
Р100–Р101 (у потоці landing вони йшли як Р50–Р51 і зіткнулись із things).

## Потік things · комора · чек · рецепти (PR #55)

Гілка `feat/things-v3`. Номери Р50–Р59. Правило те саме, що в
`DEVIATIONS-V3.md`: код — джерело правди; бандл каже — робиться як у бандлі,
мовчить — питання в `QUESTIONS-things.md`; таблиця «кадр · код» на кожен крок.
Зведення в спільний реєстр робить власник після мерджу.

---

### Р50 · things-v3, крок 1: розкладка комори за Screens «Комора · збірка» / «мобайл»

Гілка `feat/things-v3` від `origin/main` (534bd9f — main уже містить #51,
тобто новіший за d1a8839 з брифу; бралась актуальна). Припаркований
b82d732 (`wip/6b-2-pantry-calendar`) не перебазовано механічно — його шапка
вже в main (8cb1e79, Р39), а решта (чіпи, банер, колонки, рядок 48) узята
руками й звірена з кадрами. `Calendar.module.css` з b82d732 (ритм дня 44) —
потік «календар і профіль», сюди не входить.

**Стенд.** Пари комори досі знімались із дому dev на проді — сім позицій без
категорії, тобто без строку, безпеки й паспорта. Заведено
`scripts/stand-seed.mts`: той самий `buildApp` на `InMemoryRepo`, дім засіяно
за `renderVals()` кадра (стейк −9 «не їм», помідори сьогодні, фует 2 дн,
креветки «не можна», паспорти «бренд · тип», походження чек/рука/розмова/
домислене). Нічого не пише нікуди. Порти 3010/5175 (у робочій теці зайняті
чужим стендом): `npx tsx scripts/stand-seed.mts` + `API_URL=http://localhost:3010
pnpm --filter @kitchen/web dev`; спільний `.claude/launch.json` не чіпався.

| елемент | кадр 1440 («збірка») | кадр 390 («мобайл») | код | різниця |
|---|---|---|---|---|
| лічильник шапки | «113 · 10 прострочено · чек 7 вер» 13 dim | «113» | `${n}` + « · N прострочено» (days < 0 зі шкалою) + « · чек {дата}»; хвіст ховається < 768; звужений зріз — «N з M» як було | — |
| ряд чіпів | «Усе» чорнилом 30/999 · зони на card + тінь, знак 14 muted, назва 13, лічильник dim · праворуч «↕ за місцем» 13 muted | ті самі 34; другий чіп — «Фільтр» колом 34 (sliders); без лічильників у чіпах | так само: 30 / 34 на 390; чіп зони звужує до зони, повторний тап = «Усе»; лічильники з усієї комори (не зрізу); «↕ {порядок}» — кнопка, розкриває рейки; на 390 чіп «Фільтр» замість кнопки в шапці (кнопка шапки ховається) | лічильники в чіпах на 390 лишено (R2 768 їх малює); знак 16 замість 14 (союз `IconSize`, §11) |
| банер | card r14 16/20, знак flame 20 у квадраті 38 r10 amber-bg · «Десять розрахунків скінчились» 15/600 · підрядок 12 muted «Стейк −9, помідори сьогодні, фует за 2 дні — решта нижче за свіжістю» · «Приготувати з цього» ink 36 + cooking-pot · «Перевірити 10» контур 36 muted | компакт: знак 36 · заголовок · підрядок з обрізкою · одна дія-знак 36 ink; «Перевірити» — тап по картці | так само; число цифрою («2 розрахунки скінчились», відмінок `plural`); підрядок — три найтерміновіші «{назва} {слово часу}» через кому + « — решта нижче за свіжістю» при > 3; «Приготувати» → чат із префіксом «Приготуй щось із того, що горить: »; «Перевірити N» → порядок «за свіжістю» + розкриті рейки; на 390 тап по тексту = «Перевірити» | число цифрою, не словом; слово часу з домену («−9 дн», «сьогодні», «≈ ще 2 дн»), не «за 2 дні»; банер не показується під зрізом/пошуком |
| колонки зон | дві колонки з чергуванням: Свіже · Морозилка ліворуч, Холодильник · Суха шафа праворуч, gap 14; картки різної висоти незалежні | зони підряд, gap 12 | колонки з чергуванням у розмітці (`useZoneColumns`): 1 · 2 від 1000 · 3 від 1500 за шириною **екрана комори** (container), R0: стеля 1400 від 1500 | стара сітка `.body` (2 колонки від container 800) знята |
| картка зони | card r14 тінь, padding 0 18 4; хедер 44 ink, margin 0 −18 8, знак 16, назва 15/600, лічильник 13 .7 | padding 0 16 4; хедер margin 0 −16 6, назва 15/600, лічильник 12 .6 | так само; назва 14/600, лічильник 12 .7 — канон tokens-v3 «Хедер зони» (14/600, 13/400) | назва 14 проти 15 у кадрах: канон компонентів старший за кадр екрана |
| рядок | min-height 40, pad 4, gap 10; назва 14/{w} · паспорт 12 dim · чіп безпеки 22 (11) · походження 13 у слоті 16 · час 84: крапка 6 + слово 12 · кількість 56, 12 muted | min-height 48, pad 5; два стовпці: назва 15 + тег безпеки поруч, паспорт 12 під ними; праворуч час 13 над кількістю 12 dim; без походження | Components «ROW ANATOMY» (канон компонента): 48, gap 12, назва 15/500, паспорт 13 dim, безпека 24 (12/500), походження 12 у слоті 16, час 92: крапка 6 + 13, кількість 64 13 muted; 390 — сітка `n s t / p p q` (`display: contents` на назві), походження сховане; 768 (R2) — походження сховане, час 74 | кадр 1440 дрібніший за Components; взято Components. ✕ у рядку лишається (QUESTIONS §9) |
| позначка часу | крапка 6 у слоті часу, колір стану; прострочене — теж крапка danger | так само | крапка перенесена зліва від назви у слот часу; знак тривоги на простроченому (етап 2a) знято — кадр і Components малюють крапку | — |
| емфаза | 600 + колір на трьох (стейк, помідори, фует), решта 500 | так само | ⚠1: три найтерміновіші зі шкалою — `.row-hot`, 600 | — |
| порядок у зоні | за свіжістю (стейк −9 зверху) | так само | Ф2а: новіші за `added_at` зверху, однакова дата — за назвою (щоб рядки не стрибали після правки) | **лишено з коду** — рішення Ф2а; чи бандл мав на увазі порядок за строком усередині «за місцем» — QUESTIONS-things §19 |
| «Ще N ▾ розгорнути зону» | шаблон має, дані — ні; підпис кадра: «показує всі позиції зони одразу — без „ще N“» | — | усі позиції одразу | — |
| підвал «за місцем · 4 зони з 6 у кадрі …» | анотація кадра | — | не UI, не робиться | — |

Пари: `things-pantry-1440(-dark)`, `things-pantry-390(-dark)`. Тести: чіпи
(лічильники з усієї комори, звуження, «Усе»), банер (лише при прострочених зі
шкалою, «Перевірити N» → «за свіжістю»), хвіст шапки, крапка у слоті часу.

### Р51 · things-v3, крок 2: картка позиції за Components «ITEM CARD + WRITE-OFF», Responsive G6, Prototype aside

Контейнер лишається панеллю/шторкою (Р32): картка — лише вміст. Кікер шапки
панелі тепер несе зону («Свіже»), як рядок-кікер у бандлі (знак — знак
панелі `batch`, не знак зони: словник шапки панелі спільний). Три осі рядка
стали трьома блоками: походження — третій рядок паспорта, час — блок
«Строк», безпека — чіп.

| елемент | Components (380) | G6 десктоп 380 / 390 шторка | Prototype aside | код | різниця |
|---|---|---|---|---|---|
| паспорт | назва 26/700 з пунктиром · «Яловичий стейк … · чек Сільпо 1 вер» 13 dim | назва 22/700 −.025 lh 1.15 · «яловичина · Dry Aged · в/у» 13 muted · «з чека: «сирий рядок» · Сільпо 7 вер · 389 ₴» 12 dim з лінією зверху; на 390 без третього рядка | input 26/700 + паспорт 13 dim | G6: input 22/700 (правиться на місці, blur/Enter → PATCH) · бренд · тип 13 muted з трійки продукту · походження 12 dim («чек Сільпо · 7 вер» / «додано рукою · 1 вер» / «з розмови» / «домислено · 60 %»); на 390 сховано | сирого рядка чека й ціни в даних партії нема — рядок каже те, що є (`origin`) |
| кількість | 44 r8 bg: 450 15/500 · «г» 13 muted + шеврон | 40 r10 bg, те саме | + «−» «+» 32 r8 на card | 40 r10 bg: input 15/500 · одиниця = `<select>` під написом зі шевроном · ± 32 r8 (крок: шт/пач 1, г/мл 50) | крок ± — рішення реалізації (Prototype має свій `step` у даних) |
| стан | сегмент 44 r8: ціле / відкрито, обране — card + тінь | 40 r10, вкладки 34 r8 | кнопки, обране — card + тінь | radiogroup 40/34, обране — card + sh-a; тап по обраному нічого не шле | «Позначити відкритою / запакованою» кнопкою знято |
| строк | плашка 12/14 r12 danger-bg: circle-alert · «Розрахунок скінчився 9 днів тому» 14/500 · «≈ рахую від додавання 1 вер · дати на пачці не знаю» 12 muted; «Ще годиться» / «Зіпсувалось» 40 r10 контуром; «Необовʼязково…» 12 dim; рядок дати 44 r8 bg: calendar · «Дата з пачки — постав…» 14 dim · «дд.мм» | плашка 10/12 r10, заголовок 14/600; три відповіді: «Ще годиться» / «Був у морозилці» / «Зіпсувалось»; дата 40 r10, 13 dim | тон: danger прострочено · amber ≤ 3 · bg; clock / circle-alert; відповіді лише на простроченому; «Ще годиться» — стан без запису | `termBlock`: «Не псується» / «Без категорії» / «Розрахунок (Строк — коли дату ставила людина) скінчився N днів тому / — сьогодні» / «Ще N дн — краще не відкладати» (≤ 3, amber) / «Ще ≈ N дн»; підрядок «≈ рахую від додавання {дата} · дати на пачці не знаю» або «з пачки, поставив ти · до {дата}»; відповіді лише на простроченому: «Ще годиться» гасить плашку до закриття картки без запиту (Prototype `stillOk`), «Зіпсувалось» → списання з `spoiled`; поле дати поверх рядка «Дата з пачки…»; «прибрати» — лише на даті від людини | «Був у морозилці» не робиться — контракту перерахунку немає (DEBT); знаки circle-alert / trash-2 / activity у словнику нема, а `components/Icon/` — потік icon-motion: замість них `live.overdue` (triangle-alert), без знака на «Зіпсувалось» і «Списати», без activity на «На 100 г» (QUESTIONS-things §20) |
| безпека | чіп 32/999 plum-bg: ban · «не їм · категорія «мʼясо» · Оля» | те саме, «Пилип» | те саме | чіп 32/999 plum-bg: ban · «не їм» / «не можна» | охоплення (категорія / хто) у даних партії нема — Р13 → DEBT §24 |
| поживність | рядок 12/14 r12 bg: activity · «На 100 г» 14 · «≈ 271 ккал · Б 26 · Ж 18 · В 0» 13 muted | 10/12 r10, 13 | те саме + «У рецептах · N ›» | два рядки 10/12 r10 bg: «На 100 г» і «На позицію · 450 г» (з коду) | «У рецептах» — даних нема; «На позицію» — бандл не має, лишено з коду |
| місце | — (зона лише кікером) | — | — | `<select>` 40 r10 bg «Місце» — партію можна перекласти | чого бандл не має, функція з коду |
| підвал | «Зміни зберігаються самі» 12 dim · «Списати» 40 r10 контур, danger, trash-2 | 13 muted · 38 r10 | 12 dim · 40 | 13 muted · «Списати» 38 r10 контур danger (у слоті панелі `.rail-foot`) → трійка причин (⚠3) | без trash-2 (див. вище) |
| тост списання | «Списав «Помідори»» на чорнилі, чіпи «зʼїли / зіпсувалось» + «Повернути» | — | — | плашка `.undo-bar` з кроку 2c — не чіпалась (це список, не картка) | у бандлі тост на чорнилі; код — card + line2. Не в цьому кроці |

Пари: `things-card-1440(-dark)` (G6 плавуча, `--sel` за інлайн-стилем кадра,
`--click` по рядку «Стейк Портерхаус»), `things-card-390(-dark)` (шторка).
Тести: сегмент стану, ± кроком, «прибрати» лише на даті від людини,
прострочене (плашка danger · «Ще годиться» без запиту · «Зіпсувалось» →
spoiled), `termBlock` чотири стани і два писачі.

### Р52 · things-v3, крок 3: чек у панелі за Screens «Чат · збірка» (aside чека), Components «receipt artifact», Prototype

Контейнер — панель/шторка (Р32); тут вміст `IntakeCard` для чека
(`source.kind` retail_receipt / chat_receipt). Спільні класи груп
(`rgroup`, `rrow`) переведено на форму бандла — їх ділить і список покупок у
панелі (та сама сімʼя, Components). Пари знімались зі стабом
`stub-receipt.json` (15 позицій · 2 не їжа · 2 не впізнав, як у кадрі) на
стенді `stand-seed` зі списком «Молоко · Яйця · Масло».

| елемент | Screens (aside 380) | Components (400) | Prototype | код | різниця |
|---|---|---|---|---|---|
| шапка документа | «Чек Сільпо · 07.09» 15/600 · «19 рядків» 13 dim (у рядку шапки панелі) | «Чек Сільпо» 16/600 · «7 вер · 1 284 ₴ · 14» 13 dim | «Чек Сільпо» · «9 вер · 19 рядків» 12 dim | рядок шапки панелі — код (знак `receipt` + «Чек» + лічильник); у тілі перший рядок: «Чек Сільпо · 07.09» 15/600 · «19 рядків · 1 284 ₴» 13 dim; чек із чату — без магазину й суми | шапка панелі старша за кікер бандла (Р32); 17/700 + моно знято |
| група «У комору» | «У комору · 15» 12/500 sage · «зняти всі» 12 muted; рядки 40 з волосиною зверху: чекбокс 20 r6 sage + check · назва 14/500 · паспорт 12 dim · чіп «не можна · Оля» 22 plum · «✓ у списку» 12 sage (list-checks) · кількість 13 muted; «Ще 6 ▾» 36 13 muted | те саме, «У комору · 9» | рядок цілком — кнопка-тогл, знятий — opacity .5; «усі» повертає | те саме: заголовки звичайним регістром, дія muted («зняти всі» / «повернути всі»), паспорт «бренд · тип» з трійки op, «у списку» зі знаком `sys.list` без гліфа ✓ (гейт no-glyphs), кількість 13 muted; шість рядків, далі «Ще N» зі шевроном; рядок — тогл (чекбокс лишається для a11y), знятий .5 | чіп «не можна · Оля» не робиться: вето на ops чека клієнт не рахує (даних немає; Р13 → DEBT); група «Вже у списку» окремо (Components) знято — Screens ставить мітку на рядку |
| «Не для комори» | «Не для комори · 2» 12/500 muted · «у список побуту» 12 sage; рядки 40 muted зі знаком роду (spray-can, shopping-basket) · кількість 13 | те саме | «Не для комори» dim, рядки без чекбокса | те саме; знаків роду речі у словнику нема — порожній слот 16 | — |
| «Не впевнений» | «Не впевнений · 2» 12/500 amber; рядок: пунктирна рамка 20 r6 amber · «сирий рядок» 14 muted · 89 ₴ 13; чіпи 32/999 на bg «шоколад» «солодке» + «✎ своє» контуром | те саме («уточнити» amber у шапці) | — | рядок як у кадрі; чіп один — «✎ уточнити» контуром → степер кількості + одиниця + «ок» чорнилом (контракт `clarifyLine`: кількість · одиниця) | чіпи-здогадки каталогу («шоколад», «солодке») — контракту немає; чіп «своє» = «уточнити» |
| підвал | «15 додамо додому · 3 уже в списку · 2 не їжа, у список» 13 muted · «Ні» 38 текстом muted · «Застосувати 15» 38 r10 чорнилом 13/600 | «9 додамо додому · 2 не їжа, у список · 1 не впевнений» · «Застосувати 9» 44 r10 | «У комору · N» 44 r12 зі знаком | те саме, що Screens; числа живі: «уже в списку» рахує лише увімкнені рядки зі списку; «Застосувати N» = увімкнені; усі зняті — вимкнена; після застосування — «N уже вдома» + «Скасувати» | `Button size="strip"` (44/12) не чіпався — свої класи `rc-*`; «1 не впевнений» у підвалі (Components) не пишеться — Screens не має |

Пари: `things-receipt-1440(-dark)` (Screens «Чат · збірка»), `things-receipt-390(-dark)`
(шторка проти Components «receipt artifact» — кадру чека на 390 у бандлі
нема; `--click [data-trace="intake"]`). Тести: `receipt-panel.test.tsx`
(шапка, групи без капсу, паспорт, «у списку», підвал з числами; тап по рядку
→ число в кнопці й підвалі меншає, «зняти всі» ⇄ «повернути всі»,
застосовується лише обране).

### Р53 · things-v3, крок 4: рецепт, журнал, публічний рецепт за Screens («Рецепт · 1440/390», «Журнал · 1440», «Публічний рецепт · 1440»)

Бібліотека «Рецепти · 1440/390» уже була за D5 (Р31, 6b); тут — перезняті
пари на стенді (`things-recipes-*`). Панель рецепта в чаті (6b-3) не чіпалась.
Три сторінки переписано з нуля: `Recipe.tsx`, `CookLog.tsx`, `SharedRecipe.tsx`
(і їхні модулі); капс і моно зняті всюди.

| екран · елемент | кадр | код | різниця |
|---|---|---|---|
| Рецепт · шапка | пігулки 36 на card + тінь: «← Рецепти» · «Збережено» sage (bookmark-check) · «Поділитись» (share-2) · «Обговорити в чаті» (reply); 390 — знаки 38 колом | так само; незбережений — «Колись» (`sys.later`, підтверджене відхилення); «Поділитись» → `/share` зі станом рецепта; «Обговорити» → нова сесія з рецептом | — |
| Рецепт · лід | чіп «можу зараз · 6 з 6» sage-bg 26/12 + чіп «помідори · сезон · 3 дні» amber-bg з flame; h1 36/700 −.035; рядок 14 muted «clock 25 хв · ≈ 540 ккал · Б 22 · Ж 18 · В 68 · на порцію · пісне · rotate-ccw готував 2 рази»; опис 15 muted до 620; 390 — h1 26, рядок 13, без опису | чіп стану — слово з рядка бібліотеки (сервер, той самий `statusWord`), «N з M» — зі складу (`ing.p`); другий чіп — `rescues` бібліотеки («Помідори · Фует»); ккал — з каталогу без «≈» коли точно, інакше оцінка моделі з «≈» (`kcalLine`); «готував N» — `cooked_count`; 390 — опис сховано | «пісне» — ознаки в рецепта нема; «сезон · 3 дні» — рядок `rescues` не несе строку |
| Рецепт · кроки | картка 4/20: «Кроки · 4» 44 (list-ordered), рядки 14/0 з волосиною, коло 28: галочка sage зробленому · чорнило поточному · контур решті; поточний — підкладка bg на всю ширину + таймер sage-bg 30 «20:00»; 390 — коло 26, 14/1.45 | так само; тап по колу — зроблено/скасувати, поточний іде далі; таймер → Cook Mode з цього кроку | — |
| Рецепт · склад | картка 6/18: «Склад · 6» 44 (shopping-basket) + порційник 30 на bg «− 2 порції +»; рядки 46: крапка 8 (sage є · amber відкрите · line2 бракує), назва, чіп 22/11 (відкрито amber-bg · не люблю plum-bg · алергія), кількість muted; легенда 40 12 dim; 390 — 42/28/7/20 | так само; «алергія» — danger-bg з іменем, коли є; на 390 без легенди й без знака в шапці | «не люблю» у кадрі muted на bg — у коді plum-bg (рід «обмеження людини», tokens-v3) |
| Рецепт · «Готуємо» | 52 r12 чорнилом з cooking-pot + підпис 12 dim; 390 — липка знизу з фейдом | так само; на 390 липка над нижнім баром (64 + safe-area) | нижній бар на 390 кадр не малює (підсторінка) — лишено з коду (оболонка 6a) |
| Журнал · шапка | шапка Рецептів: h1 · сегмент «Збережені · 23 / Журнал» · «Знайти в журналі» 260×40 | `AppHeader` + класи сегмента/пошуку з `Recipes.module.css`; «Збережені · N» — з бібліотеки; пошук — з 8 записів (підпис кадра); на 390 — знак пошуку і знак «Збережені» | окремий маршрут `/cooklog` лишається (сегмент веде туди й назад) |
| Журнал · «За тиждень» | картка 12/16 r12: знак calendar-check у sage-bg 36 r10, «За тиждень — 5 готувань.» 600 + «Середня оцінка 4,2 · 11 позицій із того, що було вдома.» muted | так само (`sys.calendar`: calendar-check у словнику нема) | — |
| Журнал · рядки | групи «Сьогодні / Вчора / Пн · 7 вер» 13/500 muted; картка r14 0/16; рядок 64: коло 40 (sage-bg check · фото 40 r10 · undo контуром), назва 15/600, «19:42 · 27 хв · ★★★★☆ · 3 з того, що було вдома» 12 muted, вердикт курсивом 13; «Знову» 34/999 контур muted з rotate-ccw; скасоване — .5, перекреслено, «скасовано — комора повернена» | так само; тривалість — від старту до кінця готування (`runMinutes`), інакше час рецепта; зірки — `cook.rating` бурштином, порожні .3, число в aria; клік по рядку — в сесію рецепта | «1 год 40 хв» у кадрі — `formatDuration` домену дає «100 хв» до 120 (не чіпалось) |
| Публічний · шапка | без рейки: логотип · «Кухня» · «спільний рецепт» · «Увійти» | так само; «Увійти» лише гостю | — |
| Публічний · тіло | сітка minmax(0,1fr) 340, gap 32, 16/32, до 1040 по центру; h1 34; рядок 13 muted «clock 25 хв · users 2 порції · ≈ 540 ккал · пісне»; опис 15; кроки — номери чорнилом 28 без таймерів; «Склад · 6» 40 + рядки 42 «назва · кількість» («—» без кількості); картка sage-bg 16/18: «Готуй у себе.» 600 + речення 14 sage, кнопка 42 r10 «Увійти в Кухню →» (гість) / «Готуй у себе» (свій) | так само; «≈ ккал · Б · Ж · В» без «на порцію»; 390 — склад акордеоном над кроками, картка липка знизу | «пісне» — нема ознаки |

Пари: `things-recipe-1440(-dark)`, `things-recipe-390(-dark)`,
`things-cooklog-1440(-dark)`, `things-public-1440(-dark)` (свій),
`things-public-guest-1440(-dark)` (гість, `--state` порожній),
`things-recipes-1440/390(-dark)` (перезняті на стенді). Стенд дістав
рецепти й журнал за кадрами (`stand-seed.mts`). Тести: `Recipe.test.tsx`
(шапка, чіп з бібліотеки, крапки роду, кроки, порційник), `CookLog.test.tsx`
(dayLabel, runMinutes, сегмент, «За тиждень», скасоване),
`SharedRecipe.test.tsx` (гість / свій).

## Потік time-people · календар · подія · профіль (PR #53)

Гілка `feat/time-people-v3` (від `origin/main` 534bd9f, ребейз на abe01dc).
Правила — як у спільному `DEVIATIONS-V3.md`: код — джерело правди; бандл
каже — робиться як у бандлі; мовчить — питання в `QUESTIONS-time-people.md`.
Номери — Р60–Р69. Зведення у спільний реєстр робить власник після мерджу.

---

### Р60 · Календар · 1440 за Screens D3a — сітка тижнів картками (Р28 переграно власником)

Гілка `feat/time-people-v3` (від `origin/main` 534bd9f — d1a8839 + злитий
`feat/icon-motion`). Завдання власника 11.09 називає «Календар · 1440»
Screens (тижні картками, 7 колонок) і Prototype (місяць на 1440) прямо —
це і є рішення, якого Р28 чекав: стрічка на всіх ширинах знята для ≥1024,
для 390 лишається (D3b, рішення 03.09).

Зроблено за D3a: картка тижня card r14 з тінню, без чорнильного хедера —
лише dim-підпис «7 – 13 вересня · тиждень 37»; над днями — смуги тривалих
(`weekSpans` із `lib/spans`, він чекав з етапу 2), кожна у своєму ряду 20 px,
рід кольором (обмеження/традиція слива · сезон бурштин · своє шавлія · рамка
muted), відкритий край без скруглення; підпис — повний (як у легенді) лише в
тижні початку або в першому видимому, далі — сама назва (`barLabel`). Дні —
7 колонок, клітинка ≥120 r10: dow 12 dim / число 15/600, вихідні бурштином;
точкові — рядками 12/1.35 зі знаком роду (`pointIcon`: страва chef-hat,
завіз truck шавлією, приготоване — галочка muted); понад три — «ще N ·
назва» (регістр — звичайний, `moreLabel` виправлено з капсу, Р20). Сьогодні —
bg + кільце 1.5 чорнилом, унизу «Що на вечерю?» шавлією. Шапка й легенда
стоять, гортається сама стрічка (Prototype `main → overflow:auto`); «Сьогодні»
ставить картку тижня з сьогодні вгору. Межа сітка/стрічка — 1024 (кадру 768
для календаря в бандлі немає; 1024 — та сама межа, що в рейки).

| елемент | кадр D3a | код 1440 |
|---|---|---|
| h1 | 28/700 | 28/700 (роль) |
| підрядок «Вересень · тиждень 37» | 14 muted | 14 muted, за верхнім видимим тижнем |
| «Сьогодні» | 36 · 0 12 · card · sh · 13/500 · pill | 36 · 0 12 · card · sh-a+sh-b · 13/500 · pill |
| «Подія» | 36 · 0 14 · r10 · ink · 13/600 · plus 15 | 36 · 0 14 · r10 · ink · 13/600 · plus 16 (союз 12/16/18/20/24, §11) |
| чіп легенди | 30 · 0 11 · pill · 12/500 · знак 12 | 30 · 0 11 · pill · 12/500 · знак 12 |
| картка тижня | card · r14 · sh · без хедера | card · r14 · sh-a+sh-b · без хедера |
| підпис тижня | 12 dim · 12 16 0 · діапазон muted 500 | 12 dim · 12 16 0 · діапазон muted 500 |
| смуга тривалої | 20 · r6 · 0 8 · 11/500 · ряди gap 3 · 8 8 0 | 20 · r6 (0 на відкритому краї) · 0 8 · 11/500 · gap 3 · 8 8 0 |
| клітинка дня | ≥120 · r10 · 8 8 6 · gap 6 · 7 колонок gap 3 · 6 8 8 | 120 · r10 · 8 8 6 · gap 6 · 7 колонок gap 3 · 6 8 8 |
| дата в клітинці | dow 12 dim · число 15/600 · вихідні amber | те саме |
| подія в клітинці | 12/1.35 · знак 11 | 12/1.35 · знак 12 (союз) |
| сьогодні | bg · кільце 1.5 ink inset | те саме |
| «Що на вечерю?» | 12/500 sage · arrow-right 11 | 12/500 sage · chevron-right 12 (`sys.next`; `arrow-right` у словнику нема — словник у потоці icon-motion, QUESTIONS-time-people §17) |
| aside події | 340 · card r16 · sh2 | панель каркаса (6b: 400 на 1440) — Р62 |

**Що з D3a не зроблено і чому.** «Приховані: … · повернути» і «Свята: … ·
змінити» внизу за скролом — є (підвал стрічки). Aside дня з Prototype
(«Нічого не заплановано · Комора підказує: помідори сьогодні…», «Помідори —
сьогодні» на сьогодні) — контент моделі про комору, якого в календарі нема
звідки взяти без прогону; клік по дню лишається входом у нову подію, як каже
підпис D3a («Клік по дню — нова подія на цей день») і як у коді. У DEBT.
### Р61 · Календар · 390 за Screens D3b — стрічка днів, числа з кадру

Стрічка як була (рішення 03.09), числа переведено на кадр: рядок 44 порожній /
64 з вмістом, margin 0 8; жолоб 14 з відступом 8, риски 3 px кроком 5 (було
8), край 14 з r2; дата 44: dow 11, число 17/600; вміст 12 12 10 4 gap 5, лінія
line2; чіп краю 22 · 0 8 · 11/500; подія 14/1.35; сьогодні — card r12 з
тінню, без лінії; липкий підрядок «Вересень · тиждень 37 · 7 – 13» (13/600 +
12 dim) — замість заголовків тижнів, яких D3b не малює; «ПІСТ ТРИВАЄ · N
ДНІВ» капсом знято (Р20). Легенда — один ряд зі скролом (закриває DEBT §36).
«Сьогодні» — пігулка в шапці, «+» — коло 36 чорнилом; шапка й легенда стоять,
гортається стрічка.

| елемент | кадр D3b | код 390 |
|---|---|---|
| h1 | 26/700 | 26/700 (роль) |
| «Сьогодні» · «+» | 36 pill card · коло 36 ink, plus 16 | те саме |
| чіп легенди | 28 · 0 10 · 12/500 · знак 11 | 28 · 0 10 · 12/500 · знак 12 |
| липкий підрядок | 6 16 4 · «Вересень» 13/600 · решта 12 dim | 6 8 4 (стрічка з margin 0 8) · 13/600 · 12 dim |
| рядок дня | 44 / 64 · margin 0 8 | 44 / 64 · margin 0 8 |
| жолоб · риска | 14 + 8 · 3 px · крок 5 · край 14 · r2 | те саме; плюс крапка 5 на краю: суворо — заливка, мʼяко — контур (Р29, Components) |
| дата | 44 · dow 11 · число 17/600 · вихідні amber | те саме |
| вміст | gap 5 · 12 12 10 4 · line2 | те саме |
| чіп краю | 22 · 0 8 · 11/500 · знак 10 | 22 · 0 8 · 11/500 · знак 12 |
| подія | 14/1.35 · знак 13 | 14/1.35 · знак 12 |
| сьогодні | card · r12 · sh · без лінії | card · r12 · sh-a+sh-b · без лінії |
| «Що на вечерю?» | 14/500 sage · arrow-right 13 | 14/500 sage · chevron-right 12 |
| бургер у шапці | нема (бар унизу) | є — `AppHeader` оболонки (6a), не чіпаю |

Пари: `tp1-calendar-1440(-dark)`, `tp1-calendar-390(-dark)`. Засів — `api-demo`
(in-memory): сім своїх подій через POST /v1/events (мама, гості, борщ, завіз,
білкова дієта, день народження, паста) поверх каталогу сезонів.
### Р62 · Подія-артефакт за Components A1–A3 і aside Prototype — вміст `PeriodArtifact`, каркас панелі не чіпано

Одна форма на серію й подію лишається; змінено вигляд за A1 (серія), A2
(своє), A3 (системне) і aside дня Prototype/D3a (кікер зі знаком роду · дати ·
правило · мʼяко/суворо · «Що прийде» · «Прибрати» / «Записати»). Капс і
mono-мітки зняті (Р20). Числа — з кадрів 400-px карток; заголовок у панелі
1440 — 22 (aside D3a/Prototype), у шторці 390 — 24 (D3c).

| елемент | кадр (A2 · A3 · A1 · D3c) | код |
|---|---|---|
| кікер системного | знак 13 + 12/500 тоном роду | знак 12 + 12/500 тоном (сезон sun/amber · традиція church/plum · завіз truck/sage · подія дому users/sage · рамка muted без знака) |
| рід свого | сегмент 36 · 3 · r10 · bg; активний card + тінь 13/500 | те саме (`.kinds`) |
| заголовок | 24/700 −.025em (панель D3a — 22) | 22/700 у панелі, 24 у шторці (<768) |
| дати свого | «з» muted 14 · чіп 36 · 0 12 · r8 · bg · 500 · chevron-down 13 · «15 днів» 13 dim | те саме; чіп несе прозорий `<input type=date>` поверх (нативний вибір, форма бандла); тривалість — своє, «ще N днів» — системне |
| дати системного | «з 15 вер до ≈ 30 лист» 14, дата 600, «ще 81 день» 13 amber | те саме, тон роду |
| Правило | 12 muted · рамка ≥40 · 10 12 · r8 · bg · 14 | те саме; правка — input 40 r8 bg |
| мʼяко/суворо | пігулка 30 · 2 · bg; 26 · 0 12 · 12/500; суворе — plum + білий 600; мʼяке — card + sh | те саме; підказка 12 muted: мʼяко — «враховую в пропозиціях, але не забороняю» (D3a/Prototype; `SOFT_HINT` виправлено), суворо — як A2 |
| «Що з цим приготувати» | 12 muted; рядки 44 · line · 14/500 · chevron-right 14 dim → чат | те саме, `sys.next` 12 |
| «Варто докупити» | чіпи 30 · 0 10 · pill · bg · 13 | те саме |
| «Що прийде» (Prototype) | рядки 40 · знак 14 · 14 · chevron | рядки 40 · truck 12 muted · 14, без шеврона (рядок нікуди не веде — `supply` це дані партії) |
| серія: рядок | 52 · галочка 22 r6 ink / контур 1.5 line2 · 14/500 + 12 dim · слово 12 muted, суворо plum + lock | те саме без lock (знака в словнику нема, §17); знятий рядок opacity .55 |
| підвал | «N з M» 13 muted · «Ні» 40 · 0 14 · контур line2 · 14/500 · головна 40 · 0 16 · r10 · ink · 14/600; шторка — 48 r12 15/600 | те саме; «Прибрати» — текст danger 13/500 (14 у шторці) |
| «Не показувати» · «Додати в список» | зі знаками eye-off · list-plus | словами без знаків (§17) |
| «свято · своє» | один чіп «коли» | `oneDay` переведено з `custom` на `holiday`: подія дому (мама на 4 дні) — два чіпи, як у Prototype aside |

Кікер панелі — «Подія» (назва типу, як «Рецепт» у Р36); рід і назва — у
вмісті. Шапка шторки — рядок панелі (Р32), не кікер+✕ з D3c.

Пари: `tp2-event-390(-dark)` (Screens D3c, шторка з «Мама приїжджає»),
`tp2-event-a3-panel-1440(-dark)` (Components A3 через панель, сезон із
каталогу), `tp2-event-a2-panel-1440` (A2, дієта суворо), `tp2-subscriptions-panel-1440`
(Components «Календар · що впливає на кухню» — форма була з Р30, заголовок
16/600 і текст 13 виправлено під кадр).
### Р63 · Профіль за Screens D4 (1440 · 390 у трьох станах), Components «profile states», Prototype

Зроблено за D4 «Профіль · 1440» (кадр 1440×900, `nth 0`; D2a з чорнильними
хедерами карток — старіший пакет тієї ж сторінки, 1040 px): речення —
головне, широкою колонкою 17 px; Дім / Мережі / Акаунт — службове, праворуч
у колонці 360, 14 px, картки card r14 без чорнильного хедера (знак 16 +
15/600 + підпис 12 muted). Підказка — sticky-картка шавлією 280 праворуч
від речень, міняється з фокусом («Стань у рядок — скажу, що тут писати»
без фокуса); на 390 — під активним рядком 13 шавлією. Нотатки — картка
рядків 48 з датою 12 dim і «прибрати» 13; прибрана лишається закресленим
рядком .6 із «Прибрано · Повернути» шавлією 5 с. Три форми `status`
(Components): filled — чорнило; empty — плейсхолдер dim курсивом з пунктиром;
none — «нічого такого» muted + галочка в шавлієвому колі 16. Лічильник — «176 /
200» dim, за 20 до стелі бурштином зі смужкою 3 px, на стелі — «… · далі вже
мемуари». Тема — сегмент Світла · Темна · Авто (D2a/Prototype; «Авто» —
підтверджене відхилення, `theme.ts` отримав `themeSetting`/`setThemeSetting`,
auto = без власного вибору, за ОС). «Вийти» — контур line2 38; «Видалити
акаунт» — текст danger. Видалення — шторка з кікером «Назавжди», чотирма
причинами 48 r12 на bg (обрана — кільце 1.5 чорнилом, радіо 20 → 6 px
чорнило), «Лишаюсь» 50 r12 чорнилом, «Видалити назавжди» контур danger,
неактивна до вибору причини. Капс у мета знято: «власник», «учасник»,
«чекає», «чек 9 вер», «сесія закінчилась» (Р20).

| елемент | кадр D4 | код |
|---|---|---|
| h1 · вступ | 28/700 · 14 muted під ним | 28 (роль, у `AppHeader`) · 14 muted першим рядком колонки |
| сітка | 1fr + 360 · gap 24 · 24 32 0 | 1fr + 360 · gap 24 · 0 32 40 (верх дає шапка) |
| картка речень | card r14 · 4 22 · sh | card r14 · 4 22 · sh-a+sh-b |
| рядок | ≥56 · 14 0 · line · 17/1.4 · початок muted nowrap | 56 · 14 22 (margin −22) · line · 17/1.4 |
| активний | bg на всю ширину картки r10 · підкреслення 2 sage · лічильник 12 dim | те саме |
| «Мені не можна» | danger + alert-triangle 15 | danger + `cook.ban` 12 (alert-triangle у словнику нема; ban — «Не можна» дослівно; §17) |
| empty / none | dim + пунктир 1.5 line2 · «нічого такого» muted + check у sage-колі 16 (Components) | те саме, empty — курсивом (tokens-v3) |
| підказка | 280 · sage-bg r14 · 16 18 · sticky · lightbulb 13 + 12/500 sage · 14/1.5 | те саме без lightbulb (§17) |
| нотатки | підпис 14/600 + 14 muted; картка 2 20; рядок 48 · дата 12 dim 40 · 15 · «прибрати» 13 muted; прибрана .6 закреслено + «Прибрано · Повернути» sage 500 | те саме |
| джерела даних | 14/600 · 13/1.55 muted · ≤720 | те саме |
| службова картка | card r14 · 16 18; шапка знак 16 + 15/600 + 12 muted | те саме (house → `sys.home`, receipt → `sys.receipt`, user → `sys.profile`) |
| рядок Дому | 44 · line · аватар 28 (line2 · 12/600; учасник plum-bg) · 14 · роль 12 dim · more-horizontal | те саме; замість «…» — дії словами 12 muted («Передати роль», «Виключити»), як у коді |
| запрошення | dashed-коло з mail 13 · пошта muted ellipsis · «чекає» amber 500 · «Скасувати» 12 | те саме, коло без mail (§17) |
| «Лист не дійшов» | amber-bg r10 · 10 12 · 12/1.45 · «Скопіювати» 600 | те саме; «лист пішов» — та сама плашка шавлією |
| поле email | пілюля 40 · 4 · bg · «email» 13 dim · «Надіслати» 32 r16 ink 13/600 | те саме |
| Мережі | «Сільпо» 14 · «● чек 7 вер» 12 sage 500 · «Відключити» 12 dim · «Стейки Карпат · без підключення» | те саме, статуси словами в роді |
| Акаунт | рядки 44: Пошта · Тариф · Тема-сегмент 30 pill (24 · 0 10 · 12/500); «Вийти» 38 · 0 14 · r10 · line2 · 13/500; «Видалити акаунт» 13 danger | те саме, сегмент з трьома положеннями (Авто) |
| 390 · вступ | 14 (перший день) / 13 (заповнений) muted | 14 в обох (одне значення — одне написання) |
| 390 · рядок | колонкою: початок 13 muted, текст 15 (перший день — 16), ≥52 · 12 0 | 13 + 15, ≥52 · 12 16 |
| 390 · порядок | речення · Дім · Мережі+Акаунт | речення · Дім · Мережі · Акаунт · нотатки · джерела (дві останні на 390 кадр не малює — лишено внизу) |
| 390 · Дім | 12 16 · шапка 15 + 14/600 + «Запросити» 13 sage · рядки 42 · аватар 26 | 12 16 · рядки 44 (тач-мінімум) · аватар 26 |
| 390 · Мережі+Акаунт | одна картка, рядки 36 | дві картки, як на 1440 (одна форма на обидві ширини) |
| видалення | кікер trash-2 13 + 12/500 danger · h2 22/700 · 14/1.5 muted · причини 48 · 0 14 · r12 · bg · 15, радіо 20; «Лишаюсь» 50 r12 ink 15/600 · «Видалити назавжди» 50 · 0 16 · r12 · danger 14/600 | те саме без trash-2 (§17) |
| нижній бар | «П · Профіль» пʼятою ціллю | бар оболонки 6a: пʼята ціль — Календар, профіль у шухляді; не чіпаю |

Не зроблено: «Обрізано на межі 260 — доповни або скороти» (Components) —
контракт `ProfileFieldV2` не має ознаки обрізання, лише `status`; це зміна
`packages/domain`; у коді цього стану не існує — рядок реєстру, не питання (власник, 11.09).

Пари: `tp3-profile-1440(-dark)`, `tp3-profile-390-first(-dark)`,
`tp3-profile-390-filled(-dark)`, `tp3-profile-390-delete(-dark)`. Засів —
дім dev на `api-demo`: PATCH пʼяти полів і `ban: none`; нотаток без
прогону моделі немає — картка в порожньому стані.

### Стан реєстру потоку (на момент зведення)
| | |
|---|---|
| Вирішено реалізацією за кадрами | Р60, Р61, Р62, Р63 |
| Переграно власником | Р28 (сітка на ≥1024) — Р60 |
| Питання дизайн-чату | QUESTIONS-time-people §17 (знаки поза словником), §18 (сезонні смуги в тижні) |
| Борг | aside дня з Prototype; щільність каталогу сезонів (DEBT §36) |

## Потік cook-share · Cook Mode · /share · сайдбар · пакети правок 1–3 (PR #56, #58, #60, #62, #63, #67)

Реєстр потоку `feat/cook-share-v3` (від `534bd9f`). У спільний
`DEVIATIONS-V3.md` — ані рядка; зведення робить власник після мерджів.
Діапазон номерів — Р70–Р79 (Cook Mode + /share, гілка `feat/cook-share-v3`), Р80–Р85 (6b-6 сайдбар і стрічка, гілка `feat/sidebar-v3`), Р86–Р91 (пакет правок 1, гілка `fix/v3-pack-1`), Р92–Р96 (пакет 2, гілки `fix/v3-pack-2` · `fix/v3-pack-2-calendar`), Р97–Р99 (пакет 3, гілка `fix/v3-pack-3`). Борг і копі потоку — у кінці цього файлу
(окремих DEBT/COPY-DEBT потік не заводить).

## Р70–Р79 · Cook Mode + /share — тільки вигляд

Логіка, момент записів, дані, ендпоінти, міграції — не чіпались («код —
правда», 10.09, 11.09). Де кадр малює іншу поведінку — рядок нижче з
file:line того, що робить код, без реалізації. Файли потоку:
`pages/Cook/*`, `pages/Share/*`, `icons.ts` (окремий блок у кінці),
`scripts/side-by-side.mjs` (три опції для пар — `--dc-click` і `--click`
ланцюжком через « ;; », `--nav-state`).

**Cook Mode — кадр · код** (Prototype «COOK MODE» 1440 · Cook and Share «Cook
· 768» / «Cook · 390»; вигляд поверх наявних кроків, таймера, beep, wake lock):

| елемент | кадр | код |
|---|---|---|
| полотно | bg, тема застосунку; 1440 — рейка лишається зліва | bg на токенах; власна темна палітра Cook знята (hex → 0); попап Пул-3 на ≥1024 відступає на рейку 60/256, <1024 на весь екран |
| шапка 1440 | «✕ Вийти» 40 card+sh · chef-hat + назва 15/600 · sun/moon 32 у 40 card · volume-2 40 | те саме; sun/moon перемикають **наявну тему застосунку** (`theme.ts:11`); volume-2 вмикає/вимикає наявний beep і тики (`Cook.tsx` `softTick`/`beep`), нового звуку немає |
| шапка 768 | 64 · 0 20; «Вийти» 38; sun/moon 30 у 38; volume 38 | те саме |
| шапка 390 | сегменти 4 (sage · ink · line2) · ✕ 38 · пілюля «2 з 5 · Соус» 38 зі знаком списку → шторка · volume 38 | те саме; знак списку — `cook.steps` (list-ordered), бо `list` у словнику нема, а list-checks = список покупок; sun/moon — у шторці кроків |
| ліва колонка 1440 | card r16 sh 20 18 16: «20 хв · 2 порції · крок N з M» 13, прогрес 4, маршрут рядками 60 (✓ sage · поточний ink з таймером · наступні з часом, .6), «Усе для страви» чіпи 28 sage-bg, «Працює без інтернету · екран не гасне» wifi-off 13 dim | те саме; час кроку — з `s` («10 хв»), кроки без `s` — без часу; тап по зробленому — назад (`goToStep`, як було) |
| маршрут 768 | рядок чіпів 40 зі скролом: зроблений card + коло 28 sage-bg ✓ + підпис dim; поточний ink + коло 28 card + таймер; наступні — коло контур + «9 хв» | те саме; тап по зробленому — перехід (логіка була) |
| фокус 1440 | card r16 28 32 gap 24: пілюля «Крок N · коротка назва» 32 bg + крапки 8; текст 700 clamp(26,3vw,40) −.03em; чіпи інгредієнтів 34 sage-bg 14/500; lightbulb-порада; таймер bg r14 20 24: коло 48 card зі знаком (живий, поки біжить; бурштин ≤ 60 с), число clamp(44,5vw,72) 700 −.04em, «з 7:00» + смужка 4, Пауза/Старт ink 52 r12 з pause/play, «+1 хв» card 52 r12 | те саме; коротка назва = `st.t` без крапки; **порада не малюється** (Р73); знаки `cook.pause`/`cook.play` додано; число — роль `.t-timer` |
| фокус 768 | card r18 32 36; пілюля 30; текст 32/600 lh 40 −.025em; чіпи 32 (7 13); таймер 18 20: коло 44 card+sh, число 56/700, «з 7:00» + смужка 100, Пауза 44 r999 ink, «+1 хв» 44 r999 card+sh | те саме |
| фокус 390 | без картки: текст 26/600 lh 1.3 −.02em; чіпи 29 (6 11) 13/500; таймер окремою карткою r18 22 20 по центру: 72/700, смужка 160, Пауза 44 ink + «+1 хв» 44 bg; «Далі: …» рядком 24 (коло 24 контур + 13 muted) | те саме |
| низ 1440 | «← Назад» 64 r14 card+sh muted (.4 на першому) · «N · Далі: підказка» 64 card+sh (коло 32 контур 1.5) · «✓ Крок готово» 64 r14 sage 18/600; на останньому — «✓ Приготував» | те саме; дії — `advanceStep()` / `finish()` як були (`Cook.tsx:340`); тексти кнопок як у кадрі («Крок готово», «Приготував» замість «Готово», «Приготували» — копі, нижче); «Поділитись результатом» (О2) лишено текстовим рядком під низом на останньому кроці, `data-share-result` |
| низ 768 | Назад 60 r18 · «N · Далі: …» 60 r18 · «✓ Крок готово» 240×60 r18 17/600 | те саме |
| низ 390 | Назад 56 r16 · «✓ Крок готово» 56 r16 на всю ширину; 12 16 30 | те саме |
| моушн | M7: зміна кроку — слайд; таймер — знак живий | `step-slide` через `--dur-slow`; прогрес — `--dur-slow`; смужка таймера `1s linear` (тік — не рух); timer `live="timer"` поки біжить; reduce — токени 0 |

**Р70 · фінішний екран «Готово» + шторка шерінгу з нього проти розмови,
`post-cook.ts:1-9`.** Кадри «Готово · 1440/390» і «Поділитись · 390» як
екран після готування не будуються свідомо (рішення 30.08): після
«Приготував» → чат → «Списати продукти?» → картка → «Як вийшло?» → відповідь у
verdict/rating (`chat.ts:281`). Прототипна картка «Приготовано · 20 хв …» —
той самий рядок.

**Р71 · картка без фото і 6 стилів проти фото + оверлеїв A–D, `Share.tsx:1-20`.**
Кадр C3/C4: картка без фото, формати 1:1/4:5/16:9, дві палітри й шість
стилів. Код: фото користувача + чотири оверлеї брифу, сторіз 9:16 / пост
4:5, `navigator.share` з PNG або download. Не робилось.

**Р72 · «Cook Mode памʼятає свою тему окремо» (кадр 390) проти теми
застосунку, `theme.ts:11`.** sun/moon перемикають `data-theme` на html —
одну тему на все. Окремої памʼяті Cook Mode немає.

**Р73 · lightbulb-порада до кроку проти `RecipeStep { t, c, s }`, `api.ts:977`.**
Поради в даних рецепта немає (Prototype має `tip` у власних STEPS) — не
малюється; зʼявиться, коли контракт кроку принесе поле.

**Р74 · «Далі: спагеті у воду — постав воду вже зараз» (підказка кроку)
проти «Далі: назва кроку», `Cook.tsx:396`.** Prototype має `hint` у власних
даних; у коді — лише назва наступного кроку.

**Р75 · підписи таймера.** Кадр: Старт · Пауза · Далі. Код (`Cook.tsx:417`,
логіка як була): Старт · Пауза · Далі · «Спочатку» на нулі — четвертий стан
із коду, кадр його не має.

**Р76 · відновлення сесії на кроці > 0 губить таймер** — борг нижче (логіка,
не чіпалась; знайдено при парі).

**Р77 · /share — оболонка за «Поділитись · 390»:** шапка share-2 · «Поділитись
стравою» 15/600 · сегмент формату 30 на bg (активний card+sh 12/500); превʼю
на всю ширину колонки r14 (canvas, ≤ 56vh); стрічка мініатюр 54 r10
(оверлеї A–D знаками з кадру + фото); «Поділитись» ink 50 r12 + download 50
r12 контуром; на 1440 — та сама колонка до 480 по центру. Вхід — як був
(`Cook.tsx:340` finish('share'); `Feed.tsx` onShare для панелі/картки знято
ще в 6b-3, QUESTIONS §13 — тож /share зараз досяжний лише з останнього
кроку Cook Mode). Палітра експорту — `--export-ink/bg/sage/shadow` на
`.screen` у `Share.module.css` (не в спільному tokens.css), hex у Share.tsx →
0; плейсхолдер без фото — Onest (Golos у коді не завантажувався).

**Р78 · капс і «IBM Plex Mono» в оверлеях експорту, `Share.tsx:312, 423-431`.**
Оверлеї A–D пишуть «KITCHEN OS», «20 ХВ», назву капсом і моно-шрифтом,
якого в застосунку нема (падає в ui-monospace). Це вміст PNG, не екран —
копі нижче; не чіпалось.

**Р79 · сегмент формату** — кадр 1:1 · 4:5 · 16:9, код «Сторіз 9:16 · Пост
4:5» (наявні формати, Р71).

Пари (обидві теми): `cook-1440(-dark)` (Prototype: `--dc-click` «Готуємо» →
«Готуємо» в панелі, `--dc-wait 3000`), `cook-768(-dark)`, `cook-390(-dark)`
(Cook and Share; стан — `kos-cook-live` через `--init-storage`, вхід —
«Готуємо · таймер» у шапці чату або «До плити» в «Дім зараз»),
`share-1440(-dark)`, `share-390(-dark)` (`--nav-state` — стан роутера без
запису в базу; жодних записів поза домом dev). Гігієна: hex у Cook/Share →
0, MonoLabel у Cook → 0, гейти зелені (no-glyphs · no-undefined-tokens ·
no-hard-motion · icons · csp).

## Р80–Р85 · 6b-6 · сайдбар і стрічка — тільки вигляд

Гілка `feat/sidebar-v3` від main (e02ef9b). Джерела: Prototype nav
(розгорнутий сайдбар), Responsive R1 (рейка 60 ↔ сайдбар 256, одна кнопка
вгорі рейки), R2 (768: шухляда 300 поверх зі скрімом), R3 (390: та сама
шухляда з «Меню» в шапці чату). Файли: `components/TabBar/*`,
`pages/Feed/Feed.tsx` + `Feed.module.css` (знято рядок над композитором),
тести обох. Логіка навігації, стор `nav.ts`, `tokens.css` (зсув
`body.with-sidebar`) — не чіпались.

**Сайдбар — кадр · код**

| контейнер | кадр | код |
|---|---|---|
| рейка 1440 (R1) | 60, 16 0, gap 4, волосина line2 праворуч; логотип 30 (mb 6); **panel-left-open 38 r10 card+sh** (mb 8); цілі 38 r10, знак 18, активна чорнилом; крапка danger 7 на «Коморі»; лічильник списку 16 чорнилом у куті; аватар 30 line2 внизу | те саме; кнопка «панель» тепер і в рейці (раніше — лише в сайдбарі; розгортав логотип). Логотип теж лишився тоглом (Prototype nav малює його кнопкою) — обидва ведуть в одне |
| сайдбар 256 (R1 + Prototype nav) | 16 12, gap 2; шапка 0 8 14: логотип 30 · «Kitchen **OS**» 17/700 (OS шавлією) · panel-left-close 36 r10 card+sh; цілі 40 r10, 0 10, gap 12, знак 18, 14/500 muted, активна чорнилом; «Комора 10» — число danger 12/500 праворуч; «Список 4» — 18 r999 чорнилом 11/600; «Розмови · + Нова» 18 10 6, 12/500 dim / muted; групи по днях 11 dim; рядки 8 10 r10; профіль над волосиною line2: аватар 30, 13/500, «дім «Сімейна» · 2» 11 dim, settings-2 15 dim | те саме, крім: ціль зветься «Чат» (була «Стрічка», бар уже казав «Чат»); число на «Коморі» — `pantryFacts.soon` (те саме, що крапка в рейці), а не «прострочено» (Р82); рядок розмови двома рядками (Р81); замість settings-2 — шеврон (Р83); «Історія →» у кінці списку (Р84); дім «Назва» · N — із `/v1/me` (`household.name`, `members.length`) |
| шухляда 768 (R2) | 300 поверх, bg, тінь 0 12 40 .18 (= `--sh2`), 14 12, скрім rgba(26,28,30,.28); шапка логотип 32 · вордмарк · panel-left-close 36; цілі 44 r10, 0 12, gap 12, знак 19; рядки 51 (14/500 + 12 dim), 8 12; профіль 48: аватар 32, 14/500 + 12 dim | те саме; знак 18 (`IconSize` не має 19, QUESTIONS §11); скрім — токен `--backdrop` (.35 світла / .55 темна), не .28 кадра (Р85); волосини праворуч у шухляди нема, у рейки й сайдбара — є (кадри) |
| шухляда 390 (R3) | та сама шухляда з «Меню» (кнопка 40 у шапці чату, є з 6b-5); **без цілей** (вони в барі); «+ Нова розмова» 44 r10 card шавлією; групи 12 dim; рядки; профіль 48 із «дім · 3 їдці · профіль» і шевроном | те саме: цілі в шухляді сховані, «Нова» стає карткою, до рядка дому дописується «· профіль» |
| стрічка | над композитором лише композитор («Чат · збірка», Prototype) | капс-рядок «КРАЩЕ НЕ ВІДКЛАДАТИ · …» знято разом зі стилем і тестом «№1» — див. Р80 |

**Р80 · капс-рядок «КРАЩЕ НЕ ВІДКЛАДАТИ · СТЕЙК (сьогодні) · …» над композитором знято.**
У бандлі його нема; факт живе в «Дім зараз»: `store/homeNow.ts` бере ті самі
партії (`isSoon(days)`, ще й `hasScale`), панель показує «Горить · N» рядками
«назва · дні» і дію «Готуємо». Рядок додатково: (а) не фільтрував `hasScale`
— різниця лише для партій без шкали, а в них `days` порожній; (б) тапом клав
у композитор «Що зробити з X, Y? Їх краще використати першими.» — у панелі
замість цього «Готуємо» (`askBurning`). Дані панель показує ті самі — рядок
нічого не втратив, тому знято, не в реєстр-на-рішення. Разом пішов флеш
рядка (`railFlash`), який більше ніщо не читало.

**Р81 · рядок розмови в сайдбарі: Prototype — один рядок 36 (назва 13),
R1/R2/R3 — два рядки 51 (назва 14 + стан/час 12).** Зроблено два рядки
(команда: «рядок = назва 14 + стан/час 12»). Другий рядок — час `HH:MM` із
`created_at` (як було); «чекає рішення» / «паста · приготовано» (R3) — стан
із карток дому, який знає лише Feed (`housePending`), у TabBar його нема —
без нового стору не зробити, тільки вигляд.

**Р82 · число на «Коморі» в сайдбарі.** Кадр: «10» = прострочено (R2:
«113 · 10 прострочено»). Код: `pantryFacts.soon` — партії з `isSoon(days)`
(≤ 3 дн включно з простроченими) — той самий факт, що крапка в рейці з
6b-5. Окремого «прострочено» в `store/pantryFacts.ts` нема (є в
`homeNow.overdue`, але той стор живе в Feed).

**Р83 · settings-2 у рядку профілю (Prototype nav).** У словнику Icons
такого знака нема; новий `system`-знак тягне запис у спільний `motion.ts`
(тест «кожен system-знак має запис»). Поставлено шеврон `sys.next`, як R3
малює на 390. Якщо дизайн-чат дасть знак — один рядок в icons.ts + motion.ts.

**Р84 · «Історія →» у кінці списку розмов.** Кадри його не мають; це єдиний
вхід до архіву розмов (`Feed.openHistory`, `TabBar.tsx:140`) — сайдбар
показує 6 останніх. Лишено рядком 12 dim; зняти можна, коли архів матиме
інший вхід.

**Р85 · скрім шухляди** — кадр rgba(26,28,30,.28); код — токен `--backdrop`
(.35 / .55). Один токен на всі скріми (шторки, меню), у Cook/Share теж —
не дублюю. Якщо дизайн-чат хоче світліший — правка токена, спільний файл.

Пари (обидві теми, `docs/superpowers/plans/side-by-side/`): `nav-1440`
(Prototype, `--dc-click` логотип → розгорнутий nav), `rail-1440` і
`sidebar-1440` (R1, ліва й права панель кадра), `drawer-768` (R2),
`drawer-390` (R3), `feed-strip-1440` (Screens «Чат · збірка» — стрічка без
рядка). Стенд — `scripts/stand-seed.mts` (:3010, памʼять), vite :5190,
сесії по днях — `--stub-json /v1/sessions=…` (стенд розмов не сіє). Жодного
запису в базу. Гігієна: hex у TabBar → 0, гейти зелені, 542 тести.

## Р86–Р91 · пакет правок 1 (FIXES-V3 №1–№23) — відхилення від бандла за рішенням власника

Гілка `fix/v3-pack-1` від main (e67a4b8), 11.09. Кожен рядок — свідоме
відхилення від кадру, підстава — рішення власника з FIXES-V3.md. Решта
пунктів пакета (№1, №3–№10, №12, №16–№21, №23) — правки в бік кадру або
логіка, ухвалена там само; у реєстр не йдуть. Пари «було · стало» — обидві
теми, `docs/superpowers/plans/side-by-side/pack-1/` (інструмент
`scripts/before-after.mjs`: два локальні сервери, main зі `.worktrees/stand-main`
і гілка, стенд `scripts/stand-seed.mts` у памʼяті).

**Р86 · лендінг і auth-екрани завжди світлі** — підстава: пакет правок 1, №2.
Бандл (Landing Live, Auth) малює лише світлу; на токенах темна приходила сама
й у темній системній темі на телефоні лендінг вмикався темним. Тепер `/`,
`/sent`, `/invite`, `/link/*` тримають `data-theme='light'` (`theme.ts`
`LIGHT_ONLY_PATHS` з першого кадру + `lib/useLightOnly.ts` на монтуванні);
провізорні правила темної з `Landing.module.css` / `Auth.module.css` знято.
**Р42 у спільному `DEVIATIONS-V3.md` (темна лендінгу, QUESTIONS §16) —
переписати після мерджу: питання закрите рішенням власника, темної лендінгу
не буває.** Спільний файл цей потік не чіпає.

**Р87 · тости на ≤768 — угорі під шапкою, змах угору** — підстава: пакет
правок 1, №11. Errors E3 ставить тост «знизу по центру над композитором»;
на мобайлі він перекривав останні репліки. Тепер `Toast.module.css` при
≤768 — `top: 64 + safe-area`, вхід/вихід угору, `pointer capture` для змаху;
час тримає компонент (4 с / 8 с з дією, `onDismiss`). Десктоп — як E3, знизу
(рішення пізніше). Тости невдалого завантаження екрана (Комора, Список,
Рецепти, Календар) без `onDismiss` стоять до «Повторити» — інакше екран
лишався б порожнім без дороги назад.

**Р88 · кнопка «панель» унизу, в рядку профілю** — підстава: пакет правок 1,
№13. Prototype nav і R1 ставлять `panel-left-close` біля лого / вгорі рейки.
Тепер `TabBar.tsx` `.foot`: рядок профілю = аватар · імʼя · дім (увесь рядок
веде в профіль, шеврон `sys.next` знято — Р83 закрито) · кнопка 36 r10 при
правому краї; у рейці — та сама кнопка 38 над аватаром. Логотип лишається
тоглом (Prototype nav).

**Р89 · наведення на рейку — сайдбар оверлеєм** — підстава: пакет правок 1,
№14 (зразок — сайдбар чату Claude). Бандл оверлею з наведення не малює;
закріплений стан — за R1 (контент зсувається). Код: `.wrap.peek` (z-index
30, `--sh2`), затримка згортання 150 мс (`PEEK_LEAVE_MS`), лише
`(hover: hover) and (pointer: fine)` — на дотику тап по кнопці; клік по
кнопці закріплює через наявний `kos-nav-expanded` (`store/nav.ts`; ключа
`kos-rail-hidden`, названого в команді, у навігації нема — це ключ панелі
артефактів). Розкладку 256 несе клас `.wide` (закріплено АБО оверлей),
зсув контенту — як і був, `body.nav-expanded`.

**Р90 · хедери карток профілю — чорнильні** — підстава: пакет правок 1, №15.
Screens «Профіль · 1440» малює заголовки «Дім» / «Акаунт» простим рядком
15/600 зі знаком на білому. Тепер `.svcHead`: 44, ink, знак 16 + назва 14/600,
скруглення від картки — як зони комори; те саме на «Мережі» й «Нотатки»
(картки сторінки). Артефакти й панелі («Склад»/«Кроки» рецепта, подія,
підписки, «Дім зараз») — ні. **Знака для «Нотаток» у словнику нема** —
хедер без знака; питання дизайн-чату (§21 у QUESTIONS-cook-share.md, коли
той з'явиться в потоці; поки — тут).

**Р91 · пілюля сесії без меню** — підстава: пакет правок 1, №22 (знімає
№20). Responsive G1 малює випадайку розмов із пілюлі (340). Перемикач
розмов тепер один — сайдбар/шухляда: тап по пілюлі викликає те саме, що
кнопка «панель» ліворуч; шеврон стану «розкрито» і `SessionsMenu` знято
(`dayLabel` і `SessionRow` лишились у тому ж файлі для TabBar). «+ Нова»
у шапці лишається.

**Не в реєстр, але сказати:**
- №7 (назва рядка комори) — правило власника «заголовок = продукт, підрядок
  = бренд · різновид» реалізовано у `filter.ts` (`name = product ?? label`);
  на домі власника 94 зі 113 позицій несли бренд і різновид двічі. Звіт із
  таблицею — у PR.
- №10 — лок 400 мс (DA2-03, мокрий палець) лишено ЛИШЕ на «Крок готово»:
  з відкритим маршрутом подвійний тап відмітив би ще й наступний крок;
  навігація маршрутом, сегментами й «Назад» — без лока. Борг Р76
  (відновлення сесії на кроці > 0 губило таймер) закрито побічно: таймери
  кроків живуть у `timersRef` і сесії (`done`, `timers` у `CookSession`).
- №12 — блок «Чекають на тебе · N» у тій самій картці панелі під підвалом;
  знаки рядків — куди пише картка (`sys.pantry` · `sys.list` · `sys.recipes`
  · `sys.calendar` · `cook.done` для журналу); §12 для дизайн-чату відкритий.

## Р92–Р96 · пакет 2 (FIXES-V3-2 №24–№36) — відхилення від бандла за рішенням власника

Гілка `fix/v3-pack-2` від main (b3f197b), 11.09 — PR A (№24, №26–№36);
№25 — PR B окремо (Р96 допишеться там). Пари «було · стало» — обидві теми,
`docs/superpowers/plans/side-by-side/pack-2/`, записи 5–10 с для рухів.

**Р92 · чіп «Дім ●●● N» на вузькому контейнері (704–963)** — підстава:
пакет 2, №27. Responsive R2 малює лише компактні чіпи стану без «Дім
зараз». Тепер `ChatHead.module.css` у формі mid тримає згорнуту форму G3
(крапки родів + число) поруч із компактними чіпами; тап відкриває панель —
QUESTIONS §14 («як відкривати панель на 768») закрито, чіпи стану знову
ведуть до своїх дій (прострочено → комора, готуємо → Cook).

**Р93 · «думаю» — у стрічці крапками, не словом над композитором** —
підстава: пакет 2, №28. Responsive D3: «очікування словом, не крапками»,
смуга «✦ Думаю · 0:02 · Стоп» над композитором (Components «Стани дії»).
Тепер хід очікування стоїть у стрічці на місці майбутньої відповіді
(`Feed.tsx` `data-wait-turn`): дієслово реального процесу («Думаю» /
«Розбираю» — вкладення), три живі крапки (`.thinking`, Моушн-кіт §02),
справжній час; після 45 с — «Ще тримаю» / «Довгий чек, ще тримаю». Смуга
над композитором лишилась для ліміту · мережі · «нічого не змінилось» ·
конфлікту; «Стоп» — лише в слоті композитора.

**Р94 · одне головне гніздо композитора** — підстава: пакет 2, №29.
Screens «Чат · збірка» і Prototype малюють mic 42 і send 42 поруч. Тепер
одне гніздо праворуч: порожнє поле → мікрофон; є текст чи вкладення →
«надіслати» на тому ж місці; диктовка — гніздо «слухаю»; модель думає й
чернетки нема — «Стоп». Морф між станами — той самий `btn-morph` 150 мс.
Разом — №30: підпис «⌘K» з поля знято (клавіша працює, Feed.tsx).

**Р95 · артефакт праворуч від планшета, шторка лише < 600** — підстава:
пакет 2, №34. Responsive R2 (768) малює «артефакт — шторка знизу». Тепер
один поріг `ARTIFACT_SHEET_MAX = 599` (`store/panel.ts`) на всі артефакти:
< 600 — шторка з грабером і скрімом; 600–1199 — плавуча картка 380 праворуч
(G6: card r18 --sh2, без скріму, закриває ⊟); ≥ 1200 — панель у потоці.
Календар (`useMedia(ARTIFACT_SIDE)`) і «Дім зараз» (контейнер стрічки) — той
самий поріг.

**Р96 · календар 1440 за Redesign «v3 · Календар · сітка» (6c), не за
Screens D3a** — підстава: пакет 2, №25 (рішення власника: «більше
функціоналу — види, фільтри-чіпи, сьогодні / тиждень / триває»). PR B, гілка
`fix/v3-pack-2-calendar`. Що зроблено на ≥1024 (`Calendar.tsx` +
`MonthView.tsx`): шапка «Вересень 2026» ‹ › «Сьогодні» · сегмент «Місяць ·
Тиждень · Список» (памʼять `kos-cal-view`) · «+ Подія»; чіпи тривалого + легенда
за родами, що є в даних; місяць сіткою (смуги тривалого 5 r3 над датами,
до чотирьох на тиждень + «+N», точкове рядками в клітинках, сьогодні колом
26 на bg-підкладці, «кінець сезону» бурштином); права колонка 340: сьогодні
(подія дому шавлією, готування з кроком із `kos-cook-live` → Cook Mode,
«Що на вечерю завтра?» → чат із цим текстом через `composePrefix`), «Цього
тижня» (день · знак · подія · нотатка/обмеження), «Триває» (крапка роду ·
назва · «12 з 48 · до 16.10» / «до ≈ 30.09»), рядок «Приховані сезони · N ·
Свята: …» → підписки. «Тиждень» — картка тижня D3a (той самий код), «Список»
— стрічка днів D3b. Дані ті самі (`/v1/events`), нових нема.
Свідомі відмінності від кадру: легенда «готували · чек · подія» — у нас за
родами, що є в даних, без «чек» (чеків у календарі нема); «Сільпо · 14» /
«Закупка · список 6» у клітинках — теж нема даних; ‹ › — `sys.prev`
(chevron-left, як у кадрі) — глиф уже стоїть на `landing.toPanel` «У панель»,
колізія в `PENDING_DESIGN_CHAT`. **390 — без кадру в Redesign, лишається
D3b** (стрічка днів); шапка 390 — AppHeader з «Сьогодні · Підписки · Подія»
(№26).

**Не в реєстр, але сказати (пакет 2):**
- №24: кадр D1 малює кікер «КУХНЯ» над карткою і стрілку «→» у наслідку —
  капс знято (Р20), над репліками службового рядка нема (6b-5), а
  «→» у тексті забороняє канон 1.5 (гліфи-символи), знака arrow-right у
  словнику нема — наслідок читається кольором (sage) і місцем. Знак
  галереї для аркуша джерел на 390 — `sys.gallery` (lucide `image`, як у
  кадрі), доданий до словника і `motion.ts` (статичний). Drop перевірено
  у Chromium проти стенда (справжній DataTransfer/File → POST
  /v1/attachments у памʼять); на проді з цього чату не перевіряв — потрібна
  сесія власника.
- №31: підзаголовок «Нотатки · те, що зʼясувалось…» стоїть у чорнильному
  хедері 44 (Р90) — переносом він зробив би хедер дворядковим, тож
  обрізання з `title`; сама нотатка переноситься в межах колонки.
- №32: `.doccard` потрібна ще чотирьом карткам без власної поверхні
  (список · рецепт · подія · фото готування) — лишена, але переодягнена в
  поверхню v3 (card r14 + тінь, без заливки --line і рамки); пропозиції й
  рецепт-слід — без обгортки. Картка події в стрічці досі має капс-кікер 10
  (до-v3) — борг копі.
- №35: один механізм — `lib/useSheetDrag` на Sheet, шторці артефакта, «Дім
  зараз», шторці кроків Cook. Шухляда розмов на 390 — горизонтальна
  (translateX), змах униз до неї не стосується; змах уліво не робив.
- №36: міра — тап → рух 56 мс, тап → плашка 56 мс (було 51 / 1621 при
  сервері 1,5 с); `transform: scale` на картці зони не було й нема, exit —
  лише висота й opacity.

## Р97–Р99 · пакет 3 (FIXES-V3-2 №24a, №41, №42, №43, №44) — відхилення за рішенням власника

Гілка `fix/v3-pack-3` від main (5fd831a, бандл v3.1 уже в `ai/project`),
12.09. Пари — `docs/superpowers/plans/side-by-side/pack-3/`: №24a і №41
проти бандла (Prototype dragging · Components A4), №42 і №43 — «було ·
стало»; обидві теми; запис drop 5 с (`24a-drop-flow-*.webm`).

**Р97 · гніздо композитора завжди чорнильне** — підстава: пакет 3, №42
(рішення власника, уточнення №29): «одне гніздо дії завжди видиме». Screens
«Чат · збірка» малює світле коло з muted стрілкою при порожньому полі.
Тепер `.frame-btn` (ink) на всіх станах: мікрофон білим при порожньому,
стрілка білим при тексті, квадрат `sys.stop` при «думаю» і під час
диктування (A4). Світлого idle нема; там, де диктовки нема (браузер без
SpeechRecognition), — те саме чорнильне гніздо зі стрілкою, без пригашення
(`.frame-btn-idle`). Пари 42-slot-{empty,text,thinking,listening}.

**Р98 · drop → хід у розмову одразу, без чіпа** — підстава: пакет 3, №24a
(«drop → одразу картка чека в стрічці + панель, без чіпа над
композитором»). Prototype v3.1 — джерело поведінки: `onDrop → receipt()`.
Стани Responsive D1–D3 (картка «Зараз прийму» в стрічці, чотири секунди
утримання, «озброєний» композитор, чіп над композитором, пунктирна рамка
1.5) зняті разом із `DropCard`; замість них один оверлей
`components/DropZone/DropOverlay` (inset 12 · r16 · 2 px dashed sage ·
sage-bg 70 % · receipt + «Кидай — розберу» 16/600 + «чек, фото полиці або
текст» 13 muted, pointer-events none). Кинуті файли вивантажуються і йдуть
ходом без тексту тим самим шляхом, що «↑» (`submit`, черга та сама); чіпи зі
скріпки й чернетка лишаються на місці. Це зміна моменту запису (раніше drop
лише чіпляв файл, хід ішов по «↑») — за прямим формулюванням власника в
№24a; на стенді: drop → POST /v1/attachments → POST /v1/chat з вкладенням
(пара 24a-dropped-1440). Картку чека дає модель — на стенді її нема
(стаб-відповідь), на проді перевіряє власник (№39).

**Р99 · шапка Рецептів однакова на обох вкладках** — підстава: пакет 3,
№43. Screens «Журнал · 1440» малює шапку без «Записати свій», а підпис
кадру каже «пошук з 8 записів». Тепер одна шапка `RecipesHead` на обидві
вкладки: сегмент · пошук («Знайти рецепт» / «Знайти в журналі», завжди) ·
«Записати свій»; на 390 — знаки пошуку, сусідньої вкладки і «записати».
Порожні стани — один патерн (`Recipes.module.css .empty`, Components
«порожньо»): пунктирна картка r14 на місці першої картки сітки (ширина
однієї колонки з 768, ліворуч), заголовок 16/600 + текст 14 muted; копі без
змін. «Нічого за фільтром / пошуком» — той самий контур, суцільний.

**Не в реєстр, але сказати (пакет 3):**
- №44: заставка порожнього чату («Що зʼявилось удома або що готуємо?» +
  📷 🧾 🎙 + «Перелічити текстом», показувалась при порожній коморі) знята
  разом зі станом `pantryCount` і `.empty` у Feed — порожня розмова = порожня
  стрічка над композитором (Screens «Чат»). Чому гейт `no-glyphs.test` її
  не ловив: (1) набір GLYPHS — названий список, емодзі в ньому не було;
  (2) скан ішов UTF-16-одиницями, а емодзі — сурогатна пара: `clean[i]`
  віддавав половину, яку жодна перевірка не впізнала б; (3) поруч зі
  словами гліф рахувався «фразою для копірайтера», не падінням. Тепер:
  скан кодовими точками, `\p{Extended_Pictographic}` (крім © ® ™) — гліф,
  і емодзі падає завжди, навіть у фразі. Пара 44-chat-empty-1440.
- №41: у Components A4 хвиля — 12 барів fixed поруч із почутим текстом;
  власник просив «на всю ширину поля». Зроблено обидва стани: поки нічого
  не почуто — риски на всю ширину (плейсхолдер «Слухаю…» знято, хвиля і є
  «слухаю»), з першими словами — 12 барів (69 px) + почуте 14 ink одним
  рядком, прокрученим у хвіст + «0:07» 13 muted tabular; усе на осі 42.
  Композитор — кільце шавлією 1.5, «+» → мікрофон на sage-bg (пульс на
  знаку), гніздо → «Стоп». Поле на час запису сховане (`hidden`), фокус
  повертається ефектом після onEnd.
- Знаки бандла поза шкалою словника (IconSize 12/16/18/20/24): receipt 28 в
  оверлеї → 24; «+» 19 і пункти меню 17 у Prototype → 20 і 16 (як були).
- Оверлей у Prototype накриває `main` (колонку чату); у нас — `.screen` (та
  сама зона: рейка зовні).
- Інструменти: `before-after.mjs` і `side-by-side.mjs` отримали
  `--fake-media` (фейковий мік Chromium → жива хвиля), `--fake-speech TEXT`
  (підмінений SpeechRecognition → interim-слова), `side-by-side.mjs` ще
  `--dc-eval` / `--app-eval` (стани, яких клік не дає: dragging). Темна пара
  з Prototype/Components: тло полотна там зі стану бандла, не з
  `.v3[data-theme]` — перекривається в `--dc-eval`.

## Потік landing · auth · помилки · онбординг · знайомство (PR #54, #57, #61, #64, #66)

Файл потоку (правило власника 11.09: по файлу на потік, у спільні — ані рядка;
зведення після мерджів робить власник). Діапазон номерів потоку — **Р41–Р49**, етапи 10–11 — **Р50+**.
Р41–Р48 (лендінг, етап 9) устигли влетіти в спільний `DEVIATIONS-V3.md` разом із
PR #52 до появи правила — тут вони не дублюються. Правило те саме: код —
джерело правди; бандл каже — робиться як у бандлі; бандл мовчить — питання в
`QUESTIONS-landing.md`.

---

### Р49 · Етап 9а: «Перевір пошту» і «Запрошення в дім» за Auth.dc.html — сторінки лендінгу

Гілка `feat/auth-v3` (нащадок `feat/landing-v3`). `MagicLinkSent` і `Invite`
переверстано на спільний каркас `pages/Auth/AuthShell` («як у лендінгу»: шапка
68/60, hero-градієнт, центрована колонка, кікер-піл у роді, заголовок у два
тони, картка 480, підпис). Копі — з кадрів: «Лінк летить» · «Перевір пошту. /
Один клік — і ти всередині.» · «Посилання діє 15 хвилин і працює один раз.»;
«Запрошення в дім · без пароля» · «Одна комора на весь дім. / Тебе запрошують у
«{дім}».» · «Після прийняття все, що є вдома, стане спільним, і саме цей акаунт
буде твоїм тут.»; підписи й смуга конфлікту сесії — дослівно. Поведінка без
змін: маска адреси, таймер 60 с і повтор, «Змінити» → «/», прийняття →
`/v1/invites/accept` → `/app`. Sent тепер читає пошту і з `kos-last-email`
(useMagicLink кладе її туди для LinkGone) — перезавантаження /sent не губить
адресу.

**Прибрано:** `pages/SignIn/` цілком (`SignIn.tsx` з `Mark`, `RingField`,
`SignIn.module.css`) і `MagicLinkSent.module.css`; `SignIn` знято з винятків
no-glyphs — винятком лишилась лише `Admin`. Знаки: блок `auth.*` у кінці мапи
(send «Лінк летить», mail-check «Лист надіслано», user-round-x «Інший акаунт»,
house «Дім»), у `motion.ts` — `null`. `house` уже тримає «Дім зараз» — колізія з
бандла, у `PENDING_DESIGN_CHAT` (QUESTIONS-landing §16 (додаток)). `arrow-left` «На головну» —
`sys.back`.

**Свідомі відмінності від кадру (QUESTIONS-landing §16 (додаток)):**
- аватарів і «3 людини · 61 позиція в коморі» немає — `/v1/invites/info` віддає
  лише пошту, назву дому й роль; зміна контракту — рішення власника;
- назва дому в заголовку у називному («у «Сімейна»»), кадр схиляє («у
  «Сімейну»»): назва приходить з бази, схиляти її автоматично не можна;
- пошти у смузі конфлікту повні (`pylyp@gmail.com`), кадр скорочує до
  `pylyp@…` — повна адреса тут і є суттю попередження;
- ширини: бандл малює 1024 і 390 → ≥768 кадр 1024 fluid, <768 кадр 390;
- недійсне запрошення — `ErrorScreen` (Е1 у коді), кікер без роду до етапу 10;
  стан «Перевіряю запрошення…» — той самий каркас.

**Таблиця «кадр · код»** (рендер, світла, reduce; одне число — збіг):

| Перевір пошту · 1024 | кадр | код |
|---|---|---|
| шапка h | 68 | 68 |
| На головну кегль | 14/400 | 14/400 |
| кікер h | 36 | 36 |
| кікер кегль | 13/500 | 13/500 |
| h1 кегль | 56/700 | 56/700 |
| sub кегль | 17/400 | 17/400 |
| піл sent h | 64 | 64 |
| адреса кегль | 16/400 | 16/400 |
| Змінити h | 48 | 48 |
| Змінити кегль | 14/600 | 14/600 |
| повтор кегль | 13/400 | 13/400 |
| підпис кегль | 13/400 | 13/400 |

| Перевір пошту · 390 | кадр | код |
|---|---|---|
| шапка h | 60 | 60 |
| На головну кегль | 13/400 | 13/400 |
| кікер h | 34 | 34 |
| кікер кегль | 13/500 | 13/500 |
| h1 кегль | 34/700 | 34/700 |
| sub кегль | 15/400 | 15/400 |
| піл sent h | 60 | 60 |
| адреса кегль | 16/400 | 16/400 |
| Змінити h | 48 | 48 |
| Змінити кегль | 13/600 | 13/600 |
| повтор кегль | 13/400 | 13/400 |
| підпис кегль | 12/400 | 12/400 |

| Запрошення · 1024 | кадр | код |
|---|---|---|
| шапка h | 68 | 68 |
| На головну кегль | 14/400 | 14/400 |
| кікер h | 36 | 36 |
| кікер кегль | 13/500 | 13/500 |
| h1 кегль | 56/700 | 56/700 |
| sub кегль | 17/400 | 17/400 |
| картка дому padding | 14px 20px | 14px 20px |
| картка дому r | 20px | 20px |
| назва дому кегль | 16/400 | 16/400 |
| Прийняти h | 56 | 56 |
| Прийняти кегль | 16/600 | 16/600 |
| смуга padding | 10px 14px | 10px 14px |
| смуга кегль | 13/400 | 13/400 |
| підпис кегль | 13/400 | 13/400 |

| Запрошення · 390 | кадр | код |
|---|---|---|
| шапка h | 60 | 60 |
| На головну кегль | 13/400 | 13/400 |
| кікер h | 34 | 34 |
| кікер кегль | 13/500 | 13/500 |
| h1 кегль | 34/700 | 34/700 |
| sub кегль | 15/400 | 15/400 |
| картка дому padding | 14px 16px | 14px 16px |
| картка дому r | 18px | 18px |
| назва дому кегль | 16/400 | 16/400 |
| Прийняти h | 52 | 52 |
| Прийняти кегль | 15/600 | 15/600 |
| смуга padding | 10px 12px | 10px 12px |
| смуга кегль | 12/400 | 12/400 |
| підпис кегль | 12/400 | 12/400 |

Пари: `auth-sent-1024.png`, `auth-sent-390.png`, `auth-invite-1024.png`,
`auth-invite-390.png`; темна: `auth-*-dark.png` (текстів нижче AA — 0, `--dim-t`
як у лендінгу). Знімались зі стабами `--init-storage kos-last-email=…`,
`--stub-json '/v1/invites/info=…;/v1/me=…'` — API і база не потрібні.

### Р100 (у потоці landing — Р50) · Етап 10: помилки за Errors.dc.html — E1 сторінки, E2 смуги (звірено), E3 тости

Гілка `feat/errors-v3` (від main b4ee027). Копі — лише `components/ErrorState/copy.ts`
(канон, HANDOFF §7), логіка станів (401 · 429 · офлайн · unsaved cook · «відповідь
не прийшла» · ErrorBoundary) не чіпалась — змінено вигляд.

**E1 · `ErrorScreen`.** Анатомія кадру 720 × 520: розірване кільце 64/4
чорнилом із сектором-розривом угорі праворуч і крапкою 16 роду (`tone`: danger
— CRASH; amber — SERVER_DOWN, LINK_EXPIRED; sage — LINK_CONSUMED і «лист
пішов»; dim — NOT_FOUND), кікер 12/500 muted без капсу, h1 30/700 −.03em у два
тони (h1b muted; на 404 порожній — заголовок один), тіло 15/1.55 muted ≤ 460,
одна кнопка 44/10 чорнилом (`Button positive` шавлією знято — кадр малює
чорнило), код інциденту — лише коли є, знизу, 11 dim, «інцидент · KOS-…», клік
копіює (поведінка й тести ті самі). `NotFound` більше не передає `code="404"`:
кадр без коду, кікер уже каже, що це. `Logo` з екрана знято. На 390: h1 26,
тіло 15, кнопка на всю ширину внизу екрана (`margin-top: auto`). Виклики:
`ErrorBoundary`, `App` (SERVER_DOWN), `LinkGone` (обидва 410 і «лист пішов»),
`NotFound`.

Свідомі відмінності від кадру: NOT_FOUND cta «← У стрічку» у кадрі, у коді «У
стрічку» — гліфів у тексті нуль (канон), копі канонічне з `copy.ts`; на
LINK_EXPIRED / LINK_CONSUMED між тілом і кнопкою стоїть поле пошти (є з Е1 —
адресу підставляємо, а не питаємо вдруге), кадр його не малює; код інциденту
на локальному стенді порожній (немає Sentry → `captureCrash` не повертає id),
тож на парі CRASH рядок коду є лише в кадрі.

**E2 · смуги — звірено парами, не перероблялись** (етап 5). Різниця кадр →
код, для зведення: радіус 10 → 14; padding 12 14 → 12 16; кікер 12 → 10; h1
14/600 → 16/600; тіло 13/1.45 → 14/1.5; у кадрі весь текст у кольорі роду з
opacity .75/.9, у коді — кікер і знак у роді, заголовок чорнилом, тіло muted;
кнопка 32/8 13/500 → `Button secondary` на card. Смуги «На сьогодні я майже
все. / Дві відповіді — і до півночі відпочиваю.» (денний ліміт, moon) у коді
немає — стан не реалізований (COPY.md «Прийнято 10.09» називає лише текст), і
цей етап логіку не додає. Пари: auth (plum, `/v1/pantry=401`), 429 (amber,
`=429`), офлайн (card, `=abort`) — на /pantry, де рядок стану дії не
змонтований і смуги видно.

**E3 · `Toast`.** Форма кадру: 480, r14, card + sh2, pad 12 14 12 16, gap 12,
крапка 8 роду (`tone`: danger — не вдалось; amber — «Тека не піде», «Максимум N
вкладень»; sage — вдалось: «Скасовано», «Збережу в рецептах…», «Список
великий…»), текст 14/1.4 ink, дія 14/600 шавлією словом. Комора · Список ·
Рецепти · Календар передають `tone="danger"`; Стрічка (Feed) перейшла з власного
тосту (`Feed.module.css .toast`, знято) на той самий компонент — «Скасувати» і
«Повторити» — дія словом, закриває тост сама; тип `kind` отримав `warn`.
Позиція — знизу по центру над композитором (100 на десктопі, 218 на 390 —
папіркат UX-9). Тривалості 4 с / 8 с тримає той, хто показує (Feed: 4/8, undo
8) — без змін. Пари: PANTRY_FAILED (`/v1/pantry=500`), LIST_FAILED
(`/v1/shopping=500`); «Відповідь — ні», «Тека не піде», «Не збереслось» без
пари — потрібен потік у чаті / дроп / профіль.

**Недійсне запрошення** — `AuthShell` з кікером danger («запрошення ·
недійсне», house), «На головну» тим самим чорнильним пілом (Auth.dc.html).

**Інструменти:** `side-by-side.mjs` `--app-sel` (знімок одного елемента
застосунку — смуга, тост — реальною шириною), `--stub-rest STATUS` (решта GET
/v1/* заглушкою, щоб живий API на :3000 не підкидав 401 у стор інцидентів),
значення `abort` у `--stub-json` (обірваний запит = зникла мережа).

**Таблиця «кадр · код» E1** (рендер 720 × 520, світла, reduce):

| CRASH | кадр | код |
|---|---|---|
| кільце | 64/4px | 64/4px |
| кікер | 12/500 | 12/500 |
| h1 | 30/700 | 30/700 |
| тіло | 15/400 | 15/400 |
| тіло max-w | 460px | 460px |
| кнопка h | 44 | 44 |
| кнопка кегль | 14/600 | 14/600 |
| кнопка r | 10px | 10px |
| код | 11/400 | — ◦ |

| SERVER_DOWN | кадр | код |
|---|---|---|
| кільце | 64/4px | 64/4px |
| кікер | 12/500 | 12/500 |
| h1 | 30/700 | 30/700 |
| тіло | 15/400 | 15/400 |
| тіло max-w | 460px | 460px |
| кнопка h | 44 | 44 |
| кнопка кегль | 14/600 | 14/600 |
| кнопка r | 10px | 10px |

| NOT_FOUND | кадр | код |
|---|---|---|
| кільце | 64/4px | 64/4px |
| кікер | 12/500 | 12/500 |
| h1 | 30/700 | 30/700 |
| тіло | 15/400 | 15/400 |
| тіло max-w | 460px | 460px |
| кнопка h | 44 | 44 |
| кнопка кегль | 14/600 | 14/600 |
| кнопка r | 10px | 10px |

| LINK_EXPIRED | кадр | код |
|---|---|---|
| кільце | 64/4px | 64/4px |
| кікер | 12/500 | 12/500 |
| h1 | 30/700 | 30/700 |
| тіло | 15/400 | 15/400 |
| тіло max-w | 460px | 460px |
| кнопка h | 44 | 44 |
| кнопка кегль | 14/600 | 14/600 |
| кнопка r | 10px | 10px |

| LINK_CONSUMED | кадр | код |
|---|---|---|
| кільце | 64/4px | 64/4px |
| кікер | 12/500 | 12/500 |
| h1 | 30/700 | 30/700 |
| тіло | 15/400 | 15/400 |
| тіло max-w | 460px | 460px |
| кнопка h | 44 | 44 |
| кнопка кегль | 14/600 | 14/600 |
| кнопка r | 10px | 10px |

Пари (обидві теми): `errors-crash-*`, `errors-server-down-*`,
`errors-not-found-*`, `errors-link-expired-*`, `errors-link-consumed-*`,
`errors-strip-auth-*`, `errors-strip-429-*`, `errors-strip-offline-*`,
`errors-toast-pantry-*`, `errors-toast-list-*`; власні 390: `errors-crash-390-*`,
`errors-link-expired-390-light`, `errors-not-found-390-dark`.

### Р101 (у потоці landing — Р51) · Етап 11 + №37 + №40: онбординг за Onboarding.dc.html — «Семен» і знайомство одним потоком на /welcome

Гілки `feat/onboarding-v3` (від main b3f197b, PR #61), №37 `feat/intake-v3` (від main
10e4ac6, PR #64) і №40 `fix/intake-layout` (від main 5fd831a, бандл v3.1). Тільки вигляд: `shouldShowOnboarding`, `markSeenLocally`, /welcome,
«Пропустити», події track і запис семи речень профілю (PATCH `/v1/profile/<ключ>`,
«Пропустити» лишає поле порожнім) — без змін.

**«Онбординг · Семен» (≥1024) і «Онбординг · 390».** `pages/Onboarding/` переверстано
за кадрами: шапка 72/60 з логотипом-кільцем і «Пропустити» 14/500 muted; прогрес —
11 рисок 4 (28 активна / 12, ink ≤ кроку, line2 далі) у шапці, на 390 — смуга 3 під
шапкою; сітка 1fr 1fr ≤ 1200 (ілюстрація ліворуч ≤ 560, r24; текст ≤ 520); мета «01 /
11 · Знайомство» 13 (номер 600 ink, «/ 11» dim, крапка 3 line2, тег 500); h1
clamp(28, 3vw, 40)/1.05 −.035em (390: 26/1.1 −.03em); бабл Семена card r 22/22/22/6
pad 20 24 sh (390: r18/6 pad 14 16): репліки 17/1.55 (390: 15/1.5), ремарка з «*» —
окремим рядком шавлією 16/500 (390: 14/500); кнопки «назад» 48 r12 line2 (opacity .35
на першій) і «Далі» / «Почати з того, що є» 48 pad 0 22 r12 ink 15/600 (390: 52, на
всю ширину внизу). Фон — градієнт бурштину 45 % → bg. Теги карток — як у бандлі, без
капсу («ЗНАЙОМСТВО» → «Знайомство»); копі 11 карток — дослівно з масиву ONB у
Prototype (ті самі рядки, що були). Крок памʼятається в `kos-onb-step` (як у бандлі);
тест подій чистить localStorage перед кожним кейсом. Перехід — kos-in / kos-back 24 px;
тривалість `--dur-slow` (400) замість 320/400 бандла — токенами, і під reduce вона
нульова: зсуву немає, картка зʼявляється одразу. Стрічка-карусель О3 із сусідніми
картками по 180, «сіра підложка» ілюстрації, моно-підказка «← → НА КЛАВІАТУРІ» і
власна палітра (23 хекси) — знято; hex поза токенами — 0. Знак «далі» — `sys.next`
(chevron-right; кадр малює arrow-right), «назад» — `sys.back`. Клавіатура ← → і свайп 50
px — як були.

**Знайомство — повним екраном за Prototype v3.1 «Картки знайомства» (№37 → №40).**
Потік один: 11 карток Семена + 7 карток знайомства = 18 кроків на /welcome, крок
памʼятається в `kos-onb-step` наскрізно 0–17. №37 зробив знайомство «сценою як у
Семена» — це була помилка команди; №40 переверстав за кадром Prototype «знайомство»
(меню станів на «П» → «Картки знайомства»): у знайомства СВОЯ розкладка. Шапка 60:
Kitchen OS ліворуч · «Заповню потім» 14/500 muted праворуч (вихід з усього
знайомства — те, що в коді є пропуском етапу: `markSeenLocally` + /app, подія
`welcome_skipped`); риски Семена тут не показуються — під шапкою смуга 7 рисок 4 (ink
≤ кроку, line2 далі) шириною ≤ 620 по центру. Сітка ≤ 900 по центру «текст 1fr ·
ілюстрація 300» gap 32: мета «01 / 7 · Знайомство» 13 (номер 600 ink, «/ 7» dim,
тег 500 muted), заголовок 30/700/1.12 −.035em (min-h 74), підзаголовок 16/1.55 muted
(min-h 52), підпис поля «Мене звати» 13 (ban — danger зі знаком `cook.ban`), поле 56
r28 card sh 16 (placeholder dim), підказка Семена 13/1.5 шавлією + лічильник
`n/max` 13 dim (на ліміті — бурштином текст ліміту картки). Ілюстрація
`/onboarding/profile-<ключ>.png` без рамки: PNG з білим тлом лягає на bg через
`mix-blend-mode: multiply`; у темній темі — `invert(1) hue-rotate(180deg)` + `screen`
(лендінг світлий, тож «інверсія як на лендінгу» — це те саме правило, застосоване тут).
Blend стоїть на контейнері, не на img: контейнер анімується (kos-in) і сам є контекстом
накладання — img усередині змішувався б з порожнечею, і біле тло лишалось би (так і
було в №37 — №38 «білий прямокутник» цим знято; залишок 1–3 рівні у темній — від не
зовсім білого тла PNG, на око нуль). Низ у смузі ≤ 620 по центру: ← 56 r14 контур line2
· «Пропустити» 56 pad 0 16 14/500 muted · «Далі» / «Готово» flex 56 r14 ink 16/600 зі
`sys.next`. 390: та сама шапка 60 і смуга рисок (padding 20), колонка gap 14 — мета →
заголовок 26 (min-h 64) → підзаголовок → поле → ілюстрація ≤ 200 → кнопки на всю
ширину внизу. Семен (кроки 1–11) лишається за етапом 11 — його шапка знову з 11
рисками (18 рисок №37 знято). Копі, ключі полів, ліміти — з `PROFILE_ROWS`; Enter у
полі = «Далі»; ← → і свайп у полі не працюють. Події: `onboarding_started` /
`panel_reached` / `onboarding_skipped {panel}` / `onboarding_finished`, `welcome_finished`
після 18-го; «Заповню потім» — `welcome_skipped {card}` без записів (тест).

**Таблиця «кадр · код» знайомства** (Prototype v3.1, рендер світлий, reduce; поле —
`#intake`):

| Знайомство · 1440 | кадр | код |
|---|---|---|
| шапка h | 60 | 60 |
| Заповню потім кегль | 14/500 | 14/500 |
| рисок · h · смуга w | 7 · 4 · 620 | 7 · 4 · 620 |
| сітка w | 900 | 900 |
| номер · тег кегль | 13/600 · 13/500 | 13/600 · 13/500 |
| h1 кегль · letter-spacing · line-height | 30/700 · −1.05px · 33.6px | 30/700 · −1.05px · 33.6px |
| підзаголовок кегль · колір | 16/400 · rgb(107, 111, 116) | 16/400 · rgb(107, 111, 116) |
| початок речення кегль | 13/400 | 13/400 |
| поле h · r · кегль · фон | 56 · 28px · 16/400 · rgb(255, 255, 255) | 56 · 28px · 16/400 · rgb(255, 255, 255) |
| підказка кегль · колір | 13/400 · rgb(91, 122, 79) | 13/400 · rgb(91, 122, 79) |
| ілюстрація w | 300 | 300 |
| назад · r | 56×56 · 14px | 56×56 · 14px |
| Пропустити h · w · кегль · padding | 56 · 113 · 14/500 · 0 16 | 56 · 113 · 14/500 · 0 16 |
| зазори низу | 10 · 10 | 10 · 10 |
| далі h · w · кегль · r | 56 · 351 · 16/600 · 14px | 56 · 351 · 16/600 · 14px |
| низ w · padding | 620 · 0 40 20 | 620 · 0 40 20 |

| Знайомство · 390 | кадр | код |
|---|---|---|
| шапка h | 60 | 60 |
| Заповню потім кегль | 14/500 | 14/500 |
| рисок · h · смуга w | 7 · 4 · 390 | 7 · 4 · 390 |
| номер · тег кегль | 13/600 · 13/500 | 13/600 · 13/500 |
| h1 кегль · letter-spacing · line-height | 26/700 · −0.91px · 29.12px | 26/700 · −0.91px · 29.12px |
| підзаголовок кегль | 16/400 | 16/400 |
| поле h · r · кегль | 56 · 28px · 16/400 | 56 · 28px · 16/400 |
| підказка кегль · колір | 13/400 · rgb(91, 122, 79) | 13/400 · rgb(91, 122, 79) |
| ілюстрація w | 200 | 200 |
| назад · r | 56×56 · 14px | 56×56 · 14px |
| Пропустити h · w · кегль | 56 · 113 · 14/500 | 56 · 113 · 14/500 |
| далі h · кегль · r | 56 · 16/600 · 14px | 56 · 16/600 · 14px |
| низ padding | 0 40 20 | 0 20 20 ◦ |
| далі w | 121 | 161 ◦ |

Два «◦» на 390 — артефакт кадру, не відхилення: Prototype у режимі 390 стоїть у вікні
1440, тож `clamp(20px, 4vw, 40px)` дає 40; на справжніх 390 той самий вираз дає 20, і
«Далі» відповідно ширша на 40.

**Картка знайомства у стрічці** (`OnboardingCard`) сховано прапорцем
`INTAKE_ON_WELCOME = true`: компонент повертає null, легасі-верстка
(`OnboardingCardLegacy`) і її тести лишаються до мерджу пакета 2 — тоді код картки
прибирається (Feed.tsx / Feed.module.css у №37 не чіпались; тест стрічки перевіряє,
що картки нема).

**Свідомі відмінності від Prototype в знайомстві:** ілюстрації — `profile-<ключ>.png`
з `public/onboarding` (Prototype підставляє `semen-02…08`, це заглушка); «Далі» зі
шевроном `sys.next` (словник; кадр малює arrow-right), «назад» — `sys.back`; лічильник
`n/max` під полем (у Prototype його нема; ліміти — контракт полів, і людині треба
бачити, скільки лишилось). Кадр 1440 бандла Onboarding рендерить ілюстрацію лише з
файлами поруч (`ai/project/apps/web/public/…`) — для пар покладено симлінк на
`apps/web/public/onboarding`, у репо він не йде.

**Таблиця «кадр · код»** (рендер, світла, reduce):

| Онбординг · 1440 | кадр | код |
|---|---|---|
| шапка h | 72 | 72 |
| Пропустити кегль | 14/500 | 14/500 |
| рисок | 12 | 12 |
| риска h | 4 | 4 |
| активна риска w | 28 | 28 |
| номер кегль | 13/600 | 13/600 |
| h1 кегль | 40/700 | 40/700 |
| h1 letter-spacing | -1.4px | -1.4px |
| бабл r | 22px 22px 22px 6px | 22px 22px 22px 6px |
| бабл padding | 20px 24px | 20px 24px |
| репліка кегль | 17/400 | 17/400 |
| ремарка кегль | 16/500 | 16/500 |
| ремарка колір | rgb(91, 122, 79) | rgb(91, 122, 79) |
| назад | 48×48 | 48×48 |
| далі h | 48 | 48 |
| далі кегль | 15/600 | 15/600 |
| далі r | 12px | 12px |
| ілюстрація w | 560 | 560 |

| Онбординг · 390 | кадр | код |
|---|---|---|
| шапка h | 60 | 60 |
| Пропустити кегль | 13/500 | 13/500 |
| рисок | 12 | 12 |
| риска h | 3 | 3 |
| номер кегль | 12/600 | 12/600 |
| h1 кегль | 26/700 | 26/700 |
| h1 letter-spacing | -0.78px | -0.78px |
| бабл r | 18px 18px 18px 6px | 18px 18px 18px 6px |
| бабл padding | 14px 16px | 14px 16px |
| репліка кегль | 15/400 | 15/400 |
| ремарка кегль | 14/500 | 14/500 |
| ремарка колір | rgb(91, 122, 79) | rgb(91, 122, 79) |
| назад | 52×52 | 52×52 |
| далі h | 52 | 52 |
| далі кегль | 15/600 | 15/600 |
| далі r | 12px | 12px |
| ілюстрація w | 350 | 350 |

Пари етапу 11: `onboarding-1440-step01/06/11.png`, `onboarding-390-step01/11.png`;
власні темні `onboarding-1440-step01/06-dark.png`, `onboarding-390-step01/11-dark.png`;
запис `onboarding-1440-transitions.webm` (10 с: Далі ×3, назад, ← →). Бандл Onboarding —
світлий (`.v3` без data-theme), тому темні там лише власні. Пари №40 проти інтейку Prototype v3.1 (обидві теми; темна — через «Темна» в
меню профілю кадру, бо тло секції Prototype інлайнове й `data-theme` його не
перемикає; 390 — `text:390` після відкриття інтейку): `intake-1440-step12-light/dark.png`,
`intake-1440-step18-light/dark.png`, `intake-390-step12-light/dark.png`,
`intake-390-step18-light/dark.png`; запис переходу 11 → 12 (сцена Семена → шапка
знайомства, набір імені) `intake-1440-transition-11-12.webm` (10 с). Пари картки (`onboarding-card-*.png`) —
історія етапу 11, картка більше не показується. `side-by-side.mjs`: кроки `--dc-click`
через « ;; » приймають `text:Підпис` (спершу в кадрі, далі по всій сторінці) і
`rclick:<css>` (contextmenu на першому видимому збігу — меню станів Prototype);
`--stub-rest` глушить усі методи, не лише GET, і не перекриває іменованих стабів;
390-режим Prototype вмикається `text:390` після відкриття інтейку (у мобільному
режимі меню станів сховане).

## Потік design-answers-2 · відповіді дизайн-чату A–F у код (12.09)

Гілка `fix/design-answers-2` (від main 0fa82eb — бандл v3.2 з відповідями).
Тільки вигляд і словник; `TabBar` / `ArtifactPanel` / `Sheet` / `Shell` не
чіпались (скло, чат 2) — за одним винятком, названим у Р111. Джерело —
`ai/project/ANSWERS-FROM-DESIGN-CHAT.md`, «Пакет A–F · 12.09». Пари «було ·
стало» (main :5191 проти гілки :5190, стенд `stand-seed` :3012, обидві теми) —
`docs/superpowers/plans/side-by-side/design-answers-2/`.

### Р110 · Палітра світлої теми (C2): sage `#55724a` · amber `#866425` · plum `#885b77`

`tokens.css` v3-light, `tokens.test` під нові значення, `--export-sage` у
/share — та сама шавлія. Темна без змін. `design-audit` світла, «текст нижче
AA, не dim»: main — Комора amber×3 plum×1, Рецепти amber×4 sage×1, Календар
amber×12, Профіль sage×1 (**22 тонових**); гілка — **0 тонових** на всіх пʼяти
екранах. Лишаються `dim` (33 · 7 · 20 · 9 · 2 — §3, «dim не для тексту», окремий
захід) і `faint` ×5 (дні іншого місяця — за каноном), плюс danger «· сьогодні»
2.69 у шапці Кухні (opacity на пілюлі, було й у main — до окремого пункту).
Темна: лише dim і faint, як і в main. Пари: `01-pantry-*`, `02-chat-head-*`,
`02-home-now-*`, `03-calendar-*`, `02-profile-*`.

### Р111 · Словник (A) і місця вжитку

`icons.ts`: `OPERATORS` (plus · minus · x · check · chevron ×4 · arrow ×3 —
поза правилом «один знак — одне значення»), `EXCEPTIONS` (refrigerator: рейка
— склад, хедер — прилад), `RESERVED` розширено (alert-triangle «Прострочено»,
sun «Сезон», moon «Піст», sun-moon «Тема», house «Дім», bookmark «Колись», heart
«Люблю», trash-2 «Викинути», equal «Нічого не змінилось»). Зміни знаків:
Спеції → `amphora`, Зелень → `leafy-green`, домислено → `circle-dashed`
(`live.inferred`), `live.nothing` → `equal`, `cook.portions` знято (порції
словом), `cook.themeLight/Dark` знято, `live.tradition` → «Свято» (church —
свято й кікер «з традиції», стан обмеження — `live.fast` moon; уточнення
менеджера 12.09), `sys.home` → «Дім» (один референт з `auth.household`). Нові:
`sys.go` (arrow-right «Перейти»), `sys.less` (minus у степері), `sys.hide`,
`sys.toList`, `sys.hint`, `sys.mail`, `sys.trash`, `sys.more`, `sys.notes`,
`live.restrict` (lock). `PENDING_DESIGN_CHAT` — порожній; тест тримає
`OPERATORS.length === 11`, один виняток, і що нові знаки статичні (motion.ts).

Місця: Cook Mode — один тогл `sys.theme` замість сегмента sun/moon (пара
`02-cook-theme-*`); шапка чату «Прострочено N» і квадрат «Дім зараз» при
простроченому — alert-triangle, flame лише «Горить»; рядок профілю «Мені не
можна» — plum з ban (було danger); степери BatchCard / Recipe / cards —
`sys.less`; «використає:» у бібліотеці — словом (§7); порції в Cook і /r/:id —
числом. Календар: піст із каталогу — moon, свято без обмеження — church.

**Єдиний дотик до TabBar** — окремий коміт `b9c4551`: два рядки, поле
`pantryFacts.soon → overdue` (Р113). За конфлікту зі склом перекладається
одним рухом.

Пропущені знаки в бандлі, знайдені звіркою менеджера й виправлені в обох
копіях (`ai/project`, `ai 2/project`): плашка строку в Prototype (JS
`termIcon`) — alert-triangle, не circle-alert; чіпи «Піст · 12 з 46» — moon,
не church; Icons — church «Свято».

### Р112 · Стелі (B4/B5) і порядок у зоні (B7)

`lib/spans.ts`: `capLasting` — ≤ 3 тривалих за рангом обмеження → своє →
сезон, а серед сезонів «починається цього тижня → закінчується найближче»;
`tailLabel` — «ще N сезони» / «ще N». Сітка місяця (замість «до чотирьох +N»),
картка тижня і чіпи «що триває» в шапці беруть одну стелю; хвіст — muted з
бурштиновою крапкою, тап → підписки (`seasons`). Пара `03-calendar-*`: main —
7 чіпів і 4–5 смуг на тиждень, гілка — 3 + «ще 4 сезони», 3 + «ще N сезони».
**Ранг «є в коморі»** (перший у відповіді) не реалізований: `/v1/events` не
віддає звʼязку сезону з коморою (`caught_by` — інше), `seeds` — рецепти.
Потрібне поле в контракті — власник; до нього ранг починається з «починається».

Комора «за місцем»: `byTermThenName` — прострочене вгорі, далі «свіже до» за
зростанням, без строку — внизу (кадр «Комора · збірка»: стейк −9 · айсберг −4
· помідори сьогодні). `lib/useFlipRows` — FLIP на id партії: після правки рядок
їде на нове місце за `--dur-base`; reduced-motion занулює токен. Токени руху —
`--dur-fast 120 / --dur-base 240` (§9; було 150 / 250). Пара `01-pantry-*`.

### Р113 · Число на «Коморі» (B8), смуги E2 (E14), кільце (E13), запрошення (E11)

- `store/pantryFacts`: поруч із `soon` — `overdue` (days < 0 і є шкала);
  TabBar показує `overdue` числом у сайдбарі й крапкою danger у рейці; при
  нулі — нічого. «Горить N» / «Прострочено N» — чіп у шапці чату. Пара
  `05-sidebar-1920-*` (число), `01-pantry-*` (крапка).
- `Strip.module.css` під кадр E2: r10, padding 12 14, gap 12, знак 16 (mt 2),
  кікер 12, заголовок 14/600, тіло 13/1.45, весь текст у кольорі роду на
  його підкладці, кнопка 32/r8 card+ink 13/500; «мине саме» 12 без mono.
  Було: r14, 12 16, кікер 10 mono, заголовок 16 display ink, тіло 14 muted,
  `Button secondary`. **Відхилення від кадру:** opacity .75 (кікер, «мине
  саме») і .9 (тіло) знято — на бурштині .75 дає 2.9:1 і провалює пару, яку
  C2 вивів на 4.5; ієрархія — кеглем і вагою. Тести етапу 5 (`incidents`,
  `errorstate`) читають лише текст і data-атрибути — не мінялись; ActionState
  (рядок «Стани дії» над композитором, 44 px) — інший компонент, не E2, не
  чіпався. Пари `06-strip-401-*`, `06-strip-429-*`.
- `Logo` з параметрами `gap` / `gapStart` / `core`: логотип — gap 45 з
  початком −103 (= «104 15», −58°), екран помилки — gap 90 угорі праворуч,
  вузол кольору роду; CSS-кільце з clip-path в `ErrorScreen` знято. Тест
  `Logo.test` тримає обидві геометрії. Пара `07-ring-404-*`.
- Запрошення: `house` у plum-колі 44 замість аватарів, «для {пошта}» без
  вигаданих чисел. Пари `08-invite-light`, `08-invite-390-light` (auth —
  завжди світлий, №2).

## Потік design-answers-1 · залишок відповідей §1–§13 у код (12.09)

Гілка `fix/design-answers-1` (від main d9a06f5, після #70). Тільки вигляд.
TabBar / Sheet / Shell не чіпались; ArtifactPanel — один коміт, лише зняття
блоку (п. 4). Пари «було · стало» (main :5191 проти гілки :5190, стенд
`stand-seed` :3012, обидві теми) — `docs/superpowers/plans/side-by-side/design-answers-1/`.

### Р114 · Колонки зон (§2), dim (§3), ✕ у рядку (§8), «Чекають · N» (§11), «Дім · тихо» (§13)

- **Колонки зон** — `zoneColumns(w)` за шириною контейнера: одна до 1280,
  дві від 1280, три від 1500 (було 1000 / 1500). 1440 без панелі — контейнер
  1302 → дві; з відкритою карткою позиції (панель 424 fixed) — 876 → одна.
  Пари `01-pantry-cols-no-panel-*`, `01-pantry-cols-panel-*`.
- **dim → muted** для тексту, що читається: 120 місць у 23 файлах (мета,
  підписи, дати, лічильники, «мине саме», ремарки Cook Mode, кікери
  календаря тощо). dim лишається `::placeholder`, `:empty::before`,
  `[data-icon]` неактивним, `:disabled`, крапкам/заливкам (`dot-grey`,
  `kDot`, `rbox-bought`, рамка кнопки), крапці роду 404. Admin не чіпався
  (не продуктовий екран), TabBar / ArtifactPanel — за межею. `design-audit`
  «dim нижче AA»: світла 2 · 33 · 7 · 20 · 9 = **71 → 0**, темна те саме
  **71 → 0**. Лишаються `faint` ×5 (дні іншого місяця — за каноном) і в main
  danger ×2 (див. Р115). Пари `02-dim-profile-*`, `02-dim-calendar-*`,
  `02-dim-chat-*`.
- **✕ у рядку**: десктоп (`hover: hover`) — opacity 0 → 1 при наведенні рядка
  і при фокусі клавіатурою, слот 44 постійний (ширина не стрибає); тач
  (`hover: none`) — `lib/useRowSwipe`: свайп рядка вліво ≥ 48 px зсуває тіло
  на 96 і відкриває «Списати» (trash-2, danger-текст на card — danger ніколи
  заливкою); один відкритий рядок; тап по тілу відкритого закриває, не
  відкриваючи картку; реагує лише на `pointerType: touch`. У картці «Списати»
  завжди; один тап, без модалки, «Повернути» в тості — як було. Тести:
  свайп відкриває / короткий рух — ні. Пари `03-row-hover-*` (1440, наведено
  «Фует»), `03-row-swipe-390-*` (before-after: новий крок `swipe:SEL:left`
  синтетичними PointerEvent тачем).
- **«Чекають на тебе · N»** — чіп у ряду станів ChatHead: card, слово ink,
  знак `sys.mail` muted; wide — «Чекають на тебе · N», R2/G3 — «N»; при нулі
  не показується; тап скролить до першої картки, що чекає (або вантажить її
  сесію). Блок під панеллю (`extra`, №12) знято з Feed і з ArtifactPanel
  (`hasPanel` без нього; поле `extra` у store/panel лишилось невживаним);
  `PENDING_KIND` знято. Пари `04-head-pending-1440/768/390-*` (стаб
  `/v1/cards/pending` на дві картки).
- **«Дім · тихо»** — R2/G3 при нулі станів: «Дім» + «· тихо» muted без крапок
  і числа, `data-home-quiet`; тап так само відкриває панель. Пари
  `05-head-quiet-768/390-*` (стаб порожньої комори й черги).

### Р115 · danger світлої — `#b3463b` (поза списком, те саме правило, що C2)

Після переходу dim → muted аудит показав чіп «Прострочено N» у шапці Кухні:
danger `#b5473c` на `danger-bg #f7e6e3` = 4.42. На крок темніше — `#b3463b`
(4.51 на підкладці · 5.45 card · 4.90 bg). `tokens.test` під нове значення.
У `tokens-v3.md` і бандлі досі `#b5473c` — перенести з наступним експортом
дизайн-чату (`ai 2/project` цією гілкою не чіпався за домовленістю).

## Потік home-now-window (12.09)

Гілка `fix/home-now-window` (від main 53c7ae0). Тільки вигляд; дані й секції
«Дім зараз» без змін. Пари — `docs/superpowers/plans/side-by-side/home-now-window/`.

### Р116 · «Дім зараз» на ≥704 — вікно замість накладки під чіпом (рішення власника 12.09)

`HomeNowPanel`, форма `popover` → `dialog`: центроване по вʼюпорту, скрим
темний (той самий `rgba(26,28,30,.28)`, що в шторки), картка r18 `--sh`,
560 (max 92vw, max-height 88vh зі скролом), padding 20, gap 18; вхід — fade
+ scale .98 → 1 за `--dur-base` / `--ease-enter`, reduced-motion — без
анімації; `aria-modal`; Esc і клік по скриму закривають (було). Фокус: на
відкритті — на ✕ у вікні, на закритті — назад на активний до того елемент
(чіп «Дім зараз»). Шапка як була: house · «Дім зараз» · дата · ✕. Низ вікна
— «Що на вечерю?» однією головною кнопкою чорнилом 44 на всю ширину зі
знаком sys.chat (замість посилання 12 sage); дія та сама — у композитор.
Шторка 390 (G3) — без змін, посилання лишається. Тести `HomeNow.test.tsx`:
форма, фокус, кнопка, шторка. Components «home now» у бандлі досі малює
накладку — оновлюється пакетом дизайн-чату. Пари `home-now-1440-*`,
`home-now-768-*` (вікно), `home-now-390-*` (шторка без змін).
