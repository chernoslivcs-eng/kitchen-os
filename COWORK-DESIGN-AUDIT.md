# Дизайн-аудит: матеріали проти живого продукту

Задача інша, ніж QA-прогони: не «чи працює», а **«чи виглядає й рухається так,
як вирішив дизайн»**. Буквально: відкриваєш дизайн-файл і продукт поруч,
порівнюєш піксель до пікселя, токен до токена, і фіксуєш кожну розбіжність
скріном + посиланням на код.

**Важливий контекст.** Я (Клод у чат-сесії) сьогодні вже зробив власний аудит
і заявив: «база збігається — палітра, моушн-токени, фокус, знак ◌●». Частину
дрібниць виправив (коміти `f7d90dc`, `cb49a1e`). **Не вір мені на слово** — мій
аудит був по коду, не по очах. Твоя робота — незалежна перевірка на рівні
рендера, і окремим пунктом — перевірка моїх заявлених фіксів.

---

## Матеріали

Папка: `~/Downloads/Kitchen OS дизайн-бриф/` — 9 файлів `.dc.html`, v1.0.
Кожен відкривається в браузері як звичайна сторінка:

| Файл | Що всередині |
|---|---|
| `Бренд-кіт.dc.html` | знак ◌●, локапи, іконка, промо, охоронне поле, «чого не робимо» |
| `UI-кіт.dc.html` | кнопки (всі стани), поля вводу, чипи, мета-рядки, рядки списків, степер, таби, тости, модалка, порожні стани, скелетон |
| `Моушн.dc.html` | токени тривалості й кривих, живі приклади кожної анімації |
| `Мінімалізм - розгортка.dc.html` | 5 контрольних екранів напряму «преміум-мінімалізм» |
| `Хендоф - екрани.dc.html` | усі екрани, темна тема |
| `Хендоф - екрани (світла).dc.html` | контрольні екрани світлої |
| `Хендоф - десктоп.dc.html` | десктопні розкладки |
| `Патерни та оверлеї.dc.html` | патерни тла, сторіз-оверлеї (їх у проді немає — скіп) |

## Продукт

- Веб: `http://localhost:5173`, API: `:3000` — обидва запущені.
- Чистий акаунт — провізіонер:
  ```bash
  curl -s -X POST localhost:3000/v1/auth/request -H 'Content-Type: application/json' -d '{"email":"design-audit@example.com"}'
  tail -1 .qa-magic-links.log   # відкрити URL
  ```
- Наповни кухню перед аудитом (інакше половина станів не рендериться):
  кинь у стрічку «купив вершки 33% 400мл, фует 180г і багет, багет почали» →
  застосуй; попроси пропозицію; додай щось у список. Це відтворює контрольний
  стан із хендофа («Стрічка — робочий стан»).
- Теми: перемикач у Профілі або системна. **Кожен екран дивись у темній І
  світлій** — бриф вимагає паритету тем, світла «компаньйон, не дзеркало».
- В'юпорти: мобільний ~390px і десктоп ≥1200px (є окремий десктоп-хендоф).

## Що вже відомо і НЕ є знахідкою

`DESIGN-BRIEF-2.md` у корені репо — список свідомо відкритих питань (4 vs 6
табів, офлайн-стани, степер, свайпи, кільце впевненості, сторіз-оверлеї,
дизайн нових підсистем). **Їх не репорть як розбіжності** — вони вже в роботі
у дизайна. Але якщо по них бачиш щось, чого бриф не згадує — додай реплікою.

---

## Частина I · Токени й компоненти (проти UI-кіта)

Порівнюй не на око, а DevTools-ом: computed styles проти значень у кіті.

### 1. Кольори
- [ ] Випиши з продукту фактичні значення `--accent`, `--amber`, `--plum`,
  `--danger`, `--bg-body`, `--bg-surface`, `--fg` в обох темах (DevTools →
  :root) і звір із палітрою бренд-кіта. Розбіжність навіть на один тон — фіксуй.
- [ ] Семантика застосування: шавлія = дія/applied, бурштин = pending/opened,
  слива = АНТИ, теракота = помилка. Знайди місця, де колір ужито не за
  семантикою (наприклад, бурштин на помилці або шавлія на попередженні).

### 2. Кнопки — всі стани
Кіт: hover = тон на крок; pressed = ще крок + scale .98; focus = кільце 2px
шавлія offset 2px; disabled = 45% прозорості; loading = «Застосовую…».
- [ ] Пройди Tab-ом по стрічці: чи ВСІ інтерактивні елементи мають фокус-кільце
  (кнопки карток, таби, хрестики чіпів, 📎, 🎙, «↑»)? Скрін кожного без кільця.
- [ ] Pressed: чи є scale на вторинних кнопках і на «Ні», чи тільки на primary?
- [ ] Loading-стан на «Застосувати» — присутній? Текст?

### 3. Поля вводу
- [ ] Композитор: default / focus. Кіт має стани «УТОЧНЕННЯ ↳ ФУЕТ?» і
  «ERROR ✕ НЕ РОЗІБРАВ» — чи існують у проді взагалі? (Якщо ні — це знахідка
  рівня «стан із кіта не реалізовано», не бриф-питання.)
- [ ] Плейсхолдер: кіт пише «Записати в журнал…», прод — «Що купив або що
  готуємо?». Розбіжність текстів — зафіксуй, це рішення для дизайна.

### 4. Чипи, мета-рядки, вузли таймлайну
- [ ] Чипи інгредієнтів: ✓ з комори / бракує / ◔ відкрито / АНТИ / ⚠ алерген —
  звір на екрані Рецепта і в пропозиції. Всі п'ять типів є? Кольори за
  семантикою?
- [ ] Мета-рядок картки: формат «18:42 КОМОРА · ◌ ОЧІКУЄ» — збігається?
  Пульсує ◌ ОЧІКУЄ (1.2s, opacity до .55)? Зникає пульс при
  `prefers-reduced-motion`? (DevTools → Rendering → emulate.)
- [ ] Вузли таймлайну 8px із семантичним кольором — є в проді?

### 5. Рядки комори
Кіт: ● свіже/закрите · ◔ відкрите з «ВІДКРИТО · ≈3 ДНІ» · ? здогад із conf.
- [ ] Звір рендер рядка комори з кітом: гліфи, порядок, моно-мітки.
- [ ] Термін «!3дн» проти кітового «≈3 ДНІ» — як показано?

### 6. Тости й модалка
- [ ] Тост: позиція знизу, ✓/✕, тайминги (авто ~4с, з undo ~8с) — заміряй
  секундоміром. Анімація появи Y+16→0?
- [ ] Модалка повного списання (заверши готування зі списанням усієї партії):
  структура проти кітової «Списати молоко повністю?» — заголовок, пояснення,
  дії. (Патерн із чекбоксами — свідома екстраполяція, є в бріфі; але порівняй
  типографіку й відступи.)

### 7. Порожні стани
- [ ] Комора порожня / список чистий: тексти проти кітових, наявність дії.
- [ ] Скелетон завантаження (шимер 1.4с) — існує в проді? Де? Якщо ніде —
  знахідка.

## Частина II · Екрани (проти хендофів і розгортки)

Відкрий хендоф поруч із продуктом, екран за екраном. На кожен — скрін
«дизайн | прод» поруч.

- [ ] **Вхід** (magic link): структура, тексти, «v1.0 · працює офлайн».
- [ ] **Лист надіслано**: маскування email (`p***@gmail.com`), таймер
  «Надіслати ще раз · 0:42» — чи є таймер у проді?
- [ ] **Онбординг**: три CTA (📷 полиця / 🧾 чек / текстом) — збігаються?
- [ ] **Стрічка — робочий стан**: шапка «Кухня / КОМОРА 23 · СПИСОК 4» —
  чи показує прод лічильники в шапці? Композитор знизу, форма карток.
- [ ] **Комора**: сегменти зон (Усе/Холодильник/Морозилка/Сухе), лічильник
  «23 ПОЗИЦІЇ», порядок і форма рядків.
- [ ] **Пропозиція**: блоки ЧОМУ ЗАРАЗ / РЯТУЄ / БРАКУЄ (+ у список) / ІНШІ
  ВАРІАНТИ, пагінація «1/3» зі стрілками — що з цього є, чого немає.
- [ ] **Рецепт**: «ІНГРЕДІЄНТИ · ● З КОМОРИ», кроки з ✓, таймер «▷ 4:00»,
  прогрес «КРОК 3/5» у шапці.
- [ ] **Cook Mode**: великий крок, таймер, «Пауза / +1 хв», блок «НА ЦЬОМУ
  КРОЦІ», «ДАЛІ», підпис «Працює без мережі». Звір усе.
- [ ] **Світла тема**: контрольні екрани зі світлого хендофа — 1:1 по
  структурі, значення токенів світлої.
- [ ] **Десктоп**: розкладки з десктоп-хендофа проти прода на ≥1200px
  (sidebar, ширини колонок, чи не розтягнуті картки на всю ширину).

## Частина III · Моушн (проти Моушн.dc.html)

Живі приклади в файлі анімовані — порівнюй поведінку, не описи.

- [ ] Поява картки: Y+10→0, 250ms. Є? Смикається макет?
- [ ] Apply: перехід ◌→✓ без стрибка макета.
- [ ] Undo: лінія-закреслення зліва направо, потім opacity .6.
- [ ] «Система думає»: 3 крапки зі stagger — прод показує «щось вариться…»?
  Порівняй із кітовим «КУХНЯ · РОЗБИРАЮ» + дієслово.
- [ ] Стрімінг: кіт вимагає чанки по фразі й картку ПІСЛЯ тексту. Прод, здається,
  не стрімить взагалі (відповідь однією порцією) — зафіксуй фактичну поведінку.
- [ ] Cook Mode: зміна кроку вертикальним слайдом 400ms; «без анімацій, крім
  таймера й зміни кроку» — чи щось зайве рухається?
- [ ] Reduced motion: усе миттєве, пульс і шимер вимкнені.

## Частина IV · Бренд

- [ ] Іконка/favicon проти бренд-кіта: знак, охоронне поле, мінімальний розмір.
- [ ] «Чого не робимо» (ковпак/виделка, теплий градієнт, фото за лого,
  спотворення кільця) — перевір, що ніде не порушено, включно з manifest,
  share-сторінкою /r/:id і OG-зображенням.
- [ ] Заставка/сплеш PWA (manifest) — кольори й знак відповідають?

---

## Звіт

`design-audit-report.md` у корінь репо. Формат:

- Номер DA-01…, severity: **major** (порушено явне рішення кіта) / **minor**
  (дрібна розбіжність) / **note** (рішення в кіті відсутнє, прод імпровізує).
- На кожен пункт: скрін дизайну + скрін прода поруч (папка `design-audit/`
  в корені, посилання з репорту), і де можеш — файл:рядок у коді.
- Окремий розділ: **перевірка заявлених фіксів** (тост 4с/8с, pending-пульс,
  «КУХНЯ», тексти порожніх станів) — підтверджено чи ні.
- Окремий розділ: **що в кіті специфіковано, а в проді не існує** (стани
  вводу, скелетон, лічильники в шапці, таймер повторного листа…) — списком,
  це вхід для планування.
- Підсумкова таблиця: скільки перевірено / збігається / розходиться / відсутнє.
