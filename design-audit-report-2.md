# Дизайн-аудит №2 — Cook Mode, аватар профілю, шлях рецепта

**Дата:** 30 серпня 2026
**Збірка:** веб `:5173`, API `:3000` (postgres), після merge `8d9884c`
**Акаунт:** `design-audit-2@example.com`, чиста кухня (вершки 33% 400ml, фета 200g, багет 1pcs opened, помідори черрі 250g, спагеті 500g, олія оливкова 500ml)
**Готування пройдено до кінця:** так — повний цикл, включно з модалкою списання, фото й оцінкою.

**Еталони:**
`~/Downloads/Kitchen OS дизайн-бриф/` — «Хендоф — екрани» (світла/темна), «Хендоф — десктоп», «Мінімалізм», «Моушн», «UI-кіт», «Патерни», «Бриф-2 — рішення».

**Обмеження, які треба знати, читаючи звіт:**

1. **Скріншотів у файлі немає.** Розширення зберігає їх у `/tmp` на Mac, куди мій шелл не дістає. Тому всюди, де в брифі просили «скрін», я даю **computed-значення з живої сторінки** — це строгіше за скрін і його можна перевірити.
2. **Вузький в'юпорт (390px) відтворити не вдалося** — `resize_window` не змінив `innerWidth` (лишався 1728). Замість скріна шапки на 360px я поміряв фактичні ширини її елементів. Пишу це чесно, а не видаю розрахунок за спостереження.
3. **Фото рукописного рецепта я не надсилав** — у мене немає ані камери, ані зображення рукопису. Цей пункт Фокусу C лишився неперевіреним, він так і позначений у фінальній таблиці.
4. **Не звіряв з першого разу — звіряю зараз.** Кілька тверджень першого аудиту я перевірив наново (DA-14, DA-15, DA-17, DA-26) і фіксую їхній фактичний стан, а не переписую.

---

## Що виправлено з попередніх звітів (перевірено, не зі слів)

| Було | Стан |
|---|---|
| **DA-17** Cook Mode світлішав при `data-theme="light"` | ✅ виправлено — режим лишається темним у обох темах |
| **DA-26** статуси на екрані Рецептів | ✅ виправлено — «5 З 7 · ГОТУВАВ 1 РАЗ · БРАКУЄ: … · РЯТУЄ: …», фільтри «Усі / Можу зараз / Майже / Готував» |
| **DA-06** алергія їдця не маркувалась на інгредієнті | ✅ виправлено — `Recipe.tsx:36-42` тягне `eaters[].allergies` окремо від `profile.allergies`, з іменем: «⚠ АЛЕРГІЯ ОКСАНИ» |
| **QA7-01** блокер: `INSERT has more expressions than target columns` | ✅ виправлено — `packages/db/postgres-repo.ts:822` тепер `$1…$11` на 11 колонок. Перевірив наживо: картка «У список» → 3 позиції лягли в Postgres |
| Модалка списання (Бриф-2 п.4) | ✅ реалізована канонічно — див. A-розділ нижче |
| Тост 4с / 8с з undo | ✅ збігається з моушн-кітом — `Feed.tsx:234` |

---

# Фокус A · Cook Mode наскрізь

## A0 · Запуск і навігація кроків

**Точки входу — три, і вони поводяться по-різному:**

| Вхід | Файл:рядок | Стартовий крок |
|---|---|---|
| «Cook Mode» в шапці рецепта | `Recipe.tsx:114` | завжди 0 |
| «Готуємо» внизу рецепта | `Recipe.tsx:219` | `currentStep` (з чекбоксів) |
| «▷ 4:00» на кроці | `Recipe.tsx:192` | `startAt: i` — з того кроку ✅ |

З бібліотеки Рецептів: збережений → `Recipes.tsx:164` → `/recipe` → «Готуємо» → Cook Mode. Працює.

**Тапів від пропозиції в стрічці до першого кроку — 2** («Рецепт →» → «Готуємо»). Це добре: кухня не терпить глибини.

---

### DA2-01 · major · Стороннє «0» на кроках без таймера

`Recipe.tsx:191` і `Cook.tsx:489` — обидва місця:

```tsx
{step.s && ( … )}
```

Коли `step.s === 0`, React рендерить літерал `0`. На екрані рецепта — самотній нуль під кроком; у Cook Mode — нуль там, де мав би бути таймер. Класична falsy-render дірка. Фікс: `{step.s ? … : null}` або `{!!step.s && …}`.

### DA2-02 · major · Повернення на попередній крок не існує взагалі

`Cook.tsx:32` — `const [stepIdx, setStepIdx] = useState(state.startAt ?? 0)`, і єдина мутація в усьому файлі — `setStepIdx((i) => i + 1)` (`Cook.tsx:545`). Прогрес-смуга (`Cook.tsx:184-186`) — це три `<div>` без обробника; `Cook.module.css:68` не має ані `cursor: pointer`, ані `:hover`. Свайпів немає. Місклік безповоротний.

**Еталони мовчать.** Перевірив усі три, які просив бриф:
- Хендоф «екрани» №10 — тільки «Крок готово ✓» + «Приготували»;
- Хендоф «десктоп» Д05 — «Крок готово ✓», кнопки повернення немає;
- «Мінімалізм» №4 — те саме.

Жоден не малює афорданс назад. Отже це не «продукт розійшовся з макетом» — це **дірка в самому дизайні**. Виніс у «Рекомендації дизайну» (Р-1).

### DA2-03 · major · Подвійний тап перескакує крок

Виміряно: два швидкі кліки по «Далі →» → `stepIdx` 1 → 3. Кнопка не має ані `disabled` на час переходу, ані debounce. На кухні палець мокрий і тап-наосліп подвійний — це не гіпотетичний кейс. У парі з DA2-02 (немає назад) помилка стає безповоротною.

### DA2-04 · minor · Кнопка підписана «Далі →», еталони кажуть «Крок готово ✓»

`Cook.tsx:546`: `{stepIdx === total - 1 ? 'Готово ✓' : 'Далі →'}`.

Чотири незалежні еталони пишуть **«Крок готово ✓»** на КОЖНОМУ кроці: `screens-dark`, `screens-light`, `minimal`, `desktop` (Д05). Різниця не косметична: «Далі →» — це навігація («покажи наступне»), «Крок готово ✓» — це підтвердження дії («я це зробив»). Друге чесніше описує, що людина щойно зробила руками, і саме тому в еталоні стоїть ✓, а не →.

### DA2-05 · minor · Блок «НА ЦЬОМУ КРОЦІ» досі відсутній

Дірка з першого аудиту не закрита. У трьох еталонах (`screens-dark`, `screens-light`, `minimal`, плюс `desktop` Д05) під таймером стоїть:

```
НА ЦЬОМУ КРОЦІ            mono 11px #79828c
Фует · ⅔ від 120 г — краї мають загнутись і потемніти   19px/1.5 #c3c9ce
```

Це прив'язка кроку до конкретної партії з комори — єдине місце, де людина бачить «скільки саме з мого». У проді секції немає (`Cook.tsx` має тільки `ДАЛІ`).

### DA2-06 · minor · Немає вторинної «Приготували» у футері кроку

Еталон (`screens-light`, `screens-dark`): футер кроку — два елементи, `flex:1` «Крок готово ✓» + вузька `Приготували` (52px, бордер 1px). Це вихід «я закінчив раніше, ніж ваш список кроків». У проді футер — одна кнопка (`Cook.tsx:540-548`), достроково завершити готування неможливо, тільки протапати всі кроки або вийти й усе втратити.

### DA2-07 · major · Wake Lock немає — екран гасне під час готування

Пошук по `apps/web/src` не знаходить жодного `wakeLock`. Планшет біля плити гасне на другій хвилині тушкування, і людина з мокрими руками мусить його розбудити. Це кухонний кейс №1, тому major, а не note.

### DA2-08 · major · Таймер добігає нуля мовчки

`Cook.tsx:71`:

```ts
if (secondsLeft === 0 && running) setRunning(false);
```

Усе. Ні звуку, ні `navigator.vibrate`, ні повтору. Кіт (розділ «хептик») просить **сигнал, що повторюється кожні 30с до підтвердження**. Таймер, який мовчить, — це таймер, якого немає: людина відійшла, і нічого не сталося.

### DA2-09 · minor · Escape не закриває модалку списання, фокус-трапу немає

У `Cook.tsx` немає `keydown`-обробника для модалки (`Cook.tsx:195-320`). Tab виводить фокус за межі діалогу. Для порівняння — у Стрічці глобальні хоткеї є (`Feed.tsx:209-225`), тобто патерн у команді відомий, просто не застосований тут.

### DA2-10 · note · Плейсхолер одиниці латиницею

Поле «ЛИШИЛОСЬ ≈» показує одиницю як `g` — латинське, у суцільно кириличному інтерфейсі. У Коморі та в картках усюди «г». Дрібниця, але вона одна така на весь екран і тому помітна.

### DA2-11 · minor · Cook Mode на десктопі — розтягнутий мобільний

`Cook.module.css:186-188` — уся десктопна адаптація:

```css
@media (min-width: 720px) {
  .body, .head, .progress, .foot { max-width: 720px; margin: 0 auto; }
}
```

На 1728px це колонка 720px по центру з гігантською вертикальною порожнечею між кроком і футером. Хендоф Д05 має власну розкладку: крок 44px/1.3 на всю ширину, таймер і «+1 хв» в один ряд, кнопка 72px висотою. Нічого з цього не масштабується — кнопка лишається 60px (`Cook.module.css:168`), заголовок кроку не росте.

### DA2-12 · minor · Зміна кроку — стрибок без анімації

`Cook.module.css` не містить жодного `@keyframes` чи `transition` для зміни кроку (є тільки `transition: background` на сегментах прогресу, рядок 73). Моушн-кіт для Cook Mode просить **вертикальний слайд 400ms разом із прогрес-смугою**. У проді контент міняється миттєво.

Позитив: «без анімацій, крім таймера й зміни кроку» дотримано в іншу сторону — зайвого не рухається, `prefers-reduced-motion` обнуляє всі чотири токени тривалості (`tokens.css:142-149`). Це правильно.

### DA2-13 · minor · Розмір головної кнопки нижчий за еталон

Прод: `height: 60px`, `font: 700 18px`, `radius: var(--r-xl)` = 16px (`Cook.module.css:167-177`).
Еталон мобільний: `height: 64px`, `font: 700 19px`, `radius: 16px`.
Еталон десктоп (Д05): `height: 72px`, `font: 700 21px`, `radius: 18px`.

−4px і −1px на мобільному — дрібниця; але на десктопі розрив 60 проти 72 при тому, що це головна кнопка режиму, розрахованого на тап наосліп.

### DA2-14 · note · Сесія готування не переживає нічого

Стан живе тільки в пам'яті компонента. «✕ Вийти» = `navigate(-1)` — прогрес, таймер і крок зникають. F5 — те саме плюс втрата самого рецепта (див. DA2-21). У коментарі коду це визнано чесно (`Cook.tsx:3`: «Якщо колись з'явиться CookRun у БД…»), тож фіксую як відомий борг, а не як сюрприз.

### DA2-15 · note · Асиметрія «крок назад» між екраном рецепта і Cook Mode

На екрані РЕЦЕПТА кроки — чекбокси з тоглом (`Recipe.tsx:95-97`, `Set` add/delete), позначку можна зняти. У Cook Mode повернення немає взагалі. Бриф питає: «навмисна чи дірка?» — **дірка**. Той самий об'єкт (крок рецепта) в одному режимі оборотний, у другому ні, причому необоротний саме той, де людина дивиться руками в тісті. Це не рішення, це наслідок того, що екрани писалися окремо.

---

## Модалка списання — канон Бриф-2 п.4 ✅

Перевірив повільно, по одному кліку (перший, скриптований прохід прогнав модалку і я на секунду вирішив, що cook-run не створюється — записую це як методологічну помилку, не як знахідку):

- заголовок «Що списуємо повністю?» ✅
- хінт «Знята галочка = щось лишилось — партія стане відкритою» ✅
- квадратні чекбокси ✅
- зняття галочки → з'являється поле «ЛИШИЛОСЬ ≈ [g] ОПЦІЙНО» ✅ (`Cook.tsx:270`)
- живий лічильник: «Списати 1» → після зняття «Списати 0» ✅ (`Cook.tsx:307`)
- «Нічого не списувати» ✅
- ввів «100» → помідори черрі стали `{v:100, u:'g', s:'opened'}` у Коморі ✅
- cook-run створено, 5 операцій subtract ✅
- екран «Готово»: «З КОМОРИ · Частково використано 3 позиції · Кількість не вказана в 2 позиціях — комору не міняв, лише відкрив», «← СКАСУВАТИ СПИСАННЯ», «ЯК ВИЙШЛО» ★×5, «ФОТО · 📷 Зняти або додати фото», «Поділитись» / «У стрічку» ✅

Це найакуратніше зроблена частина продукту з усього, що я бачив за два аудити. Формулювання «Кількість не вказана в 2 позиціях — комору не міняв, лише відкрив» — рівно та інтелектуальна чесність, якої бракує решті екранів: система каже, чого вона НЕ зробила.

---

# Фокус B · Аватар профілю

## Стан

Аватар є на всіх 4 табових екранах. `Avatar.module.css`: `34×34`, `border-radius: 50%`, `background: var(--bg-hover)`, `font: 600 13px`. Розмір збігається з рішенням Бриф-2 1б.

### DA2-16 · minor · Правий край аватара плаває по екранах

| Екран | правий край аватара |
|---|---|
| `/app` | 1302px |
| `/pantry` | 1313px |
| `/recipes` | 1037px |
| `/list` | 1256px |

Чотири екрани — чотири різні відступи. Причина: аватар живе всередині шапки кожного екрана окремо, а шапки мають різну ширину контенту й різні `padding`. `/recipes` вибивається найсильніше (−265px від `/pantry`), бо там поруч стоїть пілюля «✎ Журнал».

Око читає це як «інтерфейс дихає» — при перемиканні табів кружечок стрибає по горизонталі. Це той тип розходження, який поодинці не помітний, а в русі помітний одразу.

### DA2-17 · minor · Тап-зона 34px < 44px

Фактичний hit target = `34×34` (`Avatar.module.css:2-4`), без `padding` і без псевдоелемента-розширювача. Рекомендація Apple HIG / Material — 44px. Те саме стосується «←» на `/profile` і `/cooklog`: `38×38`.

### DA2-18 · note · Ініціал для безіменного акаунта

`Avatar.tsx:7` — `(name?.trim()[0] ?? '·').toUpperCase()`. Для мого акаунта показав **«D»** — це перша літера логіна `design-audit-2@…`, бо ім'я в профілі порожнє, і десь вище воно підставляється з email. У макеті — «П» (ім'я). Літера з домену виглядає як ім'я, але ним не є; для нового користувача це буде випадкова літера. Фолбек `·` при цьому не спрацьовує ніколи.

### DA2-19 · note · Шапка Стрічки на вузькому в'юпорті — розрахунок, не спостереження

Змінити в'юпорт не вдалося (див. обмеження №2), тому поміряв ширини компонентів:

```
Кухня 60 + «+ НОВИЙ» 68 + «КОМОРА 6» 58 + аватар 34 = 220px + 3 gap ≈ 236px
```

На 390px це вміщується з запасом. **Але** «КОМОРА 6» — двоцифрове число дасть +8px, а якщо колись з'явиться передбачений кітом «СПИСОК N» — ще ~58px, і 390px стане тісно. Фіксую як ризик, а не як дефект.

### Навігація ✅

Аватар → `/profile` → «←» повертає **на той самий екран, з якого прийшов** (перевірив: `/pantry` → `/profile` → `/pantry`). `navigate(-1)`, а не хардкод на `/app`. Правильно.

### DA2-20 · major · На десктопі профіль лишився в шапці, у сайдбарі його немає

Хендоф Д01 кладе в сайдбар знизу:

```html
<div style="flex:1"></div>
<div class="sit on"><div style="width:26px;height:26px;border-radius:999px;…">П</div> …
```

тобто спейсер + блок користувача внизу. У `TabBar.tsx` немає ні спейсера, ні блока профілю — тільки 4 пункти, притиснуті до верху (`justify-content: flex-start`), а решта сайдбара — порожня. **Це підтверджує DA-14 з першого аудиту: досі не виправлено.**

### DA2-21 · note · «Журнал готувань» у профілі — еталон його теж не має

Бриф питав, чи хендоф №11 кладе журнал у профіль. **Ні.** Профіль в еталоні — це «Сім'я · Одна комора на всіх», «Пам'ять про людей» (АНТИ / АЛЕРГІЯ / ЛЮБИТЬ), «Обладнання». Журналу там немає. Отже єдина точка входу через «✎ Журнал» на Рецептах відповідає еталону. Лишаю note, а не розходження: одна точка входу на екран, до якого повертаються після кожного готування, — тонко, але це рішення дизайну, не регресія.

---

# Фокус C · Додавання рецепта від себе

## Мій чесний шлях людини з бабусиним зошитом

1. Пішов на **Рецепти** — шукав «+». Немає. У шапці тільки «✎ Журнал» і аватар.
2. Подивився в порожній стан — він каже «натисни „Лишити на потім"», тобто відсилає до рецепта, якого ще не існує. Замкнене коло.
3. Пішов у Стрічку й написав природною мовою: «Запиши мій рецепт борщу, бабусин: …».

**Три кроки, з яких перші два — глухий кут.** Це і є знахідка.

### DA2-22 · major · На екрані Рецептів немає жодної точки входу «додати»

Еталон має її явно. `screens-light.html` і `screens-dark.html`, шапка екрана Рецептів:

```html
<div style="font:… 'Onest'">Рецепти</div>
<button style="border:0;background:none;padding:0;cursor:pointer">
  <span class="mono" style="font-size:11px;color:#58754e;letter-spacing:.06em">+ ІМПОРТ</span>
</button>
```

(у темній — той самий елемент кольором `#93b48b`)

У проді на цьому місці — `Recipes.tsx:87-101` — стоїть «✎ Журнал» + аватар. **DA-26 у частині «+ ІМПОРТ» не закрито ні кодом, ні рішенням Бриф-2.** Порожній стан при цьому посилається на дію, яку в цьому екрані виконати неможливо.

### DA2-23 · major · «Запиши мій рецепт» → модель робить нотатку, а не рецепт

Ввід: «Запиши мій рецепт борщу, бабусин: буряк 3 шт, капуста півголовки, картопля 4 шт, цибуля, морква, томатна паста 2 ст.л., м'ясо на кістці. Бульйон годину, потім картопля, зажарка з буряка, наприкінці капуста, дати настоятись».

Отримав **картку `profile` з `kind: note`**, де `label` — увесь рецепт одним рядком. Тобто рецепт поїхав у «пам'ять про людей» замість бібліотеки рецептів. Структури немає: ні інгредієнтів, ні кроків, ні таймерів. Готувати за цим у Cook Mode неможливо — це просто текст у профілі.

Модель мала на вибір картку `recipe` (вона існує і працює — імпорт через вкладення її повертає), але для тексту без вкладення обрала `note`. Дірка в промпті, не в UI.

### DA2-24 · minor · Сирий англійський лейбл «NOTE» на картці

На картці профілю праворуч стоїть **`NOTE`** — латиницею, у продукті, де решта mono-лейблів кирилична («ОЧІКУЄ», «КОМОРА», «З ЧЕКА»). Тип картки протік у UI без перекладу.

### DA2-25 · minor · Відповідь у минулому часі на картці, яка ще ОЧІКУЄ

Текст репліки: **«Записав бабусин борщ…»** — при тому що картка має статус `◌ ОЧІКУЄ` і кнопку «Запам'ятати», тобто нічого ще не записано. Це прямо ламає центральний контракт продукту («нічого не пишеться, поки людина не натисла»): копірайт каже, що дію виконано, а система каже, що ні. Людина, яка вірить тексту, не натисне кнопку.

### DA2-26 · note · До відповіді приклеєне стороннє онбординг-питання

Хвіст репліки: «Є щось, чого ти не їси? Алергії, дієта, просто нелюбе?» — питання з онбордингу, приклеєне до відповіді на запит про рецепт. Модель добирає незакритий слот профілю в невідповідний момент.

### DA2-27 · minor · Inline-стилі замість компонентів у шапці Рецептів

`Recipes.tsx:89-100` — кнопка «✎ Журнал» намальована об'єктом стилів прямо в JSX: `borderRadius: 'var(--r-pill)'`, `padding: '5px 10px'`, `fontSize: 10`, `letterSpacing: '0.06em'`. Токени використані, тож кольори не поїдуть, але це компонент, якого немає в кіті, і наступна така пілюля буде іншою. Те саме в `Cook.tsx:523-533` («У стрічку»).

### DA2-28 · minor · Нативні `alert()` і `confirm()` замість патернів кіта

10 місць у 4 файлах:

| Файл:рядок | Що |
|---|---|
| `Recipe.tsx:66` | `alert('Не вдалося зберегти: …')` |
| `Pantry.tsx:227, 246, 257` | `alert(…)` |
| `Pantry.tsx:251` | `confirm('Прибрати з комори? …')` |
| `Shopping.tsx:48` | `confirm('Перекласти N позицій…')` |
| `Profile.tsx:409, 433` | `confirm(…)` |
| `Profile.tsx:418, 444` | `alert(…)` |

У продукті є свій тост (`Feed.module.css:280`, з `--shadow-toast` і `toast-in` анімацією) і свій `Sheet`. Системний діалог Chrome поверх ретельно зробленої темної теми виглядає як чужий шматок, і на модалці підтвердження («Прибрати з комори?») це найпомітніше — саме там текст написаний з любов'ю, а рамка навколо нього стокова.

### Не перевірено

**Імпорт фото рукописного рецепта** — не мав чим сфотографувати рукопис. Пункт лишається відкритим.

---

# Фокус D · Рецепт у чаті — контент чи плагін

## Стан: це плагін поверх, і код це підтверджує однозначно

### DA2-29 · major · `/recipe` не має `:id`, F5 губить будь-який рецепт — включно зі збереженим

`App.tsx:60`:

```tsx
<Route path="/recipe" element={<RequireAuth><RecipePage /></RequireAuth>} />
```

Немає параметра. Рецепт приходить **тільки** через `location.state`:

- зі стрічки: `Feed.tsx:349` → `navigate('/recipe', { state: { recipe } })`
- з бібліотеки: `Recipes.tsx:164` → `navigate('/recipe', { state: { recipe: r.payload } })`

`Recipe.tsx:81-92` — при відсутності state показує:

> «Рецепт не знайдено. Це не сесія — F5 його не збереже.»

Відтворив: так, саме це.

**Ключове, чого бриф не питав, а варто:** F5 губить і **збережений** рецепт теж. Бо бібліотека передає його тим самим `state`, а не за id. Тобто «☆ На потім» рятує рецепт від зникнення в базі, але не дає йому адреси. Поділитися посиланням на рецепт із пропозиції неможливо — URL один і той самий для всіх рецептів.

При цьому **адресація вже існує**: `App.tsx:65` — `<Route path="/r/:id" element={<SharedRecipePage />} />`. Тобто публічний шеринг за id працює, а власний перегляд — ні. Половина інфраструктури для `/recipe/:id` вже лежить у проді.

### DA2-30 · minor · «☆ На потім» — єдиний спосіб не втратити, і він ручний

`Recipe.tsx:59-68`. Кнопка вторинна, стоїть поруч із головною «Готуємо», і нічого в інтерфейсі не каже, що без неї рецепт зникне назавжди. Людина, яка вийшла подивитись комору й повернулась, рецепт уже не знайде — у стрічці немає лінка назад на згенерований рецепт (`Feed.tsx` після `navigate` не зберігає ані id, ані посилання в тьорні).

---

# Фокус E · Регресія навігації

### DA2-31 · major · Бейдж «Список» показує різні числа на різних екранах

Виміряно наживо: 3 позиції в списку, одна відмічена як куплена.

| Екран | бейдж на табі «Список» |
|---|---|
| `/app` | **3** |
| `/pantry` | **3** |
| `/recipes` | **3** |
| `/list` | **2** (і мета-рядок «2 / 3») |

Причина точна:

- `services/api/src/routes/shopping.ts:17` → `count: items.length` — **усі** позиції;
- `Shopping.tsx:57` → `items.filter((x) => !x.checked).length` — тільки **невідмічені**;
- `Feed.tsx`, `Pantry.tsx`, `Recipes.tsx` передають у `TabBar` серверний `count`, `Shopping.tsx` — свій `unchecked`.

Один і той самий елемент навігації, видимий одночасно, показує два різні числа. Людина відмічає покупку — бейдж на екрані списку падає до 2, вона переходить у Комору й бачить 3 знову. Це підриває довіру до бейджа взагалі, а бейдж — єдине джерело «скільки ще треба купити».

### DA2-32 · major · Правої контекст-панелі 320px немає (підтверджує DA-15)

`document.querySelectorAll('[class*=rail],[class*=context],[class*=aside]').length === 0`.

Хендоф Д01: `.rail { width:320px; border-left:1px solid #dde0e2; background:#f8f9fa; padding:24px 22px }` — «що горить, pending, пропозиція». На 1728px у нас: сайдбар 200 + контент 720 по центру + **~800px порожнечі**. Не виправлено з першого аудиту.

### DA2-33 · minor · Метрики сайдбара розходяться з Д01

| Параметр | Хендоф Д01 | Прод | Δ |
|---|---|---|---|
| ширина | `232px` | `200px` (`TabBar.module.css:74`, `tokens.css:156`) | −32 |
| padding | `20px 14px` | `24px 12px` | |
| висота пункту | `42px` | `45.7px` (padding 12px 14px) | +3.7 |
| gap гліф↔текст | `11px` | `12px` | +1 |
| radius пункту | `12px` | `var(--r)` = 12px | ✅ |
| неактивний колір | `#5c646d` | `#7b838b` (`--fg-dim`) | ✗ |
| активний фон | `#e3e6e8` | `rgb(227,230,232)` = `#e3e6e8` | ✅ точно |
| активний колір | `#1d2126` | `rgb(29,33,38)` = `#1d2126` | ✅ точно |

Активний стан у сайдбарі — заливка-пілюля, як в еталоні, без лівої акцентної смуги. Збігається.

### DA2-34 · minor · У сайдбарі немає логотипа

Хендоф Д01 відкриває сайдбар знаком Kitchen OS (SVG 26px — розірване коло `stroke-dasharray="104 15"` + акцентна точка) і вордмарком `700 17px Onest`, `padding: 0 12px 22px`. У проді `document.querySelectorAll('svg')` всередині сайдбара → **0**. Компонент `Logo` в кодовій базі є (`components/Logo/`), у сайдбар не поставлений.

### DA2-35 · minor · Бейдж у сайдбарі дрібніший за еталон

| | Хендоф | Прод |
|---|---|---|
| font-size | `11px` | `9px` |
| padding | `1px 7px` | `1px 6px` |
| background | `#58754e` | `rgb(88,117,78)` = `#58754e` ✅ |
| color | `#f2f4f0` | `rgb(242,244,240)` = `#f2f4f0` ✅ |
| radius | `999px` | `999px` ✅ |
| шрифт | mono | `IBM Plex Mono` ✅ |

Кольори точні, розмір ні. `.badge` у десктопному медіа-запиті (`TabBar.module.css:96-99`) перевизначає тільки позицію, не типографіку — тому 9px, розрахований на мобільний бейдж-кружечок над гліфом, поїхав у десктопний рядок.

### DA2-36 · note · «Комора» в сайдбарі без лічильника

Хендоф Д01 показує в сайдбарі поруч із «Комора» приглушений mono-лічильник (`margin-left:auto; font-size:11px; color:#7b838b`, напр. `23`). У проді лічильник комори живе тільки в шапці контенту («КОМОРА 6»), у сайдбарі його немає. Бейдж має лише «Список».

### DA2-37 · minor · «Історія» досі відкриває шит, а не сегмент

Пілюля «Історія» (`_seg_90znk_378`) активується візуально, але відкриває bottom sheet «ІСТОРІЯ ЧАТІВ» поверх стрічки. Бриф-2 1б описує сегмент **усередині** Стрічки — контент під шапкою, який їде зі скролом. Заголовок сегмента при цьому вже зроблений правильно, тобто половина рішення на місці. Назви сесій працюють («Що приготувати з того що є»).

### ✅ Глибокі шляхи

`/profile` і `/cooklog` за прямим посиланням рендеряться, «←» працює навіть без історії. Таб-бару там немає — за задумом. «←» 38×38 — див. DA2-17 щодо розміру.

### ✅ Тости — заміряно

`Feed.tsx:234`:

```ts
const ttl = toast.onUndo ? 8_000 : (toast.kind === 'err' ? 8_000 : 4_000);
```

4с без undo / 8с з undo / 8с на помилку. **Збігається з моушн-кітом.** Коментар у коді прямо пояснює, чому відкинули 18с. Це той рідкий випадок, коли рішення задокументоване там, де його читатимуть.

### ✅ Мікрофон — живий і зроблений добре

`Feed.tsx:632-646`, `speech.ts`:

- SVG-іконка (rect + arc + line), не емодзі ✅
- запис → `.mic-live`: `38×38`, `background: var(--danger)`, `border-radius: 10px`, `animation: micpulse 1.2s ease-out infinite` ✅ (`--dur-breathe`)
- всередині `.mic-stop` — квадрат `12×12`, radius 3px ✅
- поле під час запису: `.input-recording` — `border-color: var(--danger)`, кільце 3px ✅
- `lang = 'uk-UA'`, `interimResults = true` → текст летить у поле наживо (`onText` → `setInput`) ✅
- кнопки немає взагалі там, де браузер не вміє SpeechRecognition — чесне прогресивне покращення ✅

### DA2-38 · note · Мікрофон і «↑» співіснують; морфа з кіта немає

Виміряв композитор у двох станах:

| | порожньо | з текстом |
|---|---|---|
| 📎 attach | 48×48 | 48×48 |
| 🎙 mic | 48×48 | 48×48 (лишається) |
| ↑ submit | 48×48, `disabled` | 48×48, активна |

Кіт (Бриф-2 5б) описує **ghost 🎙, який при наборі морфиться в ↑** — тобто одна кнопка, два стани. У проді це дві постійні кнопки: мікрофон нікуди не дівається, а «↑» просто вмикається. Три кнопки 48px з'їдають 144px ширини композитора — на 390px це помітно.

**Стоп по 2с тиші:** `speech.ts` не має власного таймера тиші, покладається на `continuous = false` — браузер сам завершує розпізнавання після паузи, але поріг у нього свій і не 2с. Тобто поведінка є, але вона не наша і не керована.

### ✅ Хоткеї не зламались

`Feed.tsx:209-225`: ⌘K/Ctrl+K → фокус у композитор; ⌘Enter → Apply на найновішій незастосованій картці. Обидва працюють.

### DA2-39 · note · Конфлікт еталонів: гліф Рецептів ❋ проти ✎

- `Бриф-2` п.2: «4 таби канон (**❋** Рецепти)» — 2 входження
- `screens-light` / `screens-dark` / `desktop`: **✎** — 6 / 8 / так

Прод узяв **❋**, тобто пізніший Бриф-2, і це правильне рішення. Фіксую не як дефект, а як **розходження всередині самих матеріалів**: три файли хендофа треба оновити, інакше наступний аудит (людський чи машинний) знову спіткнеться об те саме.

---

# Рекомендації дизайну

Розділ для наступного брифу Клод Дизайну. Тут я не фіксую стан, а формулюю варіанти з їхньою ціною.

## Р-1 · Крок назад у Cook Mode (з DA2-02, DA2-03, DA2-15)

Еталони мовчать — рішення треба ухвалити з нуля. Три патерни, оцінка з позиції «мокрі руки, планшет на стільниці, дивишся згори під кутом»:

**A. Тап по сегменту прогрес-смуги**
Смуга вже є (`Cook.tsx:184-186`), сегменти вже показують done/current. Додати `onClick={() => setStepIdx(i)}`.
*За:* нуль нового UI, повна навігація (не тільки −1, а на будь-який крок), сегмент — це вже карта режиму.
*Проти:* сегмент 4px заввишки — це не тап-зона. Мокрий палець туди не влучить, а невдалий тап поруч нічого не зробить, що читається як «зламано». **Обов'язкова умова: невидима тап-зона ≥44px по вертикалі навколо смуги.** З нею — робочий варіант.

**B. Кнопка «↩» ліворуч від головної**
Футер стає `[↩ 60×60] [Крок готово ✓ flex:1]`.
*За:* найдешевше в реалізації, найочевидніше, тап-зона правильна за побудовою, узгоджується з «Приготували» з еталона (футер уже задуманий як два елементи).
*Проти:* з'їдає ~70px ширини головної кнопки — саме тієї, що розрахована на тап наосліп. І візуально симетрична пара «назад/вперед» перетворює режим-виконання на режим-навігацію, чого дизайн свідомо уникав.

**C. Свайп праворуч**
*За:* нуль пікселів, природний жест.
*Проти:* **найгірший для кухні.** Мокрий/жирний палець свайпає ненадійно, жест невидимий (людина в тісті не гортатиме довідку), і він конфліктує з майбутнім горизонтальним свайпом між кроками, якщо той колись з'явиться.

**Моя рекомендація: A + запобіжник.** Тап по прогрес-смузі з тап-зоною 44px, плюс `disabled` на 400ms після «Крок готово ✓» — це закриває DA2-03 (перескок) тим самим рухом. B тримати в резерві, якщо тестування на реальній кухні покаже, що смугу не знаходять.

**Відкрите питання, яке треба вирішити разом із цим:** що з таймером попереднього кроку при поверненні. Пропоную: **таймер не воскресає**. Повернення означає «я ще не закінчив цей крок», а не «відмотай час». Показувати таймер у початковому стані з «Пуск» — людина сама вирішить, чи запускати. Автостарт тут був би самовпевненістю системи.

## Р-2 · Де живе «+ рецепт від себе» (Фокус C)

**Варіант 1 — «+ ІМПОРТ» у шапці Рецептів → шит із двома входами**
Повертає елемент, який уже намальовано в обох хендофах (значить, дизайн його вже ухвалив). Тап відкриває `Sheet` (компонент є) з двома діями: «📷 Сфотографувати / завантажити» і «✍️ Вписати текстом».
*За:* точка входу там, де людина її шукає — я сам туди ткнув першим. Еталон уже існує, малювати з нуля нічого. Порожній стан нарешті може посилатися на дію, яка на цьому екрані є.
*Проти:* другий канал вводу поруч із чатом. Продукт побудований на тому, що все проходить через розмову, і окремий шит — перша тріщина в цьому принципі.

**Варіант 2 — «+ ІМПОРТ» відкриває Стрічку з префіксованим композитором**
Тап → `navigate('/app')` + `setInput('Запиши мій рецепт: ')` + фокус. Патерн уже реалізований — `startRefine()` у `Feed.tsx:326-329` робить рівно це для «Уточнити».
*За:* точка входу є там, де її шукають, але канал лишається один — чат. Нічого не дублюється, модель отримує явний намір у префіксі (що заодно лікує DA2-23: «Запиши мій рецепт:» — набагато сильніший сигнал для вибору картки `recipe`, ніж вільна фраза).
*Проти:* перекидання на інший екран у відповідь на тап — це маленький розрив. Людина хотіла додати рецепт, а опинилась у чаті.

**Варіант 3 — розгортання просто на екрані Рецептів**
Композитор роз'їжджається під шапкою, як гілка в Бриф-2 1а.
*За:* без переходів, без другого каналу.
*Проти:* другий композитор у продукті. Найдорожче, і найлегше розсинхронізується з головним.

**Моя рекомендація: Варіант 2.** Він дає видиму точку входу (те, чого бракує), не заводить другий канал вводу (те, що бережемо) і побічно виправляє DA2-23 через префікс. Варіант 1 — якщо тестування покаже, що перекидання в чат дратує; тоді шит має слати той самий префіксований запит у чат «під капотом», а не створювати паралельний шлях.

**Незалежно від варіанта — треба полагодити промпт:** текст без вкладення, що явно каже «мій рецепт», мусить давати картку `recipe`, а не `profile/note` (DA2-23). І перекласти лейбл `NOTE` (DA2-24).

## Р-3 · Рецепт як контент чи як плагін (Фокус D)

**Варіант A — стабільний URL `/recipe/:id`**
Кожен згенерований рецепт одразу пишеться в БД (як чернетка), `navigate('/recipe/' + id)`. «☆ На потім» перетворюється з «зберегти» на «лишити в бібліотеці» — тобто з рятування на впорядкування.
*За:* F5 перестає бути катастрофою; посиланням можна поділитися; історія браузера працює; повернення з Cook Mode веде на живу адресу. Інфраструктура наполовину є — `/r/:id` уже працює (`App.tsx:65`).
*Проти:* база наповнюється чернетками, які ніхто не збереже — треба політика прибирання (напр. видаляти незбережені старші за 30 днів). І «☆ На потім» доведеться перепридумати: він більше не рятує, він сортує.
*Що ламається:* нічого критичного. Cook Mode стартує з `recipe`-об'єкта, який однаково приходить, тільки джерело міняється зі `state` на `fetch`.

**Варіант B — рецепт розгортається ПІД карткою у стрічці (патерн Бриф-2 1а, «гілка» з бордюром 2px)**
Ніякого окремого екрана; рецепт стає частиною розмови, як і решта відповідей.
*За:* концептуально найчистіше — рецепт нарешті стає **контентом**, а не плагіном. Скрол не втрачається (нема куди виходити). Історія сесії містить рецепт цілком, тобто «покажи, що ми готували в неділю» працює само собою.
*Проти і що ламається:*
— Cook Mode мусить стартувати з розгортки — це нормально, але кнопка «Готуємо» опиняється всередині стрічки, і після виходу з Cook Mode треба вміти повернутись у ту саму розгортку в тому самому місці скролу. Це не безкоштовно.
— Рецепт — довгий (інгредієнти + 6-8 кроків із таймерами). Розгортка на 1500px висоти всередині чату ховає всю решту розмови. Патерн 1а малювався для коротких відповідей.
— Шеринг лишається без адреси, якщо не додати `/r/:id` окремо. Тобто B **не заміняє** A, а потребує його.

**Моя рекомендація: A зараз, B — як окреме дизайн-рішення пізніше, і тільки якщо взяти на себе довжину.**

Причина проста: A — це інфраструктурний фікс, який нічого не ламає й закриває DA2-29, DA2-30 і половину DA2-14 одним рухом. B — це зміна моделі продукту, і вона впирається в реальну проблему довжини, якої патерн 1а не вирішував. Робити B без A безглуздо (шеринг однаково потребує id); робити A без B — корисно вже завтра.

**Компроміс, якщо хочеться руху в бік B без повної переробки:** лишити окремий екран, але додати в тьорн стрічки постійний рядок-посилання на згенерований рецепт («◇ Борщ · рецепт →»). Тоді рецепт перестає зникати з розмови, зберігає адресу і не роздуває стрічку. Це ~день роботи і закриває найгостріше — «я вийшов і не можу знайти те, що щойно дивився».

---

# Підсумкова таблиця

## Перевірено і збігається з еталонами

| # | Що |
|---|---|
| 1 | Cook Mode лишається темним при `data-theme="light"` (DA-17 виправлено) |
| 2 | Модалка списання — повний канон Бриф-2 п.4: заголовок, хінт, чекбокси, «ЛИШИЛОСЬ ≈», живий лічильник, «Нічого не списувати» |
| 3 | Залишок 100 → партія стала `opened` з `v:100`; cook-run створено, 5 операцій subtract |
| 4 | Екран «Готово»: скасування списання, ★-оцінка, фото, «Поділитись» / «У стрічку» |
| 5 | «▷ таймер» на кроці стартує Cook Mode саме з того кроку |
| 6 | `+1 хв`, «Пауза/Пуск», «Працює без мережі · таймер живе локально» — усе на місці |
| 7 | `prefers-reduced-motion` обнуляє всі 4 токени тривалості |
| 8 | Аватар 34px, `border-radius: 50%`, є на всіх 4 табових екранах |
| 9 | Аватар → `/profile` → «←» повертає на екран-джерело, не в Стрічку |
| 10 | Тости: 4с / 8с з undo / 8с помилка — точно за моушн-кітом |
| 11 | Мікрофон: SVG, `.mic-live` 38px + `micpulse 1.2s`, стоп-квадрат 12px, цегляний бордер поля, `uk-UA`, живий interim-текст |
| 12 | ⌘K → композитор, ⌘Enter → Apply |
| 13 | Активний пункт сайдбара: `#e3e6e8` / `#1d2126` — піксель у піксель з Д01 |
| 14 | Бейдж сайдбара: `#58754e` / `#f2f4f0` / radius 999px / mono — кольори точні |
| 15 | Глибокі шляхи `/profile`, `/cooklog` рендеряться, «←» працює без історії |
| 16 | Статуси й фільтри на Рецептах (DA-26 у цій частині закрито) |
| 17 | Алергія їдця маркується на інгредієнті з іменем (DA-06 закрито) |
| 18 | Журнал готувань поза профілем — відповідає хендофу №11 |
| 19 | QA7-01 блокер виправлено, позиції пишуться в Postgres |

## Розходиться з еталонами

| # | Знахідка | Sev |
|---|---|---|
| DA2-01 | Стороннє «0» на кроках без таймера (`Recipe.tsx:191`, `Cook.tsx:489`) | major |
| DA2-03 | Подвійний тап перескакує крок (1→3) | major |
| DA2-04 | «Далі →» замість «Крок готово ✓» — 4 еталони проти | minor |
| DA2-05 | Немає блоку «НА ЦЬОМУ КРОЦІ» — 4 еталони мають | minor |
| DA2-06 | Немає вторинної «Приготували» у футері кроку | minor |
| DA2-07 | Немає Wake Lock | major |
| DA2-08 | Таймер добігає нуля мовчки; кіт просить сигнал кожні 30с | major |
| DA2-09 | Escape не закриває модалку, фокус-трапу немає | minor |
| DA2-10 | Плейсхолер одиниці латиницею «g» | note |
| DA2-11 | Cook Mode на десктопі — розтягнутий мобільний, Д05 не реалізовано | minor |
| DA2-12 | Зміна кроку без слайду 400ms | minor |
| DA2-13 | Головна кнопка 60px проти 64px (моб.) / 72px (десктоп) | minor |
| DA2-16 | Правий край аватара плаває: 1302 / 1313 / 1037 / 1256 | minor |
| DA2-17 | Тап-зона аватара 34px і «←» 38px < 44px | minor |
| DA2-20 | Профілю немає внизу сайдбара (DA-14 не виправлено) | major |
| DA2-22 | Немає «+ ІМПОРТ» на Рецептах — є в обох хендофах | major |
| DA2-23 | «Запиши мій рецепт» → `profile/note` замість `recipe` | major |
| DA2-24 | Сирий англійський лейбл «NOTE» | minor |
| DA2-25 | «Записав…» у минулому часі на картці зі статусом ОЧІКУЄ | minor |
| DA2-27 | Inline-стилі замість компонентів кіта | minor |
| DA2-28 | 10 нативних `alert()` / `confirm()` замість тоста і Sheet | minor |
| DA2-29 | `/recipe` без `:id`; F5 губить рецепт, у т.ч. збережений | major |
| DA2-31 | Бейдж «Список»: 3 на трьох екранах, 2 на `/list` | major |
| DA2-32 | Немає правої панелі 320px (DA-15 не виправлено) | major |
| DA2-33 | Сайдбар 200px проти 232px; неактивний `#7b838b` проти `#5c646d` | minor |
| DA2-34 | У сайдбарі немає логотипа (компонент `Logo` існує) | minor |
| DA2-35 | Бейдж сайдбара 9px проти 11px | minor |
| DA2-36 | «Комора» в сайдбарі без mono-лічильника | note |
| DA2-37 | «Історія» — шит замість сегмента (Бриф-2 1б) | minor |
| DA2-38 | Мікрофон і «↑» співіснують; морфа ghost 🎙 → ↑ немає | note |

## Потребує рішення дизайну

| # | Питання | Куди |
|---|---|---|
| DA2-02 | Крок назад у Cook Mode — **еталони не малюють жодного афордансу** | Р-1 |
| DA2-15 | Асиметрія: на Рецепті чекбокс знімається, у Cook Mode ні | Р-1 |
| DA2-14 | Сесія готування не переживає вихід і F5 | Р-1 / Р-3 |
| DA2-22 | Де живе «+ рецепт від себе» | Р-2 |
| DA2-26 | Онбординг-питання приклеюється до сторонніх відповідей | Р-2 |
| DA2-29 | Рецепт — контент чи плагін: `/recipe/:id` чи розгортка в стрічці | Р-3 |
| DA2-30 | «☆ На потім» рятує від зникнення, але нічого про це не каже | Р-3 |
| DA2-18 | Ініціал з домену для безіменного акаунта («D» замість «П») | — |
| DA2-19 | Шапка Стрічки: запас на 390px є, але «СПИСОК N» його з'їсть | — |
| DA2-21 | Журнал готувань має єдину точку входу (як в еталоні) | — |
| DA2-39 | **Конфлікт усередині еталонів:** ❋ (Бриф-2) проти ✎ (3 файли хендофа) | — |

## Не перевірено

| Що | Чому |
|---|---|
| Імпорт фото рукописного рецепта | немає камери й зображення рукопису |
| Скрін шапки на 360-390px | `resize_window` не змінює `innerWidth` (лишається 1728) |
| Поворот екрана | те саме обмеження в'юпорта |
| Скріншоти взагалі | зберігаються в `/tmp` на Mac, недосяжному з мого шелла |

---

## Дві помітки в кінці

**Методологічна.** Перший прохід Cook Mode я зробив скриптованим циклом кліків — він проскочив модалку списання, і я на хвилину вирішив, що cook-run не створюється. Переробив повільно, по одному тапу. Записую сюди, бо це повторювана пастка: **скриптований прохід не є проходом**; усе, що звітується про UI, має бути побачене в тому темпі, у якому його бачить людина.

**Прибирання.** У `apps/web/public/da/` лежать мої робочі копії дизайн-матеріалів (ASCII-імена, бо кирилицю в шляху мій шелл не читає через NFD/NFC). Вони віддаються з `localhost:5173/da/…` і **не мають потрапити в прод** — скажи, і приберу. Тестові акаунти `design-audit@example.com` і `design-audit-2@example.com` лишились у базі: ендпоінта видалення користувача немає.
