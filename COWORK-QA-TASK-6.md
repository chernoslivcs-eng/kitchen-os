# QA-прогін №6: онбординг, ручна робота з коморою й списками, наміри

Свіжий комміт `7737ead` («QA-5 фікси: 3 blocker + 3 major + 6 minor + provisioner»).

Два головні блоки:

1. **Онбординг — нарешті доступний.** П'ять прогонів поспіль ти не міг створити чистий акаунт. Тепер можеш (див. нижче). Перша репліка продукту — те, чого не бачив жоден прогін.
2. **Ручна робота.** Досі всі прогони йшли через чат. Цього разу — те, що людина робить руками: правки комори, складання списків, наміри «колись хочу приготувати».

Плюс регресія на 12 фіксах QA-5.

---

## Provisioner — тепер працює

`ConsoleMailer` дублює magic-link у файл. Перевірено на цьому коміті.

```bash
# 1. Запросити лінк
curl -s -X POST localhost:3000/v1/auth/request \
  -H 'Content-Type: application/json' \
  -d '{"email":"qa6-a@example.com"}'

# 2. Взяти останній рядок
tail -1 /tmp/kos-magic-links.log
# 2026-08-29T19:37:29.151Z	qa6-a@example.com	http://localhost:5173/v1/auth/verify?token=...

# 3. Відкрити URL у браузері → залогінений на чистому акаунті
```

Файл живе тільки поза продом (`NODE_ENV !== 'production'`) і поза тестами. Шлях перевизначається через `MAGIC_LINK_LOG`.

**Якщо файлу немає** — API запущений до цього коміту, попроси розробника перезапустити.

---

# Частина I · Механіка ⇒ `qa-report-6.md`

## A. Онбординг з нуля — головне цього прогону

Чистий акаунт `qa6-a@example.com`. Порожня комора, порожній профіль.

### A1 · Перша репліка

- [ ] Що на екрані `/app` одразу після входу? Порожній стан із «Скажи, що купив…» чи щось від моделі?
- [ ] Чи є кнопка «📷 Сфотографуй чек — я розкладу» (вона показується при `pantryCount === 0`)?
- [ ] Напиши перше повідомлення — **що відповідає модель?** Це `onboarding-stage1` промпт, який ніхто не бачив

### A2 · Stage 1 → Stage 2

`stage=1` поки комора порожня. `stage=2` вмикається коли ≥3 позицій **і** профіль порожній.

1. Додай 1 позицію → яка поведінка?
2. Додай ще 2 (разом 3+) → **чи модель питає про обмеження/дім/традиції?**
3. Відповідай → чи записує правильно?
4. Після запису — **чи одразу переходить до страви?** (`onboarding-stage2.md` вимагає: «Після запису ОДРАЗУ переходь до страви»)

### A3 · «Нічого немає, без обмежень»

Другий чистий акаунт `qa6-b@example.com`, наповни комору, і на питання про обмеження скажи «нічого немає, я сам».

- [ ] Чи модель нічого не записує?
- [ ] Чи **більше не питає** в наступних репліках? (правило в `onboarding-stage2.md`)

### A4 · Комора з непридатного

Чистий акаунт, додай тільки побутове: «Купив туалетний папір, мийний засіб, серветки».

- [ ] Чи модель каже прямо, що готувати нічого?
- [ ] Чи пропонує докупити мінімум під одну страву? (правило в `onboarding-stage2.md`)

## B. Ручна робота з коморою

Це те, що досі майже не тестувалось — усі прогони йшли через чат.

### B1 · Додавання вручну

- [ ] «+ Додати» → sheet → назва/кількість/одиниця/зона → «Додати»
- [ ] Позиція зʼявилась у правильній зоні
- [ ] Лічильник у шапці збільшився
- [ ] Порожня назва → помилка «Назва потрібна»
- [ ] Кількість буквами («абв») → помилка
- [ ] Кількість порожня → приймається (це валідно)

### B2 · Редагування

- [ ] Тап на партію → sheet із заповненими полями
- [ ] Зміна назви → Save → у списку нова назва
- [ ] Зміна кількості → Save → оновилось
- [ ] Зміна зони → партія переїхала в іншу секцію
- [ ] Зміна одиниці (г → мл) → відобразилось
- [ ] Escape / клік по backdrop → зміни **не** збереглись

### B3 · Стан партії

- [ ] «◔ Відкрито» → крапка змінилась на амбер
- [ ] «● Запаковано» назад → крапка зелена
- [ ] **QA4-04 регресія:** зміни value + натисни toggle **без** Save → чи value збереглось?

### B4 · Видалення

- [ ] «Прибрати з комори» → confirm → зникла зі списку
- [ ] Лічильник −1
- [ ] Партія в БД має `state: depleted`, не видалена фізично

### B5 · Чат бачить ручні зміни

**Ключова перевірка звʼязку.** Після ручних правок:

1. Додай вручну «авокадо 2 шт»
2. Нова сесія → «Що є в коморі?»
3. [ ] Модель називає авокадо
4. Перейменуй вручну «авокадо» → «авокадо стигле»
5. Нова сесія → «Що є?»
6. [ ] Модель називає нову назву, не стару

### B6 · Пошук

- [ ] При ≥8 позиціях зʼявляється поле
- [ ] Фільтрує по підрядку, регістр не важливий
- [ ] Нема збігів → «Нічого не знайшлось за «…»»
- [ ] Порожнє поле → показує все

## C. Складання списків

### C1 · Через чат

- [ ] «Додай молоко, хліб і яйця в список» → одна картка `shopping` з трьома items
- [ ] Apply → усі три на табі «Список»
- [ ] «Прибери хліб зі списку» → картка з `op: remove`
- [ ] Apply → хліб зник

### C2 · Ручні дії на екрані

- [ ] Чекбокс перекреслює позицію
- [ ] Лічильник у шапці рахує **не**викреслені
- [ ] Badge на табі оновлюється
- [ ] × прибирає позицію

### C3 · «→ В КОМОРУ» (unpack)

- [ ] Пілюля зʼявляється тільки коли є ≥1 checked
- [ ] Показує правильну кількість
- [ ] Тап → confirm → позиції зникли зі списку
- [ ] Зʼявились у Коморі як `sealed`
- [ ] Зона й одиниці перенеслись правильно
- [ ] Unchecked позиції лишились у списку

### C4 · Список у контексті моделі

**Відома дірка з QA-4** (таблиця пам'яті: «Список покупок — модель не знає, що вже в списку»).

- [ ] «Додай молоко в список» → apply
- [ ] «А молоко вже в списку?» → **чи модель знає?**

Якщо не знає — це очікувано, зафіксуй як підтвердження. `serializeShopping` не існує, список у промпт не потрапляє.

### C5 · Дублікат у списку

- [ ] «Додай молоко» → apply → «Додай молоко» ще раз
- [ ] Що станеться? Дубль, оновлення, чи модель скаже, що вже є?
- [ ] `findShoppingItemByLabel` у репозиторії існує — чи використовується?

## D. Наміри — «колись хотів приготувати»

Це `wishes` у профілі. QA-4/5 торкались поверхово.

### D1 · Запис наміру

- [ ] «Хочу колись зробити качку по-пекінськи» → картка `profile` з `kind: wish`?
- [ ] Формулювання в `label` — фразою, як сказала людина?
- [ ] Apply → видно на Профілі в блоці «Побажання»

### D2 · Намір спливає, коли доречно

1. Запиши намір: «Давно хочу спробувати різото»
2. Додай у комору рис арборіо, бульйон, пармезан
3. Нова сесія → «Що приготувати?»
4. [ ] **Чи модель згадує різото?** Профіль тепер у контексті (QA4-02), інгредієнти є

Це найцікавіша перевірка прогону: чи намір справді «чекає моменту», чи просто лежить рядком.

### D3 · Кілька намірів одразу

- [ ] «Хочу більше риби, менше цукру, і навчитись пекти хліб» → скільки ops?
- [ ] Кожен окремим `wish`, чи один рядок?

### D4 · Зняття наміру

- [ ] «Більше не хочу вчитись пекти хліб» → `op: remove`?
- [ ] Apply → зник із Профілю

### D5 · Намір vs обмеження

- [ ] wish «люблю гостре» + anti «не їм свинину» одночасно
- [ ] «Що приготувати?» → чи тримає обидва? (QA-5 це підтвердив, перевір що не зламалось)

## E. Регресія QA-5 (12 фіксів)

- [ ] **QA5-01** алерген: додай алергію на горіхи → нова сесія → «Що солодкого?» → **чи мигдаль/горіхи в пропозиції?** (має не бути)
- [ ] **QA5-01 зворотний:** «Дай рецепт з горіховим соусом» → **чи дає рецепт** із попередженням, чи відмовляє? (має давати)
- [ ] **QA5-02** статус картки: «Купив молоко» → **не** застосовуй → «Це вже в коморі?» → має бути «ні, картка чекає»
- [ ] **QA5-03** власні пропозиції: отримай `proposal` із 3 стравами → «Розкажи про другий варіант» → **чи називає правильно?**
- [ ] **QA5-04** техніка: «В мене немає духовки» → apply → `GET /v1/profile` → `equipment: {"духовка": "lacks"}`
- [ ] **QA5-04** наслідок: нова сесія → «Дай рецепт запеченої риби» → **чи пропонує альтернативу?**
- [ ] **QA5-05** межа: зроби 25+ реплік → спитай про першу → «не бачу так далеко», не «цього не було»
- [ ] **QA5-06** маркдаун: «Вечеря на 6 осіб» → якщо прийшла проза — **без зірочок**
- [ ] **QA5-07** дата: «Який сьогодні день?» → правильний
- [ ] **QA5-09** кроки: рецепт → кількість **не** дублюється в тексті кроку, перший символ з великої
- [ ] **QA5-10** opened: рецепт без `v`/`u` → «Кількість не вказана в N позиціях»
- [ ] **QA5-11** статус: «Ні» на картці → заголовок «✕ ВІДХИЛЕНО»

## F. Що НЕ існує — не тестуй, зафіксуй як gap

Перевірив код перед брифом. Цього немає взагалі:

| Функція | Що є | Чого немає |
|---|---|---|
| **Авторський рецепт** (додати свій) | `RecipeRow.origin` має `'imported'` | немає `POST /v1/recipes`, немає UI, немає repo-методу |
| **Уточнення до рецепта** | — | немає нічого |
| **Обрані / збережені рецепти** | `saveRecipe`/`getRecipe` у Repo | немає `listRecipes(user_id)`, немає екрана, немає таблиці favorites |
| **Імпорт рецепта** (фото/лінк) | `recipe_import` у `manifest.json`, `registry.ts`, `usage.ts`, `eval/model-client.ts` | немає маршруту, немає repo — виклик задекларований і не підключений |

Рецепти зараз існують тільки як побічний ефект cook-run: `POST /v1/cook-runs` зберігає `RecipeRow` і повертає `recipe_id`, звідки береться `/r/:id`. Побачити список своїх рецептів ніде не можна — тільки через Журнал, і то як записи готувань.

**Не витрачай на це час.** Одним абзацом у звіті: чи відчувається відсутність під час звичайного користування, і в якому місці ти першим потягнувся б за цією кнопкою.

---

# Частина II · Мова ⇒ `copy-report-6.md`

## Головне: онбординг

**Перша репліка продукту — предмет цього copy-report.**

Повний транскрипт стежки A1-A2, і окремо оцінка:
- Чи зрозуміло, що це за продукт, із першого екрана?
- Чи модель пояснює, як із нею говорити, чи очікує, що людина здогадається?
- Чи питання про обмеження звучить як анкета чи як розмова?
- Де б новий юзер зупинився й закрив вкладку?

## Лічильник мовних дефектів

Так само, як QA-5. Веди рахунок і дай таблицю:

| Прогін | Модель | Реплік | Дефектів | На 10 реплік |
|---|---|---|---|---|
| QA-4 | haiku-4.5 | ~24 | 8 | 3.3 |
| QA-5 | sonnet-4.5 | ~36 | 3 | 0.8 |
| QA-6 | sonnet-4.5 | ? | ? | ? |

Окремо: **чи зник русизм із рецептів** («Соль — тільки перед подачею»). У `recipe-generator.md` тепер є правило про українську з переліком базових кухонних слів.

## Стежка 18 · Мова ручних дій

Копі, яке людина бачить, коли працює руками:

- Sheet «ПАРТІЯ» / «ДОДАТИ ПАРТІЮ» — заголовки, лейбли полів, плейсхолдери
- «Прибрати з комори? Це те саме, що «зʼїли» — до історії лишиться.» — зрозуміло?
- «Перекласти N позицій із «куплено» в комору?» — зрозуміло?
- Селекти зон: «Свіже / Холодильник / Морозилка / Комора / Спеції / Напої» — послідовно?
- Помилки: «Назва потрібна», «Кількість — число або порожнє», «Не вдалося зберегти: …»

## Стежка 19 · Мова намірів

Коли модель повертається до наміру («ти хотів спробувати різото»):
- Звучить як нагадування чи як докір?
- Чи не повторює намір щоразу?
- Чи розрізняє «хочу колись» і «хочу зараз»?

## Стежка 20 · Час дієслова, третій замір

QA-4: 1/6. QA-5: 10/11 (єдиний провал — самосуперечність промпту, виправлена).

Провокуй 6+ карток, дай таблицю. Очікування — 11/11 або 6/6.

## Перевірка правил, доданих у QA-5

- [ ] **Джерело правди:** модель не каже «записав», якщо картка `[НЕ ЗАСТОСОВАНО]`
- [ ] **Межа видимості:** «не бачу так далеко» замість «цього не було»
- [ ] **Алерген як позначка:** на прямий запит дає рецепт із попередженням, не відмовляє
- [ ] **Не питає про причину** обмеження
- [ ] **Українська в рецептах:** «сіль», «олія», «часник», «перець»

---

## Формат звітів

Той самий. `qa-report-6.md` — severity. `copy-report-6.md` — «звучить / читається / не звучить».

**Обовʼязково в `qa-report-6.md`:** таблиця регресії QA-5 (12 рядків, ✓/✗/~).

**Обовʼязково в `copy-report-6.md`:** повний транскрипт онбордингу + лічильник дефектів.

---

## Обмеження

- Три чисті акаунти: `qa6-a@`, `qa6-b@`, `qa6-c@example.com`
- ~40 запитів × sonnet ≈ $0.40-0.60
- Обидва звіти untracked
- Скріншоти в `/tmp/kos-qa6-screenshots/`
- Прибери за собою: тестові акаунти лишаться в БД (ендпоінта видалення юзера немає) — просто зазнач які

## Пріоритет

1. **A** — онбординг, головне
2. **E** — регресія QA-5
3. **B** — ручна комора
4. **C** — списки
5. **D** — наміри
6. Частина II — паралельно з усім

## Час

110-150 хв.

## Deliverables

1. `qa-report-6.md`
2. `copy-report-6.md`
