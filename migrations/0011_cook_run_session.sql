-- Правка №11: готування пам'ятає сесію запуску — журнал веде назад у розмову
-- навколо готування (клік по запису → та сама сесія, без пошуку по
-- повідомленнях). Nullable: старі готування без сесії лишаються валідними,
-- для них клієнт шукає сесію за recipe_link або створює нову.
ALTER TABLE cook_run ADD COLUMN session_id uuid REFERENCES session(id) ON DELETE SET NULL;
