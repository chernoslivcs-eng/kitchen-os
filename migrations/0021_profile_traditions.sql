-- Традиції — явний вибір у профілі, а не здогад із побажань.
-- NULL = людина ще нічого не обирала (тоді традицію далі виводимо з побажань:
-- «постуємо» → православна). Порожній масив = обирала і вимкнула все —
-- це різні стани, і схема мусить їх розрізняти.
ALTER TABLE profile ADD COLUMN traditions text[];
