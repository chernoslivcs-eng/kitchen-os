-- Раунд 4, крок 6: тариф у користувача. Поки один — бета-тест; сторінка
-- профілю показує його рядком «Тариф · Бета-тест» без дії «Змінити».
ALTER TABLE "user" ADD COLUMN plan text NOT NULL DEFAULT 'beta';
