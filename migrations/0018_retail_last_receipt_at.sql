-- Полагодження розходження схеми, а не нова функція.
--
-- 0015 накотилась на прод-Neon 01.09 раніше, ніж у файл дописали колонку
-- last_receipt_at (водяний знак синку чеків). Раннер веде schema_migrations за
-- іменем файла, тож 0015 назавжди позначена застосованою — переписаний файл
-- на прод уже не поїде. Наслідок: на проді таблиця без колонки, а
-- postgres-repo.ts:1039 вставляє її в кожному upsertRetailConnection.
--
-- Зараз це не аварія: M13 живе на гілці retail/silpo і на прод не
-- задеплоєний. Вибухне в мить деплою — підключення до Сільпо, оновлення
-- токена й синк чеків одразу впадуть із «column does not exist».
--
-- Знайдено контрактним тестом, щойно він знову зміг запуститись: на свіжій
-- dev-гілці, форкнутій із прода, впав перший же upsertRetailConnection.
--
-- IF NOT EXISTS — бо на чистій базі колонку вже створила 0015. Міграція має
-- бути безпечною і там, і там.

ALTER TABLE retail_connection
  ADD COLUMN IF NOT EXISTS last_receipt_at timestamptz;
