import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    setupFiles: ['./vitest.setup.ts'],
    // Контракт писався під локальний контейнер, де запит коштує мікросекунди.
    // Проти Neon кожен запит — мережевий roundtrip в eu-west-2, і тест на
    // десяток запитів у дефолтні 5с не вкладається: «add із трійкою» падав
    // таймаутом при цілком справному репозиторії.
    //
    // Піднято тільки тут. Ліміт великий навмисно: він ловить зависання, а не
    // повільність — судити про швидкість запитів по мережі однаково не можна,
    // і 5с у цьому пакеті означали б не «швидко», а «пощастило з пінгом».
    testTimeout: 30_000,
    hookTimeout: 60_000,
  },
});
