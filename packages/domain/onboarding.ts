// Раунд 4, крок 7: онбординг «Про тебе» у стрічці — одна картка з сімома
// панелями. Тут — те, що каже сервер (без моделі) і що він просить у моделі.
// Копі панелей (заголовки, підказки) живе в apps/web (lib/profile-copy.ts),
// початки речень і ліміти — у profile-fields.ts.

import { PROFILE_FIELD_KEYS, type ProfileText } from './profile-fields.js';

/** Вітання в першій розмові — рядок сервера, у голосі, без «перш ніж готувати». */
export const ONBOARDING_GREETING =
  'Привіт. Поки на кухні порожньо, тут сім речень про тебе — гортай і закінчуй ті, які хочеш, решту зʼясуємо по ходу. Або одразу пиши, що вдома: чек, фото полиці чи просто список.';

/**
 * «Показати, що вийшло»: серверний рядок у user-turn (не зберігається в
 * історії). Модель читає актуальний [ПРО ЛЮДИНУ] і відповідає без картки.
 */
export const PROFILE_SUMMARY_REQUEST =
  '[СЕРВЕР] Людина закінчила «Про тебе». Своїми словами, у своєму голосі, 3–5 речень: що ти про неї знаєш з [ПРО ЛЮДИНУ] — спершу «не можна», потім «не їм», далі решта. Порожнє не переказуй і не вигадуй (і слів «поле», «профіль», «блок» не вживай — людина їх не бачить); якщо заповнено одне поле або жодного — два речення, без здогадів. В кінці одним реченням запропонуй почати з комори: чек, фото полиці або список, щоб було з чого готувати. Без картки.';

export const isProfileEmpty = (p: ProfileText): boolean =>
  PROFILE_FIELD_KEYS.every((k) => p.fields[k].status === 'empty');
