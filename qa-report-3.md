# Kitchen OS — QA-прогін №3 · регресія + функціонал

**Прогін:** 2026-08-29, вручну через Chrome, dev (`:5173` + `:3000`, postgres, openrouter).
**Коміт:** `af106cf`, поверх `b8a1e70` («QA-2 фікси: 3 blocker + 2 major + 3 minor»).

**Підсумок: 9 із 10 регресійних перевірок пройшли. 5 нових знахідок — 3 major, 2 minor. Blocker'ів немає.**

Головне: **вперше пройдено весь шлях Стрічка → пропозиція → Рецепт → Cook Mode → Share** через
інтерфейс, без `history.pushState`. У QA-2 цей вхід був симульований — тепер він справжній.

---

## Регресія на QA-2 фіксах

| # | Перевірка | Стан |
|---|---|---|
| QA2-01 | картка `proposal` замість прози | ✓ **виправлено** |
| QA2-02 | інгредієнти з назвами на Recipe/Cook | ✓ **виправлено** |
| QA2-02 | те саме на публічній `/r/<id>` | ⚠ **наполовину** — назви є, плейсхолдери ні (QA3-02) |
| QA2-02 | алергічна мітка | ✓ **виправлено** |
| QA2-03 | «Прибрати з комори» клікабельна | ✓ **виправлено** |
| QA2-04 | немає маркдауну у відповідях | ✓ **виправлено** — 0 з 11 |
| QA2-05 | `1 ПОРЦІЯ` на Recipe / Share / SharedRecipe | ✓ **виправлено** |
| QA2-06 | `services/api/storage/` не в git | ✓ **виправлено** |
| QA2-07 | лічильник Журналу після undo | ✓ **виправлено** |
| QA2-08 | одиниці не капсом | ✓ **виправлено** |

Докази по кожному — нижче.

### QA2-01 ✓ Картка `proposal` повернулась

«Купив моцарелу 250 г, пелаті 400 г, пасту спагетті» → apply → «Що з цього приготувати сьогодні?»

```
card: "proposal"
items: [
  {title:"Спагетті з томатом і моцарелою", character:"15 хвилин, мінімум зусиль",
   rescues:["пелаті"], needs:["часник","оливкова олія","сіль"]},
  {title:"Паста 'Капрезе'", character:"10 хвилин без варки",
   rescues:["пелаті","моцарела"], needs:["помідори","базилік","оливкова олія","бальзамік"]},
  {title:"Спагетті карбонара", character:"20 хвилин",
   rescues:["спагетті"], needs:["бекон або гамбургер","яйця","пармезан"]}
]
```

Три варіанти, різні за зусиллям (10 / 15 / 20 хв), у кожного заповнені `rescues` і `needs`,
кнопка «Рецепт →» на кожному. Вимога зі Стежки 3 виконана повністю.

### QA2-02 ✓ Інгредієнти й алергія

Recipe і Cook Mode показують `Паста спагетті`, `Моцарела`, `Пелаті` замість UUID.
Розділення `●` (є в коморі) / `○` (бракує) працює.

Алергія: «додай алергію на моцарелу» → профіль → відкрив рецепт із моцарелою →
**`Моцарела ⚠ МОЦАРЕЛА`** червоною міткою. Перевірка, яка в QA-2 не могла пройти за побудовою,
тепер проходить.

### QA2-03 ✓ Кнопка видалення вивільнена

```
tabbar: { top: 694, z-index: "10" }
кнопка:  { top: 708, bottom: 747 }
document.elementFromPoint(центр кнопки) → BUTTON
клікабельна: true
```
`z-index: 10` проти `Sheet.tsx` `zIndex: 20` — модалка тепер зверху.

### QA2-04 ✓ Маркдауну немає

11 відповідей асистента за сесію, регекс `/\*\*|^#{1,3}\s|^\s*[-*]\s|`/m` — **0 збігів**.
Середня довжина відповіді 85 символів.

### QA2-05 ✓ Множини

`Recipe: 25ХВ · 1 ПОРЦІЯ · 520ККАЛ` · `Share: 1 ПОРЦІЯ` · `SharedRecipe: 1 ПОРЦІЯ`.

### QA2-06 ✓ Сховище ігнорується

Після завантаження фото: `git status --short` — порожньо. `.gitignore:12` містить `services/api/storage/`.

### QA2-07 ✓ Лічильник Журналу

До undo: `Журнал | 1 ГОТУВАННЯ | ЗА ТИЖДЕНЬ · 1 ГОТУВАННЯ · ★4.0 · 2 З КОМОРИ`
Після undo: `Журнал | 0 ГОТУВАНЬ` + чип зник, обидва скасовані готування показані з `↩ … СКАСОВАНО`.

### QA2-08 ✓ Одиниці

Картка `intake_diff`: `250 г`, `400 г`, `1 пач`. Комора: `32.99 шт`, `400 г`. Скрізь однаково.

---

## Нові знахідки

### QA3-01 · major · Плейсхолдер дублює назву, яку модель уже написала словами

**Кроки:** будь-який згенерований рецепт → Recipe або Cook Mode → читати кроки

**Отримав** (рецепт «Спагетті з томатним соусом і моцарелою», всі 6 кроків):
```
1  Часник часник подрібни або нарізай тонкими пластинками.
2  ...підсмаж часник часник до аромату... Додай пелаті Пелаті, розім'яв томати виделкою...
3  Вари спагетті Паста спагетті на хвилину менше, ніж на пачці...
5  ...розірви моцарелу Моцарела руками на шматочки...
```

**Причина.** `renderStepContent()` підставляє в `{N}` назву інгредієнта. Модель при цьому пише
назву **ще й словами перед плейсхолдером**: у `step.c` лежить `"Часник {1} подрібни"`, і після
підстановки виходить «Часник часник подрібни».

Це протилежний бік QA2-02: раніше `{0}` не підставлявся зовсім, тепер підставляється — але
контракт із моделлю не домовлений. `card-rules.md` каже «показуй пальцем через `{N}`»,
а `sonnet-4.5` пише і слово, і плейсхолдер.

**Де правити:** промпт. Потрібне однозначне правило в `card-rules.md`:
> `{N}` **замінює** назву інгредієнта, а не супроводжує її. Неправильно: «Часник {1} подрібни».
> Правильно: «{1} подрібни» або «Подрібни {1}».

Плюс страховка в коді: перед підстановкою прибирати назву, якщо вона стоїть впритул до `{N}`.

**Severity:** major — псує кожен крок кожного рецепта, тобто основний екран продукту.

---

### QA3-02 · major · На публічній `/r/<id>` плейсхолдери не підставляються взагалі

**Кроки:** Cook Done → «Поділитись» → відкрити `/r/<uuid>`

**Отримав:**
```
1  Відварити пасту. Закинути {0} в окріп, варити 8–9 хвилин до аль денте.
2  Порізати інгредієнти. {2} нарізати кубиком, {1} порвати руками на шматки. {3} порвати...
3  Змішати. ... Полити {4}, посолити, поперчити.
```
Інгредієнти при цьому мають назви — серверний enrich `ing.n` у `cook-runs.ts:62` працює.

**Причина.** `SharedRecipe.tsx:111` рендерить `{step.t}. {step.c}` як є:
```
grep -c "renderStepContent" SharedRecipe.tsx → 0
```
Функція існує (дубльовано) у `Recipe.tsx:171` і `Cook.tsx:387`, у SharedRecipe її не додали.

**Патч:** винести `renderStepContent` у спільний модуль (напр. `lib/recipe-text.ts`) і використати
в усіх трьох екранах — заразом прибрати дублювання, через яке фікс і загубився.

**Severity:** major — це сторінка, яку відкриває той, кому надіслали посилання. Ростова петля продукту.

---

### QA3-03 · major · Підстановка не узгоджує відмінок

**Отримав** у тих самих кроках:
```
Закинути Паста спагетті в окріп        → має бути «пасту спагетті»
томати свіжі нарізати кубиком, Моцарела порвати руками  → «моцарелу»
Полити олива, посолити, поперчити      → «оливою»
додай олія оливкова                    → «олію оливкову»
посоли сіль                            → «посоли»
```

Підстановка вставляє канонічну назву в називному відмінку з великої літери в середину речення.
Українською це читається як машинний переклад.

**Два шляхи:**
- **дешевий (промпт):** вимагати, щоб `{N}` стояв у позиції, яка не потребує непрямого відмінка —
  на початку речення або після двокрапки: «Спагетті: закинути в окріп», «{1} — порвати руками».
- **дорогий (код):** відмінкові форми в каталозі. `catalog/expand` уже містить `aliases`
  з відмінками для 2341 позиції — це той самий матеріал, тільки для читання, а не для запису.

**Severity:** major для україномовного продукту.

---

### QA3-04 · minor · Генерація рецепта до 40 с без зворотного зв'язку

Два виміри в одній сесії: 18 с і 40 с. Тост «Готую рецепт…» гасне через 6 с (`Feed.tsx`,
`setTimeout(…, 6000)`), кнопка лише трохи темнішає. Решту часу — 12 або 34 секунди — людина
дивиться на екран, де нічого не відбувається, і не знає, чи натиснулось.

**Пропозиція:** тост із `onUndo`-подібним «живим» станом, який не гасне до відповіді, або спінер
у кнопці. Дешевше — не гасити тост, поки `openingRecipe === true`.

---

### QA3-05 · minor · Двозначний інтент: факт покупки губиться

**Стежка 5.** «Купив паста, треба зробити щось»

**Очікував:** `intake_diff` (купив), можливо разом із питанням
**Отримав:** жодної картки. Тільки репліка: «Паста є — на вечерю робимо спагетті? Що в коморі
ще є — з чим хочеш?»

Модель узагалі не записала покупку. Розмовно це нормально, продуктово — ні: людина сказала
«купив», і нічого не сталося. Наступне повідомлення почне з нуля.

**Пропозиція для `card-rules.md`:** якщо в репліці є явний факт про комору («купив», «поклав»,
«закінчилось») — картка обов'язкова, навіть коли решта фрази двозначна. Питання ставиться
**поряд** із карткою, а не замість неї.

---

## Що не перевірено

| Що | Чому |
|---|---|
| Household ролі (передати, виключити, `last_owner_cannot_leave`) | той самий дозвіл, що в QA-1/2 — на єдиному живому owner-акаунті без людини за столом не роблю |
| Тестові акаунти `qa3-*@example.com` | magic-link іде тільки в stdout API-процесу; моя сесія бачить лише папку репозиторію |
| Стежка 1 на порожньому акаунті | те саме — онбординг пройдено на живому акаунті, `stage=1` промпт не спрацьовував |
| Deep-link в incognito, `?next=` після signin | інкогніто не підняти, magic-link недосяжний |
| «Завантажити PNG», «Скопіювати підпис» | завантаження й буфер обміну недоступні автоматизації |
| Десктоп-адаптація (≥1024px) | `resize_window` рапортує успіх, але `innerWidth` лишається 417, `outerWidth` = 0 |
| Rate-limit тости (429) | не провокував — 6 запитів auth підряд заблокували б живий акаунт |

---

## Прибрано за собою

Комора повернута у вихідні **20 позицій**: тестові Моцарела / Пелаті / Паста спагетті видалені,
обидва cook-run'и скасовані (`restored: 2` кожен), тестова алергія «моцарела» знята з профілю
(`op: remove`, профіль знову порожній). Лишились сліди в історії: два скасовані готування
в Журналі й діалог за 29.08 у Стрічці.

---

## Вердикт

Усі три blocker'и QA-2 закриті, регресій від фіксів немає. Продукт уперше проходиться цілком
руками від першого повідомлення до публічного посилання.

Три нові major — усі про **текст усередині рецепта**: дубльовані назви, невідмінені назви,
непідставлені плейсхолдери на публічній сторінці. Жодна не ламає механіку, всі три ламають
враження від головного екрана. Дві з трьох правляться промптом, третя — п'ятьма рядками коду.

Про мову й характер моделі — окремо у `copy-report-3.md`.
