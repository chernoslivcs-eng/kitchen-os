# QA-прогін №8 — механіка

**Дата:** 30 серпня 2026
**HEAD:** `259bd2d` (бриф) поверх `3c92d9b` (merge Бриф-3)
**Акаунт:** `qa8-a@example.com`, чиста кухня → 7 позицій через чат
**В'юпорт:** 1728×784, світла системна тема

## Перевірка збірки (урок QA-7)

`git log --oneline -1` = `259bd2d`. Три проби проти запущеного API:

| Проба | Очікуване на HEAD | Факт |
|---|---|---|
| `GET /v1/recipes/:id` з неіснуючим uuid | `{"error":"not_found"}` — хендлер | ✅ саме так |
| `GET /v1/definitely-not-a-route` | `Route ... not found` — роутер | ✅ саме так |
| `GET /v1/session/today` | 200 | ✅ 200 |

Перша проба відрізняє «маршрут є, запису нема» від «маршруту нема». **Сервер на HEAD.** Штанга QA-7 не повторилась.

---

# Блокери

## QA8-01 · blocker · Хвороба двох борщів НЕ вилікувана: ідемпотентність міряє не той ключ

**Кроки:** пропозиція → тап «Рецепт →» на першій страві → чекаю → тап «Рецепт →» на ТІЙ САМІЙ страві ще раз.

**Очікуване:** той самий рецепт, другого ходу в стрічці нема.

**Фактично:** у стрічці **два рецепти-повідомлення**:

| # | Заголовок | Мета |
|---|---|---|
| 1 | Карбонара з фуетом | 25ХВ · 1 ПОРЦІЇ · 680ККАЛ |
| 2 | **Спагеті** карбонара з фуетом | 25ХВ · 1 ПОРЦІЇ · 680ККАЛ |

Це рівно той скрін, заради якого робився реліз — дві страви з однієї пропозиції.

**Корінь — точний, дві сторони одного ключа:**

`services/api/src/routes/recipes.ts:50-58` шукає збіг за **запитаною** назвою:

```ts
const recent = await repo.listRecentRecipes(ctx.user_id, 20);
const same = recent.find((r) =>
  r.title.trim().toLowerCase() === title.trim().toLowerCase() && …);
```

`services/api/src/routes/recipes.ts:101` пише рядок під **назвою моделі**:

```ts
await repo.saveRecipe({ …, title: call.recipe.t, … });
```

Клієнт (`Feed.tsx:391`) шле `pick.title` — заголовок пункту пропозиції: **«Паста карбонара з фуетом»**. Модель повертає `recipe.t` = **«Карбонара з фуетом»**. Рядки не збігаються → `find` порожній → генерація заново, щоразу.

**Доказ, що це саме ключ, а не кеш:** прямий виклик API з назвою МОДЕЛІ спрацював як треба —

| Виклик | Час | `reused` | id |
|---|---|---|---|
| `{title:'Карбонара з фуетом'}` | **268 ms** | `true` | `ce6850db…` |
| `{title:'Карбонара з фуетом', regenerate:true}` | **18 681 ms** | — | `23ebeb4b…` (новий) |
| `{title:'Карбонара з фуетом'}` знову | — | `true` | **`23ebeb4b…`** |

Тобто механізм справний — його просто питають не тим ключем.

**Побічне, з третього рядка таблиці:** після `regenerate` пошук повертає **новий** рецепт (`ORDER BY created_at DESC`, `postgres-repo.ts:741`). У стрічці лишається рецепт A, а «Готуємо», «У рецепти» й будь-який наступний тап ведуть на рецепт B. Сьогодні латентно — `grep -rn "regenerate" apps/web/src` порожній, UI цього не шле, — але ендпоінт публічний і кнопка «дай інший підхід» напрошується.

**Ще одне з тієї ж функції:** `listRecentRecipes(user_id, 20)` — вікно на 20 останніх рецептів. Двадцять один рецепт за добу, і ідемпотентність для старших назв тихо вимикається.

**Пропозиція:** ключ має бути не назва. Клієнт уже отримує `recipe_id` у картці — достатньо запам'ятати його на пункті пропозиції й не питати сервер удруге. Серверний фолбек: писати `requested_title` окремою колонкою і шукати за нею.

## QA8-02 · blocker · У рецепті-повідомленні кроки кажуть «інгредієнт» замість назви

**Кроки:** тап «Рецепт →» → дивлюсь блок КРОКИ у стрічці.

**Фактично** (дослівно, кроки 1-3):

> 1. Відварити пасту. **інгредієнт** — кинути в окріп, варити до al dente…
> 2. Підготувати фует і жовтки. **інгредієнт** — нарізати тонкими смужками…
> 3. Обсмажити фует. **інгредієнт** на суху пательню, середній вогонь…

**Той самий рецепт у Cook Mode** (`/cook`):

> Відварити пасту. **Спагеті** — кинути в окріп, варити до al dente…

Одні дані, два рендери, різний результат.

**Корінь — `apps/web/src/pages/Feed/cards.tsx:486-488`:**

```ts
function renderStepInline(content: string, ing: Recipe['ing']): string {
  return content.replace(/\{(\d+)\}/g, (_, n) => ing[Number(n)]?.n ?? 'інгредієнт');
}
```

Це самописна копія `renderStepContent` із `lib/recipe.ts`, у чиїй шапці написано «Спільна логіка для трьох екранів». Копія:

1. **не приймає `batchLabels`** — а саме інгредієнти з комори приходять як `{p: uuid}` без `n`. Payload із бази:
   ```json
   [{"p":"6b42a922…","u":"g","v":200},{"p":"874b47b1…","u":"g","v":120},
    {"p":"380e468d…","u":"ml","v":200},{"n":"яйця",…},{"n":"пармезан",…}]
   ```
   Тобто ламаються рівно ті три інгредієнти, заради яких продукт існує, а «яйця» й «пармезан» (яких нема в коморі) рендеряться нормально;
2. втрачає гвардію QA3-01 (модель пише і назву, і плейсхолдер);
3. втрачає капіталізацію QA6-11 — звідси «Відварити пасту. **і**нгредієнт» з малої після крапки.

Три раніше закриті дефекти повертаються одним рядком коду. Виклик у `cards.tsx:447` треба замінити на `renderStepContent(step.c, r.ing, batchLabels)` — `batchLabels` у компонент уже передається (`Feed.tsx:620`) і використовується поруч, у списку інгредієнтів (`cards.tsx:412`).

## QA8-03 · blocker · Таймер Cook Mode іде вдвічі повільніше реального часу

**Кроки:** крок 3 (таймер 4:00) → «Пуск» → міряю.

**Вимірювання №1** (якір `Date.now()` у сторінці): 19,4 с реального часу → **9 с** на таймері. Дрейф 10,4 с, темп 46%.

**Контроль у ТІЙ САМІЙ сторінці за той самий інтервал:**

| | значення |
|---|---|
| реального часу | 19,3 с |
| звичайний `setInterval(…,1000)`, спрацювань | **19** |
| таймер застосунку, відлічено секунд | **10** |

Сторінка не задроселена — дросель був би видний і на контролі.

**Вимірювання №3** (MutationObserver на самому елементі таймера, інтервали між змінами тексту, мс):

```
1426, 1997, 2002, 2000, 2000, 1996, 2001, 2004, 1999
```

Не джитер — **рівно 2000 мс**. Цифра на екрані міняється раз на дві реальні секунди. Таймер на 8:00 для пасти відпрацює 16 хвилин.

**Дві причини, і їх треба розділити:**

1. **Архітектурна, живе і в проді.** `Cook.tsx:84-86` — лічильник без прив'язки до годинника:
   ```ts
   tickRef.current = window.setInterval(() => {
     setSecondsLeft((s) => (s > 0 ? s - 1 : 0));
   }, 1000);
   ```
   Будь-який дросель (фонова вкладка, важка сусідня вкладка, енергозбереження, згорнутий екран) — це назавжди втрачені секунди, бо наздогнати нема з чого. Кухонний таймер має рахувати від `deadline = Date.now() + s*1000`, а не від кількості тіків. Це ж лікує і QA8-06 (збережений час).
2. **Рівно двократний множник, імовірно dev-only.** `main.tsx:22` — `StrictMode`, який у dev монтує ефекти двічі. Чесно: я **не довів**, що 2× відтворюється у production-збірці, і в звіті цього не стверджую. Але перевірити треба до релізу: якщо 2× живе і в проді, це найгірший баг усього прогону.

---

# Major

## QA8-04 · major · Стрічка не прокручується до нового рецепта — і взагалі до низу

**Кроки:** тап «Рецепт →» → чекаю 19 с генерації.

**Фактично:** `timeline.scrollTop = 4` при `scrollHeight = 2366`, `clientHeight = 699`. Рецепт зверстано на ~1600 px нижче видимої частини. Людина 19 секунд дивиться на тост «Готую рецепт…», він зникає — і на екрані не змінюється нічого. Флагманська взаємодія релізу приходить за кадр.

Те саме після F5: `scrollTop = 0`, стрічка відкривається на початку дня, а не на останньому повідомленні.

**Спільний корінь із QA8-05.** Прямий тест на тому самому елементі:

| виклик | scrollTop після |
|---|---|
| `tl.scrollTop = 500` | **500** — контейнер прокручуваний |
| `el.scrollIntoView({block:'center'})` | **953** — миттєвий скрол працює |
| `el.scrollIntoView({behavior:'smooth', block:'center'})` | **0** — не робить нічого |

`matchMedia('(prefers-reduced-motion: reduce)').matches === false`, `scroll-behavior: auto` — тобто це не reduced-motion.

Усі скроли в застосунку — плавні (`Feed.tsx:255` `scrollTo({top: scrollHeight, behavior:'smooth'})`, `Feed.tsx:795` `scrollIntoView({behavior:'smooth'})`), і всі закінчуються на нулі. Плюс окремо `Feed.tsx:253-256` міряє `scrollHeight` у `useEffect` на `[turns]` — до того, як браузер розклав високий блок рецепта, тобто ціль скролу все одно стара.

**Фікс:** прибрати `behavior:'smooth'`, а вимірювання перенести в `useLayoutEffect` + `requestAnimationFrame`.

## QA8-05 · major · Тап по «ОЧІКУЮТЬ РІШЕННЯ» в правій панелі нікуди не веде

**Кроки:** створив pending-картку → десктоп ≥1280 → тап по рядку «ПРОПОЗИЦІЯ ◌» у правій панелі.

**Очікуване (бриф D):** скрол до картки.
**Фактично:** `scrollTop` 0 → 0 при `scrollHeight` 2599 і 14 елементів `#turn-*` у DOM.

Той самий `behavior:'smooth'` (`Feed.tsx:795`), той самий результат. Обробник і id коректні — миттєвий `scrollIntoView` на тому ж елементі дає 953.

## QA8-06 · major · «Продовжити» повертає таймер на початок, а не на збережений час

**Кроки:** крок 3, таймер 4:00 → «Пуск» → дочекався 3:58 → «✕ Вийти» → «ПРОДОВЖИТИ ›».

**Очікуване (бриф B2):** той самий крок, таймер на паузі зі збереженим часом.
**Фактично:** крок 3 ✅, пауза ✅, таймер — **4:00**.

`localStorage.kos-cook-live` на момент виходу: `{stepIdx: 2, secondsLeft: 240, savedAt: …}` — 240, тобто 4:00.

**Корінь:** `Cook.tsx:185-189` пише сесію на `[stepIdx, running, done]`, а не на зміну секунд (у коментарі це свідоме рішення: «localStorage не любить тік»). Тому зберігається значення на момент останньої ДІЇ. Вийшов за 20 секунд до кінця — повернувся до повних чотирьох хвилин.

Рішення те саме, що для QA8-03: зберігати `deadline`, а не `secondsLeft`. Тоді запис на дію лишається рідким, а відновлення стає точним.

## QA8-07 · major · Нова страва мовчки затирає незавершене готування

**Кроки:** Cook Mode «Карбонара» → крок 2 → «✕ Вийти» (у стрічці рядок «Готування триває · карбонара з фуетом · крок 2/6») → відкрив «Бабусин борщ» → «Cook Mode».

**Фактично:**

| момент | `kos-cook-live` |
|---|---|
| до відкриття борщу | Карбонара з фуетом, крок 2 |
| одразу після відкриття | **Бабусин борщ, крок 1** |

Ні попередження, ні підтвердження, ні способу повернутись. Карбонара на кроці 2 зникла в момент відкриття другого рецепта — навіть не готування, просто відкриття Cook Mode.

**Механіка:** `Cook.tsx:102` відновлює сесію лише коли `saved.recipe.t === recipe.t`; назви різні → не відновив, а ефект `saveCookSession` одразу переписав.

Бриф просив зафіксувати фактичну поведінку як вхід для рішення — зафіксовано. Мінімум: питати «Кинути карбонару на кроці 2?». Максимум: тримати кілька живих сесій (людина справді може паралельно варити суп і пекти).

## QA8-08 · major · Модель пропонує алерген на спільну вечерю, і попередження живе в рекламному слоті

**Кроки:** записав Оксану з важкою алергією на арахіс (застосовано, `eaters[0].allergies = ["арахіс","арахісова паста"]`) → «зроби нам сніданок **на двох**».

**Фактично:** три страви, третя — **«Тости з арахісовою пастою»**. Позначка є:

```
ЧОМУ ЗАРАЗ
УВАГА: тут арахісова паста — у Оксани алергія на арахіс
```

**Що працює:** маркування (фікс QA7-06 тримається) — алерген названо, і названо з іменем людини.

**Що не працює, дві речі:**

1. Запит явно про двох, один із двох має важку алергію — страву все одно запропоновано. «Позначка, а не заборона» — правильний принцип для власної тарілки; для спільної вечері він не працює.
2. Попередження покладено в слот **«ЧОМУ ЗАРАЗ»** — це слот «чому саме сьогодні», тобто слот переконування. Тим самим кеглем і кольором, що й «Томати лопаються в олії». Єдина відмінність — «УВАГА:» капсом усередині речення.

**Другий бік перевірено окремо і він коректний:** «зроблю собі тост з арахісовою пастою, я вдома сам» → страву дано, алергія названа: «УВАГА: арахісова паста — у Оксани на неї алерген». Тобто модель розрізняє «для нас» і «для себе» в тексті, але не в доборі страв.

## QA8-09 · major · Escape не закриває модалку списання, фокус-пастки немає

**Кроки:** довів партію спагеті до повного витрачання → «Приготували» → модалка «Що списуємо повністю?» → Escape.

**Фактично:** модалка відкрита, `document.activeElement` = `BODY`, `dialog.contains(activeElement) === false`.

DA2-09 не закрито: ні Escape, ні початкового фокусу, ні пастки. Модалка має `role="dialog"` і `aria-label="Що списуємо повністю"` — розмітка правильна, поведінки немає.

---

# Minor

## QA8-10 · minor · «1 ПОРЦІЇ»

Мета рецепта: «25ХВ · **1 ПОРЦІЇ** · 680ККАЛ». У payload `sv: 1`. Числівник не узгоджено — потрібне те саме `plural()`, що вже є в `Recipes.tsx` («РЕЦЕПТ / РЕЦЕПТИ / РЕЦЕПТІВ»).

## QA8-11 · minor · Латинська одиниця в модалці списання

Рядок партії в модалці: «спагеті **200g**». Плейсхолдер поля «ЛИШИЛОСЬ ≈» при цьому вже кириличний — «г». Тобто фікс DA2-10 доїхав до інпута й не доїхав до відображення кількості поруч. У картках комори те саме значення рендериться правильно («400 мл», «180 г»).

## QA8-12 · minor · Підказка «тап по смузі = крок назад» висить на екрані «Готово»

Після завершення футер показує «Працює без мережі · тап по смузі = крок назад», хоча смуги прогресу на цьому екрані вже немає.

## QA8-13 · minor · «0 ГОТУВАНЬ» над списком, у якому є готування

`/cooklog`: заголовок «Журнал», мета «**0 ГОТУВАНЬ**», а нижче «СЬОГОДНІ · ↩ Карбонара з фуетом». Лічильник і список рахують по-різному (скасований ран у списку є, у лічильнику ні). Людина бачить нуль над непорожнім списком.

## QA8-14 · minor · Онбординг чистого акаунта: один CTA замість трьох

Порожня стрічка `qa8-a` показує:

> **Скажи, що купив або що хочеш приготувати**
> «купив моцарелу 250 г» або «дай рецепт з вершків і фуета» — одне поле, усе через підтвердження.
> [ 📷 Сфотографуй чек — я розкладу ]

Бриф E чекав три CTA (📷 полиця / 🧾 чек / текстом). Фактично один — про чек. Полиця не згадана; «текстом» живе тільки в підказці, не як дія.

## QA8-15 · minor · Рецепт-повідомлення потрапляє в «ОЧІКУЮТЬ РІШЕННЯ»

Права панель показувала «ОЧІКУЮТЬ РІШЕННЯ · 2» з рядками «ПРОПОЗИЦІЯ ◌» і «КУХНЯ · РЕЦЕПТ ◌». Але рецепт-повідомлення не має пари «Застосувати / Ні» — рішення від нього ніхто не чекає, це контент. Фільтр `t.card && !t.applied && !t.undone && !t.dismissed` (`Feed.tsx:785`) не розрізняє картки-рішення й картки-контент.

## QA8-16 · minor · Композитор не вирівняний зі стрічкою на десктопі

| елемент | x | width |
|---|---|---|
| `.timeline` | **268** | 720 |
| `.composer-wrap` | 268 | 1210 |
| `.composer` (форма) | **513** | 720 |

Колонка повідомлень починається на 268 px, поле вводу — на 513. Зсув 245 px, видно неозброєним оком.

**Корінь:** `Feed.module.css:459-467` притискає `.composer-wrap`, а форма `.composer` усередині лишається з `margin-left: auto` від `Feed.module.css:346-350` і центрується всередині притиснутої обгортки. `.timeline` притиснуто напряму, тому з нею все гаразд.

## QA8-17 · minor · Права панель 250 px замість 320

`Feed.module.css:499` — `width: 250px`. Хендоф Д01 `.rail` — `320px`. Решта (позиція fixed справа, `border-left`, `bg-surface-2`, `padding 22px 18px`) відповідає.

## QA8-18 · minor · Інвайт живе 7 днів, макет обіцяє 72 години

`POST /v1/households/:id/invite` → 201, `expires_at` = +7 днів. Хендоф Д06: «ПОСИЛАННЯ ДІЄ 72 ГОД · ОДНОРАЗОВЕ».

## QA8-19 · minor · `session.day` на добу позаду

`GET /v1/session/today` → `session.created_at = 2026-08-30T11:30:43Z`, `session.day = "2026-08-29"`. Сьогодні 30 серпня. На поведінку сьогодні не впливає (сегмент «Історія» показує «СЬОГОДНІ» правильно), але це поле лягає в групування журналу.

## QA8-20 · note · Порушення Rules of Hooks у Cook.tsx

`Cook.tsx:166` — ранній `return` для випадку «рецепта немає», а `useEffect` (Wake Lock, збереження сесії) стоять **після** нього, на рядках 185+. Кількість викликаних хуків залежить від наявності рецепта. Сьогодні не вистрілює, бо `recipe` стабільний на весь час життя компонента. Стане реальним, щойно `/cook` навчиться тягнути рецепт за id (а він до цього йде).

---

# Що працює — перевірено, не зі слів

### A · Рецепт у розмові

| Перевірка | Результат |
|---|---|
| «Рецепт →» лишає на `/app`, повний рецепт з'являється в стрічці, мітка «КУХНЯ · РЕЦЕПТ» | ✅ |
| Склад: заголовок, мета, бурштинова вставка «ключова помилка» з лівим бордюром, ІНГРЕДІЄНТИ з ●/○, КРОКИ з ▷ | ✅ |
| Дві колонки на десктопі (`recipe-msg-cols`, ≥1024) | ✅ |
| Інгредієнт із комори показує назву («вершки 33%», «спагеті», «фует»), не uuid — **у списку інгредієнтів** | ✅ |
| 6 кроків → показані 3 + «Показати всі 6 кроків», розгортка працює | ✅ |
| F5 → рецепт у стрічці цілий, зі структурою й діями | ✅ |
| Рецепт у відкритій із «Історії» сесії рендериться цілим, з кнопками | ✅ |
| **Нова сесія:** «нагадай, що за карбонару ти пропонував?» → «Карбонара з фуетом: яйця, пармезан, паста, сіль і чорний перець. Фует замість гуанчале.» — склад свого рецепта, без вигадування | ✅ блок [ЗГЕНЕРОВАНІ РЕЦЕПТИ] живий |
| «+ у список» → позиція з причиною «для: Карбонара з фуетом», кнопка → «✓ у списку», бейдж виріс | ✅ |
| «У рецепти» → «✓ У рецептах», у бібліотеці **один** рядок (PATCH, не INSERT) | ✅ |
| «Готуємо → Cook Mode» стартує з цим рецептом | ✅ |
| **Власний рецепт текстом** → картка `type: "recipe"` з 5 структурованими кроками, заголовок «Бабусин борщ», «У рецепти» кладе в бібліотеку | ✅ DA2-23 тримається |
| `/recipe/:id` адресний, F5 тримає, кроки рендеряться правильно | ✅ |
| Неіснуючий/чужий id → 404 `{"error":"not_found"}`, існування не підтверджується | ✅ |

### B · Cook Mode

| Перевірка | Результат |
|---|---|
| ↩ зліва від «Крок готово ✓», на кроці 1 відсутня | ✅ |
| Сегменти прогрес-смуги — `<button>` з тап-зоною **32 px** (не 4 px) | ✅ |
| Пройдений сегмент `cursor: pointer`, майбутній `cursor: default`; тап по майбутньому — нічого | ✅ |
| Тап по пройденому: крок 3 → крок 1 | ✅ |
| Таймер при поверненні — початкове значення, «Пуск», сам не тікає | ✅ |
| Реальний подвійний тап по «Крок готово ✓»: 2/6 → 3/6, один крок (лок 400 мс) | ✅ DA2-03 закрито |
| «✕ Вийти» → у стрічці «Готування триває · … · крок 3/6 · ПРОДОВЖИТИ ›» | ✅ |
| «ПРОДОВЖИТИ ›» → той самий крок, пауза | ✅ (час — див. QA8-06) |
| Рядок «Готування триває» переживає F5 | ✅ |
| Протухла сесія (savedAt −13 год) не показується і чиститься з localStorage | ✅ |
| «НА ЦЬОМУ КРОЦІ» показує інгредієнти саме цього кроку з кількостями («спагеті — 200 г») | ✅ DA2-05 закрито |
| «Приготували» на середньому кроці → одразу фініш | ✅ DA2-06 закрито |
| Кроки без таймера: жодного самотнього «0» | ✅ DA2-01 закрито в Cook Mode |
| Сигнал на нулі: `beep()` через Web Audio + `navigator.vibrate`, повтор `setInterval(beep, 30_000)`, глушиться дією | ✅ реалізовано (`Cook.tsx:157-163`) |
| Wake Lock із перезапитом на `visibilitychange` | ✅ реалізовано (`Cook.tsx:113-130`) |
| Модалка списання: заголовок, хінт, чекбокс, зняття → «ЛИШИЛОСЬ ≈ … ОПЦІЙНО», лічильник «Списати 1» → «Списати 0» | ✅ |
| Введено 100 → партія `{value: 100, state: "opened"}` у Коморі | ✅ |
| Undo повертає ВСЕ: значення й `state` (`opened` → `sealed`), включно з уведеним залишком | ✅ |
| Cook Mode лишається темним; перемикання теми ПІД ЧАС готування — крок зберігся, фон не змінився (`rgb(16,19,23)` до і після) | ✅ |
| Десктоп ≥1024 — двоколонник: `grid 545 / 455` (1.2:1), gap 56, крок `40px/600 Onest`, таймер `108px/800 Onest` за бордюром | ✅ Д05 |

### C · Композитор

| Перевірка | Результат |
|---|---|
| Одна пілюля, 📎 ghost (`border: 0`) і 🎙 у рамці (`border: 1px`) всередині фрейму справа, обидва 38 px | ✅ |
| Плейсхолдер «Записати в журнал…» | ✅ |
| Набір тексту → 🎙 морфить у ↑ (`frame-btn-solid`, `type=submit`, темний квадрат із білою стрілкою — перевірено візуально) | ✅ |
| Стирання → назад у 🎙 | ✅ |
| Усього дві кнопки у формі — окремої ↑ поруч немає | ✅ |
| «+ ІМПОРТ» на Рецептах → стрічка, поле «Запиши мій рецепт: », фокус у полі | ✅ Р-2 реалізовано |
| F5 після цього — префікс не з'являється вдруге | ✅ |

### D · Навігація і десктоп

| Перевірка | Результат |
|---|---|
| Сайдбар **232 px**, `padding 20px 14px`, лого «Kitchen OS» зверху, лічильник комори, блок користувача внизу | ✅ Д01 |
| Сайдбар на `/profile` — і блок користувача внизу **активний** (`_active_`), `body padding-left: 232px` | ✅ Д06 |
| Сайдбар на `/cooklog` і `/recipe/:id` | ✅ |
| Контент притиснутий: `.timeline` x = **268** = 232 + 36 | ✅ |
| «Історія» — інлайн у стрічці, `[role=dialog]` = 0, список сесій із назвами й «СЬОГОДНІ · 14:36 · 2 ПОВІДОМЛЕННЯ» | ✅ DA2-37 закрито |
| Тап по сесії відкриває її, сегмент вертається на «Сьогодні» | ✅ |
| Права панель на ≥1280 існує, `position: fixed`, `border-left` | ✅ (ширина — QA8-17, тап — QA8-05) |
| Бейдж «Список» = некупленим: 1 позиція, відмічена → API `count: 0`, бейдж зник на всіх чотирьох екранах, «СПИСОК N» пішов і з шапки | ✅ DA2-31 закрито |
| ⌘K, ⌘Enter | ✅ (`Feed.tsx:209-225` без змін) |

### E · Регресія QA-7

| Фікс | Результат |
|---|---|
| **QA7-01** `INSERT has more expressions than target columns` | ✅ `postgres-repo.ts:822` — `$1…$11` на 11 колонок; картка shopping → apply → 3 позиції в Postgres |
| INSERT-близнюк — інвайт | ✅ `POST /v1/households/:id/invite` → 201 |
| **QA7-06** алергія їдця | ✅ маркується з іменем; ⚠ добір страв — QA8-08 |
| **QA7-02** Великдень | ✅ «2 травня 2027 — правосл. Великдень. Великий піст з 15 березня.» Дата правильна (сьогодні 30.08.2026) |
| **DA2-23** свій рецепт текстом | ✅ картка `recipe`, не `note` |
| **DA2-31** бейдж списку | ✅ |
| **DA2-03** подвійний тап | ✅ |
| **DA2-05 / DA2-06** «НА ЦЬОМУ КРОЦІ», «Приготували» | ✅ |
| **DA2-09** Escape / фокус-пастка | ✗ **QA8-09** |
| **DA2-01** самотній «0» | ✅ у Cook Mode; ✗ у стрічці — інша поломка (**QA8-02**) |
| **DA2-10** латинська одиниця | частково — ✗ **QA8-11** |
| **DA-21** три CTA онбордингу | ✗ **QA8-14** |

### F · Хаос

| Перевірка | Результат |
|---|---|
| Дві вкладки, одна сесія: друга бачить живий рядок готування, той самий юзер і session_id | ✅ |
| Конфлікт сесій готування | ✗ **QA8-07** |
| `regenerate: true` через API видає новий id | ✅ 18 681 мс, новий id (побічний ефект — у QA8-01) |
| Дуже довгий рецепт («святкове меню однією стравою на 8 осіб») | ⚠ не відтворено: модель повернула прозу замість рецепта (`recipe: null`, `reply` заповнено) — шлях QA4-06 спрацював за призначенням, довгого рецепта я не отримав |

---

# Підсумок

| Severity | Кількість | Номери |
|---|---|---|
| **blocker** | 3 | QA8-01, QA8-02, QA8-03 |
| **major** | 6 | QA8-04…QA8-09 |
| **minor** | 10 | QA8-10…QA8-19 |
| **note** | 1 | QA8-20 |

**Найважливіше в одному абзаці.** Реліз зробив рівно те, що обіцяв: рецепт живе в стрічці, Cook Mode має крок назад і виживає вихід, навігація й десктоп нарешті збігаються з хендофом, дев'ять із дванадцяти фіксів попередніх звітів тримаються. Але три блокери б'ють у самісіньку серцевину: **хвороба двох борщів не вилікувана** (ідемпотентність порівнює запитану назву зі збереженою назвою моделі — вони ніколи не збігаються), **кроки рецепта в стрічці нечитабельні** (нова копія рендерера без `batchLabels` пише «інгредієнт» замість назви), і **таймер іде вдвічі повільніше** (лічильник тіків без прив'язки до годинника). Перші два — по одному рядку коду кожен.

Порядок, який я б узяв: QA8-02 (один виклик замінити), QA8-01 (ключ), QA8-04+QA8-05 (прибрати `behavior:'smooth'` — два дефекти одним рухом), QA8-03 (deadline замість тіків, це ж закриває QA8-06), далі решта.

---

# Що лишилось у базі

| Акаунт | Стан |
|---|---|
| `qa8-a@example.com` | комора 7 позицій, 2 рецепти в бібліотеці, їдець Оксана (алергія на арахіс), 1 позиція в списку (відмічена), 3 сесії, 1 інвайт на `qa8-b@example.com`, 4 чернетки рецептів |
| `qa8-b@example.com` | не створений — тільки інвайт |
| `design-audit@example.com`, `design-audit-2@example.com`, `qa6-a/b`, `qa7-a/b` | з попередніх прогонів; ендпоінта видалення користувача немає |

**Мій артефакт для прибирання:** `apps/web/public/qa/login8.html` — редирект на magic-link, щоб не друкувати токен у чат. Разом із `apps/web/public/da/` (копії дизайн-матеріалів із аудиту №2) **не має потрапити в прод** — скажи, і приберу обидві теки.
