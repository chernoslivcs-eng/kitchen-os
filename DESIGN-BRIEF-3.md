# Kitchen OS · бриф для Клод Дизайна №3 — відкриті питання після аудитів №1–2

Джерела: `design-audit-report.md`, `design-audit-report-2.md`,
`design-audit-desktop.md`, «Бриф-2 — рішення» (затверджений). Стан на
сьогодні: Черга 1 + Бриф-2 + черги К/Д1 реалізовані й злиті; токени, модалка
списання, мікрофон, сайдбар за Д01, адреса рецепта `/recipe/:id`, слід
рецепта в розмові («◇ Борщ · Рецепт →») — зроблені.

Тут — тільки те, що код вирішити не може: або рішення немає в жодному
матеріалі, або матеріали суперечать один одному. Вісім питань, за пріоритетом.

---

## 1. Крок назад у Cook Mode — дірка в самому дизайні

Усі чотири еталони (хендоф №10, Д05, «Мінімалізм» №4, «Моушн») показують
тільки «Крок готово ✓». **Жоден не малює спосіб повернутись** після
міскліку — а це екран, у який дивляться мокрими руками. Прод уже має
запобіжник від подвійного тапу (лок 400ms), але повернення не існує.

Аудит №2 (Р-1) розібрав три патерни й рекомендує: **тап по сегменту
прогрес-смуги з невидимою тап-зоною ≥44px по вертикалі**. Резерв — кнопка
«↩» ліворуч від головної. Свайп відхилено (мокрий палець, конфлікт жестів).

Що треба від дизайну:
- затвердити патерн і намалювати стани смуги (default / hover / тап-зона);
- вирішити долю таймера при поверненні (пропозиція коду: таймер не
  воскресає — стоїть на «Пуск», людина вирішує сама);
- зняти асиметрію DA2-15: на екрані Рецепта кроки-чекбокси знімаються
  повторним тапом, у Cook Mode повернення немає. Це має стати одним
  правилом, не двома.

**Блокує:** десктопну розкладку Cook Mode Д05 (двоколонник «крок | таймер»
мусить знайти місце для афордансу повернення — тому Д05 свідомо відкладений).

## 2. Персистентність сесії готування

«✕ Вийти» посеред готування = прогрес, крок і таймер зникають назавжди.
Рецепт тепер має адресу, а сесія готування — ні (DA2-14; код чесно визнає
борг коментарем). Питання продуктове:

- повторний вхід у Cook Mode того самого рецепта — повертає на крок, де
  людина зупинилась? Скільки живе такий стан (година? доба?);
- таймер, що йшов при виході, — «минуло N хв поки тебе не було» чи скидання;
- чи показувати в стрічці/рецепті бейдж «готування триває · крок 3/5».

## 3. Права панель 320px проти ранкової картки — узгодити ролі

Хендоф Д01 малює праву панель: ГОРИТЬ · ОЧІКУЮТЬ РІШЕННЯ · ВЕЧЕРЯ СЬОГОДНІ.
Бриф-2 п.1 затвердив ранкову картку в стрічці з майже тим самим змістом
(згорає / готовий намір / свято). Якщо зробити обидва — «що горить»
показується двічі на одному екрані десктопа.

Варіанти на вибір: панель = постійний зріз стану (десктоп-онлі), картка =
ранкова подія в таймлайні (мобайл-перший); або панель забирає на себе роль
картки на десктопі повністю; або картка скрізь, панелі немає. Потрібне одне
речення-рішення + якщо панель лишається, оновити її склад відносно картки.

**Блокує:** реалізацію правої панелі (крок 4 десктоп-черги) і ранкову
картку-плагін.

## 4. Профіль: два канони суперечать один одному

Хендоф №11/Д06 групує пам'ять **за людиною** («Оля: без лактози», «Оксана:
молюски», колонка типів АНТИ/АЛЕРГІЯ/ЛЮБИТЬ/НОТАТКА). Пізніший Бриф-2 5а
**канонізував структуру за типом запису** (блоки АЛЕРГІЇ / ДІЄТИ / ДОМАШНІ /
ВИСНОВКИ, TagInput у полі) — і прод зроблений за ним.

Для двоколонкового десктоп-профілю (Д06) треба знати, який канон переміг.
Якщо «за типом» — Д06 треба перемалювати; якщо «за людиною» — це велика
переробка екрана, і тоді питання, що робити з уже канонізованим TagInput.

## 5. Комора: три нерішені елементи хендофа №05/Д02

Рішення «зони, не кулінарні групи» — ухвалене й реалізоване. Але в еталоні
є ще три речі, по яких рішення не звучало:

- **сегменти-фільтри** зверху (Усе / Холодильник / Морозилка / Сухе) замість
  прокручування секцій;
- група **«СПОЧАТКУ ГОРИТЬ»** над зонами — комора, організована спершу за
  терміном, потім за місцем;
- **швидкий ввід** «+ молоко 1 л» прямо на екрані — другий канал вводу повз
  чат (та сама дилема, що з «+ Імпорт»: там вирішили префіксом у композитор —
  чи застосувати той самий патерн тут?).

## 6. Композитор: три версії в трьох місцях

- Десктоп-хендоф Д01: **одна пілюля 52px, мікрофон усередині поля** справа,
  плейсхолдер «Записати в журнал… ⌘K»;
- Бриф-2 5б (мобільний): мікрофон окремою кнопкою-гостом, **морф 🎙 ⇄ ↑**
  при наборі тексту;
- Прод: три постійні кнопки 48px (📎, 🎙, ↑) — 144px ширини на 390px екрані.

Чи це «десктоп так, мобільний інакше» — чи один канон? Якщо морф — куди
дівається 📎 (в поле? в «+»-меню?). Потрібен один малюнок обох станів
(порожньо / з текстом) для обох в'юпортів.

## 7. Ім'я людини: звідки воно береться

Онбординг-хендоф починає з «**Пилипе**, привіт» — але продукт ніде не питає
ім'я. Наслідок (DA2-18): аватар показує першу літеру **email** («D» для
design-audit@…), яка виглядає як ініціал, але ним не є.

Варіанти: питати ім'я одним полем при першому вході (＋тертя на вході,
якого бриф уникав); тягти з листа-відповіді; лишити анонімним і прибрати
ініціал (тоді «Пилипе, привіт» з онбордингу треба зняти). Дотично: рішення
«система = КУХНЯ, без імен-персонажів» уже ухвалене — модель, що
представляється «Ромою», лікується промптом незалежно.

## 8. Рецепт як розгортка в розмові (варіант B з Р-3)

Варіант A (адреса `/recipe/:id`) — зроблений. Слід у розмові
(«◇ Борщ · Рецепт →») — зроблений. Питання, чи йти далі до B: рецепт
розгортається **під карткою в стрічці** (патерн «гілки» з Бриф-2 1а),
а не веде на екран.

Чесна проблема, через яку B відкладали: рецепт — 1500px висоти (інгредієнти
+ 6 кроків із таймерами), розгортка такого розміру ховає всю розмову, а
патерн 1а малювався для коротких відповідей. Якщо дизайн бачить компактну
форму (акордеон? тільки інгредієнти + перший крок + «розгорнути»?) — B стає
реальним. Якщо ні — лишаємось на A + слід, і це нормальне кінцеве рішення.

---

## Поза брифом (для повноти)

- **Стрімінг чанками по фразі** (вимога моушн-кіта) — архітектурне рішення
  Пилипа (SSE-транспорт чату), не дизайнерське.
- **Онбординг-питання, приклеєне до чужих відповідей** (DA2-26) — правило
  промпту, робиться кодом після узгодження тону.
- **Оновити три файли хендофа**: гліф ✎→❋ у табі «Рецепти», 6-табний бар →
  4-табний із Бриф-2, застарілий композитор. Це задача дизайну з механічних:
  без неї кожен наступний аудит спотикається об уже вирішене.
