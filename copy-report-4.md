# Copy-report 4 — мова, голос, і чи спрацювали промпт-фікси QA-3

Той самий прогін, що `qa-report-4.md`. Усі цитати — дослівні відповіді продукту.

---

## 1. Чи працюють промпт-фікси, додані після QA-3

### 1.1 Час дієслова — `card-rules.md:6`

> ЧАС ДІЄСЛОВА: ти пропонуєш дію карткою, не робиш її. Пиши в майбутньому або
> умовному: «Запишу», «Приберу — підтвердь». Ніколи не «Записав», «Видалив»,
> «Готово» — доки людина не натиснула «Застосувати».

**Результат: 1 із 6. Правило переважно ігнорується.**

| Репліка людини | Відповідь | Вердикт |
|---|---|---|
| Купив лосось 400 г | «**Запишу.** Лосось чи копчений?» | ✓ |
| Не їм свинину | «**Записую.** Це релігійне обмеження чи просто не люблю?» | ✗ теперішній час, і картки при цьому взагалі немає |
| Дуже люблю кінзу, додавай її скрізь | «**Записав** — кінза в усе.» | ✗ |
| Моя дружина вегетаріанка | «**Записав** — у вас дома вегетаріанська кухня…» | ✗ |
| Прибери алергію на мигдаль | «**Прибираю** мигдаль зі списку алергій.» | ✗ |
| Прибери з профілю алергії… | «Розібрав, алергії **прибрав**.» | ✗ |

Найгірший випадок — «Записую» на `«Не їм свинину»`: дієслово стверджує дію, а картки
немає взагалі, тобто нічого не записується ні зараз, ні після натискання. Продукт
прямо каже неправду.

**Чому не працює.** Правило сформульоване як заборона списку слів, а модель щоразу
знаходить нову форму («Записую», «Прибираю», «Розібрав»). Плюс воно живе в
`card-rules.md`, який композиться в системний промпт разом із рештою — на haiku-4.5
одне правило серед двох десятків не тримається.

**Що зробити.** Перенести з промпту в код — це детермінована річ, їй не місце в моделі:

```ts
// model.ts, після розбору відповіді
const CLAIMED = /\b(записав|записала|прибрав|прибрала|видалив|видалила|додав|додала|зробив|готово)\b/i;
if (card && CLAIMED.test(reply)) req.log.warn({ reply }, 'past-tense-claim-with-pending-card');
```

Спершу логувати, зібрати частоту, і вже тоді або переписувати правило, або чіпляти
дешевий пост-процесор. У промпті ж замінити список заборонених слів на позитивне
формулювання з поясненням механіки: *«Картка ще не застосована. Людина натисне
"Застосувати" — тільки тоді щось зміниться. Тому будь-яке дієслово про зміну стану
пиши в майбутньому: "Запишу", "Приберу", "Додам".»*

### 1.2 `{N}` у називному відмінку — `recipe-generator.md:11`

**Результат: 1 із 2 рецептів чистий.**

«Паста карбонара» — модель скористалась конструкцією з тире, як радить правило, і все
коректно:

> `{0} — варити в підсоленій воді згідно пакунку мінус 1 хвилина`
> `{2}, {3}, {4} — збити віночком до однорідності`

«Лосось на пательні з лимоном» — два кроки з шести зламані:

> `{0} обсушити паперовим рушником, посолити {3} і {4} з обох боків`
> → **«посолити сіль і перець»** (треба «сіллю і перцем»)

> `{2} додати на пательню, розігріти на середньо-високому вогні`
> → **«олія додати на пательню»** (треба «олію»)

Це видно на живій сторінці `/r/1a02dea8-7490-4561-ba1d-1c19c8770d58` — тобто це
бачить людина, якій відправили рецепт.

Плюс те, чого правило не покриває: підстановка не має регістру. `{2}` стоїть на початку
речення, а підставляється «олія» з малої. `Лосось` з великої — бо назва прийшла з мітки
партії. У одному списку кроків одні інгредієнти з великої, інші з малої.

**Що зробити.** Правило само по собі непогане, але воно бореться з мовою. Надійніше —
змусити модель ставити `{N}` **лише** в одну з двох безпечних позицій, і перевіряти це:

- у `recipe-generator.md` замінити пояснення на жорстке: *«`{N}` ставиться тільки на
  початку кроку, після нього — тире або кома. Приклад: `{1} — подрібнити`, `{0}, {2} —
  збити`. Усередині речення `{N}` не використовуй ніколи: там потрібен непрямий
  відмінок, який ти не знаєш заздалегідь.»*
- у `apps/web/src/lib/recipe.ts` капіталізувати підстановку, коли плейсхолдер стоїть
  першим символом кроку.

---

## 2. Мова: 8 дефектів за один прогін, усі з `fastModel()`

Це найгірша вибірка за всі чотири прогони. Всі — з чату (haiku-4.5). Рецепти, які
генерує sonnet-4.5, мовно чисті.

**Русизми й прямі російські слова:**

| Сказано | Має бути | Де |
|---|---|---|
| «Яке більше **нравится**?» | «подобається» | proposal-картка з десертом |
| «або **с семінням** чіа» | «з насінням» | там само, в `desc` картки |
| «щоб **м'ягша**» | «м'якша» | там само |
| «**перекуска** дольку-дві» | «перекусити дольку-дві» | там само |
| «що з **горячим** маслом» | «гарячим» | уточнення про алергію |
| «у вас **дома** вегетаріанська кухня» | «вдома» | відповідь про дружину |

**Неіснуючі слова й розсипана граматика:**

| Сказано | Проблема |
|---|---|
| «**Спитаю уточніїв.**» | «уточніїв» — не слово. Речення нічого не означає |
| «Яка сьогодні **план**?» | «Який» |
| «**Уточню межі, перш ніж все записати.**» | граматично коректно, але це вся відповідь — модель оголосила уточнення і не спитала нічого |

Чотири дефекти з восьми — в одній картці:

> «Булочка з корицею або шоколад — обидва готові. Булочку підігріти на пательні, щоб
> **м'ягша**, або так із духовки. Яке більше **нравится**?»
> «Шоколад молочний · З мигдалем або **с семінням** чіа — **перекуска** дольку-дві…»

Це не одруківки. Це модель, яка думає російською і перекладає на ходу.

**Причина.** `fastModel()` = `anthropic/claude-haiku-4.5`. Дешева модель тримає
українську рівно доти, доки речення прості; на будь-якій розмовній інтонації
починає протікати російська. Промптом це не лікується — жодне правило про мову не
переживає п'ятдесятої репліки.

**Три варіанти, за спаданням якості:**

1. **Перевести чат на `smartModel()`.** Sonnet-4.5 за чотири прогони не дав жодного
   русизму. Дорожче, але чат — це і є продукт; там, де людина чує голос, економити
   на моделі означає економити на голосі.
2. **Гібрид:** haiku для `intake_diff` (розбір «купив X» — механічна задача, мова
   коротка), sonnet для всього, що звучить як розмова. Розділити можна по наявності
   картки типу `intake_diff` — але це вимагає двох викликів, тож радше по довжині
   й типу репліки.
3. **Лишити haiku + детектор.** Регексп на 30-40 найчастіших русизмів
   (`нравиться|перекуска|дома\b|горячий|семіння|м'ягк`) у `model.ts`, лог у
   `token_usage`, і eval-фікстура. Найдешевше, але ловить тільки те, що вже бачили.

Моя рекомендація — (1). Порахуй різницю: чат на haiku зараз ~5 900 вхідних токенів
на репліку; після фіксу QA4-01 (історія) буде ~8-10 тис. На sonnet це помітні гроші,
але це єдиний спосіб, щоб продукт українською звучав як людина, а не як переклад.

---

## 3. Голос: де він тримається, а де ні

**Тримається.** Пам'ять про готування звучить точно так, як має:

> «Ти це вже робив вчора — вийшло пересолено й сухо. На пательні з лимоном тобі
> набагато більше сподобалось. Робимо так само?»

Це найкраща репліка за чотири прогони. Коротко, з фактом, без опіки, з пропозицією.
(Єдина проблема — «вчора» неправда, див. QA4-08.)

І `rk` у рецептах усе ще працює як задумано — механіка помилки, не застереження:

> «не перевертати рибу рано — шкірка має підрум'янитись і сама відлипне, інакше вона
> розірветься й прилипне до пательні»

**Не тримається — питання без відповіді.** П'ять реплік поспіль модель ставить питання
і жодного разу не пропонує. Формально це «кухар, що уточнює». Насправді людина відповіла
двічі й отримала те саме питання втретє. Голос розсипається не на рівні слів, а на
рівні того, що розмова не рухається. Причина технічна (QA4-01), але читається це як
характер: продукт, який не слухає.

**Не тримається — питання про релігію.** На `«Не їм свинину»` → `«Це релігійне
обмеження чи просто не люблю?»`, і далі на `«Релігійне»` → **«Яка релігія, щоб я
правильно записав обмеження?»**

Дві проблеми. Технічна: без історії модель не знає, до чого стосується «релігійне», і
питає те, з чим все одно нічого не зробить. Продуктова: кухарю не треба знати релігію
людини. Треба знати «не їм свинину». Запитувати конфесію, щоб «правильно записати
обмеження» — це збір даних, який продукт не використовує і зберігати не має.

Правило в `card-rules.md`: *«Не питай про причину обмеження — релігію, стан здоров'я,
переконання. Записуй сам факт: "не їсть свинину". Якщо людина назве причину сама —
не записуй її окремо.»*

**Не тримається — тон уточнень про алергію.** На `«У мене алергія на горіхи і на
лактозу»` модель питає:

> «Під горіхами ти маєш на увазі арахіс, волоські горіхи, мигдаль, кеш'ю — усе разом?
> І лактоза — це молоко, вершки, масло, чи тільки свіжий молочний?»

Питання розумне по суті, але на алергію так не відповідають. Правильна поведінка —
записати найширшу межу одразу (всі горіхи, вся молочка), сказати про це, і
запропонувати звузити потім: *«Записую всі горіхи й усю молочку — так безпечніше.
Якщо щось із цього насправді можна, скажи, звужу.»* Зараз продукт лишає людину без
запису доти, доки вона не пройде допит — а через QA4-01 запис так і не відбувається.

---

## 4. Дрібне

- «Дай рецепт чи готуємо знову?» — дві половини питання означають одне й те саме.
  Людина не може вибрати між ними, бо різниці немає.
- Порожній стан історії: «Кожна сесія зберігається — вона зʼявиться тут завтра».
  З'являється одразу.
- Опис шоколаду в картці: «До чаю подобається більше, ніж сам по собі» — модель
  приписує вподобання самому шоколаду. Мало б бути «до чаю краще, ніж просто так».

---

## 5. Підсумок

Промпт-фікси QA-3 дали 1 із 6 на часі дієслова і 1 із 2 на відмінках. Тобто
**промптом ці класи помилок не закриваються** — треба або інша модель, або код.

Мовна якість чату впала до рівня, коли продукт українською просто соромно показувати:
вісім дефектів за прогін, чотири з них в одній картці, включно з прямим російським
словом. Це не про промпт і не про правила — це про те, що на розмовну українську
haiku-4.5 не тягне.

Голос продукту при цьому живий і правильний там, де модель має факт і не мусить
вигадувати — пам'ять про готування і `rk` у рецептах звучать саме так, як задумано.
Це добра новина: концепція голосу робоча, ламається виконання.
