{
  "name": "kitchen-os",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "eval": "pnpm --filter @kitchen/eval run start",
    "eval:diff": "pnpm --filter @kitchen/eval run start -- --diff-only",
    "test": "pnpm -r --parallel test",
    "typecheck": "pnpm -r --parallel typecheck",
    "smoke": "bash scripts/smoke.sh",
    "build:vercel-fn": "esbuild services/api/src/vercel-handler.ts --bundle --platform=node --format=esm --target=node20 --external:pg-native --banner:js='import { createRequire } from \"node:module\"; const require = createRequire(import.meta.url);' --outfile=api-dist/server.mjs && cp -R packages/prompts/versions api-dist/versions"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.24.0",
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
