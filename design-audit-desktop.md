# Десктоп: прод проти «Хендоф — десктоп»

Прицільне звіряння всіх шести екранів Д01–Д06 із хендофом. Усі числа — computed-значення з живої сторінки на 1728px, усі посилання на еталон — рядки з `Хендоф - десктоп.dc.html`.

---

## Вердикт

**Десктопної версії немає.** Є мобільна колонка, відцентрована в порожньому екрані, плюс сайдбар.

Це не перебільшення заради ефекту — це буквальний стан коду. У всьому продукті існує **рівно один** десктопний медіа-запит із власною розкладкою: `Pantry.module.css:105` (дві колонки зон). Решта екранів має один і той самий шаблон:

```css
@media (min-width: 720px) {
  .head, .body { max-width: 720px; width: 100%; margin-left: auto; margin-right: auto; }
}
```

Він повторений дослівно у `Feed`, `Shopping`, `Profile`, `Recipe`, `Cook`, `Share`, `SharedRecipe`, `SignIn`, `MagicLinkSent`. Дев'ять файлів, одне правило: «стань 720 і стань по центру». Це не адаптація під десктоп — це запобіжник проти надмірного розтягування на планшеті, який випадково опинився єдиною відповіддю на 1728px.

`Recipes.module.css` не має навіть цього — там взагалі жодного `min-width` запиту.

**Тому розходження не в тому, що щось не збігається по пікселях. Розходження в тому, що хендоф описує трьохколонковий застосунок, а прод — одноколонковий.**

---

## Головна геометрична різниця

Хендоф, `.dk` (рядок 17) + `.side` (20) + `.main` (23) + `.rail` (24):

```
┌──────────┬───────────────────────────┬──────────────┐
│ side 232 │ main flex:1               │ rail 320     │
│ навігація│ контент max-680, ЗЛІВА    │ контекст     │
│ + профіль│ padding 36px              │              │
└──────────┴───────────────────────────┴──────────────┘
```

Прод на 1728px:

```
┌──────────┬─────────┬───────────┬─────────┐
│ side 200 │ 404 ПУС │ колонка   │ 404 ПУС │
│ 4 пункти │ ТО      │ 720, ПО   │ ТО      │
│          │         │ ЦЕНТРУ    │         │
└──────────┴─────────┴───────────┴─────────┘
```

Дві принципові речі, а не одна:

1. **Правої панелі немає** — 320px контексту («що горить», «очікують рішення», «вечеря сьогодні») просто відсутні.
2. **Колонка центрована, а не притиснута до сайдбара.** У хендофі контент починається на `232 + 36 = 268px` і читається як продовження навігації. У проді він починається на `604px` — між сайдбаром і текстом 404 порожніх пікселів. Око бачить сайдбар як відірваний елемент зліва, а не як частину однієї конструкції.

Друга різниця важливіша за першу, бо вона діє на **всіх** екранах, а не тільки на Стрічці. І вона дешевша у виправленні: це `margin-left: 0` замість `auto`.

---

## Екран за екраном

### Д01 · Стрічка

| | Хендоф (рядки 36–104) | Прод | |
|---|---|---|---|
| Розкладка | 232 / main / 320 | 200 / 720 по центру / — | ✗ |
| Контент починається на | 268px | 604px | ✗ |
| Права панель | `.rail` 320px, `border-left`, `#f8f9fa` | немає (`querySelectorAll('[class*=rail]')` → 0) | ✗ |
| Шапка | «Кухня» + mono «ЧТ 28 СЕРП · СИНХРОН ОК» | «Кухня» + «+ Новий» + «КОМОРА 6 · СПИСОК 3» + аватар | ✗ |
| Композитор | **одне поле 52px**, mic 38×38 всередині справа, плейсхолер «Записати в журнал… ⌘K» | **чотири елементи**: 📎 48 + 🎙 48 + поле 48 + ↑ 48 | ✗ |
| Поле: фон / радіус | `#f8f9fa` / 14px | `rgb(248,249,250)` / 14px | ✅ |
| Поле: бордер | `#dde0e2` | `#ced3d6` | ✗ |
| Підказка ⌘K у полі | є | немає (сам хоткей працює) | ✗ |
| Максимум колонки | 680 | 720 | ~ |

**Права панель — не косметика.** Її три блоки (`ГОРИТЬ · 2`, `ОЧІКУЮТЬ РІШЕННЯ · 2`, `ВЕЧЕРЯ СЬОГОДНІ` притиснута донизу через `margin-top:auto`) — це єдине місце в усьому дизайні, де стан кухні видно **без прокручування стрічки**. Прибравши її, продукт втратив не 320 пікселів, а відповідь на питання «що зараз горить», заради якого людина відкриває застосунок.

**Композитор — окреме розходження, не дрібне.** Хендоф малює одну пілюлю з мікрофоном усередині: ввід виглядає як рядок журналу. Прод розклав це на чотири квадрати 48px, які з'їдають 144px і читаються як панель інструментів. Це різні висловлювання: «запиши» проти «обери інструмент».

### Д02 · Комора

| | Хендоф (106–150) | Прод | |
|---|---|---|---|
| Колонок зон | **3** (`grid-template-columns:1fr 1fr 1fr`, gap 28) | **2** (`502px 502px`, gap 32) | ✗ |
| Ширина контейнера | main − 72 ≈ 656 | 1080, x=424 (по центру) | ✗ |
| Заголовок | «Комора · 23» — число в заголовку | «Комора» + окремий рядок «6 ПОЗИЦІЙ» | ✗ |
| Швидкий ввід у шапці | поле 260px «+ молоко 1 л · швидкий ввід» | кнопка «+ Додати» (відкриває шит) | ✗ |
| Сортування | «Сортувати: свіжість» | немає | ✗ |
| Групування по зонах | є | є | ✅ |
| Мітка алергії | `⚠ АЛЕРГІЯ ОКСАНИ` | є (DA-06 закрито) | ✅ |

Це єдиний екран, де десктопна розкладка взагалі писалася свідомо — і вона на одну колонку коротша за еталон. Плюс два робочі елементи шапки (швидкий ввід і сортування) не перенесені.

### Д03 · Рецепт

| | Хендоф (152–213) | Прод | |
|---|---|---|---|
| Сайдбар | **є**, активний пункт «Рецепти» | **немає** — `TabBar` на `/recipe` не монтується | ✗ |
| Розкладка | `grid: 300px 1fr`, gap 40 — інгредієнти зліва, кроки справа | одна колонка 720 | ✗ |
| Кроки | вертикальний таймлайн: кружечки 26px + з'єднувальна лінія 1px | плоский список | ✗ |
| Стани кроків | ✓ зелений залитий / поточний чорний / майбутній контурний | чекбокси | ~ |
| «ЧОМУ ЗАРАЗ» | блок із лівим бордюром 2px | немає | ✗ |
| «Cook Mode» у шапці | є, `#e4ecdf` / бордер `#c2d3b8` | є, `variant="secondary"` | ~ |
| Мета в шапці | «25ХВ · 2 ПОРЦІЇ · 540ККАЛ» | «РЕЦЕПТ · КРОК N/M» | ✗ |

Найбільша структурна втрата після правої панелі. Хендоф робить рецепт **двоколонковим документом**: інгредієнти лишаються перед очима, поки гортаєш кроки. У проді інгредієнти прокручуються вгору й зникають — саме тоді, коли вони потрібні.

### Д04 · Список

| | Хендоф (217–259) | Прод | |
|---|---|---|---|
| Розкладка | одна колонка max-680, зліва | одна колонка 720, по центру | ~ |
| Заголовок | `700 22px Onest` | `700 22px Onest` | ✅ |
| «Копіювати текстом» | є | немає | ✗ |
| Степери −/+ на позиції | є (34×34 + значення 44px) | немає | ✗ |
| «ДЛЯ: ВЕРШКОВА ФЕТУЧІНІ» під позицією | є | є (`reason`) | ✅ |
| «ДЛЯ ОЛІ · АНТИ ЛАКТОЗА» кольором `#8f5c78` | є | — не перевіряв на цьому акаунті | ? |
| «У комору» на купленій позиції | пілюля на рядку | групова дія внизу | ~ |
| «+ додати в список…» поле | є | немає (тільки через чат) | ✗ |
| Бордер шапки | `1px #dde0e2` | `0px` | ✗ |

Це єдиний екран, де хендоф сам каже «центр без правої панелі» (рядок 218) — тобто одноколонковість тут канонічна. Розходження тільки в елементах: степери, копіювання, ручне додавання.

### Д05 · Cook Mode

| | Хендоф (263–296) | Прод | |
|---|---|---|---|
| Розкладка | `grid: 1.2fr 1fr`, gap 60 — крок зліва, таймер справа за бордюром | одна колонка 720 по центру | ✗ |
| Прогрес | у шапці, ширина 220px, поруч із мета-рядком | окремим рядком під шапкою, на всю ширину | ✗ |
| Заголовок кроку | `600 44px/1.3 Onest` | не масштабується на десктопі | ✗ |
| Таймер | `800 120px/1 Onest`, `tabular-nums` | не масштабується | ✗ |
| «НА ЦЬОМУ КРОЦІ» | є, `22px/1.5 #c3c9ce` | немає | ✗ |
| «ДАЛІ» | є | є | ✅ |
| «Пауза» / «+1 хв» | 64px, pill, поруч | є | ✅ |
| Головна кнопка | 72px, radius 18, `700 21px`, підпис «Крок готово ✓» | 60px, radius 16, `700 18px`, підпис «Далі →» | ✗ |
| «Працює без мережі…» | є | є | ✅ |
| Темний в обох темах | так | так (DA-17 закрито) | ✅ |

Cook Mode на десктопі — це той самий мобільний екран у смузі 720px посеред 1728. Хендоф натомість використовує ширину за призначенням: таймер стає окремою зоною справа, яку видно з двох метрів, а крок читається з чотирьох. Це різниця між «планшет на підставці біля плити» (задум) і «сайт, відкритий на ноуті» (факт).

### Д06 · Профіль

| | Хендоф (299–344) | Прод | |
|---|---|---|---|
| Сайдбар | **є**, і пункт «Пилип» унизу підсвічений як активний (`.sit on`) | **немає взагалі** — `body padding-left: 0`, `TabBar` розмонтований | ✗✗ |
| Розкладка | `grid: 1fr 1fr` — сім'я+обладнання зліва, пам'ять справа | одна колонка 720 по центру | ✗ |
| Заголовок | «Кухня Пилипа» | «Профіль» | ~ |
| «Вийти» у шапці | є | є | ✅ |
| Пам'ять: колонка типу 64px (`АНТИ` / `АЛЕРГІЯ` / `ЛЮБИТЬ` / `НОТАТКА`) кольорами | є | секції «Алергії / Побажання / Чого не їм» окремими блоками | ✗ |
| Обладнання чипами + «+ додати» пунктиром | є | «Техніка» | ~ |
| Сім'я з ролями ВЛАСНИК/УЧАСНИЦЯ + «···» | є | «Домашні» + «Запросити в дім» | ~ |

**Тут найгостріше.** У хендофі профіль — це повноцінний екран застосунку із сайдбаром, і активний стан у сайдбарі стоїть саме на блоці користувача. У проді відкриття профілю **прибирає навігацію повністю**: сайдбар зникає, `padding-left` падає до нуля, контент стрибає вліво на 200px. Єдиний вихід — стрілка «←» 38×38 у лівому верхньому куті.

Причина точна — `TabBar.tsx:24-27`:

```ts
useEffect(() => {
  document.body.classList.add('with-sidebar');
  return () => document.body.classList.remove('with-sidebar');
}, []);
```

Сайдбар живе всередині `TabBar`, а `TabBar` рендериться тільки на чотирьох табових екранах. Тому `/profile`, `/cooklog`, `/recipe`, `/cook`, `/share` на десктопі лишаються без навігації. Хендоф малює сайдбар на Д03 (Рецепт) і Д06 (Профіль) — тобто в еталоні його немає тільки в Cook Mode, і це свідомо.

Стрибок контенту на 200px при переході Комора → Профіль — це видимий ривок макета, не тонкість.

---

## Сайдбар: що всередині

Порівняння самого компонента (`TabBar.module.css:70-105` проти `.side` / `.sit`, рядки 20–22, 40–46):

| | Хендоф | Прод | |
|---|---|---|---|
| ширина | 232px | 200px | ✗ |
| padding | 20px 14px | 24px 12px | ✗ |
| логотип + вордмарк угорі | SVG 26px + `700 17px Onest`, `padding 0 12px 22px` | **немає** (компонент `Logo` у кодовій базі є) | ✗ |
| висота пункту | 42px | 45.7px | ✗ |
| gap гліф↔текст | 11px | 12px | ~ |
| radius пункту | 12px | 12px | ✅ |
| неактивний колір | `#5c646d` | `#7b838b` | ✗ |
| активний фон | `#e3e6e8` | `#e3e6e8` | ✅ |
| активний колір | `#1d2126` | `#1d2126` | ✅ |
| лічильник «Комора» | mono 11px `#7b838b`, `margin-left:auto` | **немає** (живе в шапці контенту) | ✗ |
| бейдж «Список» | mono 11px, `#58754e`/`#f2f4f0`, `1px 7px` | 9px, `#58754e`/`#f2f4f0`, `1px 6px` | ~ |
| спейсер `flex:1` | є | немає | ✗ |
| блок профілю внизу (аватар 26px + ім'я) | є, може бути активним | **немає** | ✗ |
| гліф «Рецепти» | `✎` | `❋` | див. нижче |

Кольори активного стану і бейджа збігаються **точно**, до значення. Тобто токени взяті правильно — розходиться композиція, не палітра. Це важливо: означає, що виправляти треба розмітку, а не перебирати змінні.

Про гліф: хендоф каже `✎`, Бриф-2 п.2 каже «4 таби канон (**❋** Рецепти)». Прод узяв Бриф-2 — і правильно, бо він пізніший. Але це означає, що **три файли хендофа (`екрани` світла, `екрани` темна, `десктоп`) містять застарілий гліф** і їх треба оновити, інакше наступне звіряння знову спіткнеться об те саме місце.

---

## Побічна знахідка: шапка Рецептів розсипається

`/recipes` — єдиний екран без жодного обмеження ширини. Наслідок виміряний:

```
.head — display:flex, flex-wrap:nowrap, justify-content:space-between, ширина 1528px
├─ «Рецепти»            x = 222
├─ [✎ Журнал] [аватар]  x = 918   ← посеред екрана
└─ «1 РЕЦЕПТ»           x = 1648
```

Картка рецепта при цьому має ширину **1508px** — назва страви й рядок статусу розтягнуті на півтора метра екрана.

Це заодно пояснює те, що в звіті №2 я записав як «правий край аватара плаває» (DA2-16). Причина не в різних відступах: на трьох екранах шапка обмежена 720px і трикомпонентний `space-between` виглядає нормально, а на `/recipes` та сама розмітка розпливається на всю ширину і викидає аватар у центр. Один пропущений медіа-запит, три симптоми.

---

## Що це означає в сумі

Хендоф — це **шість різних десктопних екранів**: три колонки на Стрічці, три колонки зон у Коморі, дві колонки на Рецепті, дві на Профілі, дві в Cook Mode, одна на Списку. Кожна розкладка вирішує свою задачу: тримати контекст перед очима, тримати інгредієнти перед очима, тримати таймер видимим з двох метрів.

Прод — це **один екран у шести варіантах контенту**: колонка 720px по центру. Плюс одна двоколонкова Комора.

Розходження, які я перелічив по пікселях (232 проти 200, 42 проти 45.7, `#5c646d` проти `#7b838b`), — це наслідки, не причина. Причина одна: десктопна розкладка не була реалізована, а замість неї спрацював запобіжник `max-width: 720px`, написаний для планшета.

Тому твоє відчуття точне. Це не «дрібні неточності накопичились» — це відсутній шар роботи.

---

## Черга виправлення, за співвідношенням «ефект / вартість»

**1. Притиснути колонку до сайдбара** — `margin-left: 0` замість `auto` у дев'яти медіа-запитах. Півгодини. Прибирає провал на 404px між навігацією і текстом на кожному екрані. Найбільша зміна відчуття за найменші гроші.

**2. Сайдбар на всіх екранах.** Винести його з `TabBar` в окремий `AppShell`, який обгортає всі авторизовані маршрути крім `/cook`. Прибирає стрибок контенту на 200px і повертає навігацію на Профіль, Журнал і Рецепт — як у Д03 і Д06. Пів дня.

**3. Добудувати сайдбар**: логотип угорі, спейсер, блок профілю внизу, лічильник Комори, ширина 232, `#5c646d` на неактивних, бейдж 11px. Все є в рядках 40–46 хендофа, копіюється майже дослівно. Пів дня.

**4. Права панель на Стрічці** (`.rail`, рядки 83–102). Три блоки, дані для всіх трьох уже є в клієнті: `expires_at ≤ 3 дні` для «ГОРИТЬ» уже рахується в `refreshCounts()`, pending-картки в `turns`, пропозиція — остання `proposal`. Це збірка з наявного, не нова логіка. День.

**5. Рецепт у дві колонки** (300 / 1fr) + вертикальний таймлайн кроків. День-півтора, але це найбільший виграш для реального користування: інгредієнти перестають тікати з екрана.

**6. Cook Mode Д05** — крок зліва, таймер справа, 44px/120px типографіка. День. Робити після того, як закриється крок назад (Р-1 зі звіту №2), бо розкладка мусить знайти місце для афордансу повернення.

**7. Профіль у дві колонки + Комора в три.** Косметика розкладки, по пів дня кожен.

Перші три пункти — це півтора дні, і після них десктоп перестане виглядати як розтягнутий телефон. Пункти 4–6 — це вже власне десктопний продукт.
